# TigonKV 当前对比口径

本页是 `my-work` 上 TigonKV 与 `../cxlkv` 做实验时的当前真值。`PLAN.md` 记录设计
依据和历史决策；旧恢复记录或原始 benchmark 源码不能覆盖本页的数据面合同。

## 目标架构合同（已冻结；按方案落地前数据路径仍见下表）

来源：`Scan原始Tigon对齐修改方案.md` §11.1 / `PLAN.md`「架构合同冻结」/
`kSingleTableId`。落地完成前，下表「数据路径」仍描述现版行为；不得把目标
合同误报成已实现。

| 合同项 | 冻结内容 |
|---|---|
| 单逻辑表 | `kSingleTableId=0`；API/wire 无 `table_id`；partition ≠ 多表 |
| 路由 | hash partition：`partition = hash(key)%N`，`owner = partition%vm` |
| 单 key | Put/Get/Delete/CAS/Increment 线性一致 |
| Scan（目标） | 非全局 snapshot；CXL-first + 按 partition range move-in；删正式热路径 certificate；`exhausted` 为 TigonKV 必要新增并须披露 |
| 进程 DRAM（目标） | 无随行数线性增长的堆结构；Clock tracker 须进 owner-private SWCC |
| 保留适配 | 定长 KV、RegionOffset、双区域 allocator、Busy 重试、Clock ITable 薄适配；不新增第二权威源 |

## 数据路径

| 操作/对象 | 当前路径 |
|---|---|
| owner-private row/tree | SWCC owner arena；仅 owner 访问，不走 SCC |
| shared B+Tree、row metadata、根、EBR、transport | HWCC |
| migrated payload | shared SWCC；WriteThrough SCC + 行锁 |
| 非 owner GET | shared hit 就地 SCC；miss 发 `kMigrate`，owner move-in 后 ack，requester 再走 shared |
| 非 owner PUT/CAS/Increment | shared hit 就地 SCC；miss Forward owner；已有 private row 在更新后、响应前 promote，新插入 key 暂留 owner-private |
| Delete | owner 权威；非 owner 始终 Forward，删除后按 EBR 回收 |
| 分布式 Scan | 本地 owner 走 private locator 单权威；远端 owner key-only 选取并 move-in 请求范围，发起方只从 shared CXL tree 读取且用 shared-mutation snapshot 验证；各 owner 流分页有序归并 |

PrivateRow 在 move-in 后保留权威定位壳，`migrated_smeta_off` 指向 HWCC metadata；
payload 位于 shared SWCC。move-out 摘 shared index、恢复 private value，并按 EBR
回收 metadata/payload。根、SCC bitmap/latch、migration sequence 与 transport
控制原子都按 HWCC 访问会计；payload memcpy/flush 按 SWCC 访问会计。

Scan 不把部分 CXL 行与 owner RPC 行作为并列权威源。本地 owner tree 为每个
migrated row 保留 locator，扫描时在 PrivateRow 锁下跟随
`migrated_smeta_off`；远端 owner 通过 `kScanMigrate` 对各 partition 的
key-only locator 流做 owner 级归并，再以原始 `inc_ref=false` 方式 move-in
请求页，不预读或返回 value。响应携带 shared-mutation snapshot（逻辑
key/shared 可见性变更的完成代际 + in-flight 计数）；requester 执行
`ScanSharedPartitions` 后验证 snapshot，并发 insert/delete/move-out 会使
整页重试。
后续页从 owner range move-in 重新开始。旧的流式 `ScanRPC` 协议已删除。
这是对原 TwoPLPasha“不完整则 owner range move-in、再走 CXL”的保守恢复；
KV adapter 没有复刻 `is_next/is_prev_key_real` adjacency bits，而以每
partition 的 HWCC mutation generation/count 证明 owner 选取到 CXL 读完期间
keyspace 与 shared 可见性稳定。它不把 CXL 与 owner value 并列为权威。

## 初始化、线程和 CPU 口径

reset VM 在 allocator 初始化后保持 layout=`kInitializing`，直到 transport、EBR、
全部 partition roots 就绪并已启动本机 inbound demuxer 后才发布 `kDirty` ready。
attach VM 对匹配 layout 等待该屏障，不能观察半初始化根。
layout v11 的 clean 只能由 collective checkpoint 产生：所有 VM flush 自己的
owner-private arena 与 owned SWCC ranges 并发布 ready epoch；node0 等齐后才
发布 `kClean`/`clean_epoch`，其余 VM 等待。mutation 重新标 `kDirty`，
dirty attach hard-fail。

allocator 固定 metadata 按物理池拆为 HWCC/SWCC 两域；physical used 分别等于
本池固定 metadata 加动态对象，owner arena 的 reserved capacity 不冒充 used。
迁移 HWCC budget 按 owner 分开计；Clock second chance 只有一个 HWCC atomic
policy state。worker 退出会移交 EBR retire tail，不能遗失待回收对象。

每 VM 的正式 CPU 口径是：

```text
foreground = foreground_worker_count_per_vm
demuxer     = 1
kv_threads  = foreground + 1
```

启用 affinity 时 foreground 与 demuxer 使用不同 allowed CPU。demuxer 保留原
Tigon IncomingDispatcher 的“专用接收入环线程”形态，独占本机 inbound MPSC，
但当前 deferred FIFO 并非逐结构同构。foreground 每次最多批取 64 个 deferred
请求。matching response 对对应 pending `notify_one`；任一 deferred request
到达还会唤醒 pending forwarder，使其协作 service。报告不得只写 foreground
worker 数。
心跳复用已有 runner 编排主线程，不是额外 KV service thread；它在 replay
期间仅按低频读取批量发布的 progress counter。正式报告需说明这个并发控制线程，
心跳行本身不参与吞吐汇总。

## 构建、延迟与实验声明

维护的比较构建入口是 `tigonkv`、`e2e_trace_runner`、focused tests 和 guest E2E
scripts。原始 transaction executor/benchmark 源保留用于对照，但不链接进 TigonKV
比较目标，因而 legacy `TwoPLPashaExecutor::update/remote_update` 不在比较路径。

正式延迟注入只允许 RelWithDebInfo、verbose off、extra_check off、
`foreground_enabled=true`、成功的 x86 TSC 校准和 TSC `_mm_pause`
自旋；enabled 模式没有 `sleep_for` 回退。运行时只接受唯一的
`tigon_kv.latency_inject`，19 个字段全部显式、严格类型；缺失、重复或跨
对象错位均 hard fail，不读取独立 policy JSON 作为第二权威。stage marker 由
`TIGONKV_E2E_STAGE_MARKERS` 控制；`E2E_TRACE_HEARTBEAT` 由
`TIGONKV_E2E_TRACE_HEARTBEAT_SEC` 独立控制（0 关闭）。guest 使用 5 秒心跳，
只作 stall 活性观测；汇总器忽略心跳行，也不需要打开 verbose。心跳在 replay
期间运行，其批量 progress 发布开销属于受测执行，不能表述成“计时窗口外”。

CLWB/writeback 与 CLFLUSH/invalidate 均支付完整 flush 成本，只有 invalidate
驱逐 per-thread LRU tag。当前 cxlkv 未镜像该修正前，跨仓对比只使用
`cache_model=none`；LRU 注入结果不得混比。

所有结果只能表述为 NUMA-based CXL shared-memory emulation / software
latency-injected results，而非真实 CXL 硬件性能。相同 trace、fixed key/value、
foreground 数、额外 demuxer CPU、HWCC/SWCC 容量、延迟参数和计时窗口必须与
`../cxlkv` 一并报告。
