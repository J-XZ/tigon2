# TigonKV 当前对比口径

本页描述当前正式数据面。施工合同和验收要求以
[`partition优化方案.md`](partition优化方案.md) 为准；原始实现基准始终是当前
`master` 头部。

## 架构与一致性合同

| 项目 | 当前合同 |
|---|---|
| 逻辑表 | 只有 `kSingleTableId=0`；外部 KV API 不暴露 table id，原 `MessagePiece` header 保留并固定为 0。partition 是连续半开 key range，不是多表。 |
| 所有权 | `owner = partition % vm_count`。owner-private B+Tree、`PrivateValueStruct`/lmeta、Clock tracker 和 EBR retire queue 只在 owner-private SWCC，持久链接均为 `RegionOffset`。 |
| 共享对象 | shared B+Tree、smeta、root、transport 和 EBR epoch 在 HWCC；migrated tid/valid/value image 在 shared SWCC，只经原 WriteThrough SCC prepare/finish/flush 访问。 |
| 单 key | Put/Get/Delete/CAS/Increment 线性一致。正常竞争的 `Busy` 在完整 facade 操作边界持续重试；没有固定 64 次失败上限。 |
| Clock | 使用原 PolicyClock candidate、second-chance、cursor、move-in/out 顺序。物理 HWCC 容量与 Clock dynamic budget 分别报告。 |

正式 32GiB backing 的物理划分是 HWCC 1024MiB、SWCC 31744MiB；
`hw_cc_budget_mb=1024` 是 Clock 的配置上限，Open 会在静态 HWCC 域后自动 clamp，
不把动态预算误报为物理容量。

## 四个独立硬件模拟模块

当前模拟配置位于 `tigon_kv.latency_inject`，且必须完整包含：

| 模块 | 作用与边界 |
|---|---|
| `fixed_latency` | 按 HWCC、owner-private/shared SWCC 和实际覆盖 cache line 数累计固定延迟；不读取远程 cache 状态，不含 hit/miss 过滤。 |
| `hwcc_access_count` | 只统计真正执行的 HWCC ordinary read/write，分别提供 operation、line 和可选 byte 维度。 |
| `atomic_count` | 包装实际执行的 load/store/CAS/exchange/fetch 操作，保留 memory order、CAS expected/weak 伪失败和结果；区分 HWCC、owner-private SWCC、local DRAM。 |
| `remote_cache_invalidation` | 通过稳定 `(domain,pool_id,pool_offset/cache_line)` 和共享有序事件日志重放 HWCC 脏行交接、clean invalidation、capacity eviction；SWCC 只接受真实 flush/explicit handoff。 |

每个模块及内部统计维度可独立关闭，不会隐式启用其他模块。四模块全关时热路径
只有 relaxed process-local feature bitmask 分支；`TIGONKV_DISABLE_HARDWARE_SIMULATION=ON`
提供编译期移除 slow path 的对照。启动摘要使用 `TIGONKV_HARDWARE_SIM_STATS`，
不再输出旧 `swcc_raw/hwcc_raw/swcc_misses` cache-model 字段。远程 event record
为 64B；默认 4,194,304 条（256MiB）保留在 HWCC instrumentation 区，溢出、sequence
缺口或重放非法状态 hard-fail。

## 数据路径

- owner-private 行只由 owner 访问；migrated locator 指向 HWCC smeta，payload 只在
  shared SWCC SCC allocation。
- 非 owner 点操作 CXL-first：稳定 shared miss 才 forward 或请求 owner move-in。
  Delete 始终由 owner 作为权威。
- 迁移保持单一权威版本；locator、reader/writer/ref pin、adjacency 和 EBR 顺序由
  原 helper/Clock 维护。非法 offset、会计 underflow 与协议不变量硬失败。
- 生产协议直接复用原 B+Tree、TwoPLPasha helper/SCC、PolicyClock、EBR 和
  `MessagePiece` 骨架的 RegionOffset 薄适配；不存在 `kv_shared_*`、pending map、
  timeout、tombstone、后台迁移器或第二份索引/状态机。

## Scan

`Scan(start, end_exclusive, limit)` 不是跨 partition 全局 snapshot。从
`PartitionForKey(start)` 起顺序推进：本地使用 owner locator；远端先 CXL shared
Scan。只有 CXL adjacency 不完整时才向该 partition owner 发送原式 range move-in；
owner 只 move-in，不返回 value。首次 CXL probe 保持 master 原三分支；move-in
后的同参重跑才启用 lower-bound 左边界薄适配（只要求 `next_real`）。owner
already-migrated 再 move-in 时按邻居观测重建当前行 adjacency bit，修复 insert
`clear_adjacent` 留下的洞。请求方重新 probe 后继续当前 range，再进入下一
range。内部最大键 sentinel 只提供 next/right-boundary 锁，永不作为用户结果。

不会混合 partial CXL 与 owner value，不做全分区 k 路归并，也没有
`ScanCertificate`、mutation generation、全局 Scan mutex 或结果缓存。

## 初始化、线程与实验口径

初始化状态只有 `Initializing → owner-ready bitmap → Ready`。attach 必须验证 layout
和完整配置合同，不能观察半初始化 root；没有 clean/dirty checkpoint 或恢复协议。

正式 4VM 口径为每 VM `foreground=4 + demuxer=1`，即 `kv_threads=5`；所有 service
和后台 CPU 都必须披露。demuxer 用原式 `BufferedReader → worker SPSC` CXL-only
骨架，每轮最多 64 条只是 drain 批预算，不是操作 retry 上限。完成 replay 的
foreground worker 在最终 barrier 前继续服务自己的队列；控制线程不会伪装为 worker
poll transport。

正式比较固定 `model_cxl_search_overhead=false`、32B key/value、相同 trace/计时窗口；
结果是 NUMA/ivshmem shared-memory emulation，不得表述为真实 CXL 硬件性能。

## 延迟

固定延迟只允许 RelWithDebInfo、`verbose=false`、`extra_check=false`、foreground
注入和成功 TSC 校准；等待使用 `rdtsc + _mm_pause`，没有时钟或 sleep 回退。
disabled 路径不读 TSC。所有访问经执行型 `mem_access`/atomic wrapper 记录，并只
在 tree/row/Clock/EBR 锁与 pin 的安全结算点支付。

RPC request 在 transport publication 后结束本阶段；cooperative Await 没有 active
foreground scope；peer request 是独立 scope；matching response 之后新建 local
continuation。完整审计和当前验证产物见
[`延迟插入审计报告.md`](延迟插入审计报告.md)。
