# TigonKV 当前对比口径

本文描述当前正式数据面。结果是 NUMA/ivshmem shared-memory emulation，不是真实 CXL
硬件测量；所有对照必须使用各自项目的镜像、配置、二进制和 trace。

## 架构合同

| 项目 | 当前实现 |
|---|---|
| 逻辑表与路由 | `kSingleTableId=0`；连续半开 key range，owner 为配置的 partition owner。 |
| owner-private | owner B+Tree、row/lmeta、Clock tracker 和 retire queue 位于 owner-private SWCC，只由 owner VM 访问。 |
| shared HWCC | shared tree、root、smeta、EBR epoch、transport 和跨 VM 原子元数据。 |
| shared SWCC | migrated tid/valid/value image，只经 TwoPLPasha WriteThrough SCC 读取、写入、flush/invalidate。 |
| 一致性 | 保留 OLC、row/smeta lock、SCC、PolicyClock、EBR、locator/ref/adjacency 和原 wire framing。 |
| 外部操作 | Put/Get/Delete/Scan/CompareExchange/Increment；单 key 线性一致，Busy 在完整 facade 边界重试。 |

不引入第二索引、第二协议状态机、后台搬运器、结果缓存、全局 Scan mutex、pending map、
timeout/tombstone 或额外 ivshmem。业务 backing 只有 `/dev/ivpci0` 对应的一个共享设备。

## 内存和容量

根配置的正式物理划分为 32GiB backing：HWCC 1024MiB、SWCC 31744MiB，shared backing
与 VM compute 位于不同 NUMA 节点。`hw_cc_budget_mb` 是 PolicyClock 动态预算，不能
当作物理容量；静态布局 metadata 和业务 allocator 都从同一份实际区域计算，不能为
已删除模块保留隐藏空间。完整布局见 [内存布局.md](内存布局.md)。

## 唯一硬件模拟

配置位于 `tigon_kv.latency_inject.fixed_latency`，只含六个字段：
`enabled`、`cache_line_bytes`、`swcc_fixed_ns_per_line`、`hwcc_fixed_ns_per_line`、
`foreground_enabled`、`background_enabled`。每次真实访问按覆盖 line 数累加固定延迟，
在安全 scope 出口以校准 TSC busy-wait 结算。

不保留 HWCC/SWCC 访问统计、原子操作计数、远程 cache 状态、命中/未命中延迟过滤、
shared event log、replay、全局序号或模拟统计 schema。SCC 的业务 readable bitmap、
clflush、clwb、writeback 和 invalidate 仍保留；PolicyClock 的 LRU/second-chance 是
生产迁移策略，不是延迟模拟 cache。

前台 KV 操作使用 foreground scope；CXL receive 和 RPC dispatch 使用独立 background
scope。disabled 时不读 TSC、不建 TLS、不访问共享状态。`LATENCY_SIM_COMPILE_OFF=ON`（独立 build）
提供编译期关闭对照。

## 正式实验

正式 VM 口径为 4VM，每 VM `foreground=4 + demuxer=1`；报告必须披露 service/background
CPU、NUMA、物理 HWCC/SWCC 容量、PolicyClock budget、构建类型、固定延迟参数、trace、
warmup 和计时窗口。默认先跑一轮无延迟代表性 trace，再跑小型非零 fixed-latency canary；
不以历史实验目录或旧 VM backing 证明当前代码。

TigonKV 原生范围 Scan 与其它项目的单全局树结构不同，Workload E 必须单独标明，不能
声称两者工作量天然等价。YCSB 入口和 clean-VM 规则见 [YCSB指南.md](YCSB指南.md)。
