# TigonKV 当前对比口径

本页是 `my-work` 上 TigonKV 与 `../cxlkv` 做实验时的当前真值。下一步目标与
施工约束见 `partition优化方案.md`；其中尚未落地的内容不能覆盖本页的当前
数据面描述，旧恢复记录或原始 benchmark 源码也不能覆盖本页。

## 目标架构合同（已冻结；按方案落地前数据路径仍见下表）

来源：`partition优化方案.md` 与 `kSingleTableId`。落地完成前，下表「数据路径」
仍描述现版行为；不得把目标合同误报成已实现。原始 Tigon 代码以当前 `master`
分支头部为准。

| 合同项 | 冻结内容 |
|---|---|
| 单逻辑表 | `kSingleTableId=0`；外部API无`table_id`，目标内部wire保留原`MessagePiece.table_id`并固定为0；partition ≠ 多表 |
| 路由 | `tigon_kv.partitioning.ranges` 的连续半开范围；`owner = partition%vm` |
| 单 key | Put/Get/Delete/CAS/Increment 线性一致 |
| Scan（现版） | 非全局 snapshot；从起始范围起顺序 CXL-first + adjacency；仅不完整时单 partition `kScanMigrate`；正式热路径无 certificate 或 k 路归并 |
| 进程 DRAM（现版） | Clock tracker、private root 均在 owner-private SWCC（`PrivateRow` 链表 + arena control）；`migrated_key_count` O(1)；`allocator_local_dram_bytes=0` |
| 保留适配 | 定长 KV、RegionOffset、双区域 allocator、Busy 重试、Clock ITable 薄适配；不新增第二权威源 |

## 数据路径

| 操作/对象 | 当前路径 |
|---|---|
| owner-private row/tree | SWCC owner arena；仅 owner 访问，不走 SCC |
| shared B+Tree、row metadata、根、EBR global/local epoch、transport | HWCC |
| EBR retire queue（现版） | 进程 DRAM；目标按方案迁入 owner-private SWCC |
| migrated payload | shared SWCC；WriteThrough SCC + 行锁 |
| 非 owner GET | shared hit 就地 SCC（`host_id=requester`）；miss 发 `kMigrate`，owner move-in 后 ack，requester 再走 shared |
| 非 owner PUT | shared hit 就地 SCC；existing shared miss 先 `kMigrate`，随后由 requester shared 写；true create 才 owner insert/move-in ack，随后 requester shared 写 |
| Delete | owner 权威；非 owner 先 `kMigrate` 建立共享引用，再 Forward owner delete（Busy 只在 `KVStore` 操作边界重试） |
| 分布式 Scan | 从 `PartitionForKey(start)` 起按范围顺序：本地 `ScanOwned`；远端先 `ProbeSharedScanPage`（`scanForUpdate`+adjacency，SCC 读用 requester `host_id`）；不完整才对当前 partition 发 `kScanMigrate`（owner 只 move-in，**先 ack 再** Clock move-out）；重探针完成当前范围才进入下一范围。无 ScanCertificate / 混源 / k 路归并 |

PrivateRow 在 move-in 后保留权威定位壳，`migrated_smeta_off` 指向 HWCC metadata；
payload 位于 shared SWCC。move-out 摘 shared index、恢复 private value，并按 EBR
回收 metadata/payload。根、SCC bitmap/latch、migration sequence 与 transport
控制原子都按 HWCC 访问会计；payload memcpy/flush 按 SWCC 访问会计。

Scan 不把部分 CXL 行与 owner RPC 行作为并列权威源。本地 partition 走
`ScanOwned`（private locator）；远端 partition 先 CXL `ProbeSharedScanPage`
（`scanForUpdate` + 原 next/prev adjacency；leaf 写锁下禁止再 `lookup`）。
仅 CXL 空/邻接不完整时对该 partition 发 `kScanMigrate`：owner 按 key-only
选取并 `EnsureInShared`，响应只带 `exhausted`/`no_predecessor`，不回 value；
请求方重探针后只继续当前范围。正式热路径已删除 ScanCertificate /
mutation-generation 全局证书。`exhausted` 与 `no_predecessor` 是任意
start_key 下的必要 EOF/开左边界提示，须披露。

## 初始化、线程和 CPU 口径

reset VM 在 allocator 初始化后保持 layout=`kInitializing`，直到 transport、EBR、
全部 partition roots 就绪并已启动本机 inbound demuxer 后才发布 `kDirty` ready。
attach VM 对匹配 layout 等待该屏障，不能观察半初始化根。
layout v11 的 clean 只能由 collective checkpoint 产生：所有 VM flush 自己的
owner-private arena 与 owned SWCC ranges 并发布 ready epoch；node0 等齐后才
发布 `kClean`/`clean_epoch`，其余 VM 等待。mutation 重新标 `kDirty`，
dirty attach hard-fail。

allocator 固定 metadata 按物理池拆为 HWCC/SWCC 两域；physical used 分别等于
本池固定 metadata 加动态对象，owner arena 的 reserved capacity 不冒充 used。
迁移 HWCC budget 按 owner 分开计：`physical_hwcc_capacity_bytes` 是物理区大小，
`owner_migration_dynamic_budget_bytes` 是 Open 在测完静态域后
对 `(hw_cc_budget_mb*1MiB - EBR)` 与 `physical − static` 取 min 再按
`vm_count` 均分的结果，二者不得混报。正式对比配置可将
`hw_cc_budget_mb` 设为与 `hwcc.size_mb` 相同的 **1024**（全物理额度）；
layout/transport/EBR 静态占用由 Open **自动 clamp**，不要求 JSONC 预先抠
headroom。Clock 仅按原始 HWCC budget gate 驱逐；不以 shared payload 水位伪造
预算。`active_shared_rows` 来自 owner-private arena 的 `migrated_key_count`（O(1)）。
Clock second chance 只有一个 HWCC
atomic policy state。worker 退出会移交 EBR retire tail，不能遗失待回收对象。

每 VM 的正式 CPU 口径是：

```text
foreground = foreground_worker_count_per_vm
demuxer     = 1
kv_threads  = foreground + 1
```

启用 affinity 时 foreground 与 demuxer 使用不同 allowed CPU。demuxer 保留原
Tigon IncomingDispatcher 的“专用接收入环线程”形态，独占本机 inbound MPSC，
但当前 deferred FIFO 并非逐结构同构。foreground 每次最多批取 64 个 deferred
请求。matching response 对对应 pending `notify_one`；任一 deferred request
到达还会唤醒 pending forwarder，使其协作 service。报告不得只写 foreground
worker 数。`DumpStats` 含 `deferred_queue_peak`（§4.7 服务性）以及
`scan_ops` / `scan_partition_probes` / `scan_migrate_rpcs` /
`scan_owner_rows_movein_attempted` / `scan_rows_returned`（§13）；Scan 热路径
只做 TLS 递增，边界一次聚合，不为每结果行加共享原子。
心跳复用已有 runner 编排主线程，不是额外 KV service thread；它在 replay
期间仅按低频读取批量发布的 progress counter。正式报告需说明这个并发控制线程，
心跳行本身不参与吞吐汇总。

## 构建、延迟与实验声明

维护的比较构建入口是 `tigonkv`、`e2e_trace_runner`、focused tests 和 guest E2E
scripts。原始 transaction executor/benchmark 源保留用于对照，但不链接进 TigonKV
比较目标，因而 legacy `TwoPLPashaExecutor::update/remote_update` 不在比较路径。

正式延迟注入只允许 RelWithDebInfo、verbose off、extra_check off、
`foreground_enabled=true`、成功的 x86 TSC 校准和 TSC `_mm_pause`
自旋；enabled 模式没有 `sleep_for` 回退。运行时只接受唯一的
`tigon_kv.latency_inject`，19 个字段全部显式、严格类型；缺失、重复或跨
对象错位均 hard fail，不读取独立 policy JSON 作为第二权威。stage marker 由
`TIGONKV_E2E_STAGE_MARKERS` 控制；`E2E_TRACE_HEARTBEAT` 由
`TIGONKV_E2E_TRACE_HEARTBEAT_SEC` 独立控制（0 关闭）。guest 使用 5 秒心跳，
只作 stall 活性观测；汇总器忽略心跳行，也不需要打开 verbose。心跳在 replay
期间运行，其批量 progress 发布开销属于受测执行，不能表述成“计时窗口外”。

CLWB/writeback 与 CLFLUSH/invalidate 均支付完整 flush 成本，只有 invalidate
驱逐 per-thread LRU tag。当前 cxlkv 未镜像该修正前，跨仓对比只使用
`cache_model=none`；LRU 注入结果不得混比。

所有结果只能表述为 NUMA-based CXL shared-memory emulation / software
latency-injected results，而非真实 CXL 硬件性能。相同 trace、fixed key/value、
foreground 数、额外 demuxer CPU、HWCC/SWCC 容量、延迟参数和计时窗口必须与
`../cxlkv` 一并报告。
