# TigonKV 当前验证清单

本页只记录当前源码需要通过的最小验证，不依赖外部记录、构建身份或配对结果。所有
命令在 Tigon 自己的 `my-work` checkout 执行；三个消费者最终同步使用同一个
latency_sim SHA。

## 构建矩阵

```text
build-debug-ninja-clang18-co_off-check_on  Debug+O0+NDEBUG, compile-on, latencycheck
build-debug-ninja-clang18-co_off          Debug, compile-on
build-debug-ninja-clang18-co_on           Debug, compile-off
build-relwithdebinfo-ninja-clang18-co_off RelWithDebInfo, compile-on
build-relwithdebinfo-ninja-clang18-co_on  RelWithDebInfo, compile-off
```

每个目录都必须由 Ninja 配置，且分别传入准确的
`-DLATENCY_SIM_COMPILE_OFF=OFF/ON`。Debug 使用 `-O0 -g3`、无 LTO；RelWithDebInfo 使用
`-O3 -march=native -g3 -flto=full`。默认配置中的三个固定延迟字段均为 `0`。

普通变体至少执行：

```bash
ctest --test-dir <build-dir> -E '^e2e_' --output-on-failure -j1
```

普通测试包括配置解析和拒绝、`latency_modes_test`、allocator/B-tree/ring 功能、SCC
协议、Remote Delete 协议和脚本 source-state 检查。Remote Delete 测试必须在 compile-on
和 compile-off 都真实运行，不能因为宏而变成空测试；它覆盖真实 owner claim、取消与
回滚、同一 row ABA、Busy/Rejected/Failed 终态、迟到/重复 response、超时和 tag 上限。

`hardware_sim_disabled_benchmark` 只做小型真实路径 smoke，compile-on+0ns 与 compile-off
使用分别构建的二进制；不保存跨项目的 VM、trace、backing 或结果。

## E2E08 checker 单轮

推荐入口：

```bash
bash scripts/e2e/run_vm_e2e.sh --execute --profile latencycheck --suite 08 \
  --config experiment_config.jsonc --rounds 1 --record-count 4096 \
  --out-dir exp_data/e2e08_runs/<new-run>
```

入口自动验证 Debug/O0/NDEBUG、compile-on/checker-on、`extra_check=false`、本项目
latencycheck prefix 和 `VALGRIND_LIB`，不接受手工 runner/pool/build 参数。

在 host 上先停止其它项目的 QEMU、ivshmem 和残留 PID，再用本仓库自己的
`tigonkv_kill_vms.sh`、`tigonkv_init_vms.sh`、`tigonkv_check_vms.sh` 创建干净的四 VM
实例。三个项目有意共享 `/mnt/xz_vm_storage`、`/mnt/xz_shared_mem/ivshmem_shared_mem`
和 `10022..10025`，由 `/run/lock/shared-vm-e2e-resources.lock` 保证串行；只使用当前
`image/root.img`、当前 runner/二进制、当前配置和当前项目重新初始化的 backing 内容。

每次 invocation 只运行一次本项目独立 VM 上的 Debug+O0/NDEBUG E2E08 latencycheck；
修复后使用新的独立输出目录继续验证。上述共享路径和端口只表示物理资源互斥，
不表示复用其它项目的镜像、VM writable 内容、prepared state、pool 或工具目录；切换项目
必须先停止并清理上一个项目，再用当前项目的镜像和脚本重建。

历史代表性结果（不能替代当前新建输出目录）：`CHECKER_WORKING_MISMATCH_FOUND`，运行目录为
`exp_data/e2e08_runs/v11_checker_20260808_tigon_final_retry2/`。VM0 的 init 在
`checkpoint=1` 首次报告 `class=background generation=3 status=MISSING domain=HWCC
logical=invalid actual=WRITE`，随后产生 `LATENCYCHECK_CHECKPOINT_FAIL`/SIGABRT；摘要为
`target_accesses=2279282 expectations=6 matched=6 missing_windows=1 unconsumed_windows=0 checkpoints=1 sticky_error=true`。
`workflow.log` 记录 `TIGONKV_FAIL_FAST ... first_vm=0 first_exit=1 cleanup_status=0`，
VM1–3 随即收到终止信号；该历史业务插桩缺口不构成当前验收结论。

两次前置同步曾被严格判为 harness invalid：专用前缀的远端运行需要显式
`VALGRIND_LIB=.../libexec/valgrind`，且版本探针必须指定 `--tool=latencycheck`；修复
后才执行上面的唯一有效 checker 工作流。

## 当前版本证据（2026-08-12）

当前三个消费者统一使用 latency_sim `e9b976c7fd205b266610966f60eb7354d166d637`。
在该 SHA 和当前 Tigon2 source fingerprint
`8f17b92579b6ad3a4623f3aea0402c439a226300ffd2f3b5bc2e2e0e5f86ec68` 下，独立的
4096-record `CHECK_CLEAN` 证据为：

- `exp_data/e2e08_runs/tigon2_clean_2_20260812T103000Z/`
- `exp_data/e2e08_runs/tigon2_clean_3_20260812T104000Z/`
- `exp_data/e2e08_runs/tigon2_clean_4_20260812T105000Z/`
- `exp_data/e2e08_runs/tigon2_clean_5_20260812T110000Z/`

较大规模证据为 `exp_data/e2e08_runs/tigon2_clean_20000_20260812T111000Z/`。
这些 `run_result.json` 均记录 Debug/O0/NDEBUG、compile-on/checker-on、
`extra_check=false`、`pool_reset_count=1`、`runner_exit_code=0` 和
`cleanup_status=verified`。旧 v11 目录只用于历史首错诊断，不参与当前连续 clean 或
较大规模结论；生产/fixed-latency 性能配对证据另行记录，不能由 checker 结果替代。
