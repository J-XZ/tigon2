# TigonKV 当前验证清单

本页只记录当前源码需要通过的最小验证，不依赖外部记录、构建身份或配对结果。所有
命令在 Tigon 自己的 `my-work` checkout 执行；公共依赖必须是
`a15cc4ee4d6d79057dc8f44f2f3467a3158de637`。

## 构建矩阵

```text
build-debug-ninja-clang18-co_off-check_on  Debug+O0, compile-on, latencycheck
build-debug-ninja-clang18-co_off          Debug, compile-on
build-debug-ninja-clang18-co_on           Debug, compile-off
build-relwithdebinfo-ninja-clang18-co_off RelWithDebInfo, compile-on
build-relwithdebinfo-ninja-clang18-co_on  RelWithDebInfo, compile-off
```

每个目录都必须由 Ninja 配置，且分别传入准确的
`-DLATENCY_SIM_COMPILE_OFF=OFF/ON`。Debug 使用 `-O0 -g3`、无 LTO；RelWithDebInfo 使用
`-O3 -march=native -g3 -flto=full`。默认配置中的三个固定延迟字段均为 `0`。

普通变体至少执行：

```bash
ctest --test-dir <build-dir> -E '^e2e_' --output-on-failure -j1
```

普通测试包括配置解析和拒绝、`latency_modes_test`、allocator/B-tree/ring 功能、SCC
协议、Remote Delete 协议和脚本 source-state 检查。Remote Delete 测试必须在 compile-on
和 compile-off 都真实运行，不能因为宏而变成空测试；它覆盖真实 owner claim、取消与
回滚、同一 row ABA、Busy/Rejected/Failed 终态、迟到/重复 response、超时和 tag 上限。

`hardware_sim_disabled_benchmark` 只做小型真实路径 smoke，compile-on+0ns 与 compile-off
使用分别构建的二进制；不保存跨项目的 VM、trace、backing 或结果。

## E2E08 checker 单轮

在 host 上先停止其它项目的 QEMU、ivshmem 和残留 PID，再用本仓库自己的
`tigonkv_kill_vms.sh`、`tigonkv_init_vms.sh`、`tigonkv_check_vms.sh` 创建干净的四 VM
实例。只使用当前 `image/root.img`、当前 runner/二进制、当前配置和新建 backing。

本轮只运行一次本项目独立 VM 上的 Debug+O0 E2E08 latencycheck，不追加第二轮去修复
业务插桩缺口。使用专属 `/mnt/tigon2_vm_storage`、`/mnt/tigon2_shared_mem`、
`11022..11025` 和本地 checker 前缀；不复用其它项目的镜像、VM、backing 或工具目录。

结果：`CHECKER_WORKING_MISMATCH_FOUND`。VM0 在 checkpoint=1 首次报告
`status=MISSING`，随后 `LATENCYCHECK_CHECKPOINT_FAIL`/SIGABRT；摘要为
`target_accesses=2279282 expectations=6 matched=6 missing_windows=1 unconsumed_windows=0 sticky_error=true`。
这证明自动 expectation、checkpoint 最终一致性对账和首错快速失败机制均实际运行；该
业务插桩缺口不在本轮继续修复范围内。
