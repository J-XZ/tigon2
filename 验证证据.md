# 当前源码验证证据

本文件只记录当前工作树的实际构建与验证，不将旧日志或旧 VM 产物表述为当前结果。

## 源码与二进制身份

- 原始基准：`master` = `ccd567a50116b7bada06df71a3bf0a07c424572e`。
- 最终验收提交（2026-07-31）：`my-work` HEAD = `988d2cc03e759b137f9fb7c7a4609702e7d09596`
  （`fix: restore B+Tree leaf split backward link`）。后续证据全部绑定该提交。
- RelWithDebInfo `e2e_trace_runner`：
  `44ffc64e51f7459d2778985ee5eb870f687d526ab63d364b4f5e2d1874777367`。
- 正式根配置 `experiment_config.jsonc`：
  `0d473a78665ae0c9acb9ebe35daa396a4ca7f0a8443887504982145bd62a5186`。
- 最终验收证据目录：
  `/mnt/xz_vm_storage/tigon2-final-acceptance-20260731T134818Z/`。
- 说明：旧版聚合摘要（6f9ae437…）采用的方法细节已不可复现，统一以上述
  提交/二进制/配置 SHA-256 作为身份。

## 最小 Debug 验证

- `cmake --build build-debug -j2 --target tigonkv kv_partition_test`：通过。
- `build-debug/kv_partition_test`：通过。该测试仅检查固定 32B 的
  owner-private `TableBTreeOLC` layout/callback 与尾零 key；不承担并发或端到端语义。
- `git diff --check`：通过。

## 隔离 4VM 端到端验证

本轮在启动前执行 `tigonkv_kill_vms.sh --allow-state-change`，启动后由
`tigonkv_check_vms.sh` 确认四个 guest、SSH、ivshmem 和同一 backing 正常；运行时没有
`kv_engine_test`、CTest 或其他 trace runner。结束后再次执行 kill，并确认 QEMU、runner
与 `/mnt/xz_shared_mem` 挂载均不存在。

- 输出目录：[exp_data/ycsb_e_helper_unified_20260729](/root/code/tigon2/exp_data/ycsb_e_helper_unified_20260729)。
- 配置 SHA-256：`e31237224a29ddaadcef3a9f9b2406440f6f72d35443a052841ec82d9303006c`；
  trace 汇总 SHA-256：`9c87cfeef106eaaa150e274bda5b2c32d99c3d44cbd2ed05b8b04b3a62ab7693`。
- 物理 shared backing 32GiB，HWCC 1GiB，shared SWCC 31GiB；未改变总 HWCC 容量。
  VM RAM 2GiB、VM NUMA 0、shared NUMA 1；每 VM `foreground=4 + demuxer=1`。
- RelWithDebInfo、延迟关闭、32B key/value；`load=100000`、Workload-E `run=500`、四 VM。
- load（181.798s）和 run（18.076s）均有四个独立
  `e2e_trace_runner[nodeN]: passed.` marker。run 合计 473 次 Scan、23,236 行返回、787 次
  partition probe 与 313 次 `scan_migrate_rpcs`，覆盖 range move-in 后的 CXL adjacency
  路径。

本项目采用实验室快速验证标准：该隔离 E2E 是公开 KV 语义的主要证据；不以长压、
allocator 耗尽、崩溃子进程或全量单测代替它。

## C2/M 停滞定位（2026-07-30）

以下不是通过证据，只记录一次为定位停滞而执行的 Debug 4VM 复现。启动前后均确认
恰好四台 QEMU、shared NUMA=1、VM NUMA=0、同一
`/mnt/xz_shared_mem/ivshmem_shared_mem` backing，结束后已停止 runner/QEMU 并卸载
backing。

- 复现日志：[lmeta-lockdiag-run](/mnt/xz_vm_storage/tigon2-stageM-20260730T083000Z/lmeta-lockdiag-run)。
  `load` 通过；`run Workload-E` 在合计 256 个操作后 60 秒无进展，故按脚本规则失败，
  不能记为 E2E pass。
- 临时诊断 runner SHA-256：
  `efc5660d1fa173b799307314db089b28b9327d70cb3568440eb1c2642dd255ad`。
  诊断代码已从工作树移除，未进入生产源码。
- GDB 栈：[gdb snapshot](/mnt/xz_vm_storage/tigon2-stageM-20260730T083000Z/gdb-lock-run-logs/gdb-vm2-live.txt)。VM2 的一个 worker 持有
  `OffsetClockTracker::lock` 后进入 `PolicyClock::move_row_in` →
  `MoveInForMigrationManager`；另一 worker 在同一 Clock lock 等待，其余 worker
  在 `AwaitResponse`，demuxer 仍在 `RunCxlIncomingLoop`。
- 一次可移除的 lmeta owner/锁字诊断没有输出 `TIGONKV_LMETA_WAIT`；因此本轮没有证据
  证明等待来自遗留 lmeta 锁或 lmeta 锁未释放。此前 GDB 轮次曾在
  `MoveInForMigrationManager` 的 `next_lmeta` 等待点取到栈，但没有观察到同进程持有者。
- 随后又覆盖了 adjacency helper 的直接 lmeta lock 和 `TwoPLPashaMetadataShared::lock`
  CAS 自旋；同样没有出现 `TIGONKV_MIGRATION_LOCK_WAIT`。这一次也复现为 256 操作后
  停滞，故 shared smeta/adjacency lmeta 的持续锁占用仍没有证据支持。该临时 runner
  SHA-256 为 `db47d3655d5431d390c96c03411c3aab56f6359bb269c7201350caf053837fc6`。

后续更严格的结构化诊断推翻了“Clock/B+Tree 固定锁点停滞”这一暂定范围：

- fresh 4VM、完整 load、每 worker 只保留首个 run 操作的最小 trace 通过；每 worker
  前 16 个操作会受较快 worker 提前退出、对应 mailbox 无人消费的诊断夹具效应影响，
  因而未把该前缀结果当作 production 根因。
- 完整 627-op/worker trace 的三次间隔 GDB 快照位于
  [full-multisnap-gdb](/mnt/xz_vm_storage/tigon2-stageM-20260730T083000Z/full-multisnap-gdb)。
  活跃线程在不同快照间持续经过 `move_row_in`、`move_row_out`、Clock、B+Tree 和
  smeta；没有固定在同一锁或同一 key。Debug-only lmeta/smeta owner 检查没有发现
  bad unlock、owner mismatch 或持续锁泄漏。
- 在原 `TwoPLPashaMessageHandler::move_in_scan_range()` 循环临时加入逐范围/逐行
  begin/end（现已全部删除）后，日志
  [scan-row-verbose-run](/mnt/xz_vm_storage/tigon2-stageM-20260730T083000Z/scan-row-verbose-run)
  证明每个 `move_row_in` 都返回，但相同 partition lower-bound 请求被高速重复：
  partition 1 的四种固定请求分别重复约 900--1130 次，partition 2/3 同样达到数百至
  数千次。20 秒内 VM1/2/3 各完成约 6.8--7.1 万次逐行 move-in，却没有完成一个新的
  facade 操作；这是真正的 Busy 活锁，不是 Debug 运行偏慢。
- 正式 range lower boundary
  `user14219342279036479019`、`user18315261750911614162` 和
  `user561762740360889868` 均不在本轮 load trace。owner 的原
  `ITable::scan(min)` 因此从第一个 `key > min` 的真实行开始迁入；该 partition
  首行没有 migrated predecessor，`prev_real=false`。requester 按 master 的
  `key == min / size == limit / otherwise` 三分支重扫时进入 `otherwise`，永久再次
  要求 `prev_real && next_real`，于是第二次 probe 返回 Busy，facade 重试完整 Scan
  并再次发送同一 move-in。

当前直接根因是“cold、start 不存在（包括跨 partition lower boundary）”与 exact-min
adjacency predicate 的组合。`partition优化方案.md` §3.9.5 同时要求 lower-bound
start 不存在可用、owner 只迁入 `[min,max] + first > max`、requester 以同一参数重跑；
`下一步修改.md` K1 又明确禁止 resolved-min 并要求保留 exact `key == min` predicate。
而 master 的 `move_in_scan_range` 不迁入 predecessor。三条约束在该输入上无法同时
满足，源码和上述计数已给出反例。按固定顺序当前仍先完成 C2；到 K1 前必须先以这份
源码证据修正唯一设计合同，不能通过改锁顺序、timeout、sleep、额外 Scan 状态或反复
move-in 掩盖。C2 仍未完成，M 也未通过。

## C2 B+Tree 主体复核（2026-07-30）

- 本阶段重新记录 `master=ccd567a50116b7bada06df71a3bf0a07c424572e`，并逐段对照
  `git show master:common/btree_olc_cxl/BTreeOLC_CXL.h` 与当前
  `common/btree_olc/BTreeOLC.h` 的原 adjacent primitive。
- 已撤销三处不属于 RegionOffset/allocator/EBR/root/domain-access 薄适配的
  控制流改写：`NodeLock::iteratorEnter` 恢复原先的单次 load/locked 检查后
  `fetch_add`；Scan 不再追加 iteratorEnter 后的平行 restart 分支；leaf split
  不再无锁回写旧后继 leaf 的 `pre_`。leaf merge 中 master 已有的后继 `pre_`
  维护保持不变。
- `remove_and_process_adjacent_tuples` 已恢复原 terminal
  `assert(result_saved); assert(saved_success); return saved_success;`。原
  `common/btree_olc/BTreeOLC.h` 同一算法有相同断言；`下一步修改.md` J1 要求
  Busy/Missing 必须在进入 adjacent callback 前返回，因此不得把 callback 的 false
  扩展成新的正常删除结果。
- `PersistentOffset<void>` 现有编译期槽宽断言，保持一个 `RegionOffset` 槽；leaf
  `pre_/next_` 与 inner child 仍以该槽计算 `maxEntries`，没有改变 page size/fanout。
  persistent 节点不含 `boost::offset_ptr`；解析只经 tree allocation binding。
- 临时 lmeta/smeta/range verbose 诊断已经全部从 production tree/protocol 删除，
  `rg -n 'TIGONKV_(LMETA|SMETA|SCAN_MOVE)' kv protocol common` 无命中。
- 本轮修改前的 C focused gate 已通过：

  ```text
  ctest --test-dir build-debug -j1 --output-on-failure \
    -R '^(kv_startup_test|region_allocator_test|cxl_ebr_test|btree_binding_test)$'
  4/4 passed, 2.46s
  ```

  C1/C3--C6 仍在同一阶段静态终检中；在其余 C 阶段修改收敛后，必须按唯一 gate
  再执行一次，不能把本次提前 gate 当作阶段完成。

## C4 静态阻塞证据（2026-07-30）

`DualRegionAllocator::ResolveSharedPayload()` 当前若 `expected_owner` 的 payload
range 不命中，会扫描并接受任意其他 owner 的 shared-SWCC arena。这直接违反
`下一步修改.md` C4 的“owner 不匹配必须拒绝”以及 lmeta cached payload 以 row owner
作为唯一 resolver binding 的要求。现有 `region_allocator_test` 的跨进程 case正确地
以 `expected_owner=0` 让 VM1读取 VM0 的共享 payload；它并不要求、也不能证明传入
错误 owner 后仍应成功。

生产的 `TwoPLPashaMetadataShared::get_scc_data()` 仍通过
`CXLMemory::shared_payload_offset_to_pointer(offset,bytes)`，该 wrapper只传当前进程
`owner_shard_`。因此在删除 fallback 前必须先逐调用点把“payload的 owner（由 smeta
所在 dynamic-HWCC arena/分区 owner确定）”传入，而不能直接删 fallback 后用远端 Scan
或 Move-in 试错。本项是 C4 的未完成直接根因；尚未修改 Scan、SCC 状态机或测试调度。

## 阶段 C 完成证据（2026-07-30）

- 本阶段基准仍为
  `master=ccd567a50116b7bada06df71a3bf0a07c424572e`；完成时当前
  `common core protocol kv` digest 为
  `2b2874ec8c9f0ffbe9757fe6b4e2b8c8a5d7fbec40fa0cb1e0efe8f9aa23ab10`，
  相对 master 的生产路径清单 digest 为
  `65832b93dc499702e2e5df4dc7bff66de6a5c2d5ad0c89cc1cb052ca17bcd8aa`。
- C2 恢复 master 的 iterator/restart、split backlink 和 adjacent callback
  terminal contract；C3 的 private root 直接绑定 owner-private atomic slot，
  split/merge/reattach 都由同一 slot 验证。
- C4 已以源码证据修正 SCC payload resolver 合同：master
  `TwoPLPashaMetadataShared::get_scc_data()` 仅携带 whole-pool offset，
  因而 `ResolveSharedPayload(offset, bytes)` 必须且仅能匹配一个 immutable
  shared-payload descriptor，不能把当前 VM owner 当作路由条件。owner-private 与
  dynamic-HWCC 仍严格校验 owner；owner-private 还必须等于本进程
  `bound_owner_shard`。unknown static root 已拒绝，非 owner `KVPartition` 只构造
  shared handle，不再解析 owner-private arena。没有新增 persistent state、cache、
  registry 或 Scan 状态。
- 为定位 C4 的边界问题，最小 `btree_binding_test` 在严格 owner 断言后失败；GDB
  栈定位为 fixture 在绑定 owner 0 时以 owner 1 分配 dynamic-HWCC tree，随后又以
  owner 1 解析 private root。fixture 已在每次 owner allocator/private-tree 操作前
  切换到对应 owner；production 则采用上项 owner-only private handle，未放宽 resolver。
- C5 静态调用图确认六个 engine API 在 `EbrOperationScope` 后、首次 tree/row
  访问前调用 `PollTransport()`，facade 仅在 Busy 完整 attempt 之间服务 transport；
  C6 的 external worker meta handoff 保留 `last_freed_epoch`，TLS 仅为 non-owning
  binding。
- 静态检查通过：`git diff --check`；没有三参数
  `ResolveSharedPayload` 调用、没有 production whole-pool row resolver 旁路、没有
  `PersistPrivateRootIfChanged`/root mirror 或 persistent `offset_ptr`。
- 阶段唯一 gate（未并行、无 QEMU/4VM）通过：

  ```text
  ctest --test-dir build-debug -j1 --output-on-failure \
    -R '^(kv_startup_test|region_allocator_test|cxl_ebr_test|btree_binding_test)$'
  4/4 passed, 2.13s
  ```

  对应二进制 SHA-256：`kv_startup_test`
  `838be000e40fbbc8d2080544bc9aefb1d8cf8beedabdf5f31df0921a235fc9f0`，
  `region_allocator_test`
  `a7e6a0cc6f95ba198688e55a84e3e863f39385828d3cb76080863e392059b5a2`，
  `cxl_ebr_test`
  `1f95e6d0c65b681bebbc9ad2508c2f38116a7b9b11cc6baeee0c50a0c0ae919c`，
  `btree_binding_test`
  `034085f5df784b4a99b34bfe242cc41b73cfbd602e1429053135f2c82e0c7670`。

此前“C2/C4 未完成”的记录仅是当时诊断快照；本节以当前源码、当前 gate 与 GDB
栈为准。Scan cold lower-bound Busy 活锁没有在 C 阶段修改，仍严格留待 K1 的合同修正。

## 阶段 D 完成证据（2026-07-30）

- 本阶段重新记录
  `master=ccd567a50116b7bada06df71a3bf0a07c424572e`，当前
  `HEAD=e7a17282f109fcd80ae13d3d8fc02a1c863f675d`。D 的相关源码摘要为：
  `protocol/TwoPLPasha/TwoPLPashaHelper.h`
  `6fd1627c48c6360f8a64f8d2b39d39604a6948fced805f9311b33ba9940890e7`；
  `kv/engine/kv_partition.cpp`
  `78a19922d7ff74e88fdd694819423b445081624bd98df3c8712c308c985d07e2`；
  `kv/engine/kv_types_layout.h`
  `4b3f51337718e842f30dd0e877aa4a20cdb569bf7521fee8d4efe565bb6c3d7e`。
- D1：legacy 与 offset-KV 都实际经过同一 `TwoPLPashaHelper` 的 B+Tree
  insert/remove callback 模板、adjacent migrated-bit 更新体、shared valid
  publish、move-in/move-out 和 shared delete primitive；各调用点仅保留
  `RegionOffset`/瞬时 VA resolver、固定 value bytes 或 owner successor-lock
  输出适配。没有保留五个被禁止的 KV-only 结果类型
  `KvSharedResult`、`KvDeleteSharedResult`、`KvMoveOutSharedResult`、
  `KvMoveInPublishResult`、`KvRemoteDeletePrepareResult`。
- D2：正式 TwoPLPasha move-out 的 `DCHECK` 已是比较而不是赋值；没有把该
  Debug-only 原 bug 扩大为新的 Release 分支或结果状态。
- D3：owner lmeta 仍是 storage-parameterized 的原主体，legacy pointer alias
  和 KV RegionOffset alias 保持相同自然布局、锁和状态字段；没有独立
  `PrivateMetadataLocal` struct、命名 padding 或第二份 lock/unlock。
- 先重新构建本轮二进制，再串行执行阶段唯一 gate；执行前后 `ps` 均未发现
  CTest、QEMU、guest runner 或测试进程：

  ```text
  ctest --test-dir build-debug -j1 --output-on-failure \
    -R '^(scc_protocol_test|kv_shared_protocol_test)$'
  2/2 passed, 0.08s
  ```

  对应二进制 SHA-256：`scc_protocol_test`
  `ebf07e49203a22b9eb3e18f92627df2d39fe5c0d0150bf65fccd54b8755b0adc`，
  `kv_shared_protocol_test`
  `aa7ef536ec000008b5c6f37bf583c88faa9c95da33702621ec69f0b8c7e4bad5`。
- `git diff --check`通过。K1 的 Scan cold lower-bound Busy 活锁尚未修改；它
  归属后续 K 阶段，不能因本 D gate 通过而视为已解决。

## 阶段 E 完成证据（2026-07-30）

- 本阶段重新记录
  `master=ccd567a50116b7bada06df71a3bf0a07c424572e`，当前
  `HEAD=e7a17282f109fcd80ae13d3d8fc02a1c863f675d`。E 相关源码摘要：
  `core/Table.h` `a4adc7022a39e925ac7affb8520c9f612ba04a00048eb065c18073d71c536553`，
  `core/CXLTable.h` `66faff922896c61e2715e73a1db0e4cbf38a02cd451d031179f7db05364e79bd`，
  `kv/engine/kv_migration.{h,cpp}`
  `925589ce03425af5785d2cb72358124d751e7a4b67219ed541d462262490a567` /
  `cc1357a578cadc84967b272102775bacc2321510c62a7d09012431b1baa4d120`。
- owner table 只实例化 `core/Table.h::TableBTreeOLC` 的原主体：KV 通过
  `RegionOffsetRowStorage` 提供 owner-private RegionOffset、allocator/EBR 和
  临时 meta/data view；`KvPartitionTable` 仅为该实例提供固定 table/partition
  binding。shared table同样使用原 `CXLTableBTreeOLC` 及 RegionOffset reference
  policy；没有第二个完整 table class、裸 shared VA offset 旁路、旧
  `LockNeighborhood`/`BreakAdjacencyLocked`/`RefreshAdjacencyLocked` 或
  `lookupAdjacent` 主体。
- 原 table 的 current-row callback 参数修正为真实 `cur_data` view；insert
  duplicate 的未发布 lmeta/ValueStruct由 storage policy 直接释放，成功
  remove只在 adjacent callback成功后由同一 policy进入 EBR。
- E2 已把两段 owner-private 分配从 `KVPartition::AllocateValue` 机械移入
  `RegionOffsetRowStorage::AllocateAndConstruct`：lmeta构造后仅在第二段
  ValueStruct allocation 抛出 `bad_alloc` 时直接 free 未发布 lmeta，随后原样
  抛出；没有 allocation transaction、rollback list 或第二状态。
- 执行前后 `ps` 未发现 CTest、QEMU、guest runner或测试残留。先重建二进制后，按
  阶段唯一顺序串行执行：

  ```text
  ctest --test-dir build-debug -j1 --output-on-failure -R '^btree_binding_test$'
  1/1 passed, 0.19s
  build-debug/kv_partition_test --focused-stage=E
  passed, 0.00s
  ```

  E fixture 的一次确定性第二段 OOM case保留一块仅够 lmeta 的 free slot并耗尽其余
  arena；断言失败后 owner-private used不变、tree无该 key，释放填充块后同 key 插入
  成功。二进制 SHA-256：`btree_binding_test`
  `034085f5df784b4a99b34bfe242cc41b73cfbd602e1429053135f2c82e0c7670`，
  `kv_partition_test`
  `ef6d80a4fbf50d13956d328c56798517e39c3f5f8216dc55f406766a8bcd1e03`。
- `git diff --check`通过。Scan cold lower-bound Busy 活锁未在 E 阶段修改，仍留待
  K1。

## 阶段 F 完成证据（2026-07-30）

- 本阶段重新记录
  `master=ccd567a50116b7bada06df71a3bf0a07c424572e`，当前
  `HEAD=e7a17282f109fcd80ae13d3d8fc02a1c863f675d`。源码摘要：
  `protocol/Pasha/PolicyClock.{h,cpp}`
  `937834749ddf878fd2198429bb5c61ca2d43807bf420992141a541a82e2fd030` /
  `6dd2f099ca2961a5191c3e314c28521bec30048c5be378eb36a04cb5f0c49470`；
  `kv/engine/kv_partition.cpp`
  `11b45e60ab3bf49558698fde6c90596cb96f45f3694278a6b6d124b7fd8e42ef`。
- PolicyClock 现在拥有唯一 nested `ClockTracker` 主体；KVPartition只提供
  owner-private control/node 的分配、释放和 offset resolve。没有残留独立
  `OffsetClockTracker`、KV tracker 链表算法或进程 DRAM tracker lock。成功 move-in
  在 tracker lock 内用原 callback 已取得的 local row 直接 track，未再进行成功后的
  table search；node allocation失败或非 SUCCESS 均不 track。
- 首次 F gate 的最小失败为 `Clock row has null data`。Debug/GDB 在
  `PolicyClock::ClockTracker::track` 断点证明
  `EnsureInShared` 传入的 tuple 两个字段均为 null；调用栈为
  `EnsureInShared → PolicyClock::move_row_in → ClockTracker::track`。修复为进入
  move-in前恰好一次 `ITable::search`，稳定 miss 直接返回，成功 callback使用该
  tuple；没有恢复 post-publication lookup 或新增状态。
- Policy budget 继续直接读取 bound owner-private
  `TOTAL_HW_CC_USAGE`；静态检查无 `SyncHwCcUsage`、`NoteSharedAccess`、
  `size_total_hw_cc_usage` 或 `set_total_hw_cc_usage`。second chance仍只由原 shared
  lookup的 `get_migrated_row(..., ref=true)` 设置。
- 先重建二进制，GDB诊断后只重复最小 F gate一次；前后 `ps` 均无 CTest、QEMU、runner
  或测试残留：

  ```text
  build-debug/kv_partition_test --focused-stage=F
  passed, 0.00s
  ```

  二进制 SHA-256：`266d01b39be8f9943952e249b4ca87dde75e598e702e3dca680ee8f5e0a9d2bf`。
  `git diff --check`通过；Scan cold lower-bound Busy 活锁仍未修改，留待 K1。

## 阶段 L 完成证据（2026-07-30）

- 本阶段重新记录
  `master=ccd567a50116b7bada06df71a3bf0a07c424572e`，当前
  `HEAD=e7a17282f109fcd80ae13d3d8fc02a1c863f675d`。静态复核确认 facade 的
  `RunWithBusyRetry` 是唯一完整 KV 操作重试点：仅真实 `kBusy` 继续、drain本 worker
  inbox后 yield，没有次数上限、timeout或 sleep；engine/transport 仅返回一次结果。
- 当前工作树已无通用 `EncodeResult`；migration 与 remote-insert 保留各自固定四态
  enum，requester穷尽映射为 Ok/NotFound/Busy/OutOfMemory。删除遗留 bool
  encode/decode wrappers，避免 Missing/AlreadyExists/NoMemory 被压扁为 Busy；encoder
  以各 enum 的显式无 default switch 写入原始0--3字节，未知内部 enum直接 fatal。
  decoder的坏长度或raw>3继续由 transport caller fatal，绝不进入 facade retry。
- `decrement_ref_cnt`/`increment_ref_cnt` 各自在持有 smeta latch下检查
  underflow/overflow，release/abort路径删除重复 ref read；allocator及owner-private
  retire counter的集中减法在`before<delta`时直接 hard-fail，不再抛出可被上层误分类的
  会计异常。源码摘要：`kv/engine/region_allocator.cpp`
  `0394a518a5b85e276f7c5cf957fec401c5dbbe7fbd8889a5454181e4ceaf2e88`，
  `protocol/TwoPLPasha/TwoPLPashaHelper.h`
  `d151d2d1308184b1e9f7b09ea7e45462a6bbb55f1c56054e27506242d69a5d31`，
  `protocol/TwoPLPasha/TwoPLPashaMessage.h`
  `2264760b96ffa6e54211aa562d7ddd22b26194839d1f3f4f18d60669022217a8`。
- 串行重建后执行阶段唯一 gate；前后无 CTest、QEMU、guest runner或 ivshmem：

  ```text
  build-debug/kv_engine_test --focused-stage=L
  passed, 0.00s
  ```

  二进制 SHA-256：`78474a13f74cf08e3af910d6e1669dfa675bb248072e3b43fd940f7e39b221a0`。
  `git diff --check`通过。

## 阶段 K 完成证据（2026-07-30）

- 本阶段重新记录
  `master=ccd567a50116b7bada06df71a3bf0a07c424572e`，当前
  `HEAD=e7a17282f109fcd80ae13d3d8fc02a1c863f675d`。先直接核对
  `git show master:protocol/TwoPLPasha/TwoPLPashaExecutor.h` 的 remote
  `key == min / size == limit / otherwise` predicate，以及
  `git show master:protocol/TwoPLPasha/TwoPLPashaMessage.h` 的 forward-only
  `ITable::scan(min) → move_row_in` 循环。二者在 cold、min不存在时不迁入
  predecessor，无法满足 exact predicate 的`prev_real`要求；此前 Debug 4VM 的重复
  move-in 计数与 GDB 排除了锁停滞，完整反例见本文件开头。
- 因此先修正唯一设计合同 `partition优化方案.md` §3.9.5，并同步封闭清单 K1：每个
  remote fragment 仅在既有结果 vector 为空且首个`key >= min`时把该行作为左边界、
  只要求`next_real`。不传输或保存 resolved-min，不增加 Scan state、cache、分页或
  owner value；后续行严格保留 master predicate，`key > max`不伪装 limit，sentinel
  也不额外放宽 adjacency。
- K1 生产改动只有 `ScanSharedPartition` 的上述无状态布尔适配；K2 已确认 local
  callback 使用原 `take_read_lock_and_read` 拷贝出的 `private_value`，没有第二次
  SCC read；K3 静态确认 production 无 `TwoPLPashaScan.h`、
  `TwoPLPashaScanFragment` 或 `TwoPLPashaScanAcquire`，local/remote driver 和唯一
  range move-in 循环仍分别位于原 helper/Message 主体。源码摘要：
  `kv/engine/kv_partition.cpp`
  `3dc045ba54bf92f17aa9f17d6b36909bbfeca8d3e09c46a0f1e13c38afcb7839`，
  `protocol/TwoPLPasha/TwoPLPashaHelper.h`
  `4f9d3015edd3aa33425fa9420b9ee804bccd389b27783d91791d5e6d078c3e46`，
  `protocol/TwoPLPasha/TwoPLPashaMessage.h`
  `dc7e2ebdcb6d04d84957a83f7899664c0d0ae4581f599612073cf783b77d1016`。
- focused fixture构造 owner move-in 后的真实邻接形状：首个 lower-bound row 的
  prev/next均缺失时请求 range move-in；仅补齐右边界后，以完全相同 min/max/limit
  重跑成功并从该 lower-bound row 返回。它也覆盖 local migrated copy 与 exact-min
  remote fragment。先重建二进制后，严格运行一次阶段唯一 gate；无 CTest、QEMU、
  guest runner或 ivshmem 进程残留：

  ```text
  build-debug/kv_partition_test --focused-stage=K
  passed, 0.00s
  ```

  二进制 SHA-256：`cb7fec4de8ffc10aceba4a4b54db6d367c76c6edcfe4a03ee6268d735b7808e0`。
  `git diff --check`通过。该 targeted evidence已消除 cold lower-bound adjacency
  活锁的直接根因；真实 4VM A/E 按固定矩阵只在 M 验证，尚不把它误报为完成的4VM证据。

## 阶段 M 诊断与阶段 K 终端边界补充证据（2026-07-30）

- M 的唯一 Debug 小规模集成 gate 首次运行时，原 2048 行 remote Scan fixture 使 owner
  长时间逐行执行既有 `move_in_scan_range`；这不作为通过结果。将 test-only fixture
  收缩为 32 行（M 的小规模集成口径；不改变正式容量或 Clock policy）后重跑，GDB 证明
  首轮 range move-in 已返回，但 child 随即发送第二个完全相同的
  `DATA_MIGRATION_REQUEST_FOR_SCAN`。本轮 CTest 已主动停止，阶段 M 未通过、未启动
  4VM，也没有把中止运行当作验证证据。
- Debug/GDB 最小根因：每个 owner-private table 的原 all-`0xff` sentinel 是唯一物理
  右边界。`KVPartition::MoveInForMigrationManager` 对它沿用 `next_lmeta == nullptr`
  而发布 `next_real=false`；无界 Scan 的终端行遂进入未改的 master “其他” adjacency
  分支并要求 `prev_real && next_real`，从而要求同一范围再次 move-in。该结论来自 child
  的第二个同参 request 与 parent 再次停在同一 `move_in_scan_range` 栈帧，非 timeout
  或 Busy 重试推断。
- 按 `partition优化方案.md` §3.9 和 K1 先同步合同后，生产补丁仅在该 sentinel 的
  move-in 调用既有 helper 时传入 `next_migrated || terminal_sentinel`；它发布已有
  `next_real` 位表示闭合的物理右边界，remote callback 的原三分支、消息、范围循环和
  所有普通行均未改变。K fixture 新增 terminal-sentinel move-in 后同一无界 Scan 成功
  返回 b/c 两个真实行的复现。
- 重新记录 `master=ccd567a50116b7bada06df71a3bf0a07c424572e`、
  `HEAD=e7a17282f109fcd80ae13d3d8fc02a1c863f675d`；先确认无 CTest、测试二进制、
  QEMU、runner 或 ivshmem 残留，重建后只运行受影响的唯一 focused gate：

  ```text
  build-debug/kv_partition_test --focused-stage=K
  passed, 0.00s
  ```

  当前二进制 SHA-256：
  `1ad22b1017ff81783d3bddebbc3997859931ebefded6847a43530f213ad2e8aa`。
  `git diff --check`通过。M 仍须在此修复后按清单仅重跑一次，才可回答集成层是否完全
  消除；阶段 M/4VM 尚未完成。

## 阶段 M 第二次诊断（2026-07-30，未通过）

- 在 K 的 terminal-sentinel 薄适配及 K gate 后，M 串行 CTest 的
  `unit_tests`、`kv_layout_test`、`btree_binding_test`、`kv_partition_test` 已通过；
  `kv_engine_test` 未完成。Debug/GDB 在 node1 的 `KVEngine::Forward`/`AwaitResponse`
  捕获 `REMOTE_INSERT_REQUEST`（`key=H-counter`、`owner=0`），其 foreground worker 与
  inbound demuxer 均在 `sched_yield`；说明 requester 已发出 request 但没有收到 response。
- 同时的进程检查发现测试 parent 已不在 CTest 的活进程集合，child 继承输出 pipe 后持续
  等待，故 CTest 不会自然返回。该失败与原先第二个同参 Scan range move-in 不同；不能将
  terminal-sentinel K repair 误报为 M 已通过。
- 为区分崩溃与活跃循环，另作一次不计 gate 的 Debug/GDB parent-follow 复现：预期的
  corrupt/binding/misroute death 子项均输出 `TIGONKV_TRANSPORT_FATAL` 后被 GDB detach；
  实际 route fixture 的 node0 parent 与 node1 child 均持续占 CPU（约 87%/200%），
  未触发 SIGABRT/SIGSEGV/exit_group。已按失败处理清理 ctest、GDB、child 及所有 VM/
  runner/ivshmem 残留。它证明这是 active protocol/liveness path，非已确认的 Scan
  adjacency 重复 RPC。
- 下一步仍停在 M：先以当前 `PollTransport`/demux/worker-0 mailbox 与原 Message
  worker-id 归属做最小结构化诊断，取得 owner 未发送还是 response 未路由的直接证据；
  在根因确认前不修改生产代码、不启动 M 的 4VM 子项，也不重跑 CTest。

## 阶段 I 回归修复证据（2026-07-30）

- M 的最小直接复现 `build-debug/kv_engine_test` 给出 owner node0 的结构化 fatal：
  `remote insert rollback found a published placeholder`。GDB `catch throw` 越过测试自身
  预期的 binding/attach 异常后，捕获真实栈：
  `KVPartition::PromotePrivate(H-counter) → PolicyClock::move_row_in →
  ClockTracker::track → ClockTrackerLocalRowOffset`；后者因传入 row 的 data 为 null 抛出。
  该异常发生在 helper 已发布 shared placeholder 之后，旧 catch 随即错误尝试 unpublished
  rollback，掩盖了根因并让 requester 等不到 response。
- 与 `EnsureInShared` 的同一原 PolicyClock 调用形态核对后，`PromotePrivate` 不再构造
  `{nullptr,nullptr}`，而直接复用 `private_table_->search(&fixed_key)` 返回的原
  `ITable::row_entity`；查找缺失只返回既有 `FAIL_OOM`。没有改动 Clock 主体、rollback、
  消息、SCC 或 Scan。
- 阶段 I focused fixture 补充唯一回归：remote placeholder move-in 必须以真实 row 建立
  Clock tracker，随后 requester publish、owner Get 返回正确 fixed value。清场后重建并
  仅运行规定 gate：

  ```text
  build-debug/kv_partition_test --focused-stage=I
  passed, 0.00s
  ```

  `master=ccd567a50116b7bada06df71a3bf0a07c424572e`，
  `HEAD=e7a17282f109fcd80ae13d3d8fc02a1c863f675d`；当前 SHA-256：
  `kv_partition.cpp=7943c2652f720c11b36896710dbe9d9a37013e996513cf678f3a2f2941ce0b86`，
  `kv_partition_test=b78d10f1246adee664d82aa0beb214d3e27090aff207010878aaff4707b8dafc`。
  `git diff --check`通过，且无测试/QEMU/runner/ivshmem 残留。下一步按 M 失败处理
  仅重跑一次完整 M gate；4VM 仍未启动。

## 阶段 J 完成证据（2026-07-30）

- 本阶段重新记录
  `master=ccd567a50116b7bada06df71a3bf0a07c424572e`，当前
  `HEAD=e7a17282f109fcd80ae13d3d8fc02a1c863f675d`。源码摘要：
  `kv/engine/kv_partition.cpp`
  `64848a768eb95ddcfe64a0abd38842b442e2b4e711b17b57d4c90f5ba489d063`，
  `protocol/Pasha/PolicyClock.cpp`
  `6dd2f099ca2961a5191c3e314c28521bec30048c5be378eb36a04cb5f0c49470`，
  `protocol/TwoPLPasha/TwoPLPashaHelper.h`
  `6fd1627c48c6360f8a64f8d2b39d39604a6948fced805f9311b33ba9940890e7`。
- J1：对照 master 的 PolicyClock 删除调用顺序，当前
  `delete_specific_row_and_move_out` 只在 callback 成功后按 key untrack；没有
  对 `migration_policy_meta`/retired smeta 的成功后访问。共享 helper 在同一
  callback 内完成 SCC invalidation、shared index remove、remote ref 结算和最终
  write-lock clear，之后才允许 EBR retire。
- J2：`KVPartition::DeletePrivateForMigrationManager` 在进入
  `remove_and_process_adjacent_tuples` 前，用该行已经持有的 owner write 或 remote
  ref/write 保护完成 valid、owner locator、SCC payload、reader/ref/write 及 valid-bit
  preflight；竞争或 stable missing 在此返回。callback 只校验预检行身份，严格执行
  master 的 adjacency → current invalidation → shared-index remove → untrack/retire
  顺序；callback 内不再返回 Busy 或恢复 valid。任何预检后行变化、索引删除失败或协议
  不变量破坏均 hard-fail；仅本调用预留的 shared write bit 在异常或未进入 callback 时
  结算。
- 先重建二进制，再严格运行一次阶段唯一 gate；无 CTest、QEMU、guest runner、
  ivshmem 或测试进程残留（遗留 tmux 空 shell 未运行测试/VM）：

  ```text
  build-debug/kv_partition_test --focused-stage=J
  passed, 0.00s
  ```

  二进制 SHA-256：`fd9497cef0b88e0bd84eab2590d1f9551c10d04e2f02336135e6355e6f4b5ed0`。
  `git diff --check`通过；Scan cold lower-bound Busy 活锁尚未修改，按顺序留待 K1。

## 阶段 I 完成证据（2026-07-30）

- 本阶段重新记录
  `master=ccd567a50116b7bada06df71a3bf0a07c424572e`，当前
  `HEAD=e7a17282f109fcd80ae13d3d8fc02a1c863f675d`。源码摘要：
  `kv/engine/kv_partition.cpp`
  `e54328016431e41346a67968704eb6dda2e5fae547cb89a6b0e09bdf78365231`，
  `protocol/TwoPLPasha/TwoPLPashaHelper.h`
  `6fd1627c48c6360f8a64f8d2b39d39604a6948fced805f9311b33ba9940890e7`。
- owner CAS/Increment 和 Put均经原 `take_write_lock` continuation取得唯一
  lmeta→smeta 权威锁；shared continuation由 helper 返回，调用者不再重新读取
  `is_migrated`/locator 或取得第二把 remote lock。CompareFailed、decode/overflow
  失败均按对应 continuation release/abort。
- remote insert创建的唯一 invalid placeholder遇到
  `FAIL_ALREADY_IN_CXL` 现在直接 hard-fail，不再冒充成功；真实
  `FAIL_OOM` 先经原 adjacent callback摘除未发布 placeholder并恢复邻接，再返回
  OutOfMemory，而不是 Busy。rollback 不调用 Delete/Clock，不把 valid 置 true，成功
  tree remove仍由 E 的 storage policy仅退休一次。
- 先重建二进制后，严格运行一次阶段 I focused mode；前后无 CTest、QEMU、runner或
  测试残留：

  ```text
  build-debug/kv_partition_test --focused-stage=I
  passed, 0.00s
  ```

  二进制 SHA-256：`89e5066d37241cde549bc2b47260854e98b54fcb596e0abdc62ec202b772d1cc`。
  `git diff --check`通过；Scan cold lower-bound Busy 活锁仍未修改，留待 K1。

## 阶段 G 完成证据（2026-07-30）

- 本阶段重新记录
  `master=ccd567a50116b7bada06df71a3bf0a07c424572e`，当前
  `HEAD=e7a17282f109fcd80ae13d3d8fc02a1c863f675d`。源码摘要：
  `core/CxlIncomingDispatcher.h`
  `cd8344912093158af62c93e46cca4785913815432256391ace2747befe91ffcb`，
  `core/Dispatcher.h`
  `7559c1940e7520a36731b4e131e170c438db57892767036ccecc9e79fd94a1ee`，
  `protocol/TwoPLPasha/TwoPLPashaMessage.h`
  `dc7e2ebdcb6d04d84957a83f7899664c0d0ae4581f599612073cf783b77d1016`，
  `kv/engine/kv_engine.cpp`
  `ec6a03d03ab4a88fce17689ed0dbadf4b0701f2d54fb792a6ad76ffac4863c28`。
- CXL receive只有从原 `IncomingDispatcher` 调用的
  `RunCxlIncomingLoop`：一个 `BufferedReader`、一个 Message、原 yield/backpressure
  与 worker-id/destination检查；无 current-only dispatcher class、drain batch、
  pending map或第二服务线程。KV request/response使用原
  `TwoPLPashaMessageFactory/Handler` 的 transaction-free overload，保留 table id 0、
  原 frame字段和 owner completion；未发现
  `TwoPLPashaMessagePrimitive`、`RpcKind`、`RpcResult`、`EncodeResult`或通用结果
  序列化。
- 先重建二进制后执行阶段唯一 gate；执行前后无 CTest、QEMU、runner或测试残留：

  ```text
  build-debug/kv_engine_test --focused-stage=G
  passed, 0.00s
  ```

  二进制 SHA-256：`c4f688d76be05941a148f8046d6730e535e9d35008c35ef426f40e42147af8a6`。
  `git diff --check`通过；Scan cold lower-bound Busy 活锁仍留待 K1。

## 阶段 H 完成证据（2026-07-30）

- 本阶段重新记录
  `master=ccd567a50116b7bada06df71a3bf0a07c424572e`，当前
  `HEAD=e7a17282f109fcd80ae13d3d8fc02a1c863f675d`。源码摘要：
  `kv/engine/kv_partition.cpp`
  `11b45e60ab3bf49558698fde6c90596cb96f45f3694278a6b6d124b7fd8e42ef`，
  `kv/engine/kv_engine.cpp`
  `ec6a03d03ab4a88fce17689ed0dbadf4b0701f2d54fb792a6ad76ffac4863c28`，
  `kv/engine/kv_engine.h`
  `d4037c6b3bbb8700eb73c91a0d691515c67a4317c49c23231bcbf06a9802050f`。
- H1 静态调用图确认 `TryPinShared` 唯一调用原
  `get_migrated_row_result(..., inc_ref=true)`，该同次 lookup 返回
  Done/Missing/Busy；没有结果后第二次 shared B+Tree lookup、negative cache 或
  routing cache。
- H2 当前范围内已完成最终 owner table sentinel接线：reset时每个 owner-private
  原 table仅插入一枚 all-`0xff` physical tuple，公开 point API拒绝它、Scan在原
  callback中不输出它。CAS/Increment、Delete及Scan协议主体的进一步收敛按固定顺序
  分别留在 I/J/K，未提前改写。
- H3 静态确认仅有 `KVEngine::worker_runtime_` 与唯一 foreground worker-id TLS；
  facade只取得 engine 当前 slot，slot同时持有完整 RuntimeStats、max_tid和
  `EBRMetaLocal`，不存在 KVStore runtime vector、TlsRuntime或 Scan TLS 统计副本。
- 先重建二进制，再按唯一 gate严格串行运行；执行前后无 CTest、QEMU、runner或测试
  残留：

  ```text
  build-debug/kv_partition_test --focused-stage=H
  passed, 0.00s
  build-debug/kv_engine_test --focused-stage=H
  passed, 0.00s
  ```

  对应 SHA-256：`kv_partition_test`
  `266d01b39be8f9943952e249b4ca87dde75e598e702e3dca680ee8f5e0a9d2bf`，
  `kv_engine_test`
  `c4f688d76be05941a148f8046d6730e535e9d35008c35ef426f40e42147af8a6`。
  `git diff --check`通过；Scan cold lower-bound Busy 活锁仍未修改，留待 K1。

## Scan Busy 活锁修复与合同对齐（2026-07-30）

- GDB 证据目录：`/mnt/xz_vm_storage/tigon2-scanbug-20260730T142639Z/`（第二 probe
  Busy、`prev=1 next=0` middle-row、K1 二进制已存在仍活锁）。
- 根因两处：（1）K1 在首次 CXL probe 启用，接受远处 island，使
  `move_in(min,limit)` 无法修到尾缘；（2）already-migrated 只更新邻居、不重建
  当前行 bit，insert `clear_adjacent` 后洞可永久存在。
- 生产补丁：首次 probe master-strict；move-in 后同参重跑启用 K1；
  `MoveInForMigrationManager` already-migrated 按邻居观测写回当前行
  prev/next bit。已同步 `partition优化方案.md` §3.9.5、`下一步修改.md` K1、
  `master差分allowlist.md`、`当前对比口径.md`。
- focused K：`build-debug/kv_partition_test --focused-stage=K` 通过（含
  gap-before-island 与首次/重跑 K1 作用域断言）。
- Debug 4VM Workload E 验证：`/mnt/xz_vm_storage/tigon2-scanfix-20260730T153421Z/`
  `load pass` + `run pass`，EXIT:0；runner SHA-256
  `4d0fec8b1cd2671fcc394b52a91d1b9f24a6e5fca3f70d1fe778dfcacdd4f163`。

## 阶段 M 完成证据（2026-07-30）

- 证据目录：`/mnt/xz_vm_storage/tigon2-stageM-20260730T160450Z/`（含 m-direct、
  10k A/E traces；A/E 以 `m-ycsb-logs-retry` 为准）。
- Host focused gate（Debug，`-j1`，无 QEMU 并行）：
  `unit_tests|kv_startup|region_allocator|cxl_ebr|kv_layout|btree_binding|
  scc_protocol|kv_shared_protocol|kv_partition|kv_engine` 全部 Passed。
- m-direct：4 key / 4 load + 20 run，`EXIT:0`，四 VM `passed.`。
- 10k YCSB A/E（stall=90，根正式 config，Debug runner）：`a load/run` 与
  `e load/run` 均 pass（`m-ycsb-retry.stdout`）。首次 E 曾因并发
  remote-insert/`FAIL_ALREADY_IN_CXL` 过度 hard-fail 与脏 guest 干扰失败；按
  master 忽略 already-shared（须持 requester pin）修复后重跑通过。
- 根 `experiment_config.jsonc` SHA 全程不变：
  `13065176283f0700cbfe175c7962f2c5fdbe0305e8f18c54894d8b56fbae3e85`。

## 阶段 N 清理证据（2026-07-30）

- 删除死字段：`retired_pending_bytes`/`reclaimed_total_bytes`、
  `strict_swcc_access`、`hwcc_atomic_ns`、`SharedLayoutHeader::IsCompatible`、
  无引用 `ycsb_experiment_config.jsonc`、`--skip-standalone-load` no-op。
- `rg` N1 静态闸门（排除施工清单正文）清零；`bash -n tigonkv_run_ycsb_experiment.sh`
  通过。`SharedAccessState`/`OwnerNextRowLock` 仍为 §10.1/owner next-row 薄
  适配，未当作死代码删除。

## remote-insert already-shared 与阶段 O（2026-07-30）

- `InsertRemotePlaceholder`：`FAIL_ALREADY_IN_CXL` 且持有 requester pin 时按
  master 编码 Inserted；无 pin 才 fatal。focused I 增加 race-insert case。
- O：删除 `DelayActiveScopeNow`/`scope_depth`；MPSC/helper/demuxer 仅在无
  reservation 的非嵌套 scope 结算；`ReadStaticDomainUsedBytes`；
  `RegionAllocator` control/block 双域；EBR retire Private wrappers；lmeta latch
  PrivateAtomic + state 自 `tid`。
- `latency_modes_test` Passed；随后 host 11-target 闸门（含 latency）全部 Passed。
- 生产源码摘要（`common core protocol kv`）：
  `b880971a4b981bc47e1725b808c66579523f70690256de58161707cf66b5df81`。
- runner SHA-256：
  `509e862d189d3a0e13f211434669c5a63cb6a36533c6f2e137284d08a22b2fe2`。

## 最终提交 988d2cc 全面验收证据（2026-07-31）

### throwaway pool 代表 key Get（§5.4 / P2 第 4 项）

- 流程：以正式 1M canonical load（SHA 见 `canonical-load.sha256`）在独立
  throwaway pool 完成一次 4VM load（value oracle = 32×`0x5a`），随后 16 个 worker
  文件只含 8 条 GET（4 owner + 4 non-owner，清单见
  `throwaway-8get-list.txt`），`TIGONKV_E2E_REQUIRE_GET_FOUND=1`。
- 结果：`round1-workloade-run` 四 VM 均 `passed`；pool 已重置（
  `throwaway-pool-reset.txt`）。

### Debug 全量 CTest（含 3 个 guest）

- `ctest --test-dir build-debug`：**19/19 Passed**，含 host 16 项与 guest
  `e2e_08_test`（52.95s）、`e2e_09_test`（55.20s）、`e2e_ycsb_test`（76.55s）。
- 日志：`ctest-logs/debug-full-19-20260731T135000Z.log`。

### 正式 1M A/B/C/D/E（fresh pool 逐 workload，最终提交）

| workload | load | run |
|---|---|---|
| A | 193.0s / 1,000,000 ops | 34.4s / 1,499,929 kv ops |
| B | 192.6s / 1,000,000 | 27.9s / 1,000,000 |
| C | 192.1s / 1,000,000 | 28.0s / 1,000,000 |
| D | 193.0s / 1,000,000 | 33.6s / 1,000,000 |
| E | 191.9s / 1,000,000 | 507.2s / 1,000,000（scan_ops=950,195） |

A–D 产物：`formal-abcd/`（`YCSB实验报告.md`、`ycsb_summary.json`、`run_meta.json`）；
E 产物：`/mnt/xz_vm_storage/tigon2-1m-formal-20260731T100813Z/`。

### latency-enabled 4VM focused A/E

- 配置：`latency/latency-focused-config.jsonc`（cxlkv profile 派生，
  `cache_model=none`、`cache_hits_enabled=false`；enabled/foreground/merge/stats
  均 true，swcc=25ns/line、hwcc=117ns/line）。
- 结果：A、E 的 load/run 全部 `passed`；`TIGONKV_LATENCY_STATS` 显示注入生效
  （vm0 E-run：swcc_raw=27,526,252、hwcc_raw=117,431,415、
  delayed_ns=14,427,631,855，cache_model=none）。

## YCSB-E Scan 大数据量 stall 根因与修复证据（2026-07-31，原 bug修复.md 合并）

### 症状与门禁

- 4VM × 4 foreground + demuxer=1，Workload E `run` 心跳长期 `ops=0 total=0`；
  宿主判定 `stall: no E2E_TRACE_HEARTBEAT growth for ${STALL_SEC}s`。
- 修复前 Rel50k 六次运行五次硬 stall（仅一次偶发通过）；修复后门禁：

| Gate | Result |
|---|---|
| Rel 20k / 25k E | PASS |
| Rel 50k E | PASS ×2（约 20–30s 跑完） |
| Rel 100k E | PASS（约 45s） |
| Rel 1M E（正式入口 `tigonkv_run_ycsb_experiment.sh`） | PASS：load≈192s、run≈507s、1,000,000 ops、scan_ops=950,195、scan_rows=47,998,859、无 ops=0 |
| Debug 100k E（`STALL_SEC=90`） | PASS（约 90s） |
| unit_tests / kv_layout / btree_binding / kv_partition / kv_engine | 全部 PASS |

正式 1M E 产物：`/mnt/xz_vm_storage/tigon2-1m-formal-20260731T100813Z/`
（`YCSB实验报告.md`、`ycsb_summary.json`、`run_meta.json`）。

### 根因证据链

1. GDB 多轮采样（`tigon2-scanfix-rel50k-rootcause-gdb-*`）：owner move-in 线程在
   `move_in_scan_range → move_row_in → MoveInForMigrationManager` 轮转，其余 worker
   在 shared Scan / `smeta->lock()` 自旋；`scan_range_migrate_inflight` 四分区恒为 1。
2. perf（`tigon2-scanfix-rel50k-perf-*`）：`_lookupForNextKeyUpdate` /
   `BTreeLeaf::lowerBound` 为私树下降热点；smeta lock 与 shared scan callback 占
   主要 CPU。
3. 临时 SCANTRACE 埋点（已回滚）：同一行 `prev=0` 失败十多万次；re-probe 在
   move-in 后仍 80–100% `migration_required`；失败行分布 `prev=0 next=1` ≈75%。
4. 宿主侧共享池树解析（/tmp/walk_*.py 留档）：失败行（如
   user14862824324880098893、user16376311102338332121）的**私树前驱已迁移**
   （is_migrated=1、共享树存在），但 smeta `prev_real=0` 稳定不变，repair 75 次
   无效；叶链检查发现 `LEAF B.pre_` 指向错误叶子而 `A.next_=B`（单向链）——即
   split 漏回链的现场形态。

### 修复与 master 的关系

- master `BTreeOLC.h::BTreeLeaf::split` 同样漏 `old_next->pre_ = newLeaf`（其
  erase-merge 路径有回链，说明 split 为笔误）；本次在
  `common/btree_olc_cxl/BTreeOLC_CXL.h` 补上（3 行逻辑 + 同域记账）。
- 详细取舍（legacy 树不修）与验证状态见 [修改日志.md](修改日志.md)
  「2026-07-31：YCSB-E Scan 大数据量 stall 修复」。

### 复现命令（修复后用于回归）

```bash
REPRO=/mnt/xz_vm_storage/tigon2-stageP-20260730T170434Z/e-repro-bisect
env -u TIGONKV_E2E_TEST_VALUE_HEX -u TIGONKV_E2E_REQUIRE_GET_FOUND -u TIGONKV_E2E_SCAN_MAX_KEY \
  -u TIGONKV_VM_SSH_BASE_PORT -u TIGONKV_VM_SSH_KEY -u TIGONKV_VM_REMOTE_ROOT \
  -u TIGONKV_SHARED_MEMORY_PATH \
  TIGONKV_E2E_TRACE_RUNNER=/root/code/tigon2/build-relwithdebinfo/e2e_trace_runner \
  TIGONKV_POOL_INITER=/root/code/tigon2/build-relwithdebinfo/cxl_pool_initer \
  TIGONKV_EXPERIMENT_CONFIG_JSONC=/root/code/tigon2/experiment_config.jsonc \
  TIGONKV_E2E_ROUNDS=1 TIGONKV_YCSB_WORKLOADS=E \
  TIGONKV_E2E_STALL_SEC=600 TIGONKV_E2E_TIMEOUT_SEC=1800 \
  TIGONKV_E2E_SCAN_EXPECT_NONEMPTY=1 \
  bash scripts/e2e_trace/run_guest_ycsb_workflows.sh "$REPRO/traces-50k" <LOGS> 1 E
```
