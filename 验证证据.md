# TigonKV 当前验证证据

> 2026-08-06 V7 复查结论：本页 V6 数据没有全部绑定当前父仓与公共库最终源码，
> 不能作为当前验收证明；尤其远程 Delete 竞态尚未闭合。新的结构化证据必须按
> `/root/code/tigon延迟模拟改造_进一步修改v7.md` 生成到持久 v7 artifact 目录。

本文只保留当前 fixed-latency-only 验证摘要。原始日志、完整 trace、VM writable copy
和旧实验轮次不进入 Git；本文件是重启后仍可复查的持久证据入口，不依赖 `/tmp`
施工日志。

> 以下 V5/V6 结果只作为历史输入（当时父仓候选 `961c769`、公共 gitlink
> `813c985`），不得重新标注为当前结果。

## Host build/test

历史候选（gitlink `813c985`）四变体 host validation manifest：

```text
Debug compile-on / compile-off + RelWithDebInfo compile-on / compile-off:
  四变体全部非 VM CTest 19/19 PASS
compile-off ELF 符号检查（e2e_08/e2e_09/e2e_trace_runner/unit_tests）:
  V6 独立审计脚本（先保存完整 nm 输出再匹配，带必中 fixture 自测）在
  Debug/O0 与 RelWithDebInfo 均 clean；manifest 记录 elf_audit=clean
配对短基准（CPU=4，provenance 前置：父仓/子模块干净、索引 gitlink == checkout、
  build cache 一致、binary build-meta 的 source-state 头 == 当前 HEAD、强制
  clang-18/RelWithDebInfo/LTO ON）：9 对（噪声方向不稳定自动扩），verdict PASS，
  报告绑定最终候选
```

逐变体命令、CTest 摘要、source-state、关键 binary hash 与 ELF 审计见
`/root/code/latency_sim_v5_artifacts/tigon2/host_validation_manifest.json`；配对
基准逐样本见 `paired_benchmark_report.json`。输出只包含 benchmark 时间，不增加
生产访问数、原子数或模拟统计。

定向测试覆盖 0B/单 line/跨 line/非对齐/重复访问、不同 HWCC/SWCC 延迟、原子和 CAS
语义、nested scope、前后台线程隔离、RAII 早返回、旧配置（含旧布尔开关）拒绝和业务
allocator 容量。V6 新增：真实 BufferedReader/recv/demux（size+dequeue 组合）精确
行 ledger；远程 Delete 的真实异常/迟到响应/owner claim 竞态（owner 已 claim 后
requester 回滚必须 hard fail）、stale sequence 幂等丢弃。

## VM 验证记录

以下只接受当前仓库独立 clean-VM 流程产生的结果（R6525，4VM，本仓库
`image/root.img`、当前 binary/config、新建 backing）：

| 场景 | 结果 | 备注 |
|---|---|---|
| e2e08 | PASS | compile-off 1 轮 + compile-on+0ns 1 轮 + compile-on 非零 canary 1 轮 |
| e2e09 | PASS | 1 轮 |
| YCSB load + workloada + workloade | V6 重建 | 各 1 轮，run_meta 绑定最终 clean source，reproduce_command 为完整可执行命令 |

canary 只用于受控 wall-time/存活性验证，不导出访问数或模拟统计；结果不跨项目复用。
V6 历史命令、逐样本、parent/gitlink SHA 与 binary/config/trace hash 见
`/root/code/latency_sim_v6_artifacts/tigon2/`
（final.sha、final_gitlink.sha、source-state、commands.md、test-summary.md、
paired_benchmark_report.json、host_validation_manifest.json、ycsb run_meta）。

## 数据瘦身

清理前后仓库物理占用约为 `15G -> 6.4G`；稀疏文件 apparent size 约为 `79M -> 39M`。
删除的精确路径包括历史 `exp_data/`、`results/`、`emulation/image/mkosi.cache/`、
builder `emulation/image/root.img` 及 manifest、旧 build 变体、YCSB-cpp 子模块
`build/` 和 Python `__pycache__/`。保留运行时 canonical `image/root.img`、当前
`build-debug/` 与 `build-relwithdebinfo/`；builder 镜像可由
`emulation/image/make_vm_img.sh` 重新生成。
