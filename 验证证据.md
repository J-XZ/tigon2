# TigonKV 当前验证证据

> 2026-08-07 V8 复查：远程 Delete 仍有 state ABA，且本页 VM/YCSB provenance 是运行后
> 生成，不能证明日志来自所声明的 binary/source-state。本页降级为历史证据；V8 要求见
> `/root/code/tigon延迟模拟改造_进一步修改v8.md`。

> 2026-08-06 V7：公共库 final SHA 为
> `8e0a1d07d08a158fe52a6b03544c5d54a86d74f6`。父仓库最终 SHA、完整 source-state、
> build provenance、测试日志、ELF 原文、VM/YCSB run_meta 和 cleanup 校验统一以
> `/root/code/latency_sim_v7_artifacts/tigon2/` 为准；V5/V6 目录和下文历史说明不是
> 当前完成证据。

本文只保留当前 fixed-latency-only 验证摘要，不记录访问计数或模拟统计 schema。原始
日志、完整 trace、VM writable copy 和 backing 不进入 Git；artifact 目录中的每个场景
都必须绑定当前父仓库 source-state、公共 SHA、构建身份、binary/config/trace/image
hash 和清理结果。

## Host build/test

最终矩阵使用 canonical Ninja + clang-18 构建目录：

```text
Debug / compile-on       build-debug-ninja-clang18-co_off
Debug / compile-off     build-debug-ninja-clang18-co_on
RelWithDebInfo / on     build-relwithdebinfo-ninja-clang18-co_off
RelWithDebInfo / off    build-relwithdebinfo-ninja-clang18-co_on
```

任务书定义的非 VM CTest 集合排除了需要真实 QEMU 的 `e2e_08_test`、`e2e_09_test` 和
`e2e_ycsb_test`；四个变体各运行一次，真实结果为 20/20 PASS。远程 Delete 定向测试
在两种 Debug compile 模式通过，并覆盖真实跨进程 Delete/Forward/owner/response/demux、
cancel/claim 竞态、迟到/重复 response、RAII 异常、latency scope 和
`UINT64_MAX` sequence 边界。`region_allocator_test` 的 fork 场景也显式避免在子进程
重置 pool 时继承父 scope。

compile-off Debug/O0 与 RelWithDebInfo 的 `e2e_08`、`e2e_09`、`e2e_trace_runner`、
`unit_tests` 均执行了完整 `nm`、`readelf`、`objdump` 输出后再做 forbidden-symbol
匹配；匹配文件必须为空，审计 fixture 自测必须命中。四变体 build manifest 记录
完整 parent/source-state、gitlink、compiler、build type、LTO、compile mode 和 binary
hash，配对 benchmark 只接受这些 manifest。

## Remote Delete 协议证据

稳定 HWCC control slot 的转移表、唯一 writer、rollback 边界、线性化点和测试映射见
`/root/code/latency_sim_v7_artifacts/tigon2/remote-delete-state-machine.md`。协议不把
跨节点状态放进会退休的 row；owner 的精确 claim 是 `Pending -> Executing` CAS，
requester 只有 `Pending -> Cancelled` 成功时才能恢复 row，终态发布在 response 之前。
mailbox 的有界 retired identity ledger 先于当前 operation 检查迟到 response。

## VM/YCSB 证据门

正式完成前必须存在并通过 artifact validator 的本仓库独立 R6525 2-NUMA 4VM 场景：

| 场景 | 要求 | 当前证据 |
|---|---|---|
| E2E08 compile-on + 0ns | 一轮 | 见 `e2e08_compile_on_zero/` |
| E2E08 compile-off | 一轮 | 见 `e2e08_compile_off/` |
| fixed-latency canary | 一轮非零延迟 | 见 `e2e11_canary/` |
| E2E09 | 一轮 | 见 `e2e09/` |
| YCSB | load、workloada、workloade 各一轮 | 见 `ycsb_*` 场景目录 |

每个场景必须使用 Tigon 自己的 `image/root.img`、当前 runner/binary、当前配置、当前
trace 和新建 backing；不得复用 CXLKV/SIDLE 的任何 VM、镜像、backing、binary、trace、
配置或结果。停止 VM 后必须验证无本项目 QEMU/ivshmem、backing 已移除、VM storage 已
清理；若 host/guest 条件不能满足，只能在 `test-summary` 和 `artifact-validation`
中标记 `BLOCKED_ENVIRONMENT`，不能用其它项目结果替代。

## 持久入口

```text
final.sha
latency_sim_final.sha
source-state.json
commands.json
test-summary.json / test-summary.md
build-manifest-*.json
elf/{debug-off,relwith-off}/
benchmark_report.json
*/run_meta.json
artifact-validation.json
```

这些文件均位于 `/root/code/latency_sim_v7_artifacts/tigon2/`；最终 `final.sha` 必须
等于当前 `my-work` HEAD，且 parent、gitlink、checkout、clean 状态和所有 artifact
hash/计数一致。
