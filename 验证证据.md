# TigonKV 当前验证证据

本文只保留当前 fixed-latency-only 验证摘要。原始日志、完整 trace、VM writable copy
和旧实验轮次不进入 Git；复查时以命令输出和
`/tmp/fixed-latency-only-cleanup-tigon2.md` 为准。

## Host build/test

已执行：

```text
Debug × compile-on / compile-off: cmake configure + build，所有非 VM CTest PASS
RelWithDebInfo × compile-on / compile-off: cmake configure + build，所有非 VM CTest PASS
latency_modes_test: PASS
hardware_sim_disabled_benchmark: compile-on+0ns 与 compile-off 两个独立二进制 PASS
compile-off ELF 符号检查: 无 simulator/TLS/TSC/parser 符号 PASS
```

微基准（同一真实 adapter 路径）中，单次访问的计费簿记约 `1 ns/op`，真实
RegionAllocator init/Allocate/Free/RecordMetadata 路径累计约 `24 ns/op`。输出只包含
benchmark 时间，不包含访问数、原子数或模拟统计。

定向测试覆盖 0B/单 line/跨 line/非对齐/重复访问、不同 HWCC/SWCC 延迟、原子和 CAS
语义、nested scope、前后台线程隔离、RAII 早返回、旧配置（含旧布尔开关）拒绝和业务
allocator 容量。

## VM 验证记录

以下只接受当前仓库独立 clean-VM 流程产生的结果（R6525，4VM，本仓库
`image/root.img`、当前 binary/config、新建 backing）：

| 场景 | 结果 | 备注 |
|---|---|---|
| e2e08 | PASS | compile-off 2 轮 + compile-on+0ns 2 轮 + compile-on 非零 1 轮 |
| e2e09 | PASS | 2 轮 |
| YCSB load + workloada | PASS | 1 轮 |

canary 只用于受控 wall-time/存活性验证，不导出访问数或模拟统计；结果不跨项目复用。

## 数据瘦身

清理前后仓库物理占用约为 `15G -> 6.4G`；稀疏文件 apparent size 约为 `79M -> 39M`。
删除的精确路径包括历史 `exp_data/`、`results/`、`emulation/image/mkosi.cache/`、
builder `emulation/image/root.img` 及 manifest、旧 build 变体、YCSB-cpp 子模块
`build/` 和 Python `__pycache__/`。保留运行时 canonical `image/root.img`、当前
`build-debug/` 与 `build-relwithdebinfo/`；builder 镜像可由
`emulation/image/make_vm_img.sh` 重新生成。
