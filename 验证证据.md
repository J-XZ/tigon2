# TigonKV 当前验证证据

本文只保留当前 fixed-latency-only 验证摘要。原始日志、完整 trace、VM writable copy
和旧实验轮次不进入 Git；复查时以命令输出和 `/tmp/fixed-latency-only-cleanup-tigon2.md`
为准。

## Host build/test

已执行：

```text
Debug: cmake configure + build；非 E2E CTest 15/15 PASS
RelWithDebInfo: cmake configure + build；非 E2E CTest 15/15 PASS
compile-off: cmake configure + build PASS
latency_modes_test: PASS
hardware_sim_disabled_benchmark: runtime-disabled 与 compile-off 各 5 次 PASS
```

disabled benchmark 的当前样本中位数约为 2.04–2.08 ns/op；两种关闭方式的差异处于
短测噪声范围内。输出只包含 benchmark 时间，不包含访问数、原子数或模拟统计。

定向测试覆盖 0B/单 line/跨 line/非对齐/重复访问、不同 HWCC/SWCC 延迟、原子和 CAS
语义、nested scope、前后台线程隔离、RAII 早返回、disabled fast path、旧配置拒绝和
业务 allocator 容量。

## VM 验证记录

以下表格只接受当前仓库独立 clean-VM 流程产生的结果：

| 场景 | 当前结果 | 备注 |
|---|---|---|
| 4VM 无延迟代表性 trace | PASS | 2026-08-03，R6525，端口 10022–10025；本仓库 `image/root.img`、当前 RelWithDebInfo binary/config、新建 `/mnt/xz_shared_mem/ivshmem_shared_mem`；e2e08 1 轮/10,000 keys 的 init/fill/read 全通过 |
| 4VM 无延迟 YCSB trace | PASS | 本仓库独立生成的 4×4 trace（load 10,000、run 4,000、uniform、sample-stride 1）；workload A 的 load/run pass |
| 4VM 非零 fixed-latency canary | PASS | 临时配置 `swcc=1ns/line`、`hwcc=2ns/line`；同一 trace 的 workload A load/run pass，无超时、统计导出或卡死 |
| VM 停止与 backing 清理 | PASS | 本仓库 kill/cleanup 后无 QEMU、ivshmem-server；`/mnt/xz_vm_storage`、`/mnt/xz_shared_mem` 无残留 |

上述结果只使用本仓库的镜像、配置、binary、trace 和运行目录；没有复用其它项目的
VM 磁盘、backing 或测试结果。canary 只用于受控 wall-time/存活性验证，不导出访问数。

## 数据瘦身

清理前后仓库物理占用约为 `15G -> 6.4G`；稀疏文件 apparent size 约为 `79M -> 39M`。
删除的精确路径包括历史 `exp_data/`、`results/`、`emulation/image/mkosi.cache/`、
builder `emulation/image/root.img` 及 manifest、旧 build 变体、YCSB-cpp 子模块
`build/` 和 Python `__pycache__/`。保留运行时 canonical `image/root.img`、当前
`build-debug/` 与 `build-relwithdebinfo/`；builder 镜像可由
`emulation/image/make_vm_img.sh` 重新生成。
