# TigonKV 硬件模拟当前实现

本文描述 `my-work` 分支当前的 TigonKV 接入。公共固定延迟库只通过
`thirdparty_libs/latency_sim` 使用；三个消费者同步使用同一个最终 latency_sim SHA，
Tigon 不维护第二套模拟器。

当前三个消费者统一固定在 latency_sim
`e9b976c7fd205b266610966f60eb7354d166d637`；官方 Valgrind 子模块保持
`42b08ed5bda9aa2b18d826a99a10312f826e3b85`，不在消费者子模块中维护修改。

## 配置与计费

唯一配置入口是 `tigon_kv.latency_inject.fixed_latency`，对象必须完整且只能包含：

```jsonc
{
  "cache_line_bytes": 64,
  "swcc_fixed_ns_per_line": 0,
  "hwcc_fixed_ns_per_line": 0
}
```

默认值为两个域 `0ns`。没有运行时关闭开关、访问统计、remote-cache 模型或共享事件
日志；旧字段和未知字段由 compile-on parser 拒绝。延迟按真实访问覆盖的 cache line
累加到线程的 scope，最外层安全出口才结算。每个 RMW 都按一次读加一次写收费两次，
原子返回值、CAS 的 `expected` 更新和 memory order 保持真实语义。

`mem_access.h` 只表达 HWCC、owner-private SWCC、shared payload 和 transport 的域
语义。SCCManager 和 allocator 保留真实 `clflush`、`clwb`、`clflushopt` 及 fence；它们
是生产一致性协议，不是额外的模拟操作。B+Tree/OLC、PolicyClock、EBR、allocator、
MPSC ring 和 TwoPLPasha WriteThrough 的真实一致性行为全部保留。

## scope 与 compile-off

Put/Get/Delete/Scan/CAS/Increment 使用 foreground scope，receive demuxer 和 RPC
dispatch 使用独立 background scope。Remote Delete 等待时挂起前台 scope，把 transport
poll 放进 background/deferred RAII 区段；只有 row/control/SCC/EBR 安全边界释放后才会
结算。超时会回滚 requester 的 `Pending` 状态，不能永久忙等。

`LATENCY_SIM_COMPILE_OFF=ON` 是唯一 compile-off 方式，必须使用独立构建目录；wrapper
退化为真实原操作，scope 为 no-op，消费者不解析或注册模拟器。默认 OFF（compile-on）。

`LATENCY_SIM_VALGRIND_CHECK=ON` 是独立的 compile-on checker 变体，只允许
`CMAKE_BUILD_TYPE=Debug`，且所有项目与公共库目标均为 `-O0`；任何 Valgrind 检查不得
使用 RelWithDebInfo/Release。Tigon2 的 clang-18 checker 目标额外使用 DWARF-4 以兼容
仓库内固定的 Valgrind 3.18.1。checker 使用本项目自己的
`thirdparty_libs/latency_sim/.latency_sim/latencycheck/install` 前缀，不能跨项目复用。
同步到 guest 后通过 `VALGRIND_LIB=<prefix>/libexec/valgrind` 定位固定 tool；E2E 摘要只
接受 clean、有效首错 mismatch 或 harness invalid 三态，首错后不继续其它阶段。

构建参数固定为：

```text
Debug:           -O0 -g3，无 -march=native，无 LTO
RelWithDebInfo:  -O3 -march=native -g3，编译与链接 -flto=full
Release:         -O3 -march=native，编译与链接 -flto=full
RelWithDebInfo/Release 保留 -DNDEBUG；普通 Debug 不添加 -DNDEBUG。checker Debug+O0
变体为了只运行 production 访问流额外定义 -DNDEBUG，runtime policy 使用
`verbose=false`、`extra_check=false`。
```

## Remote Delete 协议

`remote_delete_controls[requester][worker]` 是稳定 HWCC control slot，身份包含
requester、worker、partition、sequence、target row 和 key。状态的唯一生产路径为：

```text
Empty -> Pending -> Cancelled -> Acknowledged -> Empty
                 \-> Executing -> Deleted|Rejected|Failed
```

requester 先发布完整 `Pending(tag)`；owner 只用
`Pending(message_tag) -> Executing(message_tag)` 精确 CAS claim。claim 前 requester
可以用同 tag 取消并恢复 row；claim 后 requester 不再触碰 row，由 owner 负责拒绝恢复
或删除退休。终态 control word 先发布，再发送 response。sequence 到上限直接失败，
禁止 wrap/reuse。

每个 worker 只在自己的 mailbox 操作槽上工作。超时或完成时保留有限的生产 retired
identity 记录，使旧的 Deleted/Busy/Failed response 在空闲、下一路由或下一 operation
到达时安全丢弃；未来或冲突身份仍视为协议错误。测试只使用生产 transport、真实 control
word 和一个窄的环境同步点来重现 cancel-before-claim、claim-before-deadline、同一
row ABA、owner Busy/Rejected、迟到与重复 response 以及 tag exhaustion。

## 最小验证

四种独立构建目录分别覆盖 Debug/RelWithDebInfo 与 compile-on/compile-off：

```bash
cmake -S . -B build-debug-ninja-clang18-co_off -G Ninja \
  -DCMAKE_BUILD_TYPE=Debug -DLATENCY_SIM_COMPILE_OFF=OFF
cmake -S . -B build-debug-ninja-clang18-co_on -G Ninja \
  -DCMAKE_BUILD_TYPE=Debug -DLATENCY_SIM_COMPILE_OFF=ON
cmake -S . -B build-relwithdebinfo-ninja-clang18-co_off -G Ninja \
  -DCMAKE_BUILD_TYPE=RelWithDebInfo -DLATENCY_SIM_COMPILE_OFF=OFF
cmake -S . -B build-relwithdebinfo-ninja-clang18-co_on -G Ninja \
  -DCMAKE_BUILD_TYPE=RelWithDebInfo -DLATENCY_SIM_COMPILE_OFF=ON
ctest --test-dir <build-dir> -E '^e2e_' --output-on-failure -j1
```

定向测试包括配置解析/拒绝、公共延迟 geometry、allocator/B-tree/ring 功能、SCC
协议、Remote Delete 协议和 source-state 脚本。`hardware_sim_disabled_benchmark` 只
作为短的真实路径 smoke，不能生成或依赖跨项目结果。

VM E2E 使用本仓库自己的 `image/root.img`、runner、配置、guest binary、trace 和运行
目录。宿主机 `/mnt/xz_vm_storage`、`/mnt/xz_shared_mem/ivshmem_shared_mem` 与
`10022..10025` 是三个项目有意共享的物理资源，由
`/run/lock/shared-vm-e2e-resources.lock` 串行保护；这不表示复用其它项目的镜像、VM
writable 内容、prepared state、backing 内容、工具或结果。

每次 Debug+O0 latencycheck E2E08 invocation 只运行一个独立 round：确认目标进程有非零
插桩访问、公共库自动生成 expectation 并在 checkpoint 做最终一致性对账，以及首次
mismatch 会快速失败。业务插桩修复必须通过新的独立 invocation 继续推进，直到原验收
条件满足；Tigon2 的代表性历史证据记录在 `验证证据.md`，不替代当前新建输出目录。
