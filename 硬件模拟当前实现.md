# Tigon2 硬件模拟当前实现

本页只描述当前 `tigonkv` 路径，不描述 legacy benchmark 的旧统计。

## 配置与关闭路径

`experiment_config.jsonc` 和所有 e2e/YCSB fixture 都使用严格的：

```text
tigon_kv.latency_inject.fixed_latency
tigon_kv.latency_inject.hwcc_access_count
tigon_kv.latency_inject.atomic_count
tigon_kv.latency_inject.remote_cache_invalidation
```

四个模块各有 `enabled` 和独立维度开关；未知、重复、缺失、旧 cache-hit/cache-miss
字段及非法几何在启动时拒绝。配置摘要和 shared-layout digest 同时覆盖这些字段。
四模块全关时只读取一次 relaxed process-local feature mask；编译选项
`TIGONKV_DISABLE_HARDWARE_SIMULATION=ON` 可移除 slow path，用于反汇编和 benchmark
对照。

## 访问和原子

- `Private*` 是 owner-private SWCC；`SharedPayload*` 是共享 SWCC；树节点、root、
  EBR、transport 和共享同步对象使用 HWCC wrapper。
- HWCC ordinary wrapper 在真实 read/write 执行后记录 operation、cache-line 和
  可选 byte；普通 SWCC 访问不进入 HWCC ordinary totals。
- `CountedAtomic*` 在真实 load/store/CAS/exchange/fetch 操作之后记录，保留原
  memory order、CAS failure order、weak CAS 伪失败和 expected 更新。原子不同时
  记 ordinary read/write；统计按 HWCC、owner-private SWCC、local DRAM 分域。
- SCC WriteThrough 的真实 flush 后才发 SWCC explicit handoff；普通 SWCC write
  不会替业务协议自动失效其他 VM 副本。

## 远程事件模型

每个已 attach 的 pool 注册稳定 `(memory_domain,pool_id,pool_offset)` 映射。所有
跨 VM 远程结果进入共享的全局 sequence/event log，记录提交顺序，结束时进行完整
sequence 校验和离线 replay。模型区从 HWCC allocator 保留，默认容量为
`4,194,304 × 64B = 256MiB`；溢出、缺口、重复、非法 offset 或 replay 不一致
都会 hard-fail。

HWCC replay 区分 dirty-owner→remote-reader、dirty-owner→remote-writer/RMW、
clean-copy invalidation 和 dirty capacity eviction，并支持 LRU/LFU、实例、组相联、
容量和 aging/tie-break 配置。SWCC 只统计 explicit visibility handoff，不把模型
状态当成业务可见性证书。

## 验证入口

主要实现位于 `kv/engine/latency_inject.{h,cpp}` 与
`kv/engine/mem_access.h`；覆盖矩阵和审计规则见
`延迟插入审计报告.md`。验证包括 `latency_modes_test`、`region_allocator_test`、
shared protocol/partition/engine focused tests、完整非 VM CTest、关闭版 runtime
与 compile-off benchmark、4VM e2e08/e2e09/trace 以及小规模 YCSB smoke。

硬件模块 smoke 必须保持固定延迟值为零；正式功能 e2e/YCSB 结果使用四模块全关。
