# TigonKV 硬件模拟当前实现

本文只描述当前 `tigonkv` 路径。硬件模拟已经收敛为 fixed-latency-only；旧的访问
统计、原子计数、远程 cache 模型、共享事件日志和第二 ivshmem 不属于当前实现。

## 公共实现来源

固定延迟的公共实现（配置六字段解析、计费公式、scope、TSC busy-wait、pool 校验
与全部通用 wrapper）来自固定 Git 子模块 `thirdparty_libs/latency_sim`（gitlink
`5ed2a2e7cf670e52141a7d1908c4c62d70335cfd`，`my-work` 分支）。Tigon2 不再自维护
simulator；公共合同以子模块 `README.md`、`docs/` 与 `schema/fixed_latency.schema.json`
为准，本文只描述 Tigon2 接入点。

## 配置

唯一合法配置是 `tigon_kv.latency_inject.fixed_latency`，字段必须完整且只能是：

```jsonc
{
  "enabled": false,
  "cache_line_bytes": 64,
  "swcc_fixed_ns_per_line": 0,
  "hwcc_fixed_ns_per_line": 0,
  "foreground_enabled": true,
  "background_enabled": true
}
```

`Config::FromJsonc` 对同一原始 JSONC 调用
`latency_sim::ParseFixedLatencyJsonc(raw_text, ConfigLayout::kTigon2)`，六字段的唯一语义
权威是工具库；Tigon2 parser 只保留外层 `tigon_kv.latency_inject` 结构检查和
known-key 拒绝。解析器对外层未知、重复、缺失、错误类型以及历史字段 hard-fail；固定延迟
对象的六字段由公共解析器从原始 JSONC 统一校验，因此字符串中的 `//` 或 `/* */` 不会被
误删。
`enabled=true` 还要求
RelWithDebInfo、`verbose=false`、`extra_check=false` 和成功的 x86 TSC 校准；不能用
clock、sleep 或调度等待替代 TSC busy-wait。

## 计费规则

`kv/engine/mem_access.h` 是 Tigon2 极薄的项目内存域语义层（HwccRead、PrivateRead、
SharedPayloadRead、TransportRead 等），直接调用工具库公共 wrapper；计费入口全部在
工具库内：

```text
pending_delay_ps += touched_swcc_lines * swcc_fixed_ps_per_line
pending_delay_ps += touched_hwcc_lines * hwcc_fixed_ps_per_line
delay_ns         = round(pending_delay_ps / 1000)   // 仅作用域退出时一次
```

每行延迟在配置时转换为定点（皮秒），整个 scope 用 checked 定点累计，只在安全出口
统一取整：`0.4ns/line × 5` 精确等于 `2ns`、`1.25ns/line × 4` 精确等于 `5ns`；
乘法/加法/地址范围溢出 hard fail，不饱和到 `UINT64_MAX`。进程初始化时注册不可变的
HWCC/SWCC 映射边界，启用后每次收费先校验为 pool-local offset；错误 domain、越界和
无 scope 的真实共享访问都 hard fail。

wrapper 先执行真实 load/store/CAS/exchange/fetch，再按该次访问覆盖的 cache line
收费。同一行的两次真实访问收费两次；wrapper 嵌套不会把一次操作重复收费。原子
wrapper 保留真实返回值、CAS expected 更新、成功/失败结果和 memory order，但不累计
操作数。`SwccWriteback`/`SwccInvalidate` 只是审计标签，不增加第二份固定延迟；真实
`clflush`/`clwb`/fence 由 SCCManager 和 allocator 原样保留。纯本地 DRAM 原子
（B+Tree 对象内 root 槽、表统计等）直接使用原始 `std::atomic`，不经过 wrapper。

以下属于业务一致性而不是模拟统计，必须保留：

- TwoPLPasha SCC WriteThrough 的 readable bitmap、`clflush`、`clwb`、writeback 和
  invalidate 顺序；
- owner-private 规则、HWCC root/smeta/EBR/transport 原子、B+Tree OLC 和 PolicyClock；
- 业务 allocator 的容量/运行时统计。PolicyClock 的 LRU/second-chance 只决定迁移
  victim，不是延迟 cache。

## Scope 和安全点

- `KVStore` 的 Put/Get/Delete/Scan/CAS/Increment 建立 foreground scope。
- CXL receive demuxer 和 RPC request dispatch 建立独立 background scope；不继承前台
  pending delay。
- 独立 `KVStore::PollTransport()` 和 `KVEngine::Memory()` 在自身 background scope 中
  运行；前台线程注册也建立可识别 scope。
- 每个线程独立保存 pending delay。nested scope 只在最外层安全出口结算；RAII 早返回
  和异常会清空 pending 状态。
- Forward/PollTransport 的等待期切换 foreground scope 使用 RAII 挂起/恢复：挂起只
  延后 pending，busy-wait 延到最外层出口（EBR 和所有 guard 退出后）再结算。
- 释放 B+Tree/OLC latch、row/smeta lock、Clock/allocator lock、EBR guard、ring
  reservation、pin 和 SCC 发布中间态后才允许 busy-wait。

`enabled=false` 时，fast gate 之外不读 TSC、不建立 TLS、不换算地址、不获取锁、不做
统计原子、不创建共享状态/线程，也不映射额外设备。`LATENCY_SIM_COMPILE_OFF` 是唯一
编译期移除开关（默认 OFF；ON 必须使用独立 build 目录，如
`build-relwithdebinfo-compile-off`），私有 `TIGONKV_DISABLE_HARDWARE_SIMULATION`
已删除。compile-off ON 时 JSONC `enabled` 无法重新启用已移除的路径。

## 代码和测试入口

- 实现：`kv/engine/mem_access.h`（项目薄适配）+ `thirdparty_libs/latency_sim`（公共实现）。
- 生命周期：`KVEngine::Open` 在静默边界 `Configure({}) -> ClearPoolRegistrations ->
  RegisterPool(kHwcc/kSwcc) -> Configure(config.hardware_simulation)`；析构/关闭在全部
  worker join 后按静默边界收尾。
- EBR 安全出口：`PollTransport` 的每个 peer request 在独立 background scope 中执行，
  EBR guard 缩窄到 dispatch 内部并在 scope 结算前释放；外层 foreground scope 用工具库
  挂起/恢复机制延后到最外层出口结算。`DeferredLatencyScope` 已删除（不再复制 deferred
  算法）。
- 访问接入：`common/btree_olc_cxl`、`kv/engine`、CXL transport、SCC 和 TwoPLPasha。
- 定向测试：`tests/latency_modes_test.cpp`。
- 禁用 benchmark：`tests/hardware_sim_disabled_benchmark.cpp`。
- 配置拒绝、布局和 allocator 测试：`tests/tigonkv_tests.cpp`、
  `tests/kv_layout_test.cpp`、`tests/region_allocator_test.cpp`。

验证报告只记录当前构建、测试和本仓库干净 VM 的结果，不导出访问次数或模拟统计
schema。历史实现由 Git 历史保存，不在本文追加施工日志。
