# TigonKV 可见性与一致性设计

TigonKV 使用 `SCCManager` 的 WriteThrough 路径维护 migrated shared payload 的跨 VM
可见性。宿主 CPU 的硬件一致性、`msync` 或 checkpoint 都不是应用层跨 VM 协议。

| 对象/路径 | 区域 | 一致性责任 |
|---|---|---|
| owner-private row、private B+Tree | owner-private SWCC | 只有 owner 访问，使用原本地 OLC/atomic。 |
| shared B+Tree、root、smeta | HWCC | 跨 VM 原子元数据、OLC latch 和 EBR。 |
| migrated shared payload | shared SWCC | 行锁内按 SCC prepare/read/write/finish，必要时 flush/invalidate/writeback。 |
| transport ring | HWCC | 原 MPSC release/acquire 发布、消费和损坏 hard-fail。 |

SCC readable bitmap 只决定本 host 是否需要执行协议规定的 cache-line flush；它不是
fixed-latency 模拟器的命中/未命中模型，也不产生访问统计。写路径在原行锁和 SCC
发布顺序内清除其它 host 的 readable 状态、写回 payload，再释放锁。不要把这段业务
协议删掉或拆成无锁异步通知。

`tests/scc_test_backend.*` 提供非一致 host cache copy 的确定性测试后端；
`scc_protocol_test`、`kv_shared_protocol_test` 和 `kv_partition_test` 覆盖读取、写回、
迁移发布和负向可见性。fixed latency 只在 wrapper 累计 pending delay，并在安全
scope 出口结算，不改变 SCC 的 bit/flush/writeback 语义。

实验结果必须描述为 NUMA/ivshmem emulation，并说明 4VM、worker/demuxer、容量、NUMA、
trace 和固定延迟参数。
