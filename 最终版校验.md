# TigonKV 当前验收清单

本文是当前实现的短验收清单，不复述旧四模块或旧实验日志。

## 源码与配置

- [x] `latency_inject` 只有 fixed-latency 配置、line coverage、pending delay 和校准
  TSC busy-wait。
- [x] 旧访问统计、原子计数、remote cache/event/replay、instrumentation reserve 和
  第二 ivshmem 已从生产路径删除。
- [x] 业务 HWCC/SWCC offset、容量、allocator layout 和 layout version 28 一致。
- [x] SCC WriteThrough、owner-private、OLC、PolicyClock、EBR、真实 flush/invalidate
  和生产 pacing/backoff 仍在调用图中。
- [x] 旧 JSONC 字段 hard-fail；配置 fixture、根配置和脚本使用相同六字段。

## 构建和测试

- [x] Debug clean build。
- [x] RelWithDebInfo clean build。
- [x] compile-off build。
- [x] 非 VM host CTest（排除需要真实 QEMU 的测试）。
- [x] fixed-latency geometry/atomic/scope/RAII/disabled 定向测试。
- [x] runtime-disabled 与 compile-off disabled benchmark 各 5 次，结果在噪声内。

## VM

- [x] 使用本仓库自己的 kill/cleanup、镜像、配置、binary 和新 backing 创建 4VM。
- [x] 无延迟代表性 trace 通过。
- [x] 临时非零 fixed-latency 前台/后台 canary 通过且 clean shutdown。
- [x] 测试后 QEMU、ivshmem-server、PID、`/mnt/xz_vm_storage` 和
  `/mnt/xz_shared_mem` 均已停止/清理。

VM 命令、端口、镜像路径、trace 摘要和清理前后占用见 [验证证据.md](验证证据.md)
及 `/tmp/fixed-latency-only-cleanup-tigon2.md`；没有使用历史目录代替当前验证。
