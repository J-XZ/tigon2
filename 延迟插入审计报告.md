# TigonKV 软件延迟插入审计要求

> 本文件同时记录最终实现必须满足的规则、复核方法和本轮最终审计结论。它是
> [partition优化方案.md](partition优化方案.md) §3.11 的当前审计真值；历史轮次
> 通过 Git 查询，不再用本文件描述未完成的施工状态。

## 0. 当前审计结论（2026-07-30）

- 本轮把 legacy 与 KV 的 owner-local read/write lock、SCC refresh、read release 和
  commit release 收敛到同一 resolver 参数化的 `TwoPLPashaHelper` 主体。resolver 只把
  master 的 `migrated_row` VA 或 KV 的 `migrated_smeta_off` 解析为同一个 HWCC smeta；
  没有新增访问域、缓存、锁、flush 或延迟结算点。
- Scan Busy 活锁修复后：already-migrated 重建当前行 adjacency bit 仍在既有 smeta
  latch 内经原 `set_bit`/`clear_bit`（`HwccAtomicLoad/Store`）完成；邻居更新仍走
  `set_adjacent_migrated_rows`。未在 latch/Clock 锁内结算延迟，未新增 `mem_access`
  分类或 scope。
- 阶段 O：已删除全部 `DelayActiveScopeNow`/`scope_depth`；MPSC 与 helper
  release/abort 只累计访问，在非嵌套 `LatencyScope`/`EndScopeAndDelay` 安全点结算；
  `RunCxlIncomingLoop` 每轮一次 receive scope；`ReadStaticDomainUsedBytes` 覆盖
  Open/Memory 的 static domain 原子读；`RegionAllocator` 拆分
  `control_is_hwcc_`/`block_is_hwcc_`/`block_is_shared_payload_`（dynamic HWCC：
  control=private SWCC、block=HWCC；shared-payload block 走 SharedPayload*）；
  owner-private EBR retire 路径补齐 Private atomic/read/write。
- O4：共用 helper 旁 `LocalMetadataAccess`；offset-backed lmeta 临界区按
  Lock→lock→Read→…→Write→Unlock→unlock 记录；已删除
  `KVPartition` 外层 `RecordPrivateMetadata*`/`local_refreshed` 补记。
- `latency_modes_test`（Debug）已通过；disabled 热路径仍只经
  `InstrumentationEnabledFast` 门禁。
- Debug focused K、阶段 M host CTest、m-direct 与 10k A/E（stall=90）证据见
  [验证证据.md](验证证据.md)。正式 latency-enabled A/E 仍只在阶段 P 执行一次。
- Scan-range migrate 单飞：`PartitionDirectoryEntry::scan_range_migrate_inflight`
  （layout 26）仅 `HwccAtomicLoad/Store/Rmw` 于 TryBegin/End、`EnsureInShared`/
  remote-insert / ScanLocal 门禁与 point-migrate move_out 跳过；不在 leaf/
  Clock/smeta 锁内结算。Scan-migrate 可在 move_out 前 Flush；AwaitResponse
  优先排空响应（§3.9.1）。未新增 mem_access 分类。远端 ScanShared 不读该 flag。

## 1. 启用门禁与禁用开销

- 唯一配置入口是 `tigon_kv.latency_inject`；字段必须完整、类型严格，
  duplicate、unknown、missing 和非法数值都应 hard-fail。
- `enabled=true` 只允许 `RelWithDebInfo`、`verbose=false`、
  `extra_check=false`，并要求 foreground 注入开启。
- 启用前必须完成 x86 TSC 校准；校准失败或运行时状态异常必须 hard-fail。
- 延迟等待只用 `rdtsc + _mm_pause`，不得用 `sleep_for`、`sleep_until`
  或调度器 `yield` 作为回退。
- `enabled=false` 的热路径门禁使用低开销 relaxed load；不得读取 TSC、
  建立 TLS cache/filter 状态或执行访问分类。
- 正式比较默认使用 `cache_model=none`，除非 TigonKV 和 cxlkv 使用完全相同的
  cache 模型与参数。

## 2. 内存区域分类

- 进程本地配置、范围边界、TLS 和统计：不插入 CXL 延迟。
- owner-private B+Tree、row、allocator control/used-peak counter 和 Clock
  私有状态：通过 SWCC private wrapper 计费。动态 HWCC/shared-payload arena
  的不可变边界在 HWCC；边界发布是 HWCC write/flush，之后的 allocate/free
  不得为会计再访问全局 HWCC。
- 跨 VM 共享的树节点、root/layout、smeta、EBR 和 transport：
  通过 HWCC wrapper 计费。
- 共享 value payload：普通访问按 SWCC/SCC payload wrapper 计费；
  `clwb`/`clflush` 等维护操作单独按配置计费。
- 任何对象的计费依据是其实际物理区域和访问类型，不能依据函数名、
  owner 身份或调用来源猜测。
- 按实施方案收敛为固定长度 value 后，每次 payload 访问只按实际固定字节数
  计费，不再为已删除的动态长度字段增加访问。

## 3. 作用域与等待结算

- 前台 Get/Put/Delete/Scan 在 facade 进入 foreground scope。
- demuxer 和 peer request 服务使用独立 scope，不能继承调用线程的前台
  计费状态，也不能漏记其真实共享内存访问。
- 软件延迟只在非嵌套阶段 scope 的安全末端统一结算。不得在 B+Tree latch、共享
  smeta latch、Clock 全局锁、逻辑 row lock/ref、EBR 关键等待或 ring reservation
  持有期间 busy-spin。transport ready / response / valid 等跨 VM 可见发布可以
  先于最终软件延迟完成，前提是本阶段累计的真实访问已记入、且结算点不再持有
  上述锁/pin/reservation。
- 不得在 B+Tree latch、共享 smeta latch 或 Clock 全局锁内执行延迟忙等。
- SCC 保持原 `prepare_read` / `finish_write` 调用顺序：位图更新、`clflush` 或
  `clwb` 均在原 smeta latch 覆盖范围内完成；延迟仅在随后安全结算点支付，不能把
  `finish_write` 拆成无锁的位更新与 flush。
- 首次启动中，加入 VM 仅在 layout 仍为 `Initializing` 时构造其 own-private
  arena/root 并发布 owner-ready；`Ready` 后的 attach 只等待/读取已发布的 HWCC
  layout，绝不再次写 root。二者的 state/root/ready 原子访问都走 HWCC wrapper，
  但启动等待不作为前台操作延迟结算。
- 若协议要求持有 reader/ref/write pin 才能保护对象寿命，可在 pin 存续期间
  结算，但结算后必须再次确认发布顺序和对象仍然有效。
- transport 满、协议冲突和测试编排的退避不属于软件延迟；不得把这些等待计入
  内存访问，也不得用它们掩盖遗漏的延迟。

## 4. 计费规则

- 一次真实内存访问只记一次；wrapper 嵌套不得重复计费。
- payload copy 与后续 flush 是两种真实动作，应分别计费。
- transport ring 位于 HWCC，只按 HWCC read/write/atomic 计费。
- allocator header、Clock 元数据、树节点和 row 必须按其最终放置位置计费，
  不能只计 payload 而漏掉元数据。
- 原子读改写按配置约定记为一次 atomic；不得再叠加等价 read/write。
- B+Tree 的延迟域绑定必须稳定且可验证；禁止依赖“本线程最近访问的树”等隐式
  状态，以免并发时把 private/shared 节点归错类。
- `insert_and_process_adjacent_tuples`、
  `remove_and_process_adjacent_tuples` 与 `lookupForNextKeyUpdate` 的 CXL
  机械适配只复用现有 `TreeAccessScope` 和 `RecordTreeAccess`：目标 leaf 与为
  相邻回调额外锁定的 leaf 均按其 allocator domain 记录一次节点访问。回调中的
  key/value 访问继续由既有 leaf entry wrapper 记录；不会在 leaf latch 覆盖范围
  内结算延迟，仍由外层操作安全点统一结算。
- `Memory()` 只能读取本 VM 的 owner-private allocator counters；实验 runner
  聚合各 VM 的动态部分，静态 HWCC 前缀只由 VM0 报告一次。
- 统计必须满足 `raw_accesses = hits + misses`，并能分别核对 read、write、
  atomic、flush 次数和累计模拟时间。

## 5. 最终路径审计清单

功能实现稳定后，逐条沿真实调用栈检查：

1. owner-private Get，以及 remote shared-hit/shared-miss Get。
2. Put 的 existing/new-key 路径，以及 CAS、Increment。
3. owner 和 remote Delete。
4. owner 本地 Scan、remote Scan、move-in 后重试和跨范围 Scan。
5. Clock victim 选择、move-out、move-in 和失败回滚。
6. transport enqueue/dequeue、response 发布、peer request 服务和 EBR。
7. 初始化、attach 与 root/layout 发布。

每条路径都要记录并核对：

- 访问对象实际位于 HWCC、SWCC private 还是共享 SCC payload；
- 操作是 read、write、atomic、copy 还是 flush，以及实际字节数；
- 延迟结算时持有哪些锁、pin 或引用；
- 结算是否早于相应发布点；
- 同一真实访问是否被上层和下层重复记录；
- 禁用延迟时是否只剩最小门禁开销。

如果功能修复改变了上述任一调用路径，先完成该功能的无延迟测试，再重做对应
条目的审计；不要在已知功能 bug 尚未修复时反复调整埋点。

## 6. 必要验证

先在 `latency_inject.enabled=false` 下执行：

- 与改动直接相关的单元测试；
- B+Tree binding、shared protocol、partition、engine、allocator 测试；
- Debug 下针对历史故障的重复测试；
- 四 VM 小规模 Get/Put/Delete/Scan 与 YCSB-A/YCSB-E smoke。

功能稳定并完成代码审计后，再执行：

- `latency_modes_test` 和严格配置解析测试；
- RelWithDebInfo 下 enabled/disabled 对照；
- 四 VM 小规模 enabled 延迟测试；
- 统计恒等式、访问分类、无 sleep fallback 和发布前结算的自动化断言；
- 必要时用 GDB 定位 stall，不能把超时当成协议错误的位置证据。

## 7. 完成标准

- 最终 Get/Put/Delete/Scan、migration、transport 和 EBR 路径均已逐项审计。
- 启用模式没有遗漏、重复、错误区域或错误字节数的计费。
- 禁用模式没有 TSC、cache/filter 或其他非必要热路径开销。
- 没有在锁内 busy-spin，没有发布后才补延迟，也没有 sleep 回退。
- 功能测试先通过，随后延迟模式测试通过。

后续只维护这份当前规则和最终审计结论；历史轮次、旧符号和已替换实现通过
Git 查询，不再追加到本文。
