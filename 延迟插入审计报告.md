# TigonKV 固定延迟插入审计

本文件是当前实现的短审计结论。它只覆盖 fixed-latency-only 路径；历史模块和历史
实验轮次不在这里维护。

## 1. 配置与 fast path

合法配置入口只有 `tigon_kv.latency_inject.fixed_latency`，六个字段为
`enabled`、`cache_line_bytes`、`swcc_fixed_ns_per_line`、`hwcc_fixed_ns_per_line`、
`foreground_enabled`、`background_enabled`。未知、重复、缺失和历史字段必须被解析器
拒绝。启用要求 RelWithDebInfo、verbose/extra_check 关闭和校准成功的 x86 TSC。

禁用路径通过一个进程本地 relaxed feature gate 直接执行原始操作：不读取 TSC、不建立
TLS、不分配状态、不使用共享映射、不使用锁或统计原子。编译期关闭选项应移除 wrapper
slow path；`hardware_sim_disabled_benchmark` 对比运行时关闭与编译期关闭。

## 2. 访问覆盖

每次执行型 wrapper 只记录它所执行的真实访问，按地址范围覆盖的 line 数累加：

```text
pending_delay_ns += touched_swcc_lines * swcc_fixed_ns_per_line
pending_delay_ns += touched_hwcc_lines * hwcc_fixed_ns_per_line
```

零字节不收费，非对齐和跨 line 访问按首尾 line 计算，同一真实访问的重复调用各自收费。
原子操作作为一次真实 RMW/load/store 收费，不再叠加 ordinary read/write；CAS 成功和
失败都执行一次 wrapper，保留 expected、weak/strong 和 memory order 语义。

访问域复核：

| 路径 | 域 |
|---|---|
| owner-private row、private tree、private allocator/Clock/EBR | owner-private SWCC |
| shared tree、root、smeta、EBR epoch、transport | HWCC |
| migrated payload、SCC bitmap 保护的 value image | shared SWCC |
| local DRAM、配置、进程 runtime stats | 不收费 |

SCC 的真实 copy 产生的共享内存读写按对应 wrapper 收费；flush、invalidate、writeback
指令及其审计标签不额外收费。SCC 位图和发布顺序不因删除模拟统计而改变。业务
memory/runtime stats 仍可存在，但不得作为硬件模拟 schema 或访问计数输出。

## 3. Scope 与结算点

前台 facade 建立 foreground scope；CXL receive demuxer 和 RPC dispatch 建立独立
background scope。每个线程有独立 pending delay，nested scope 共享当前线程状态并
只在最外层结束时结算。RAII 析构在异常和早返回时清理状态。

结算点必须位于以下对象之外：B+Tree/OLC latch、row/smeta/Clock/allocator 锁、EBR
critical guard、ring reservation、SCC 发布中间态以及仍阻塞其它 worker 的 RPC/队列
状态。若访问发生在异步 worker 中，worker 自己建立 scope 并在自身安全出口结算，不能
借用提交线程的 scope。

等待使用校准 TSC 与 `_mm_pause` busy-wait；禁止 `sleep_for`、`nanosleep` 和 scheduler
fallback。生产 Put 的 poll/yield/backoff、OLC 的原有竞争退避和协议 Busy retry 是
业务控制流，不得替换成模拟延迟或为了延迟测试而删除。

## 4. 验证矩阵

- 0B、单 line、跨 line、非对齐、溢出和不同 HWCC/SWCC 数值；
- 同一行重复访问、普通 load/store、exchange/fetch、CAS 成功/失败和 memory order；
- nested scope、foreground/background 隔离、异步 worker、异常/早返回清理；
- disabled 不校准 TSC、不建立 TLS、不产生共享状态；
- 旧配置字段 hard-fail，DumpStats 不包含模拟统计 schema，布局不保留隐藏 reserve；
- Debug、RelWithDebInfo、compile-off build，随后才是本仓库干净 4VM 的无延迟 trace 和
  非零 fixed-latency canary。

详细当前结果见 [验证证据.md](验证证据.md)；比较边界见
[当前对比口径.md](当前对比口径.md)。

## 当前实现边界

上述结论全部基于固定子模块 `thirdparty_libs/latency_sim`（gitlink
`5ed2a2e7cf670e52141a7d1908c4c62d70335cfd`）的公共实现：Tigon2 不再自维护
simulator/TSC/TLS/计费/六字段 parser，只保留 `mem_access.h` 薄语义层、`KVEngine`
生命周期与 EBR 安全 scope 分类。审计入口改为 `latency_modes_test`、
`hardware_sim_disabled_benchmark` 与 `硬件模拟当前实现.md`。
