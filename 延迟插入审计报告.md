# TigonKV 延迟插入审计

本文记录当前软件延迟模拟的接入位置、边界和验证要求。修改
`LatencySimulator`、`mem_access`、`latency_inject` 配置、cache filter、scope
或访问埋点时必须同步更新本文。

## 启用门禁与等待实现

- 运行时唯一配置位置是 `tigon_kv.latency_inject`。该对象及其 19 个字段全部
  必填、类型严格，且每个字段只能出现一次；顶层/其它对象中的 latency 字段、
  duplicate、unknown、missing 或错误类型均 hard fail，不能回退到 C++ 默认值。
  `cache_capacity_lines` 与 `cache_associativity` 必须大于零。
  `TIGONKV_POLICY_CONFIG_JSON`/`CXLKV_POLICY_CONFIG_JSON` 不是第二配置权威，
  runner 收到非空值会拒绝并提示改用完整 experiment config。
- `latency_inject.enabled=true` 仅允许
  `RelWithDebInfo + verbose=false + extra_check=false`。构建类型由 CMake
  注入，不可识别、Debug、Release、verbose 或 extra_check 均在创建
  `KVStore` 前拒绝。`e2e_trace_runner` 还对独立的
  `TIGONKV_E2E_VERBOSE/CXLKV_E2E_VERBOSE` 做同样门禁。
- 当前没有独立 merge worker，迁移和 EBR 维护都发生在 foreground 请求内；
  因此启用注入时必须同时 `foreground_enabled=true`。仅开
  `merge_enabled` 会形成零注入的伪启用配置，配置校验会直接拒绝。
- 启用前必须成功完成 x86 TSC 校准。校准值非有限数或不大于零时直接失败；
  运行中校准状态异常则打印 `TIGONKV_LATENCY_FATAL` 并 abort。
- `DelaySpinNs` 只使用 `rdtsc + _mm_pause` 忙等，不含
  `sleep_for`、`sleep_until`、`yield` 或非 x86 降级。代码中其余 sleep/yield
  是协议冲突、transport 背压或测试编排退避，不是延迟注入回退。
- 所有纳秒配置使用 `double` 解析，接受小数和指数形式；非有限值和负数被拒绝。

## Scope 与 fast gate

- `mem_access::LatencyScope` 是统一 RAII scope。门面 Put/Get/Delete/Scan/CAS/
  Increment/MoveOut/Checkpoint/PollTransport 使用 `kForeground`。
- MPSC inbound demuxer是实际读取入站 HWCC ring 的线程，单独建立
  `kForeground` scope；scope 不跨线程继承。前台 worker 执行 deferred
  request 时保存外层 pending、建立隔离 request scope，并在发布 response
  前先于安全点结算该请求的数据库访问延迟，避免把 peer 延迟转嫁给帮助线程
  的本地操作。
- transport 访问也遵守因果发布边界：enqueue 在 `is_ready` release-store
  前结算本次 ring/data HWCC 延迟；demuxer 在把 response 标为 done/notify
  或把 request 放入 deferred FIFO 前结算 recv 延迟。不能只在整批 recv 或
  request scope 析构时补齐，否则 peer 会在模拟延迟实际支付前继续执行。
  dequeue 每次 ready poll 后结算，释放 slot 前再次结算；full-ring retry
  在下一次探测前结算，避免宿主机速度放大轮询次数。
- shared SCC 访问在不持 HWCC smeta latch、但 reader/ref pin 或 write lock
  仍有效时结算 payload/maintenance 延迟；随后才发布 reader/write 完成。
  这保留原 2PLPasha 的行级占用时序，也防止 peer 在模拟延迟支付前消费新值。
- 当前 Clock move-in/move-out 与 EBR 回收均同步发生在前台操作或 checkpoint
  内，没有独立后台维护线程，因此没有额外建立 `kMerge` scope，也不会重复
  计算同一次迁移。未来若增加独立维护线程，必须在线程任务入口建立
  `kMerge`。
- wrapper 只累计线程本地 pending delay；默认在最外层 scope 退出时补齐，
  只有上述跨 VM 因果发布边界会在不持 HWCC latch 的协议安全点提前结算。
  `enabled=false` 时 scope 和访问 wrapper 均在 relaxed fast gate 返回，
  不进入 TLS/cache 状态。

## 当前计费窗口

- owner-private `PrivateRow` latch：SWCC atomic；实际触及的行头字段范围及
  key/value memcpy：SWCC read/write，不计固定 value 容量中未访问的尾部。
- migrated shared payload 的 GET/PUT/CAS/INCR、move-in/out 和 SCAN：
  SWCC read/write；SCC `clflush/clwb`：SWCC flush。两种 maintenance 都
  强制支付完整 flush 成本且计 miss；CLFLUSH/invalidate 移除 LRU tag，
  CLWB/writeback 保留已有 tag但不装入缺失 tag。
- `TwoPLPashaMetadataShared::atomic_word` 的 load/store/CAS：HWCC atomic
  load/store/RMW；flags、ref count、writer preference、value length 和 Clock
  second-chance metadata：按真实普通 HWCC read/write 记录。
- private/shared B+树：每次 node visit 以一条 metadata cacheline 近似，并对
  latch 与实际 key/value/link 连续范围精记；private 节点归 SWCC，shared 节点
  归 HWCC。下降路径按 read，insert/erase/split/merge 的连续源/目标范围按
  read/write；不再把 mutation 访问的整条下降路径误标成 write，也不把每次
  操作伪计为完整 4 KiB 页面。shared published-root slot 按 HWCC atomic
  load/store 记录。
- MPSC enqueue/dequeue 的实际消息数据区和 entry metadata：HWCC
  write/read；head/tail/count/ready 按实际 HWCC atomic 访问记录。空 ring
  探测不伪造 payload 访问。
- CXL EBR epoch、layout/root、partition migration sequence、HWCC allocator
  shard/free-list 与共享 domain counter 均按实际 HWCC atomic 访问记录；
  SWCC allocator/owner arena 的 owner-local 原子不伪计成 HWCC。EBR 与
  partition 的不可变拓扑/key/value 尺寸在 worker attach 时读取一次并缓存到
  进程本地，避免每个操作人为重复收取 HWCC 配置读取。
- checkpoint/allocator visibility 的实际 `clflush` 范围记为 SWCC
  invalidate（写回并驱逐）；SCC 的 `clwb` 记为 writeback、`clflush` 记为
  invalidate。HWCC header maintenance 不误计为 SWCC。data write 与随后
  maintenance 是两个真实动作，不视为重复埋点。

明确不覆盖：初始化配置生效前的 pool/layout 构造访问、进程本地 DRAM
容器/TLS/统计、日志和第三方库，以及真实 CPU cache 的预取、MLP、乱序和
coherence invalidation。allocator 的进程内 TLS cache 本身不计；checkpoint
只计真实 flush 范围。数据结构不同，所以与 cxlkv 比较 raw line 数量级与
分池自洽性，不要求绝对相等。

MPSC 数据区属于 HWCC；其真实 CLWB/CLFLUSH 不再另加一笔 SWCC maintenance
费用，transport 的 HWCC data read/write 已是本比较口径。单仓额外加费会同时
错分 pool 并造成重复计费。

## 防重复与正确性约束

- 调用点只通过 `kv/engine/mem_access.h` 记录，不散落直接
  `GlobalLatencySimulator().Record*`。
- shared payload 的 memcpy 与 SCC flush 分开记录；transport 只在成功传输
  消息时记录数据区；B+树 wrapper 已从“入口整页一次”改为“每个实际遍历节点
  一次”，避免根节点重复及整页过计。
- cache filter 仅改变 miss/hit 延迟，不改变 raw。每个 pool 必须满足
  `raw=hits+misses`，总延迟必须等于 SWCC 与 HWCC delayed ns 之和。
- fixed-hit 模型没有地址状态；writeback/invalidate 不抽 RNG，后续普通访问
  仍按固定概率。当前 cxlkv 未镜像 maintenance 修正前，跨仓正式对比仅允许
  `cache_model=none`，per-thread-LRU 只能在两仓同步补丁后使用。

## 针对性验证

- `latency_modes_test`：disabled fast gate、range 折行、fixed hit、组相联
  LRU、cache-hits disabled、writeback 保留与 invalidate 驱逐、maintenance
  强制完整成本、统计 reset，以及启用时 TSC 校准路径。
- `unit_tests`：19 字段逐项缺失、错误类型、重复、跨对象错位、双
  `latency_inject`、零 cache capacity 均拒绝；Debug 启用拒绝；
  RelWithDebInfo 下 verbose/extra_check 拒绝；小数/指数纳秒解析；门面
  启用/禁用时统计副作用。
- `kv_shared_protocol_test`：共享元数据访问产生非零 HWCC raw/miss。
- `btree_binding_test`、`kv_partition_test`：private/shared tree、迁移、SCC
  路径功能回归。
- `kv_engine_test`：transport/demux scope 接线后的并发与消息回归，并在
  latency-enabled focused ring 上断言 enqueue publication 与 dequeue slot
  release 后 pending 已结算、transport 只产生 HWCC 统计。

正式启用报告必须保留构建类型、verbose/extra_check、cache model、
cache-hits 状态，以及 SWCC/HWCC 的 raw/hit/miss/delayed ns。

## §14.19 最终路径复核（Scan / Wire / Clock 落地后）

复核日期：2026-07-27；相对 commit 含 CXL-first Scan、变长 Wire、SWCC Clock
tracker、O(1) migrated_key_count。

### disabled 快门

- `InstrumentationEnabledFast()` = `g_instrumentation_enabled` 的 relaxed load。
- `mem_access::{Record,LatencyScope,Delay*}` 均先过该门；`enabled=false` 时不
  读 config、不读 TSC、不更新 filter/TLS pending、不 spin/sleep。
- `DelaySpinNs` 仍禁止 sleep 回退；transport/Clock 路径上的 `yield`/`sleep_for`
  仅用于 ring 背压与 demuxer idle，不是注入实现。

### 变长 Wire（§11.4）

- `SendTransportMessage` 使用 `WireSize(message)=header+value_size` 入环；
  `network_tx_bytes` 按实际 wire 累计。
- demuxer `recv` 后 `ValidWireFrame(received, message)` 校验
  `received == header+value_size`；`network_rx_bytes` 用 `received`。
- `MPSCRingBuffer::enqueue/recv` 对 `entry->data` 只 `TransportWrite/Read`
  `data_size`/`dequeue_size` 字节，不再按满 `sizeof(KvMessage)` 计费。

### Clock 锁外结算（§11.15）

- `ClockEvictUntilUnderBudget`：在 `MoveOutForMigrationManager` 前
  `ClockUnlock`；MoveOut 内再短持锁做 untrack。
- `PolicyClock::move_row_in`：重路径在锁外，成功后短锁 `ClockTrack`。
- track/untrack/cursor：补齐 SWCC `RecordPrivateRowState*`（含 clock 链接）
  与 directory `clock_head/tail/cursor` 的 HWCC read/write；`migrated_key_count`
  走 `HwccAtomicRmw`。Record 可在自旋锁内；`DelayActiveScopeNow` 仍只在
  MoveOut/promote 等协议安全点、锁外调用。

### Scan / 点路径

- CXL probe / ScanOwned 在 `RequestServeDepthGuard` 内，嵌套 Poll 不 ServeDeferred。
- shared 点路径无 helper 内 sleep；Busy 上抛由 engine yield 重试。
- owner-private SWCC 与 shared payload 仍分池计费；Clock 链接不进进程 DRAM。

### 验证

- `latency_modes_test`、`kv_partition_test`、`kv_engine_test` 覆盖 disabled
  gate、Clock move-out、变长 wire 与 Scan；正式对比仍要求
  RelWithDebInfo + `cache_model=none` + `--no-latency` 或双方同步 latency。

