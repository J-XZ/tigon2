# 当前恢复入口

当前开发与验证直接以 `my-work` 工作树为准，不再使用旧 `tigonkv-plan` 分支、
服务器临时构建目录或旧实验日志作为实现合同。

当前共享布局版本为 9，allocator 的 HWCC/SWCC 固定 metadata 与动态对象分别
做 physical accounting。远端 Scan 的交付路径是 owner range move-in/pin 后由
requester 只读 shared CXL tree；旧 `ScanRPC`/部分 CXL 双源路径不属于当前合同。
每 VM 的 KV CPU 口径必须写作 `foreground=N + demuxer=1`。

恢复后先阅读：

1. [当前对比口径.md](当前对比口径.md)：当前数据路径、线程/CPU 和公平对比口径；
2. [PLAN.md](PLAN.md)：设计决策和验收矩阵；
3. [YCSB指南.md](YCSB指南.md)：当前脚本默认值和正式运行约束。

从源码重建：

```bash
git submodule update --init --recursive
cmake -S . -B build-relwithdebinfo -DCMAKE_BUILD_TYPE=RelWithDebInfo
cmake --build build-relwithdebinfo -j2
ctest --test-dir build-relwithdebinfo --output-on-failure
```

真实 4 VM 验证使用 `experiment_config.jsonc`、
`scripts/e2e/run_guest_e2e_workflows.sh` 和
`scripts/e2e_trace/run_guest_ycsb_workflows.sh`。VM、SSH、NUMA 与共享 backing
必须由调用者明确准备；仓库脚本不得依赖旧 VM 进程、guest 临时文件或未清理 pool。

构建目录、VM 镜像、Python cache 与实验输出都是主机相关派生物，不应当作可移植
恢复状态。`thirdparty_libs/YCSB-cpp` 应保持仓库固定的版本并从源码重新构建。
