# TigonKV 双区域内存布局

`DualRegionMappedPool` 将共享 backing 划为不可重叠的 HWCC 与 SWCC；这是一种
NUMA/ivshmem 模拟布局，不是实际 CXL 介质声明。

```text
backing mapping
├─ HWCC [hwcc.offset_mb, hwcc.offset_mb + hwcc.size_mb)
│  ├─ SharedLayoutHeader、allocator/layout、partition directory 与 shared roots
│  ├─ shared B+Tree、smeta（latch/ref/adjacency/Clock second chance）
│  ├─ transport ring、EBR global/local epoch
│  └─ 仅由 SCC 外保护的跨 VM 原子同步状态
└─ SWCC [swcc.offset_mb, swcc.offset_mb + swcc.size_mb)
   ├─ owner-private arena：owner B+Tree、PrivateValueStruct/lmeta、Clock tracker、EBR retire queue
   └─ shared SCC allocations：tid/valid/header/payload
```

所有共享布局只保存 `RegionOffset`，绝不发布进程虚拟地址。private arena 仅 owner VM
访问；它仍使用本机 cache coherence 与原本地 OLC/atomic/spinlock，不额外套 SCC。
shared SWCC 的 tid/valid/row image 只能与 payload 一起经 SCC prepare/finish/flush；
其余跨 VM 同步字段不得放入 shared SWCC。

启动依次发布 `Initializing → owner-ready bitmap → Ready`。attach 校验 layout、
配置 hash、区域边界、VM/partition 数和固定 key/value 尺寸；没有 checkpoint、
clean/dirty 状态、restart generation 或恢复路径。

分配器按 `AllocationDomain` 严格记账，跨域释放与非法 offset 硬失败。正式配置为
32GiB backing：HWCC 1024MiB（offset 0）、SWCC 31744MiB（offset 1024MiB）。
`hw_cc_budget_mb` 是 PolicyClock 的 dynamic budget 参数而非物理容量；Open 在 layout、
allocator、transport 与 EBR 静态占用后 clamp 可用动态预算。报告必须同时给出
`physical_hwcc_capacity_bytes` 和 `owner_migration_dynamic_budget_bytes`。

未迁移行只存在 owner private row；migrated 行由 shared SCC value 和 HWCC locator
构成单一权威。move-in/out、Delete 保持原 helper 的 locator、pin、adjacency、
shared-index 和 EBR 顺序；Delete 才完整退休相应对象。

实现和 focused 覆盖主要在 `kv/engine/region_allocator.*`、
`kv/engine/kv_types_layout.h`、`tests/region_allocator_test.cpp`、
`tests/kv_partition_test.cpp` 与 `tests/cxl_ebr_test.cpp`。
