# TigonKV 双区域内存布局

这是 NUMA/ivshmem shared-memory emulation 布局，不是实际 CXL 介质声明。一个业务
backing 被划为不重叠的 HWCC 与 SWCC：

```text
backing
├─ HWCC [hwcc.offset_mb, hwcc.offset_mb + hwcc.size_mb)
│  ├─ SharedLayoutHeader、allocator/layout、partition directory、shared roots
│  ├─ shared B+Tree、smeta、Clock second-chance bit
│  ├─ transport ring、EBR global/local epoch
│  └─ 跨 VM 原子同步元数据
└─ SWCC [swcc.offset_mb, swcc.offset_mb + swcc.size_mb)
   ├─ owner-private B+Tree、row/lmeta、Clock tracker、EBR retire queue
   └─ 经 SCC 保护的 migrated tid/valid/value image
```

共享布局只保存 `RegionOffset`，不保存进程 VA、vptr 或 allocator 对象。owner-private
区域只有所属 VM 访问；shared SWCC payload 必须按 SCC 的 prepare/read/write/finish
顺序操作。HWCC 的 root、smeta、epoch 和 transport 使用真实 release/acquire/OLC/EBR
语义。

启动按 `Initializing → owner-ready bitmap → Ready` 发布；attach 校验 layout version、
config hash、区域边界、VM/partition 数和定长 key/value。旧 backing 不得用于证明当前
布局。

当前 layout version 为 28。普通 layout metadata 后的所有配置 HWCC 都属于业务
allocator；不存在为访问统计、事件日志或其它延迟模拟模块保留的隐藏区域。默认根配置
为 32GiB backing：HWCC 1024MiB、SWCC 31744MiB。`hw_cc_budget_mb` 是 PolicyClock
动态预算，报告时必须和物理容量分开。

实现入口：`kv/engine/region_allocator.*`、`kv/engine/kv_types_layout.h`、
`tests/region_allocator_test.cpp`、`tests/kv_layout_test.cpp` 和
`tests/cxl_ebr_test.cpp`。
