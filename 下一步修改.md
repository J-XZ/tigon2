# 下一步修改：当前实现全面审查后的封闭工作清单

> 审查日期：2026-07-29
> 原始实现基准：每个施工阶段开始时实时解析的当前 `master` 头部；本文不固定
> 历史 SHA
> 本轮审查时的生产源码摘要（`common core protocol kv`，只标识审查输入）：
> `6f9ae4376007d822a0c2c35f2bade4eb2dd7fbfe7146d8786f1387090191df7c`

该摘要及P阶段最终摘要唯一按仓库根目录下的全部普通文件计算，包含tracked与
untracked生产文件，不按Git状态筛选：

```bash
find common core protocol kv -type f -print0 |
  LC_ALL=C sort -z |
  xargs -0 sha256sum |
  sha256sum
```

最终一行的首字段就是摘要。不得改成只hash diff、`git ls-files`或忽略untracked文件
的另一种算法。

## 0. 文档定位与结论

1. 本文是对当前工作树的审查结果和后续执行清单，**不是第二份设计合同**。
   `partition优化方案.md` 始终是唯一施工合同；本文不得改变其中的目标、固定施工
   顺序、正确性边界和最终验证要求。
2. 审查基准只使用当前 `master` 头部和当前工作树，不以旧日志、历史 SHA、旧 VM、
   旧 backing 或旧实验结果替代源码证据。
3. 当前实现已经完成不少基础改造，但仍存在启动发布竞态、原实现复用不充分、
   CAS/Increment locator 竞争、remote-insert rollback 可见性、Scan/Clock/Delete
   并发语义偏差、private root 双层权威、offset resolver 只验全池边界、
   固定协议开关依赖 `Context` 默认值、公共操作 EBR 重复进入、
   owner lmeta复制并扩大的布局、
   内部仍接受短 key/value并补零、每-worker 统计双槽、测试容量夹具失配、
   guest入口host/remote进程隔离名单不完整、
   引用/allocator 会计下溢保护不完整、
   错误分类、
   owner-private/HWCC 裸访问、allocator control/free-block 访问域混淆和延迟结算
   问题。当前状态不能表述为已完成
   `partition优化方案.md`。
4. 本文只记录本轮静态审查确认的问题和修改/测试方案。本轮不修改生产代码，也不
   运行测试；后续施工必须先取得相应最小复现、Debug/GDB 栈或不变量证据，再实施
   直接根因的最小修复。
5. 后续采用实验室快速验证标准：

   - 同一阶段的多个问题合并修改后只运行一次阶段 focused gate；问题项中的
     “必要验证”是覆盖责任，不代表每条 bullet 都单独启动一次测试；
   - 不在每个小修改后重复跑全部 CTest 或完整 YCSB；
   - 功能稳定后只执行一次 `partition优化方案.md` §5 规定的最终验证；
   - 已通过阶段不因无关修改重跑；失败调试只反复运行最小复现，直接根因修复后
     再把该阶段 gate 完整运行一次；
   - unit/CTest 与端到端测试绝不并行；
   - 任何真实 4VM 测试前后都清理旧 QEMU、旧 runner、旧
     `kv_engine_test`/`kv_partition_test` 和旧 backing，确认四台 VM、SSH、
     ivshmem、NUMA 和每 VM runner 都正常后才采信结果。

## 1. 本轮审查范围与方法

本轮逐项检查了：

- `partition优化方案.md` 的 §3.0--§3.16、§4 固定施工顺序和 §5 最终验证；
- 当前 `master` 与工作树在 `common/`、`core/`、`protocol/`、`kv/`、CMake、
  tests、scripts、tools 和根目录 Markdown 的差异；
- B+Tree/OLC 与 private/shared root、TwoPLPasha helper、SCC、PolicyClock、
  Message/dispatcher、MPSC、allocator、EBR、owner/private/shared 路径、Delete、
  CAS、Scan、RuntimeStats 和 Busy retry；
- 正式 4VM 配置、全部有效测试 fixture、HWCC/SWCC 容量、range partition、32B
  KV、worker/demuxer 口径和现有验证证据；
- 旧版 `下一步修改.md` 中每个结论是否仍适用于当前源码。

后续每个生产补丁仍必须重复以下最小差分闸门：

```bash
git diff --function-context master...HEAD -- common core protocol kv
git diff --function-context master -- common core protocol kv
git ls-files --others --exclude-standard -- common core protocol kv
git diff --check
```

第二条是包含index和未提交工作树修改的当前事实；第三条列出的每个生产新文件必须再
执行`git diff --no-index --function-context /dev/null <path>`并纳入同一allowlist。
`master...HEAD`只用于区分已经提交的分支差异，不能替代后二者。若`--no-index`
因“确有差异”返回1，只按正常diff结果处理；其它返回码才是闸门失败。

所有当前差分只能归入 `partition优化方案.md` §3.0 的 A--E 类。不能归类的控制流
必须恢复到 `master`，不能通过补文档把它加入 allowlist。

## 2. 已确认满足或旧问题已修复的事项

以下项目不再作为待修问题。后续不得为了“再次修复”而改写它们：

1. 正式配置的物理 HWCC 已是 1024MiB，shared backing 为 32GiB；不存在正式路径
   仍只有 16MiB HWCC 的问题。现有小容量 fixture（包括两个16MiB-HWCC JSON
   fixture及B3逐项列出的代码内fixture）不代表正式容量，但违反当前测试下限，
   已统一单列为 B3，不能再据此增加小容量兜底。
2. `KVStore` facade 对真实 `Busy` 已使用持续重试，没有固定 64 次操作失败上限。
   demuxer 每轮最多 drain 64 个 Message 只是批处理预算，不是 KV retry 上限。
3. `KeyString` 已按完整 `fixed_key_size` 返回 key，不再在首个 `\0` 截断。
4. `kv_pin_shared_ref` 首次 `prepare_read` 已覆盖完整 SCC allocation。
5. `kv_unpin_shared_ref` 这一条 KV-only 路径已对 ref underflow hard-fail；但原
   helper 的通用 release 路径仍只用 `DCHECK`，allocator/EBR 的部分减计数也会
   先回绕再抛可被误分类的异常，统一收敛责任见 L1，不能把这一条局部修复误报为
   全部会计已安全。
6. 每 worker `max_tid` 已显式绑定到 `KVEngine::WorkerRuntime::max_tid`；TLS 只保存
   非 owning slot 指针，不再形成函数级无限生命周期权威状态。
7. remote insert OOM 路径已有 placeholder/next-row/shared 对象的回滚框架；旧文档
   所述“完全没有 OOM 回滚”已不成立。但现有回滚会把 unpublished placeholder
   临时设为 valid 后再调用普通 Delete，仍有可见性问题，必须按 I2 收敛；不能把
   “已有框架”误写成“回滚协议已经正确”。
8. 正式 `KVStore` API 已移除 public `MoveOut` 调试操作；仅保留内部 callback。
9. private metadata 不再嵌入 Clock reverse link；Clock node 是独立 owner-private
   offset 节点。
10. `TwoPLPashaHelper::cxl_tbl_vecs` 外层已固定 size=1，生产调用固定 table id 0。
11. 正式 `tigonkv` target 未链接 legacy Transaction/Executor 或旧
    `libcxlalloc_static.a`。
12. CTest 已给全部注册测试设置共同 `RESOURCE_LOCK tigonkv_test_environment`，
    guest E2E 另有 `RUN_SERIAL`。该静态编排是正确的，但人工启动测试时仍必须执行
    本文 §21 的进程隔离检查。
13. `验证证据.md` 中记录的生产源码摘要与当前源码一致；现有问题是覆盖不足，不是
    证据绑定到了另一份源码。
14. 当前生产符号扫描未发现 `ScanCertificate`、mutation generation、全局 Scan
    mutex、Scan cache、checkpoint/recovery 状态、`kv_shared_*` 命名路径或公开
    `MoveOut` 回归。

## 3. 固定执行顺序

必须严格按下列顺序施工。一个阶段未完成其 focused 验证，不得进入下一阶段；不能
因为某项严重度更高而跳序：

1. 阶段 A：§3.0 差分闸门、无法归类控制流和A4/A5测试调度隔离边界；
2. 阶段 B：§3.2、§3.8 range、固定 KV、配置和内部哨兵；
3. 阶段 C：§3.16 common、B+Tree、allocator、EBR 和启动发布；
4. 阶段 D：§3.15、§3.1 SCC/metadata/helper 发布序列；
5. 阶段 E：§3.4 原 table wrapper 和 adjacency callback；
6. 阶段 F：§3.7 PolicyClock；
7. 阶段 G：§3.14 IncomingDispatcher、Message、worker SPSC 和 wire；
8. 阶段 H：§3.3 ValueStruct/lmeta、点操作锁、TID 和 shared miss；
9. 阶段 I：§3.5 remote PUT/CAS/Increment；
10. 阶段 J：§3.6 local/remote Delete；
11. 阶段 K：§3.9 单 partition Scan 和顺序跨 partition 编排；
12. 阶段 L：§3.10 唯一 Busy retry 与错误分类；
13. 阶段 M：一次合并的 focused 功能 gate 和一次简短隔离 4VM
    Get/Put/Delete/Scan、A/E；
14. 阶段 N：§3.12 删除 current-only 双路径和死代码；
15. 阶段 O：§3.11 最终延迟审计；
16. 阶段 P：§5 一次最终验证，随后 §3.13 根目录文档终检。

下面的问题编号按这一施工顺序排列，不按严重度排列。

### 3.1 最小验证矩阵

下表是后续测试的唯一调度表。各问题节中的“必要验证”只说明该 gate 必须覆盖哪些
断言，不额外增加测试轮次。

| 阶段 | 只运行一次的 gate | 明确不运行 |
|---|---|---|
| A | 函数级 diff、调用图、CMake RelWithDebInfo静态口径、A5三个入口`bash -n`、`git diff --check` | build、CTest、4VM |
| B | 现有 `unit_tests --config-only` 的配置/fixture/固定模式/exact-width纯case，加现有 split/script 两个短测试 | KVStore数据面、allocator 多进程、真实 YCSB 生成/回放 |
| C | startup/allocator/EBR/B+Tree四个focused target；只做operation-entry静态调用图，公共API直接case并入H | CTest 全集、engine数据面、4VM |
| D | D1--D3 合并运行 SCC 两个 focused target | engine 全量、4VM |
| E | `btree_binding_test`加`kv_partition_test --focused-stage=E`的table/row-allocation/adjacency最小case | partition其它协议、YCSB |
| F | `kv_partition_test --focused-stage=F`一次Clock case | partition其它协议、每个Clock问题各跑一轮 |
| G | `kv_engine_test --focused-stage=G`一次transport/message case | engine点操作/Scan、4VM、故障矩阵 |
| H | partition/engine各一个`--focused-stage=H` shared lookup/点操作/worker-runtime case | CTest 全集、Scan/YCSB |
| I | `kv_partition_test --focused-stage=I`合并CAS/Increment交错与remote-insert rollback | partition其它协议、workload |
| J | `kv_partition_test --focused-stage=J`合并Delete生命周期/邻接确定性交错 | partition其它协议、长allocator压测 |
| K | `kv_partition_test --focused-stage=K` local/remote Scan case | partition其它协议、4VM E；统一延后到 M |
| L | `kv_engine_test --focused-stage=L` Busy/Invalid/Corruption三类最小case | engine其它数据面、大型death-test矩阵 |
| M | 一次合并 file-backed 功能 gate；随后清场，只做一轮小型 4VM direct smoke 和 A/E | A--E 全集、延迟模式 |
| N | `rg`、调用图、受影响 target 重建 | 重复功能测试、4VM |
| O | 先完成最终调用图八列延迟审计表并同步`延迟插入审计报告.md`，再把O1--O6合并为一次`latency_modes_test`非嵌套scope/ring settlement + HWCC/Clock/EBR/lmeta/allocator access-domain gate | latency-enabled YCSB；延后到 P |
| P | 严格执行一次 `partition优化方案.md` §5 | 任何额外压力轮次 |

除 A、N、P 的静态/最终闸门外，阶段命令固定如下，不能由施工者自行扩大或缩小
target 集合；全部使用 `-j1`：

```bash
# B
build-debug/unit_tests --config-only
ctest --test-dir build-debug -j1 --output-on-failure \
  -R '^(ycsb_partition_splits_test|ycsb_scripts_test)$'

# C
ctest --test-dir build-debug -j1 --output-on-failure \
  -R '^(kv_startup_test|region_allocator_test|cxl_ebr_test|btree_binding_test)$'

# D
ctest --test-dir build-debug -j1 --output-on-failure \
  -R '^(scc_protocol_test|kv_shared_protocol_test)$'

# E
ctest --test-dir build-debug -j1 --output-on-failure -R '^btree_binding_test$'
build-debug/kv_partition_test --focused-stage=E

# F
build-debug/kv_partition_test --focused-stage=F

# G
build-debug/kv_engine_test --focused-stage=G

# H
build-debug/kv_partition_test --focused-stage=H
build-debug/kv_engine_test --focused-stage=H

# I、J、K 分别在各自代码完成后执行对应一个mode
build-debug/kv_partition_test --focused-stage=I
build-debug/kv_partition_test --focused-stage=J
build-debug/kv_partition_test --focused-stage=K

# L
build-debug/kv_engine_test --focused-stage=L

# O
ctest --test-dir build-debug -j1 --output-on-failure -R '^latency_modes_test$'
```

每个 target 内只加入对应问题节列出的最小直接 case；不得通过重复进程、随机长压或
参数矩阵把“一次 target”变相扩大成过度测试。`--focused-stage`是 A4固定的测试
专用早期dispatch，不是通用filter；I/J/K 虽使用同一 binary，但各自只执行一个
不同的直接case。

执行规则：

1. 一个阶段含多个问题时，先完成该阶段全部修改，再运行表中的一次 gate。
2. gate 失败后只运行失败 case 或更小复现；修复后再运行该阶段 gate 一次，不从
   阶段 A 重新开始。
3. 只有修改触及已通过阶段的生产调用链，才重跑该阶段 gate；注释、文档和无关
   测试代码变化不触发功能重测。
4. 不为了获得 filter 能力新建一套复杂测试框架。优先在现有最小测试 target 中
   增加一个直接断言或轻量 test mode；验证完不保留临时日志/GDB 脚本。
5. M 之前不启动 QEMU。P 之前不运行标准 A--E，也不运行 latency-enabled A/E。

### 3.2 封闭实现决策

下列规则用于消除施工者自由裁量；后续实现不得在同一目标下自行选择另一种同样
“可行”的架构：

1. 每个问题节的“修改方案”只有一种允许实现。文中的“不增加/不恢复/删除”均是
   验收条件，不是建议。
2. 需要适配 master 时，只能增加 `RegionOffset` resolver、固定 KV row view、
   allocator callback、transaction-free completion 和显式结果枚举；这些适配不得
   保存权威状态，也不得拥有锁循环、迁移、adjacency、Clock、Scan 或消息分发
   控制流。
3. 如果执行中发现本文指定的 master primitive 无法机械适配，必须停止该问题，
   记录函数级 diff、Debug/GDB 证据和冲突的不变量，先修订本文；不得现场改选第二
   套状态机、fallback 或“临时可用”路径。
4. 所有正常失败均使用显式枚举，不以异常文本、默认 switch 分支或 bool 多义值
   分类。异常只承载不可恢复的不变量/损坏并 hard-fail。
5. 测试 fixture 统一使用不小于 `HWCC=32MiB、SWCC=32MiB` 的 file-backed pool；
   只有专门验证容量配置拒绝的 case 可使用更小容量。不得为 16MiB fixture 添加
   驱逐、回收或启动兜底逻辑。正式物理容量仍固定为
   `HWCC=1024MiB、SWCC=31744MiB`，不能被测试配置改写。
6. Clock 配置预算严格沿用 master
   `TwoPLPashaExecutor`：

   ```text
   configured_per_owner =
     (hw_cc_budget_mb * MiB - CXL_EBR::max_ebr_retiring_memory) / vm_count
   effective_per_owner =
     min(configured_per_owner, remaining_physical_dynamic_hwcc / vm_count)
   ```

   先要求总配置预算大于原 1MiB EBR reserve，再做整数均分；静态 HWCC 只用于物理
   capacity clamp，不计入 `TOTAL_HW_CC_USAGE`。不得删除原 EBR subtraction，也
   不得把物理 used counter 重新作为 Clock policy counter。

### 3.3 施工合同逐节闭环表

实施记录必须按下表回填“修改提交/函数级 diff/阶段 gate 证据”；任一行为空都不能
进入阶段 P，也不能宣称完成：

| `partition优化方案.md` | 本文唯一落点 |
|---|---|
| §3.0 原实现复用与 allowlist | A1、A2、A3、A4、A5、C2、C3、C4、D1、D2、D3、E1、F1、G1、G2、H2、H3、K3、N1 |
| §3.1 shared metadata/SCC | D1、L1 |
| §3.2 range/路由/内部哨兵 | B1、B2、B3、B4、B6、B7 |
| §3.3 ValueStruct/lmeta/点操作 | D3、E2、H1、H2 |
| §3.4 原 table/adjacency | E1、D1 |
| §3.5 remote PUT/CAS/Increment | I1、I2 |
| §3.6 Delete 生命周期 | J1、J2 |
| §3.7 PolicyClock | F1、F2、F3 |
| §3.8 固定 KV | B1、B5 |
| §3.9 Scan | K1、K2、K3 |
| §3.10 Busy retry/错误分类 | L1 |
| §3.11 延迟 | O1、O2、O3、O4、O5、O6 |
| §3.12 删除双路径 | N1 |
| §3.13 根目录文档 | P1 |
| §3.14 dispatcher/Message | G1、G2 |
| §3.15 SCC/内存访问 | C4、D1、D2、D3、O2、O3、O4、O5、O6 |
| §3.16 common/allocator/EBR/startup | C1、C2、C3、C4、C5、C6、O2、O3、O4、O5 |
| §4 固定顺序/克制测试 | 本文 §3 的 A→P 顺序、A4、A5及B7 |
| §5 最终验证 | P2 |

若某个后续新发现不能归入表中既有问题，先把它按合同所属阶段插入本文并更新本表；
不得把它延后到 P 的“顺手修复”，也不得越过当前未完成阶段提前施工。

### 3.4 最小新增代码闸门

本项目的完成标准不是“用新代码实现相同行为”，而是让正式生产调用图尽可能回到
master 原主体。后续每个阶段同时受以下硬约束：

1. 修改任何生产函数前，必须先记录
   `git show master:<path>` 中对应函数。能通过类型替换、末尾默认模板参数、
   callback 或 RegionOffset resolver接入的，不得复制函数体。
2. 先删/恢复、后新增。补丁审查顺序固定为：

   ```text
   恢复 master 条件/循环/锁范围
   → 删除 current-only 平行主体和状态
   → 最后加入不可避免的薄适配
   ```

   不能先写一套“正确的新实现”，再把旧实现留到 N阶段处理。
3. 不设武断的净 LOC 数字，但设置**新增符号结构预算**。下表之外的新 production
   class、状态字段、后台线程、cache、map、queue、retry loop、wire field或
   persistent layout字段默认禁止：

   | 阶段 | 唯一允许的新增结构 |
   |---|---|
   | B | 一个 canonical internal-sentinel helper；既有 Config/exact-width校验分支、helper-context固定赋值和digest literal；不得新增运行时模式字段或padding helper |
   | C | `OwnerAllocatorControl`、`OwnerPrivateClockTrackerControl`；现有 layout 的一次 publication拆分及 transport/EBR 两个 immutable static-domain bounds；现有 tree root binding的 private/HWCC域适配；C4 的一个纯 `ContainsRange`、三个 row data resolver、static transport/EBR typed accessor和一个统一 `ProtocolFatal`；原 `EBRMetaLocal`机械进入既有per-worker DRAM slot且TLS只作non-owning binding；现有 `RegionAllocator` handle固定区分 control 与 block 两个访问域，不新增 allocator class |
   | D | 原 `TwoPLPashaHelper` 内的 offset/resolver overload与最窄结果 enum；原 `TwoPLPashaMetadataLocal` 的一个 raw-pointer默认/RegionOffset specialization共用storage主体；不得新增 helper class或第二个lmeta struct |
   | E | 原 `TableBTreeOLC` 的末尾 `BTreeTemplate`/`RowStoragePolicy`，以及原 `CXLTableBTreeOLC` 的末尾 `SharedRowReferencePolicy`；只允许 legacy默认 raw/offset_ptr policy和 KV RegionOffset policy，`BTreeTemplate`只能选择已有 `btreeolc::BPlusTree`/`btreeolc_cxl::BPlusTree`，不得新增 tree/table class |
   | F | 原 `PolicyClock::ClockTracker` 内的 RegionOffset storage适配；不得新增 policy/victim算法类 |
   | G | 一个 legacy/KV共用的 CXL receive free-function；现有 Message factory/handler 的 transaction-free completion adapter；不得新增 dispatcher class |
   | H | 复用 engine现有 worker slot；不得新增 point状态机、统计槽或 cache |
   | I--K | 原 helper结果的短生命周期 continuation/枚举；K只额外允许在现有 `TwoPLPashaHelper` 内机械抽取 master local/remote scan callback各一个无状态 template driver，不得新增 Scan class、持久字段或并行协议 |
   | L | 复用 C4 的 `ProtocolFatal` 和既有 facade loop；不得新增 retry/fatal体系 |
   | O | 无状态 inline `mem_access` hook；不得新增延迟状态、scope stack、计数镜像或 allocator wrapper class |

   A、M、N、P 不允许新增生产结构；A/N 只恢复或删除，M/P 只验证/文档。
4. 表中“允许”不代表必须新增。若 master 原类型/函数稍作模板化即可覆盖，必须复用
   原符号；只有编译期 storage/access差异确实无法表达时才使用预算中的薄适配。
5. C4 不得发展成通用 object registry或 range-cache class。只在现有
   `DualRegionAllocator` non-owning handle中保存：

   - 当前已绑定的一个 `bound_owner_shard`；
   - attach时从现有 immutable `OwnerDynamicArenaDescriptor[]` 机械复制的同型
     bounds；
   - 已有 owner-private offset/stride。

   不新增另一种 descriptor、allocation map或每次 key lookup cache。
6. Debug barrier、fault injection、verbose字段和 assert helper只进入现有 test
   target或 `#ifndef NDEBUG` 局部；问题复现完成后在 N删除，不得成为 Release路径。
7. 每阶段修改后必须执行以下静态代码量/符号闸门：

   ```bash
   git diff --function-context master -- common core protocol kv
   git diff --numstat master -- common core protocol kv
   git diff --check
   ```

   对每个新增 production symbol记录所属问题编号和无法直接使用 master符号的原因。
   无编号、仅为“更通用/更安全/更清晰”的新增必须删除。`numstat` 只用于发现异常
   膨胀，不允许以测试通过为膨胀辩护。
8. 阶段完成报告必须同时给出：

   - 删除的 current-only 算法主体；
   - 直接复用的 master函数；
   - 保留的薄适配符号；
   - production新增/删除行数；
   - 未解释新增符号数，必须为 0。
9. `git diff --name-only` 中每个文件必须标注问题编号。除必要的生产文件、对应现有
   test target、CMake最小连线和 P阶段根目录文档外，不修改 README、legacy
   benchmark、thirdparty、无关脚本或格式；发现顺手重构立即回退。
10. 提交边界严格沿用施工合同：每个问题完成其 focused gate 后按问题编号提交并
    推送；同阶段共用 gate 时可以让多个连续问题引用同一份 gate 证据，但不能把
    无关问题塞进同一提交。A--O、最终综合测试、延迟审计和根目录文档终检全部完成
    前，不得创建或推送“项目完成”提交；不新建分支、不提 PR。

## 4. 阶段 A：差分闸门仍未真正闭环

### A1. `master差分allowlist.md` 的若干“已复用”结论与源码不符

#### 当前证据

- 当前文档声称 `KVPartition` 不直接拥有 point/SCC/ref/reader/writer 状态机，
  transport handler 不直接拥有行协议，且 table/message/dispatcher 已是原实现的
  同一 primitive。
- 实际上：

  - `kv/engine/kv_partition.cpp` 仍有约 2200 行 current-only 实现；
  - `kv/engine/kv_engine.cpp` 仍有约 1500 行并含 owner handler 分支；
  - `protocol/TwoPLPasha/TwoPLPashaHelper.h` 相对 `master` 仍有大规模新增协议体；
  - `common/btree_olc_cxl/BTreeOLC_CXL.h` 相对 `master` 约为
    `+1091/-287`，但现有 allowlist 没有逐函数证明 split/merge/restart/lock
    控制流未改变；
  - `core/CxlIncomingDispatcher.h`、`kv/engine/kv_migration.*` 的
    `TableBTreeOLC`、`TwoPLPashaMessagePrimitive` 都是 current-only 类或主体。

因此现有 allowlist 的文件级描述把待完成目标提前写成了当前事实，不能作为
§3.0 的通过证明。

#### 修改方案

1. 每个阶段开始时先记录当时的`git rev-parse master`作为该阶段证据，再直接用
   `git show master:<path>`、
   `git diff --function-context master...HEAD -- common core protocol kv`和
   `git diff --function-context master -- common core protocol kv`重新生成函数级
   差分清单；其中最后一条才是包含尚未提交施工内容的当前事实。
   同时用`git ls-files --others --exclude-standard -- common core protocol kv`
   列出生产untracked文件，并对每个文件执行
   `git diff --no-index --function-context /dev/null <path>`。当前
   `core/CxlIncomingDispatcher.h`、`kv/engine/kv_worker_context.h`必须按此进入审查，
   不能因未被Git跟踪而从allowlist消失。`TwoPLPashaScan.h`已按K3删除，不得再要求
   它进入差分。记录值不写回成后续阶段的固定基准，master变化时下一阶段重新
   解析。
2. 对每个改动函数记录：

   - 对应 master 文件/函数；
   - A--E 分类；
   - 仅有的类型、RegionOffset、内存域或 transaction 剥离差异；
   - 保留的锁、回调、返回值、发布和失败顺序；
   - 调用该函数的 legacy 与 KV 入口。

3. 无法归类的控制流先恢复到 master，再继续后续功能阶段。
4. allowlist 只记录已经由源码和 focused 测试证明的事实，不写计划状态。
5. 不以“实验室项目”为由跳过函数级核对；实验室标准只减少冗余测试，不允许扩大
   协议差分。
6. 同一次 allowlist 固定附上
   `git grep -n -i 'TODO\\|local DRAM\\|HWcc' master -- common protocol`的逐项
   归类，避免施工者把任意旧TODO解释成扩写授权：

   - `TwoPLPashaHelper.h`中原ref位置“remove ultimately”和
     migration-policy“move to HWcc”只按D1既定布局兑现；
   - `CXL_EBR::EBRMetaLocal`的local DRAM注释按C6/H3保留为每worker进程DRAM，
     不是迁入共享区的TODO；
   - 固定Clock正式路径不触及`PolicyEagerly/PolicyFIFO/PolicyLRU`的owner-local
     pointer注释；
   - B+Tree等待、Message/TwoPL理论锁检查、未链接协议/benchmark中的其它TODO均
     不是生产内存放置TODO，保持master，不顺手实现。

   最终表中每个命中只能写“由既有问题编号兑现”或“未链接/非内存放置、保持
   master”，不能生成新cache、状态、恢复或后台机制。

#### 必要验证

- 静态运行上述两条相对master的`git diff --function-context`，并核对
  `git ls-files --others`的生产集合；每个untracked生产文件均有一份
  `/dev/null`函数级diff；
- 对 B+Tree、helper、Clock、Message、dispatcher、KVPartition 生成函数/调用者
  列表；
- 保存上述master TODO命中及固定归类，确认生产相关内存放置命中全部落到D1/C6/H3，
  其余没有被借题扩写；
- `git diff --check`；
- 本阶段不跑 4VM，不重复跑功能测试。

#### 完成条件

每个已提交、未提交和untracked生产差分都能真实归入 A--E；文档不再声称尚未完成
的复用已经完成。

### A2. RelWithDebInfo 默认值设置过晚，且 LTO 只进入编译、没有进入最终链接

#### 当前证据

- 根 `CMakeLists.txt` 先按 `CMAKE_BUILD_TYPE` 进入 Debug/RelWithDebInfo 分支，
  最后才在 `elseif(NOT CMAKE_BUILD_TYPE)` 中把默认值设为 RelWithDebInfo。
  因而不显式传 `-DCMAKE_BUILD_TYPE` 的 single-config configure 不会进入本项目
  自定义的 `-O3 -g3 -march=native`/LTO 分支，只得到 CMake 自带的
  RelWithDebInfo flags。
- 当前 RelWithDebInfo 分支只调用 `add_compile_options(-flto...)`，没有对应的
  link option；不能据此证明最终 `e2e_trace_runner` 是 full-program LTO。
- `../cxlkv/AGENTS.md` 的当前公平口径是 Release/RelWithDebInfo
  `-O3 -march=native`，主项目 production target 的 LTO 同时进入 compile/link，
  RelWithDebInfo 另保留 `-g3`。当前 TigonKV 配置和最终报告都尚未闭合这个口径。

#### 修改方案

1. 对 single-config generator，在任何 build-type 条件判断之前、且仅当调用者没有
   显式指定时，把默认 `CMAKE_BUILD_TYPE` 设为 RelWithDebInfo；不覆盖显式值，
   不改变 multi-config generator。
2. 保留当前 GNU `-flto`、Clang `-flto=full` 的最小分支，并在同一
   RelWithDebInfo 分支把完全相同的 flag 加入 link options。不要引入新的 IPO
   helper、toolchain 文件或 target 包装层。
3. 保留 `-O3 -g3 -march=native`；Debug 仍为 `-O0 -g3` 且没有 LTO。不要借此
   修改编译器选择或给测试单独建立另一套性能 flags。
4. P阶段报告双方实际 compiler/version 及 production binary 的 compile/link
   flags；“公平”以披露且满足双方各自当前构建合同为准，不在本问题中强行重写
   TigonKV 工具链。

#### 必要验证

- A阶段只静态检查 CMake 控制流和生成的配置，不构建、不跑测试；
- P阶段已有的唯一 RelWithDebInfo 重建中，用 verbose command各取一条
  `tigonkv` production object编译和最终 `e2e_trace_runner` 链接命令，确认二者
  都含正确 LTO flag及 `-O3 -g3 -march=native`；
- P阶段已有的 Debug 构建命令确认不含 LTO；不为 A2 新增 build/test轮次。

#### 完成条件

默认和显式 RelWithDebInfo 都进入同一 production flags，LTO 同时覆盖编译和最终
链接，最终公平报告能够用真实命令而非 CMake 注释证明构建口径。

### A3. build-debug 的 CTest E2E默认取用 build-relwithdebinfo 二进制

#### 当前证据

- `tests/e2e_multivm_common.sh` 在未显式设置时固定
  `TIGONKV_E2E_BINARY_DIR=$root/build-relwithdebinfo`，并由此选择
  `e2e_08/e2e_09/e2e_trace_runner/cxl_pool_initer`。
- 根 `CMakeLists.txt` 注册 `e2e_08_test/e2e_09_test/e2e_ycsb_test`时没有给这些
  test传当前 binary directory。
- 因而执行合同要求的
  `ctest --test-dir build-debug --output-on-failure`时，三个 guest E2E会默认运行
  RelWithDebInfo产物；CTest所在目录和真实 guest binary build type不一致。
- 即使给上述三个test传入当前binary dir，`e2e_ycsb_test.sh`的trace缺失分支调用
  通用prepare时仍未设置`TIGONKV_YCSB_PARTITION_SPLITS`；后者固定默认
  `build-relwithdebinfo/ycsb_partition_splits`。因此首次Debug CTest会隐式依赖
  另一build tree中可能过期或不存在的splitter，不能算完整绑定Debug产物。

#### 修改方案

1. 只在根 CMake现有三个真正运行 guest binary的 test property中追加：

   ```text
   ENVIRONMENT "TIGONKV_E2E_BINARY_DIR=${CMAKE_BINARY_DIR}"
   ```

   `e2e_ycsb_entrypoints_test`只检查 shell help，不使用 binary，不加该环境。
2. `e2e_multivm_common.sh` 的 direct-script默认仍保留
   `build-relwithdebinfo`；不要为本问题增加 CLI参数、build目录探测或第二套 wrapper。
   `TIGONKV_POOL_INITER/TIGONKV_E2E_TRACE_RUNNER`继续从该唯一 binary dir派生。
3. 只在`tests/e2e_ycsb_test.sh`现有`need_prepare`分支，把
   `TIGONKV_YCSB_PARTITION_SPLITS`显式设为
   `$TIGONKV_E2E_BINARY_DIR/ycsb_partition_splits`并先要求其可执行，再调用原
   prepare；不修改通用prepare的direct-script默认，不复制splitter逻辑。这样
   Debug/Rel CTest各使用同一build tree的工具，B6的临时config副本仍只包住这次
   prepare。
4. 测试 marker继续打印实际 `binary_dir`。P阶段同时记录 guest内同步后的可执行文件
   SHA-256，并与对应 host build target一致，不能只凭目录名判断。

#### 必要验证

- A阶段只静态检查生成的 CTest environment，不启动 VM；
- P阶段合同既有的 Debug全CTest确认三个 marker均指向
  `build-debug`，后续标准 RelWithDebInfo E2E marker指向
  `build-relwithdebinfo`；
- `ycsb_scripts_test`静态确认CTest wrapper只从
  `TIGONKV_E2E_BINARY_DIR`选择splitter且缺失时失败，不执行splitter或生成trace；
- 复用两阶段本来就要执行的 E2E，不新增单独 Debug/Rel 4VM轮次。

#### 完成条件

从哪个 build tree调用 CTest，guest E2E就同步并运行该 tree的runner/pool/splitter；
Debug证据不再被 RelWithDebInfo或另一build tree的旧二进制冒充。

### A4. 两个单体测试binary无法表达阶段focused gate，当前命令会提前并重复测试

#### 当前证据

- `kv_engine_test`一个无参数`main()`同时执行transport malformed、worker绑定、
  routing、全部点操作、跨进程RPC、Scan及history；阶段G运行它会在H--K施工前
  提前依赖这些current-only数据面，H/L又会整套重复。
- `kv_partition_test`也是单体；按旧矩阵在E/F/H/I/J/K各运行一次，会让后加入的
  CAS/Delete/Scan case在更早阶段提前执行，并在之后每阶段重复。
- CTest的`-R`只能筛test target，不能筛binary内部case。“每阶段运行一次target”
  因而不等于“只做该阶段必要测试”。

#### 修改方案

1. 不新增test target、registry、filter库、fixture框架或shell wrapper。只把现有
   两个`main()`改为接受唯一可选参数：

   ```text
   kv_engine_test    --focused-stage=G|H|L
   kv_partition_test --focused-stage=E|F|H|I|J|K
   ```

   参数必须完整匹配上表；重复参数、未知stage或其它参数立即打印usage并返回2。
   B7的`unit_tests --config-only`保持独立的单一早退，不扩成同一通用框架。
2. 各问题节本来要求新增的最小直接case各自成为一个普通test-only函数
   `RunFocusedStageX()`；stage参数只用一个穷尽switch调用对应函数、打印固定marker
   后退出。函数之间不互调，不通过“run all previous stages”建立累积模式，也不
   增加随机seed/重复次数/参数矩阵。
3. 无参数路径保持现有CTest语义：执行原完整body，并各调用一次所有已经实现的
   focused函数；只用于阶段M合并gate和阶段P最终CTest。不得让无参数路径默认为
   某一stage，也不得删除现有广覆盖断言来缩短最终测试。
4. 每个stage函数只建立自身最小fixture；能复用该binary现有`ConfigFor`、临时文件、
   deterministic barrier/fault hook就直接复用。stage结束必须join子线程/子进程、
   release worker并删除自己的临时backing，不能把状态传给下一mode。
5. §3.1中的direct binary命令严格串行执行；执行前后仍做§21进程检查。它们不经过
   CTest resource lock，因此不得与CTest、另一个mode或4VM并行。
6. selector只改`tests/kv_engine_test.cpp`和`tests/kv_partition_test.cpp`；零
   production符号、零CMake test注册变化。每个后续阶段只在自己首次需要时加入
   对应函数，不在A阶段预写空case或未来框架。

#### 必要验证

- A阶段只静态检查上述固定命令/允许值和现有CTest仍无参数，不构建；
- 每个后续阶段只运行§3.1列出的mode并看到唯一stage marker；未知值的返回2只在
  第一次接入selector时做一个直接断言，此后不重复；
- M/P无参数CTest各自按合同运行完整body；不额外逐mode再跑一遍；
- `rg`确认没有第三个test binary使用该selector，也没有通用case registry/filter。

#### 完成条件

阶段focused gate只执行当前阶段的直接根因case；完整广覆盖测试集中留在M/P，
不再提前依赖未施工路径或因复用同一target造成过度重复。

### A5. 三个guest入口的进程隔离名单不一致，跨套残留可与下一轮共存

#### 当前证据

- `scripts/e2e_trace/run_guest_ycsb_workflows.sh::assert_host_isolated`按
  `/proc/exe`检查host测试，但名单漏掉会打开dual backing的`cxl_ebr_test`和
  `cxl_pool_initer`；
- `tests/e2e_multivm_common.sh`作为`e2e_08_test/e2e_09_test`共同preflight，没有
  做同等host测试进程检查；
- trace入口的guest cleanup只匹配`e2e_trace_runner`，08/09入口只匹配本次
  `e2e_${suite}`。前一套失败后残留的另一种runner仍可持有同一ring/backing，而
  下一套会继续reset；
- trace入口的`assert_expected_qemu_group`只筛“命令行含当前backing”的QEMU；
  08/09共同preflight只由`tigonkv_check_vms.sh`逐个验证四个pidfile。旧QEMU若使用
  另一backing，两条检查都可能通过，主机上实际会同时存在新旧VM并争用CPU/NUMA；
- CTest `RESOURCE_LOCK`只能串行化由同一CTest调度的已注册测试，不能保护本文
  阶段命令和人工直接入口。仅靠它不能满足“unit与E2E、新旧runner绝不共存”。

#### 修改方案

1. 只在现有`scripts/tigonkv_vm_common.sh`增加一个无状态shell函数
   `tigonkv_assert_host_test_isolated`。它逐个读`/proc/[0-9]*/exe`并按**完整
   basename**拒绝下列实际会打开backing、transport或同一测试环境的进程：

   ```text
   unit_tests
   latency_modes_test
   kv_layout_test
   btree_binding_test
   kv_partition_test
   kv_engine_test
   kv_startup_test
   scc_protocol_test
   kv_shared_protocol_test
   region_allocator_test
   cxl_ebr_test
   e2e_08
   e2e_09
   e2e_trace_runner
   cxl_pool_initer
   ivshmem-server
   ```

   函数只报告pid/exe并失败，绝不擅自kill host测试；读取消失的`/proc`项视为进程已
   退出。`ivshmem-server`虽不是本仓`ivshmem-plain`新4VM组的一部分，但可能是旧
   实验留下的共享内存service并继续占用CPU/NUMA，因此任何命中都必须先由调用者
   清场；不要把它误认为本轮允许的基础设施。不要改成模糊`pkill`、全系统锁服务
   或后台monitor。
2. `tests/e2e_multivm_common.sh`在检查QEMU/同步binary/reset pool之前调用该函数；
   `run_guest_ycsb_workflows.sh`删除自己的不完整副本并调用同一函数。08、09和YCSB
   三个直接/CTest入口由此使用同一host名单。调用发生在本轮
   `cxl_pool_initer/e2e_*`启动前，所以不会把本轮进程误判为残留。
3. 保留两个现有guest `/proc/exe`清理函数和SSH控制流，只把匹配目标固定扩为同一
   remote build目录下：

   ```text
   e2e_08
   e2e_09
   e2e_trace_runner
   ```

   每轮sync/reset前在四台VM逐一kill这三种**精确路径**及其`(deleted)`形态，然后
   再扫描一次，任一仍存在即失败；不按进程名截断匹配、不杀guest其它程序。失败
   trap/退出清理也调用同一已有函数，保证失败套不会遗留给下一CTest。
4. 把trace脚本现有`assert_expected_qemu_group`机械移动并收敛成
   `scripts/tigonkv_vm_common.sh::tigonkv_assert_qemu_group <empty|expected>`，不
   保留本地副本或第二个扫描函数。该无状态函数只扫描一次主机全部
   `/proc/*/exe` basename为`qemu-system-*`的PID：

   - `empty`固定要求集合为空；`tigonkv_init_vms.sh`在现有授权kill/旧backing
     cleanup已经完成、但创建新backing和启动第一台QEMU**之前**调用
     `tigonkv_assert_host_test_isolated`及本模式。任一旧QEMU或
     `ivshmem-server`仍存在就立即失败，绝不能先启动新四台再由事后check发现；
   - `expected`先从配置storage的`vm_0..vm_(count-1)/qemu.pid`读取恰好四个
     非重复、存活PID，逐一验证`/proc/<pid>/exe` basename为`qemu-system-*`且
     cmdline含当前**精确backing路径**，再要求这些pidfile PID与上述主机全集
     精确相等。旧QEMU即使使用另一backing也必须失败，不能只统计当前backing
     命中数。

   `tigonkv_check_vms.sh`在任何SSH/NUMA检查前调用`expected`；trace workflow也调用
   同一模式。因08/09共同preflight已经调用`tigonkv_check_vms.sh`，三个guest入口
   由此共用同一QEMU集合证明。函数只报告额外/缺失PID并失败，不kill、不启动、
   不停止VM，不增加锁文件或monitor；旧VM清理仍只走用户已授权的现有kill/init
   入口。
5. §21的人工`pgrep`清单同步加入上述三个guest binary、`cxl_pool_initer`和
   `ivshmem-server`，作为开始任何direct gate前的外层确认。脚本自检与人工检查
   是同一隔离合同，不新增测试调度器。

#### 必要验证

- A阶段只静态检查三个入口都调用唯一host函数、两个guest cleanup都包含三条精确
  remote路径，init的prestart调用`empty`且trace/check-vms调用同一QEMU函数的
  `expected`模式，执行`bash -n`；不启动测试/VM；
- B阶段原`ycsb_scripts_test`只补静态断言上述名单一致，不创建伪进程、不新增
  target；
- M/P真实运行沿用本来就要执行的入口，日志记录preflight与四VM cleanup成功；
  不为A5单独再跑一次E2E。

#### 完成条件

新VM启动前主机QEMU/旧ivshmem service为空；任何guest suite开始reset或同步前，
host不存在单元/focused/旧runner进程，主机全部QEMU恰为本轮四个pidfile对应进程，
四个guest不存在08/09/trace任一旧runner；隔离检查只有一份host名单和一份QEMU
集合函数，且没有新增后台机制。

## 5. 阶段 B：range、固定 KV、配置和内部哨兵

### B1. `fixed_key_size < 32` 时内部最大键哨兵的构造和识别不一致

#### 当前证据

- `TableBTreeOLC` 构造哨兵时只把前 `key_size_` 字节置为 `0xff`，其余
  `FixedKey` 字节保持 0：
  `kv/engine/kv_migration.cpp::TableBTreeOLC`。
- `KVPartition::IsInternalMaxSentinel` 却检查 `FixedKey` 的全部 32 字节是否为
  `0xff`。
- local/shared Scan 的无界 max 和 shared Scan 内部 sentinel 又把完整
  `sizeof(FixedKey)` 置为 `0xff`。
- 正式配置固定 32B，因此现有 4VM E 不能发现该问题；但通用配置明确允许
  1--32B key，`tests/e2e_08.cpp` 仍有 8B 配置。

这会使非 32B 配置无法识别真实哨兵，并破坏空 partition、最后一个真实 key 和
right-boundary 的判断。

#### 修改方案

1. 只保留一个内部 sentinel helper：

   - 前 `fixed_key_size` 字节为 `0xff`；
   - `FixedKey` 剩余存储字节保持 canonical zero；
   - 用户可见比较只比较固定 key 宽度。

2. table 初始化、engine 公共保留键校验、local/shared Scan max、sentinel 识别和
   split 校验全部调用同一 helper。
3. 不扩大 tree key，不新增 sentinel flag，不为不同 key 宽度保留第二种哨兵。

#### 必要验证

- 阶段 B 的 config-only gate只直接覆盖`fixed_key_size=8`和`32`时唯一helper生成、
  识别、比较字节及split拒绝；纯函数同时标明该key是point/Scan-start保留键，但
  不创建KVStore/table/backing、不调用公共数据面；
- empty end映射的inclusive max与非空exclusive end等于该宽度全`0xff`只检查纯
  predecessor/helper结果，不运行Scan；
- 每partition实际插入一枚、公共point/Scan-start返回InvalidArgument、空partition、
  最后真实row、right-boundary锁及“结果永不含哨兵”按合同延后到H2最终
  ValueStruct/lmeta接线后的同一次H gate；不得在B的current-only tree上提前实现
  或测试；
- 不跑完整 YCSB。

#### 完成条件

阶段B先证明所有宽度只有一个canonical表示；H gate再证明最终table/engine/Scan的
创建、比较、锁和过滤全部使用它。

### B2. 配置接受 0 partition，且“partition 少于 VM”与 allocator 实现冲突

#### 当前证据

- `Config::Validate()` 只拒绝
  `partition_count > kMaxPartitions`，没有先拒绝 0，随后直接访问
  `partition_ranges.front()`/`back()`。
- `tests/tigonkv_tests.cpp` 明确把 `partition_count=1, vm_count=4` 作为合法配置。
- allocator 却把 `control_partition=node_id`：

  - `InitializeOwnerPrivateArenas()` 在 `node_id >= partition_count` 时抛错；
  - `BindOwnerPrivateAllocators()` 调用 `Arena(node_id)`；
  - 动态 HWCC/shared-SWCC allocator control 因此依赖“每个 VM 至少有同 id
    partition”。

通用配置合同与实际启动合同不一致。正式 4/4 恰好掩盖了问题。

#### 修改方案

1. `Config::Validate()` 在任何 vector 访问前拒绝 `partition_count==0`。
2. 保留原 modulo owner 规则和“任意正 partition 数”合同，不能简单把
   `partition_count >= vm_count` 写成新限制。
3. 每 VM 的动态 allocator control 应是 owner control，而不是借用
   `Arena(node_id)` 的 partition control；该 allocator/storage 修改属于阶段 C，
   阶段 B 只固定配置合同，避免提前改 common：

   - 仍放该 VM 的 owner-private SWCC；
   - 每 owner 只保留一份；
   - 不让无 partition 的 VM 读取其他 owner 的 private SWCC；
   - 不增加全局 HWCC allocator 热状态。

4. owner-private per-partition arena 仍只为实际 partition 建立；不要恢复多表或
   新建 owner→partition 路由 cache。

#### 必要验证

- 阶段 B 只验证配置层：0 partition 拒绝、1/4 与 7/4 的 Validate/owner mapping；
- 需要 allocator 的 1 partition/4 VM Open 延后到阶段 C startup gate，避免同一
  多进程场景在 B、C 各跑一次；
- `partition_count=0` 稳定返回配置错误且不越界；
- 正式 4/4 配置仍通过；
- 不在阶段 B 运行 startup target 或 4VM。

#### 完成条件

阶段 B 完成时，Validate 已安全拒绝 0 并保留任意正 partition/vm 的 modulo 合同；
阶段 C gate 再完成其端到端条件：所接受组合能启动且不再依赖
`control_partition=node_id`。

### B3. 多个有效 file-backed focused fixture 仍小于 32MiB+32MiB，现有清单漏报

#### 当前证据

- `tests/fixtures/experiment_config.jsonc`、`multivm_trace_config.jsonc` 仍配置
  `HWCC=16MiB、SWCC=16MiB、hw_cc_budget_mb=4`；
- `tests/kv_engine_test.cpp`、`kv_startup_test.cpp` 和
  `tigonkv_tests.cpp` 的真实 `KVStore` backing 同样使用 16MiB+16MiB；
- `tests/btree_binding_test.cpp` 的 file-backed dual pool只有
  `HWCC=2MiB、总量=8MiB`；
- `tests/cxl_ebr_test.cpp` 只有 `HWCC=4MiB、总量=16MiB`；
- `tests/kv_shared_protocol_test.cpp` 虽使用匿名mmap而不是文件，但同样初始化完整
  dual-region layout，仍只有`HWCC=2MiB、总量=8MiB`；
- `tests/region_allocator_test.cpp` 的 mapped dual-pool attach fixture仍用
  `HWCC=1MiB、总量=kBytes`；
- `kv_partition_test` 已是 32MiB+32MiB，不需要再改；
- `tests/fixtures/ycsb_experiment_config.jsonc` 是无人引用的旧
  `512MiB/16 partition/1000B value` 口径；
- 根目录正式 `experiment_config.jsonc` 已是
  `HWCC=1024MiB、SWCC=31744MiB、4 partition、32B KV`，不能为了迁就小测试夹具
  修改正式容量或加入复杂小容量回收逻辑。

这会让 C/D/H 等阶段 gate 继续在已经明确禁止的容量下运行；C1 新增固定
owner-control slot 后尤其可能把必要 layout bytes不足误诊为协议错误。旧清单只列
两个 JSON fixture并不完整。

#### 修改方案

1. 所有会实际 `Open`/mmap `DualRegionMappedPool`、`KVStore`，以及上述
   `kv_shared_protocol_test`完整dual-layout的有效 focused fixture统一机械改为：

   ```text
   total/size = 64MiB
   HWCC offset/size = 0/32MiB
   SWCC offset/size = 32/32MiB
   ```

   `KVStore::Config` fixture的 `hw_cc_budget_mb` 固定为32；直接
   `DualRegionConfig` fixture没有该字段，不另造 policy 配置。
2. 只改容量常量及由其派生的 offset。保留各自的 partition 数、128B value、
   trace路径、worker数、测试次数和 latency-disabled 设置；不增加数据量、不把
   focused case顺手改成正式 workload。
3. `tests/fixtures/e2e_multivm_config.jsonc` 的 32GiB 物理拆分及 1000B guest
   synchronization 用途保持不变。
4. 无引用的 `ycsb_experiment_config.jsonc` 延后到 N1 删除，不保留第三份“看似正式”
   YCSB 口径。
5. 纯内存、专门测试 `RegionAllocator` OOM/invalid attach 的小映射可以保留原容量；
   它们不启动 dual-region KV layout。容量拒绝单测如需小于32MiB，必须在 case内
   显式构造且只断言拒绝；不得再增加可启动的小容量 file-backed dual fixture。
   `region_allocator_test` 只给现有 `Mapping` 增加一个默认仍为原 `kBytes` 的
   size参数，DualRegion case传64MiB；纯 allocator OOM case继续用原小映射和原
   迭代次数，不能因本项变成长压。

#### 必要验证

- 与 B1、B2、B4、B5、B6、B7 共用一次阶段 B gate；只在现有 config case 中
  parse两个 JSON fixture并检查32MiB+32MiB；
- 其它容量只做静态 `rg`；对应 dual/file-backed target在 C/D/H 原定 gate自然运行，
  不为机械增容提前或额外启动；
- `rg` 确认除明确的 invalid-capacity/单 region allocator case外，不再存在可启动
  的小容量 file-backed dual fixture；
- `multivm_trace_config` 的实际脚本执行延后到阶段 M，不在 B 额外启动 E2E。

#### 完成条件

所有有效 file-backed dual/KV功能夹具及上述shared-protocol完整dual layout满足
32MiB+32MiB下限；正式1024MiB HWCC和正式物理布局完全不变。

### B4. 固定 TwoPLPasha/SCC/Clock 模式仍依赖 `Context` 默认值，digest 也未完整锁定

#### 当前证据

- `KvMigrationRuntime::Install()` 只显式设置
  `model_cxl_search_overhead=false`，其余正式要求的
  `enable_phantom_detection=true`、`enable_scc=true`、
  `enable_migration_optimization=true` 依赖 `core/Context.h` 当前默认值；
- `SharedLayoutConfigDigest()` 只记录前者，没有记录另外三个固定布尔；
- digest当前已经把每个实际latency字段按名称累计一次；该部分不再是待修问题，
  后续只需在加入三个固定协议布尔时保持现有字段集合和顺序，不能借机重排或增加
  派生字段；
- 这些模式不应成为新的用户配置或运行时分支；parser继续只接受
  `Clock/OnDemand/WriteThrough`。

#### 修改方案

1. 在现有 `KvMigrationRuntime::Install()` 创建的 `helper_context` 上明确写四个值：

   ```text
   enable_phantom_detection = true
   enable_scc = true
   enable_migration_optimization = true
   model_cxl_search_overhead = false
   ```

   不增加 `Config` 字段、CLI/JSON键、setter或模式分支。
2. `SharedLayoutConfigDigest()` 以同名固定 literal各累计一次上述四个布尔；保留
   现有 `migration_policy/when_to_move_out/scc_mechanism` 字符串和 internal
   sentinel literal。
3. 逐项对照 `Config` 中实际影响 latency simulator的字段，确认当前完整 latency
   block每项恰好一次并原样保留。`hwcc_atomic_ns`只是三个实际 atomic latency的
   派生最大值，不单独进入摘要；不得为了本项重写现有digest helper。
4. 不修改 `core::Context` 的 legacy 默认值，不为本单表固定模式清理 master宏。

#### 必要验证

- 与 B1--B3、B5、B6、B7 共用一次阶段 B gate；
- 在现有 digest/config case中改变任一实际共享/协议/延迟字段都会改变摘要，只改
  node/path/SSH等 host-only字段不变；
- 静态确认 helper context四项均为显式赋值，parser没有相应可变模式键；
- 不新增 helper、startup或4VM测试。

#### 完成条件

正式生产模式不再随 `Context` 默认值漂移，layout identity恰好记录全部固定协议模式
和每个实际 latency字段一次。

### B5. facade 已强制定长，但内部 row adapter 仍接受短 key/value并补零

#### 当前证据

- `FixedKey::From(key,fixed_size)` 接受 `key.size() <= fixed_size`并零填充；
- `KVPartition::PutPrivate/PutShared/CompareExchange*` 等直接入口只拒绝超长值，
  对短值调用 current-only `PadFixedValue`；
- 因此通过 facade的正式调用虽是 exact width，直接 handler、helper测试或后续
  adapter仍可形成第二种“短输入等价尾零”的语义；
- CAS expected的空串是合同明确的 missing-create sentinel，不是应补成
  `fixed_value_size`个 NUL的普通 expected value。

这与 §3.8“生产 key/value始终定长”不符，也保留了不必要的 padding代码。

#### 修改方案

1. 唯一普通 key构造 `FixedKey::From` 固定要求
   `key.size()==fixed_size`；空 Scan end和内部 sentinel继续由各自显式 helper处理，
   不经过普通 key构造。
2. 所有 private/shared row write、insert、CAS desired、wire decode后的 value
   adapter都要求 `value.size()==fixed_value_size`，不再接受短值。删除
   `PadFixedValue`及所有条件补零分支。
3. CAS expected只允许两种形态：

   - 空串：仅表示“key缺失时允许按 I阶段 remote insert创建”；若key存在，直接与
     完整当前值比较并得到 CompareFailed；
   - 恰好 `fixed_value_size`：普通比较。

   空串不得传给 row update、不得补零、不得持久化。
4. Increment的 canonical decimal helper本来就返回完整定长 buffer，保持不变；
   Get/Scan始终复制完整 value。owner/remote message factory按 G2同一 size公式编码
   完整定长 value，handler exact-length校验后不再做 padding。
5. 配置仍允许合同既有的短边界文本，但只在
   `Config::Validate()` 现有 `normalize_boundary` 内完成一次规范化：先拒绝
   超长边界，再对该 `std::string` 原地 `resize(fixed_key_size, '\0')`，随后才
   调用 exact-width `FixedKey::From`。空首/尾无穷边界保持空，不参与该步骤；
   相邻单侧复制先完成、再规范化。YCSB `FixedTraceKey`输出同样在调用前已经是
   exact width。不得新增“宽松配置”overload、`FromPadded`或第二个 key
   constructor。

#### 必要验证

- 与 B1--B4、B6、B7共用阶段 B唯一 gate；
- 一个直接 helper case覆盖 exact key/value成功、短/长 key/value拒绝、尾部真实
  NUL作为定长字节保留，以及 CAS empty expected不被补零；B2现有配置 case同时
  保留一个短边界输入，确认它只在解析阶段规范化；
- 实际 private/shared/remote路径不在 B提前运行，随 H/I/G各自原定 gate自然覆盖；
- `rg`确认生产无 `PadFixedValue`、`size() < fixed_*`补零分支或第二个宽松
  FixedKey构造；不新增长度矩阵。

#### 完成条件

从 facade、wire到 row storage只有 exact-width一种普通 key/value语义；仅 Scan空end
和 CAS空expected保留合同规定的显式例外。

### B6. trace身份、默认workload和操作数口径尚未闭合

#### 当前证据

- `tools/ycsb_partition_splits.cpp` 的 `trace_digest` 只按读取顺序累计 load 中
  canonical PUT key；它不覆盖原始 trace行、操作类型/长度、文件边界、A--E run
  trace或生成后的文件内容，却以 `trace_digest=`输出。
- `scripts/e2e_trace/prepare_ycsb_traces.sh` 的默认
  `TIGONKV_YCSB_WORKLOADS`仍是 `"A B C D"`，根正式入口
  `tigonkv_run_ycsb_experiment.sh` 的默认也仍是 `a,b,c,d`。调用者忘记显式覆盖时
  会生成一套缺 E 的“正式”目录，直到 P阶段才失败。
- §3.2/§5要求保存实际 load/A/B/C/D/E trace digest并用它绑定最终证据；当前
  key-only FNV不能承担该身份用途。
- Workload A按既定公平trace规则把一个YCSB `UPDATE`展开成`GET+PUT`两条KV操作。
  根入口的`run_meta.json::operation_count`记录的是展开前请求数，而runner
  `E2E_TRACE_TIME_US ops=`统计实际回放的有效trace行；A的后者因此大于前者。当前
  `summarize_ycsb_experiment.py`仍把后者输出为泛化的`ops/ops_per_sec`并在
  “YCSB实验报告”中没有解释分母，容易把replayed KV throughput误报成logical
  YCSB request throughput。
- summarizer按目录聚合但不拒绝缺node、重复node、marker phase与目录stage不符或
  缺失/错误线程topology；即使外层workflow本应失败，单独对残缺/拼接日志运行它仍
  会产出看似完整的正式报告。
- split工具输出的`representative_key_hex`当前取自每个partition的stride采样区间
  首项，而不是full-load中按`FixedKey`顺序的首个真实key。它一定来自load，但会随
  sample stride跳过更小key，与P2已经固定的throwaway代表key定义不一致。
- `e2e_trace_runner`已经支持`TIGONKV_E2E_REQUIRE_GET_FOUND=1`，但
  `run_guest_ycsb_workflows.sh::run_fixed()`只把Scan选项和
  `TIGONKV_E2E_TEST_VALUE_HEX`拼入远端`env`，没有传递该开关。P2若只在host设置
  它，guest仍使用默认0并把NotFound改成成功，代表key检查会产生伪通过。
- `tests/e2e_ycsb_test.sh`准备缺失的100k trace时调用
  `prepare_e2e_ycsb_traces.sh`；后者再让split工具原地修改当前
  `TIGONKV_EXPERIMENT_CONFIG_JSONC`。CTest当前把该变量设为根正式config，因此一次
  Debug功能准备会用100k样本改写已经固化的正式split，且发生在P阶段记录config
  SHA之后。
- 根正式入口即使收到`--skip-build`，trace分支仍无条件以`-j4`重建
  `ycsb_partition_splits`；该选项只跳过末尾runner build。P阶段已经完成唯一
  RelWithDebInfo构建，这次隐式重建既多余，也使“skip build”与实际证据不符。
- B6拟令`summarize_ycsb_experiment.py`必须显式接收同次
  `run_meta.json`，但旧窄功能入口`run_e2e_ycsb_rounds.sh`末尾仍无参数调用该
  summarizer。这个入口只由100k load+A功能回放使用，准备路径不生成run metadata；
  若不同时处理，四机回放即使成功也会在最后一步必然失败。为它另造一份“正式”
  metadata又会产生第二套操作数口径。

#### 修改方案

1. split工具保留现有 FNV计算，仅把变量和输出标签改为
   `load_key_digest`；它只证明 canonical key采样输入可复现，不能再称为文件
   digest。split digest及采样/分布算法不变。
2. 两个正式通用入口的默认值分别唯一改为
   `TIGONKV_YCSB_WORKLOADS="A B C D E"`和`workloads='a,b,c,d,e'`；显式环境/CLI
   覆盖仍保留。旧 `prepare_e2e_ycsb_traces.sh`/`run_e2e_ycsb_rounds.sh`明确传
   `a`的窄 load+A入口保持原用途，不把它们冒充正式全集。
3. 不为一次最终证据给准备脚本增加 manifest状态或测试入口。P阶段在全部生成成功
   后，从 trace目录内用系统 `find`/`LC_ALL=C sort`列出以下固定相对路径，并逐个
   调用系统 `sha256sum`：

   ```text
   load/worker0.txt ... load/worker15.txt
   workloada/worker0.txt ... workloade/worker15.txt
   ```

   先把排序后的路径写入 P证据目录的临时清单；确认恰好96行、每个 phase恰好16行
   且路径无重复，再在 trace目录内按该清单生成证据目录中的
   `trace_sha256.tmp`。行格式保持
   `<sha256><two spaces><relative-path>`；`sha256sum -c`成功后原子 rename为
   `trace_sha256.txt`。失败删除两个临时文件，不留半份 manifest。不修改只读
   trace目录，不引入 Python版 hash、JSON manifest或另一种 digest。
4. P阶段在复制/同步前以 trace目录为 cwd对证据目录中的 manifest运行一次
   `sha256sum -c`。guest同步会把全局
   `worker(vm*4+local).txt`重命名为该VM目录内的`worker(local).txt`，所以不能把
   96行host manifest原样交给任一guest（那会检查不存在的路径并产生伪失败）。
   在完成正式A--E工作流后，对每个实际
   `round1/workload{a..e}/{load,run}` guest目录，从同一host manifest临时筛出该
   phase、该VM的4行，并只在stdin中把全局worker号机械改为0--3；先确认恰好4行，
   再通过SSH执行：

   ```text
   cd <该次实际guest trace目录> && sha256sum -c -
   ```

   load行虽然源自同一canonical load，仍分别核对五个fresh workflow真正使用的
   五份remote目录；run行分别取对应workload。不得把不存在的“guest完整trace根”
   当成验证对象，也不得跳过重命名直接比较文件名。最终证据记录host manifest自身
   SHA-256、一次host校验状态及4VM×5 workload×2 phase的状态汇总；筛选后的4行只
   经stdin传递，不落成第二份manifest，不在guest留manifest。不要让split工具重复
   读取A--E或承担文件完整性。
5. 不改变A的`--update-read-before-write`、trace格式、runner热路径或
   `E2E_TRACE_TIME_US ops=`兼容marker。只做以下报告层机械澄清：

   - `run_meta.json`保留现有`operation_count`数值，增加固定
     `vm_count=4`和字符串
     `operation_count_semantics=logical_ycsb_requests_before_update_expansion`；
   - trace生成完成后，正式证据用与B6 canonical文件集合相同的96个文件，按runner
     现有前置规则“行非空且首字节不是`#`”分别汇总
     load/A/B/C/D/E的`replayed_trace_operations`；格式错误仍由原parser/runner
     hard-fail，不另写第二个语法解析器；
   - 根入口在trace已实际生成（或`--skip-trace-gen`但全部预期文件已存在）后，用
     shell现有文本工具按上述前置规则统计固定16个load文件及metadata中所选
     workload的各16个文件，并原子更新同一`run_meta.json`，加入object
     `replayed_trace_operations`；键恰为`load`加每个所选的`workload<字母>`，值为
     各phase总数。先写同目录`.tmp`、重新parse并核对键集合后rename；形式固定为
     `awk 'length($0)>0 && substr($0,1,1)!="#" {n++} END{print n+0}'`，它只做与
     runner相同的前置计数，不解析语法。`--prepare-only --skip-trace-gen`的短脚本
     测试若没有trace可只生成无该object的初始metadata并退出；任何实际run及P正式
     prepare都必须有object。生产/runtime不读取它；
   - `summarize_ycsb_experiment.py`增加必需参数
     `--run-meta <同一次out_dir/run_meta.json>`并校验上述semantics字符串；根入口
     和现有假log测试都显式传入，不能按cwd或相邻旧目录猜metadata；summarizer还
     要求`load + run_meta.workloads`精确键集存在，并对每个workload的load/run四node
     `replayed_trace_ops`之和分别等于`load`和对应workload值，不相等直接失败而非
     只在报告中显示；
   - 同一summarizer对每个metadata列出的workload/round必须恰好找到load/run各
     `vm_count`行，node集合精确为`0..vm_count-1`、无重复，marker `phase`与目录
     stage相等；每行topology固定为metadata的foreground=4、demuxer=1、
     kv_threads=5且affinity已报告为`distinct_allowed_cpus`。缺组、多组或不符直接
     失败，不新增日志恢复/容错模式；
   - 原log marker仍读取`ops`，但离开parser后逐个精确重命名：每node row的
     `ops`→`replayed_trace_ops`，round的
     `ops_sum`→`replayed_trace_ops_sum`、
     `ops_per_sec`→`replayed_kv_ops_per_sec`，case的
     `avg_ops_sum`→`avg_replayed_trace_ops_sum`、
     `ops_per_sec_from_avg_round_max`→
     `replayed_kv_ops_per_sec_from_avg_round_max`；CSV字段、JSON key和Markdown
     表头/取值同时改，不能只改显示文字或留下另一份旧别名；
   - 报告同时打印metadata中的logical请求数和各phase replayed计数。A--D正式
     case级吞吐比较只使用双方同一trace的
     `replayed_kv_ops_per_sec_from_avg_round_max`；round JSON中的
     `replayed_kv_ops_per_sec`只作单轮诊断。Workload E虽仍按合同完整运行并报告
     同一两种计数，但TigonKV原生range-partition顺序Scan与cxlkv单全局树存在结构
     差异，必须独立成节标记为“非同结构工作量”，不得纳入A--D汇总、几何平均、
     优胜比例或同工作量headline。相同trace digest只证明请求输入相同，不把两种
     Scan结构变成等价执行工作量；也不得为追求等价而修改Tigon原Scan。若另需
     logical request throughput，只以同一计时窗口用metadata的1M作分子并明确
     “A的一个update含两个KV API”，不能混用两个分母或把两列相加。

   这些都是一次性metadata/离线汇总，不给`RuntimeStats`增加字段，也不为每条操作
   增加计数。
6. split工具在现有“遍历`complete_keys`并累计四个`complete_counts`”的同一个循环
   中，为每个partition再维护一个optional最小`FixedKey`；遇到更小真实load key时
   替换。输出`representative_key_hex`只改为这四个full-load最小值，任一partition
   无值沿用现有空分区hard-fail。`sampled_keys`、split选择、1.25核对、
   `load_key_digest`均不变；不得再排序full load、重读文件或建立代表key选择模式。
7. `run_guest_ycsb_workflows.sh::run_fixed()`只机械读取
   `TIGONKV_E2E_REQUIRE_GET_FOUND`（默认0）并把
   `TIGONKV_E2E_REQUIRE_GET_FOUND=<0|1>`加入现有远端`env`命令，和已经转发的
   test-value/Scan开关相同。只接受字符串0或1，否则在同步/启动runner前返回2；
   不新增runner选项、probe、GET模式或第二个guest workflow。
8. 不改变通用`prepare_ycsb_traces.sh`“生成并把split写入调用者显式config”的合同。
   只在`tests/e2e_ycsb_test.sh`的`need_prepare`分支中，于现有本轮log目录创建一个
   `mktemp` config，逐字节复制当前
   `TIGONKV_EXPERIMENT_CONFIG_JSONC`，仅对这一次prepare命令设置
   `TIGONKV_EXPERIMENT_CONFIG_JSONC=<临时副本>`。当前wrapper没有trap，因此只新增
   一个EXIT trap，变量初始为空，主体固定为
   `[[ -z ${prepare_config:-} ]] || rm -f -- "$prepare_config"`；无论
   prepare/guest成功失败都删除该单文件，不能
   删除或覆盖本轮logs/trace。正常路径无需提前再删一次，也不建立通用cleanup
   函数/临时目录。
   trace生成后实际guest run继续使用原config，因此Debug功能trace不会改写正式
   split/config SHA；不得增加“skip split”模式、第二个准备脚本或把临时config当
   运行配置。
9. 根正式入口中现有splitter build与runner build共用同一个`skip_build`判断；
   `--skip-build`时只检查既有
   `build-relwithdebinfo/ycsb_partition_splits`可执行并直接使用，缺失则返回2。
   非skip路径把两个既有build命令统一为项目规定的`-j2`，不增加job参数、另一个
   build开关或自动configure。P阶段第3项显式传`--skip-build`，因此不在唯一Rel
   build后重复编译。
10. 删除`run_e2e_ycsb_rounds.sh`末尾对正式summarizer的调用。该窄入口继续只负责
    逐轮fresh pool→load→A、四VM pass/fail和保存原始log；Debug CTest不消费吞吐
    报告。不得给它或`prepare_e2e_ycsb_traces.sh`补造简化
    `run_meta.json`、猜测logical/replayed操作数，亦不得让summarizer恢复可选
    metadata。带严格metadata校验的CSV/JSON/Markdown只由根正式实验入口生成。

#### 必要验证

- 与 B1--B5、B7共用阶段 B唯一gate中的现有
  `ycsb_partition_splits_test`和`ycsb_scripts_test`，不新增 target；
- split fixture只把期望标签从 `trace_digest`改成`load_key_digest`，连续两次仍
  产生相同 split/key digest；
- `ycsb_scripts_test`只静态检查两个通用入口默认 workload列表都含 E、两个旧窄
  入口仍显式为A；不为 P阶段一次性的 manifest命令增加 script mode/mock或提前
  生成 trace；
- 该测试现有两份短假log通过显式`--run-meta`同步断言上述五个重命名后的
  replayed字段、旧字段全部不存在及原max-duration公式，并检查run metadata含固定
  semantics/vm_count/replayed object；现有完整4-node组同时证明严格group/topology
  校验，不另造缺失node矩阵，不生成真实YCSB trace；
- 同一script静态断言guest命令显式转发
  `TIGONKV_E2E_REQUIRE_GET_FOUND`且非法值被拒绝；不启动SSH/runner，不为一个环境
  变量增加mock；
- 同一script只静态确认CTest wrapper把prepare的config指向`mktemp`副本、trap删除
  的唯一目标就是该副本且后续guest命令仍引用原config；不为此mock生成器、不生成
  真实YCSB trace、
  不启动VM。原config SHA不变由P阶段本来就记录的前后SHA证明；
- 同一script静态确认两个splitter/runner build都受唯一`skip_build`控制且不存在
  `-j4`；既有prepare-only/skip-trace-gen短case不触发构建，不增加真实compile；
- 同一script静态确认窄`run_e2e_ycsb_rounds.sh`不再调用summarizer，根正式入口
  仍以显式`--run-meta`唯一调用；不为该删除增加另一份假log或真实4VM轮次；
- split现有fixture直接从自己的16个load文件计算四个partition的full-load最小
  padded key，并与四行`representative_key_hex`逐字节相等；stride=64/16分别按
  各自生成的split核对，不要求两个不同split集合得到相同代表key，不另造fixture；
- P阶段才对真实正式 trace执行一次host校验，并在既有A--E运行后核对实际同步到
  guest的映射；同时断言每个phase四VM runner `ops`之和等于该phase预先记录的
  `replayed_trace_operations`，A的logical count仍为1M且replayed数按实际展开记录；
  不在 B重复长工作。

#### 完成条件

split采样摘要和实际 trace文件身份不再混淆；默认正式准备完整生成 load+A--E，
host canonical文件及guest实际重命名后的输入都由同一份确定、可复查的 SHA-256
manifest绑定；Debug trace准备不改写其运行/正式config；logical YCSB请求与实际
回放KV操作使用两个明确、不混淆的分母；窄功能入口不再伪装成缺metadata的正式
报告入口。

### B7. 阶段B的`unit_tests`会提前运行尚未施工的完整KV数据面

#### 当前证据

`tests/tigonkv_tests.cpp`是单一`main()`：

- 前半段覆盖Config、JSON、range和fixed-width输入；
- 后半段创建file-backed `KVStore`，执行private/shared相关Put/Get/Scan/CAS/
  Increment、4线程操作、Memory/Runtime和reattach。

当前阶段B命令通过CTest运行整个`unit_tests`。这会在阶段C--L恢复B+Tree、EBR、
helper、Clock、transport和点操作之前，提前依赖将被删除的current-only生产路径；
既违反固定施工顺序，也把一个配置gate扩成无关的多线程功能测试。CTest本身不能对
这个单体binary选择前半段。

#### 修改方案

1. 只把现有`main()`签名改成接收参数，并接受唯一测试参数`--config-only`；未知
   参数立即返回2。不得引入test registry、filter库、新target或新脚本。
2. 保持现有前半段断言和执行顺序。在最后一个纯Config/range/fixed-width断言后、
   第一次创建临时backing或调用`KVStore::Create`之前加入唯一早退：

   ```text
   if (--config-only) {
     删除本case已创建的临时JSON
     打印固定pass marker
     return 0
   }
   ```

   纯case不得启动engine、mmap dual pool、创建worker线程或访问QEMU。无参数路径
   完整执行原测试，内容和CTest注册名`unit_tests`不变。
3. 阶段B先按§21确认无测试/QEMU，再直接运行一次
   `build-debug/unit_tests --config-only`，随后只用一次CTest命令运行现有
   `ycsb_partition_splits_test|ycsb_scripts_test`。这两条串行命令共同构成阶段B
   唯一gate，不把direct binary与CTest并行。
4. B1的persistent sentinel、B5的真实row/wire路径仍按文中既定H/G阶段延后；
   `--config-only`只验证无需最终table的数据。阶段M/P无参数运行完整
   `unit_tests`，不再额外运行config-only。

#### 必要验证

- 阶段B唯一执行§3.1列出的两条命令；固定marker证明config-only在backing创建前
  结束，进程扫描证明没有KV test/runner/QEMU并行；
- 静态确认CTest的`unit_tests`命令仍无参数，后半段断言未删除、未跳过；
- 不新增CMake target/test，不为参数解析写通用框架，也不在B运行任何KV数据面。

#### 完成条件

阶段B只验证它拥有的配置/定长/trace脚本合同；完整KVStore功能测试仍在相关生产
路径完成后执行，没有提前依赖current-only实现或重复测试。

## 6. 阶段 C：common、B+Tree、allocator、EBR 和启动

### C1. static layout 的 `magic` 早于 dynamic arena 边界发布，存在真实启动竞态

#### 当前证据

当前启动顺序是：

1. `DualRegionAllocator::Initialize()` 在
   `region_allocator.cpp` 中发布 `layout.magic`；
2. `KVEngine::Open(reset=true)` 之后才分配/发布 transport 和 EBR；
3. 最后才调用 `FinalizeStaticHwccLayout()` 填充
   `owner_dynamic_arenas[]`；
4. 非 reset VM 的 `Attach()` 只等待 `magic`；
5. 它取得 transport/EBR root 后即可进入 `InitializeOwnerPrivateArenas()`；
6. 若此时 dynamic descriptor 仍为 null，该函数直接 return，dynamic allocator
   handle 未建立，后续 owner root/sentinel 分配会报“allocator not initialized”。

此外，`DualRegionAllocator::Attach()` 在通过 HWCC magic 后还调用
`RegionAllocator::Attach`读取全局 SWCC allocator header做校验。该 header位于
non-coherent SWCC且只应由 VM0初始化；加入 VM不得为构造全池 offset helper读取它。
当前 `swcc_` handle后来又被大量生产代码当成全池 resolver，扩大了错域解析面。

`kv_startup_test` 在观察到 magic 后立即 fork/attach，具备触发方向，但当前窗口很窄，
没有可控 barrier，因此通过不能证明没有竞态。

这违反 §3.16 “VM0 先发布不可变 arena 边界、transport、EBR，再让其他 VM 初始化”
的顺序。

#### 修改方案

1. 不新增第三种 startup state、generation、timeout 或恢复协议。
2. 固定使用 `layout.magic` 作为 immutable-static-layout 的唯一发布 gate，调用
   顺序只能是：

   ```text
   DualRegionAllocator::Initialize
     → 构造 persistent header、static allocator、null root directory；
       layout.magic 保持 0，绝不在此发布
   KVEngine::Open(reset=true)
     → 分配并发布 transport root 与 global EBR root
     → DualRegionAllocator::FinalizeStaticHwccLayout
       计算并 flush 全部 owner_dynamic_arenas[]
     → DualRegionAllocator::PublishStaticHwccLayout
       校验 transport/EBR root 与每个 dynamic descriptor 均非 null/非零，
       flush immutable header，最后一条写为 magic release-store
   DualRegionAllocator::Attach
     → acquire 等待 magic，再校验 config/layout/roots/descriptors
   每个 VM
     → 初始化自己的 owner control、partition arena和C阶段原B+Tree root
     → H阶段接好最终Table/ValueStruct/lmeta后，由同一owner initializer插入sentinel
     → 发布 owner-ready bit
   VM0
     → 等齐 owner-ready bit 后 release-store Ready
   ```

   `PublishStaticHwccLayout()` 是唯一写 `kSharedLayoutMagic` 的函数，且只能调用一次；
   `FinalizeStaticHwccLayout()` 只计算 descriptor，不发布 magic。不得复用
   `state=Ready` 作为 static-layout gate，因为非 VM 必须在 Initializing 阶段创建
   自己的 owner-private 状态。
   VM0按现有唯一分配顺序同时记录两个 immutable static-domain bounds：
   transport（ring array及全部 entry allocations的最小 begin/最大 end）和 EBR
   object；offset/bytes相对现有 HWCC allocator region。二者非空、不重叠、位于
   dynamic-HWCC begin之前，且各自 root落在对应 bound内，才允许发布 magic。
   不增加通用 allocation map或每对象 descriptor。
   上述是最终唯一production顺序；C阶段只实现并验证static publication、owner
   control/arena/root和ready barrier本身，不在current-only row上创建临时sentinel。
   sentinel insert这一个原table调用按合同在E/H最终row接线后补入同一initializer，
   并由H gate验证；没有第二个startup入口、临时row或额外state。
3. 删除 `InitializeOwnerPrivateArenas()` 中“descriptor 为空就静默 return”的半初始化
   路径；观察到发布 gate 后为空属于启动不变量破坏，应带诊断 hard-fail。
4. 同时落实 B2 已固定的配置合同，按以下唯一布局把 per-owner control 与
   partition id 解耦：

   - 在 owner-private SWCC 总区的最前部保留连续的固定 owner-control slots，
     其后才排列现有 per-partition arena。每个 slot的布局唯一固定为：

     ```text
     Align64(OwnerAllocatorControl)
     + Align64(RegionAllocatorHeader for dynamic HWCC)
     + Align64(RegionAllocatorHeader for shared payload SWCC)
     ```

     直接复用现有 external-header allocator；不得再从某个 partition arena分配
     这两个 header，也不得为无 partition owner另写 allocator；
   - `DualRegionPersistentHeader` 增加 immutable
     `owner_controls_offset/bytes/stride`，stride恰好是上面 slot总长，并相应 bump
     一次 layout/header version；
   - 几何公式固定为：

     ```text
     owner_private_total =
       AlignDown((swcc_payload_bytes * owner_private_fraction), 64)
     owner_controls_offset = swcc_allocator_metadata_bytes
     owner_controls_bytes = vm_count * owner_control_stride
     partition_arenas_offset =
       owner_controls_offset + owner_controls_bytes
     partition_arena_stride =
       AlignDown((owner_private_total - owner_controls_bytes) /
                 partition_count, 64)
     shared_payload_start =
       swcc_allocator_metadata_bytes + owner_private_total
     ```

     control总长溢出、`owner_private_total <= owner_controls_bytes`或 arena stride
     不足 `Align64(sizeof(final OwnerPrivateArenaHeader)) +
     2*RegionAllocator::kAlignment`（一个最小 owner allocation的block header与
     payload）时直接拒绝配置；
     对齐余量保持 reserved/unused，不分给另一域也不计 used；
   - 每项只保存 `magic/version/owner_shard`、dynamic-HWCC 与 shared-payload 两个
     固定 external `RegionAllocatorHeader` sub-offset、两类 `DomainCounter`、F2
     使用的唯一 per-owner `TOTAL_HW_CC_USAGE`，以及与 master 一致的每
     foreground-worker/epoch 一条 retire head/count；sub-offset由 slot几何唯一
     计算，不通过 allocator获得。retire queue 是 per owner，不按 partition
     再分片，也不在 EBR 推进时遍历所有 partition；
   - `InitializeOwnerPrivateArenas(node_id)` 先 placement-new 自己的 control，再只
     placement-new本 slot内的两个现有 `RegionAllocatorHeader`，随后只初始化
     `partition % vm_count == node_id` 的 arena；即使该 owner没有 partition，
     control和两个 dynamic allocator也必须可用；
   - `BindOwnerPrivateAllocators`、`DynamicHwccUsedBytes`、
     `SharedPayloadUsedBytes`、allocate/free 会计只访问
     `OwnerControl(node_id)`，不再以 `Arena(node_id)` 代替 owner control；
   - 每个实际 partition 的最终 `OwnerPrivateArenaHeader` 在本阶段同时机械固定：
     C3 的 atomic private root、F1 的
     `OwnerPrivateClockTrackerControl{lock,head,tail,cursor}`；删除现有
     per-partition retire heads/counts，避免保留第二套 EBR 队列；
     owner reset时一次初始化 `PTHREAD_PROCESS_PRIVATE` Clock lock，非 owner/attach
     不 init/destroy；
   - retire record 仍从该对象所属操作传入的实际 `queue_partition` owner-private
     arena分配，record除原对象 offset/bytes/domain/private-partition外只增加一个
     `record_partition`，使单 owner queue reclaim时能用同一 arena回收 record
     自身；必须校验该 partition实际属于 owner。不得为 record另建 owner arena、
     allocator或DRAM容器；
   - 阶段 C 只完成上述 storage/init/resolver。现有
     `KVPartition::ClockTracker*` 暂时改为访问最终 control以保持可编译和行为不变，
     F1 再删除该算法并把控制流迁回 PolicyClock；Clock node/link和 retire queue仍
     留在对应 owner-private SWCC，不搬进全局 HWCC。

   不保留旧 `OwnerPrivateArenaHeader::dynamic_*` 字段作为镜像或 fallback。
   `OwnerControl(node_id)` 只允许 `node_id==bound_owner_shard`，按 immutable slot
   几何校验后返回本进程的 non-owning handle；任何 VM不得读写另一 slot。
   同时删除 `DualRegionAllocator` 长期保存的全局 `swcc_ RegionAllocator`：
   VM0只在 `Initialize` 内用一个栈局部 `RegionAllocator` placement-init全局 SWCC
   header并保留 owner-private prefix，随后不再用它分配；attach绝不能调用
   `RegionAllocator::Attach`读取该 SWCC header。SWCC region-relative offset的
   编解码和 capacity只使用 `DualRegionAllocator` 已有 `pool_ + config_` 做纯整数
   运算，owner-private分配继续走现有 partition arena，shared payload分配继续走
   owner control中的 external header。删除公开 `swcc()`全池 resolver；C4 的 typed
   resolver和 allocator内部纯转换取代全部调用。不得增加 range-only
   `RegionAllocator` mode、nullable-header fallback或另一种 view class。
   `Memory()` 中每个 VM只把自己的完整 slot stride计入
   `allocator_swcc_metadata_bytes`，VM0仍只额外报告一次全局 SWCC allocator
   header的编译期固定字节（非 VM0不得为报告读取它）；每个 partition arena
   header仍由其 owner各计一次。这样 runner汇总后
   control/header/sentinel均恰好一次，reserved空闲容量不冒充 used。
5. C1/C3/F1 所需的上述 owner-private header变更合并成**一次**
   layout/header version bump；F1 不得再次改持久布局。
6. 保留唯一状态序列 `magic/static publication → Initializing → owner bitmap →
   Ready`；其中 magic 只是 immutable layout 的发布标志，不是新增可变状态。不要
   增加等待 deadline。

#### 必要验证

- 纳入阶段 C gate；C1--C6 完成后与 allocator/B+Tree/EBR target 合并运行一次；
- 在 Debug startup test 中加一个仅测试可见的 barrier，强制 VM1 在 VM0 static
  allocation 与最终 descriptor/root 发布之间 attach；
- 一个受控 4 进程 layout-only startup case同时覆盖1 partition/4 VM、发布窗口和
  每VM只初始化自己的allocator/root；它直接使用C2原B+Tree root binding，不构造
  KV current-only row/sentinel，不另做重复轮次；
- 断言 owner control offset/stride 不依赖 partition 数，1/4 中四个 control 均有效，
  每个 control slot内恰有两个原 `RegionAllocatorHeader`，只有 VM0有 partition
   arena；任何 VM都没有读写别人的 control；
- attach访问跟踪确认非 VM0没有读取全局 SWCC allocator header，生产中不存在
  `swcc()`全池 resolver或 range-view fallback；
- 每个实际 partition 的 atomic private root和Clock control均落在其
  owner-private arena；每个 owner-control 恰有一组 worker/epoch retire
  head/count，跨该 owner 的 partition共用，Clock lock只由 owner初始化一次；
- layout-only case中Ready前所有owner bit齐全、Ready后root均非null；最终
  production initializer中“sentinel先于owner bit”的接线与实际行断言延后到H2；
- 四进程 `Memory()` 聚合恰好计四个 owner-control slot、实际 partition arena
  header各一次，未使用的owner-private reserve不计used；sentinel物理/逻辑会计
  只在H gate用最终对象补断言；
- 不启动真实 QEMU；file-backed 多进程即可定位本项。

#### 完成条件

任何 attach 能越过现有发布 gate 时，transport、EBR 和全部 immutable arena
descriptor 已完整可见；1 partition/4 VM的layout/owner root可完成Ready屏障，不存在
“成功 Attach 但 dynamic allocator 未初始化”的状态。最终sentinel先于owner-ready
的production接线由H2完成，不能用C阶段过渡row冒充。

### C2. `BTreeOLC_CXL.h` 的大规模差分尚无逐函数算法等价证明

#### 当前证据

当前文件相对 master 有约 `+1091/-287` 行差异。必要的 RegionOffset、allocator
binding、root slot、EBR 和 `mem_access` 确实会产生较大机械差分，但现有审计没有
逐函数证明以下原算法保持不变：

- split/merge/restart；
- leaf/inner latch 范围；
- scan/update traversal；
- `is_last_tuple` 和 adjacent callback；
- root load/store；
- return/Busy 语义。

仅在 allowlist 中写“机械适配”不足以满足 §3.16。

#### 修改方案

1. 从 `git show master:common/btree_olc_cxl/BTreeOLC_CXL.h` 的函数主体逐个对照。
2. 只保留：

   - persistent raw/`boost::offset_ptr` → 明确的 domain-relative
     `RegionOffset`；
   - allocator/root/EBR binding；
   - 稳定 tree-domain `TreeAccessScope`；
   - 原真实访问点的 `mem_access`；
   - `partition优化方案.md` §2第15条已确认的 adjacent callback 实参最小修正。

3. 恢复所有与上述项目无关的条件、循环、restart、锁范围和返回值。
4. 本地 `common/btree_olc/BTreeOLC.h` 保持 master，不借本项现代化。
5. `BTreeOLC_CXL` 的最终 persistent node布局只保存 `RegionOffset`：

   - inner child slots、leaf `pre_/next_`均为 tree allocation binding相对 offset；
   - 在 master原指针赋值处调用 `allocation.ToOffset`，在原解引用前调用
     C4范围校验后的 `allocation.FromOffset`；
   - iterator/callback中的短生命周期 raw node pointer可留进程 DRAM，不能写回
     node；
   - `static_assert(sizeof(RegionOffset)==sizeof原offset_ptr槽)`并重新断言
     leaf/inner `maxEntries`与 master相同，不能因适配改变 fanout/page size；
   - C3 的 root slot是唯一 persistent root；process-local tree handle不保存第二个
     offset_ptr/raw root权威。

   `boost::offset_ptr`只能继续存在于未链接 legacy参考代码，正式
   BTree/CXLTable persistent类型不得使用。不得为转换增加 node wrapper、pointer
   registry或 generation。

#### 必要验证

- 纳入同一次阶段 C gate，不在 C1 后单独重跑 startup；
- focused `btree_binding_test`：lookup/insert/remove/split/merge、private/shared
  binding、nested domain、offset reattach；
- 静态扫描正式 CXL BTree node/leaf类型无 persistent `boost::offset_ptr`，并断言
  page fanout/layout未变；
- adjacency 只做接口 smoke，完整邻接语义留到阶段 E；
- 函数级 diff 审计；
- 不跑 YCSB。

#### 完成条件

除 offset/域/计费/已确认原 bug 外，CXL B+Tree 的算法控制流与 master 逐函数一致。

### C3. private B+Tree root 仍以进程内 mirror 为运行时权威

#### 当前证据

- `OwnerPrivateArenaHeader::private_root` 是普通 `RegionOffset`，不是同 VM worker
  共享的 CPU atomic root slot；
- private tree 没有调用 `bind_published_root()`，其原 `root_` 留在可重建的进程内
  wrapper；
- `KVPartition::PersistPrivateRootIfChanged()` 在 operation 结束后才把 wrapper 当前
  root 复制到 owner-private SWCC，并维护
  `persisted_private_root_offset_/private_root_publishes_` 镜像状态；
- shared tree 已直接绑定 `directory_.shared_root`，所以当前 private/shared root
  并非同一原式 load/store primitive 的两个访问域适配。

这与 §3.16 的固定要求冲突：原 `root_` load/store 位置应直接绑定区域内 atomic
`RegionOffset`；private slot 在 owner-private SWCC 使用同 VM CPU atomic，不能只在
操作后复制一次进程内 VA handle 的 root。

#### 修改方案

1. 把 `OwnerPrivateArenaHeader::private_root` 唯一改为
   `std::atomic<RegionOffset>`；本轮与 C1 的 owner-control/layout 修改合并成一次
   layout/header version bump，不额外保留旧字段或兼容分支。
2. `BTreeOLC_CXL` 保留一个 `bind_published_root(std::atomic<RegionOffset>*)`
   primitive，但 root wrapper 按树实例的固定 allocation binding选择访问域：

   - private tree：`PrivateAtomicLoad/PrivateAtomicStore`；
   - shared tree：`HwccAtomicLoad/HwccAtomicStore`。

   memory order固定为 acquire load/release store；不把域选择散布到 split/merge
   算法，也不依赖“最近访问树”的可变 TLS。
3. 每个 private table 构造后立即绑定其所属
   `OwnerPrivateArenaHeader::private_root`；shared table继续绑定现有
   `PartitionDirectoryEntry::shared_root`。原 tree operation、makeRoot、root merge
   在 master 原位置直接读写该 slot。
4. reset 时 owner 在 Ready 前把自己的 private root slot原子初始化为 null，再由原
   root/sentinel 创建路径发布首个 root；attach 只 acquire-load 已发布 slot并解析
   offset。非法/null-after-ready/越域 offset hard-fail。
5. 删除 `PersistPrivateRootIfChanged()`、
   `persisted_private_root_offset_`、`private_root_publishes_` 及仅验证镜像次数的
   测试/统计。进程内 tree/table object只保留 non-owning binding，不缓存权威 VA
   root。
6. private root 不使用 SCC、flush handshake或跨 VM lock；非 owner不得读取。不得
   为 reattach 增加第二个 root、generation或恢复协议。

#### 必要验证

- 与 C1、C2 共用阶段 C 唯一 gate；
- `btree_binding_test` 在现有 private binding case 中强制一次 root split和一次
  root merge，断言 master 原位置分别执行一次 owner-private atomic publish；
- 同进程第二个 non-owning handle和析构后 reattach都从同一 slot解析当前 root，
  不依赖旧 wrapper；
- shared root对应操作只计 HWCC atomic，private root只计 SWCC atomic；
- `kv_startup_test` 断言 Ready 后每个实际 partition 的 private root非 null且只由
  owner初始化；
- 静态确认 `PersistPrivateRootIfChanged/persisted_private_root_offset_/
  private_root_publishes_` 全部消失。

#### 完成条件

private/shared tree 共用 master 原 root load/store主体，只因绑定 slot 的内存域不同；
owner-private atomic slot是 private root 的唯一权威。

### C4. 多个持久 offset resolver 只检查“落在整段 HWCC/SWCC”，未检查所属 arena/domain

#### 当前证据

- `KVPartition::ValueFromOffset/ClockNodeFromOffset` 直接
  `regions_.swcc().FromOffset(offset)`，可把 shared-payload 或其他 owner-private
  arena 内地址解释为本 partition 对象；
- `MetadataFromValue` 对 `value->meta` 只检查非 null，随后同样按整段 SWCC解析；
- `SharedMetadataFromOffset` 只用 `IsHwccAddress`，无法排除 layout、transport、EBR
  或其他 owner dynamic arena；
- 原 `CXLTableBTreeOLC::BTreeOLCValue::row` 仍是
  `boost::interprocess::offset_ptr<void>`；`search/scan/adjacent`先调用 `.get()`
  得到 VA才返回，无法在解析前证明它属于该 partition owner的 dynamic-HWCC
  smeta arena，也不满足共享布局只保存 `RegionOffset`；
- cached `scc_data_off` 只检查“在 SWCC”，无法证明它位于该 row owner 的
  shared-payload arena；
- EBR reclaim 根据 record domain选择整段 `hwcc().FromOffset` 或
  `swcc().FromOffset`，corrupt domain/offset可能释放错误区域；
- `MPSCRingBuffer::entries_buffer` 仍持久化
  `boost::offset_ptr<char>`；transport/EBR root解析也只检查整段 HWCC，无法证明
  offset落在 C1 对应 static domain；
- `RegionAllocator::FromOffset` 对其**自身绑定的 region**已有边界检查，问题在于
  上述生产数据路径绕过具体 binding，直接使用全物理 pool resolver。

这不满足“非法 offset、错 owner/domain hard-fail”和“非 owner不得读取
owner-private SWCC”。全池边界内的错误 offset 仍然是协议损坏，不能等到解引用后
偶然失败。

#### 修改方案

1. `DualRegionAllocator` 在 init/attach 时从已经通过 C1 magic gate发布的 immutable
   descriptor 机械复制进程内、non-owning bounds；直接复用现有
   `OwnerDynamicArenaDescriptor` 类型，不新增 range-binding class/type，不保存
   权威状态或 key路由结果：

   ```text
   owner-control = C1 的 owner_controls_offset/stride，仅本 bound owner slot
   owner-private = 由已有 owner_private_arenas_offset/stride 按 partition计算
   dynamic/shared = 现有 OwnerDynamicArenaDescriptor[owner] 的两段 offset/bytes
   ```

   这是现有 allocator handle 的边界副本，不是 key routing cache；attach 时校验
   不重叠、不越物理区且与 owner/partition modulo一致。现有
   `BindOwnerPrivateAllocators(node_id)` 同时在进程内 handle记录唯一
   `bound_owner_shard`，仅用于阻止 private resolver跨 owner；不持久化。
   C1 的 `OwnerControl(node_id)` 是 control专用 typed accessor，不算第四个
   data-path resolver；它只接受 bound owner并用同一个 `ContainsRange` 校验该
   fixed slot。
   持久 offset基址不得由调用者猜测，固定为：

   ```text
   private ValueStruct/lmeta/Clock/retire record:
     SWCC region-relative RegionOffset
   shared tree smeta leaf reference:
     HWCC allocator-region-relative RegionOffset
   smeta atomic_word低37位 SCC payload:
     沿用 master cxlalloc 的 whole-pool-relative offset
   ```

   第三种格式不能改成 SWCC-relative，因为这会改变原 atomic-word 编码；
   `ResolveSharedPayload`明确接收 whole-pool offset，把 immutable
   `swcc_offset_bytes + descriptor.shared_swcc_offset/bytes`转为同一整数坐标后再
   `ContainsRange`，最后才解析 VA。构造 smeta时反向执行同一转换并 hard-check
   `<2^37`。该编码只作为`EncodeSharedPayload`与resolver成对放在现有
   `DualRegionAllocator` binding内；删除
   `CXLMemory::pointer_to_pool_offset/pool_offset_to_pointer`及公开的无domain
   `FromPoolOffset`旁路，不能让其它对象借whole-pool坐标绕过typed bounds。
2. 只提供三个 data-path resolver：

   ```text
   ResolveOwnerPrivate(offset, bytes, partition, expected_owner)
   ResolveDynamicHwcc(offset, bytes, expected_owner)
   ResolveSharedPayload(whole_pool_offset, bytes)
   ```

   三者内部共用一个无状态纯整数 `ContainsRange(binding,offset,bytes)`；它只供
   resolver和阶段 C边界单测使用，不作为生产 fallback。三者都拒绝
   null（除调用点明确允许 null）、`bytes==0`、加法 overflow、未对齐、
   越界和（owner-private/dynamic-HWCC的）owner不匹配；`ResolveOwnerPrivate` 还必须
   满足 `expected_owner==bound_owner_shard`。`ResolveSharedPayload` 不接受 owner
   参数：原 `TwoPLPashaMetadataShared::get_scc_data()` 与 master 同样只有 SCC
   whole-pool offset，既无 allocation-owner 字段也无调用参数；它必须遍历仅HWCC
   immutable layout中的互不重叠 shared-payload descriptors，要求恰好一个命中后才
   解析，绝不能依据当前VM owner选择，也绝不能接受任何非 shared-payload 域。这是
   domain validation，不是裸全池 fallback、缓存或新状态。失败携带
   pool/offset/bytes/partition/owner 后调用本阶段建立的唯一
   `[[noreturn]] ProtocolFatal`（最小声明放
   `kv/engine/protocol_fatal.h`，输出结构化上下文后 abort）；L1 复用该函数收敛其余
   corruption分类，不再建立第二个 fatal helper。resolver只做整数范围检查和瞬时
   VA解析，不读其他 owner的 private allocator metadata。
3. `ValueFromOffset`、`MetadataFromValue`、Clock node resolver和
   `RegionOffsetRowStorage::Resolve` 统一调用
   `ResolveOwnerPrivate(..., partition_id, owner_shard)`；只有 owner路径能取得该
   binding。
4. smeta locator统一调用
   `ResolveDynamicHwcc(..., OwnerForPartition(partition))`；smeta中解析的 SCC payload
   及 lmeta cached payload统一调用无owner的 `ResolveSharedPayload`，后者按上述唯一
   descriptor 集合验证 whole-pool encoding。不能先全池 `FromOffset` 再用
   `IsHwcc/IsSwccAddress` 兜底。
   E1同时把原 `CXLTableBTreeOLC` leaf的 row reference机械改为可选 policy：
   legacy默认仍是 `offset_ptr<void>`，KV实例的 `StoredRow=RegionOffset`且其
   `Resolve`直接调用本项 `ResolveDynamicHwcc`。shared table
   search/scan/adjacent在原 `.get()`位置调用 policy，不能让 `KVPartition`先收到
   未验证 VA再补检查。
5. B+Tree node/root继续使用 C2/C3 的 tree allocation binding内 resolver，不改为
   上述 row resolver；static layout/transport/EBR root 继续使用各自已有静态
   domain binding。C1新增的两个 immutable bound只提供
   `ResolveTransport(offset,bytes)`和`ResolveEbr(offset,bytes)` typed accessor，
   内部仍共用 `ContainsRange`；不是 row resolver、allocation registry或动态状态。
   `CXLMemory::commit/wait root`按 root index调用对应 typed accessor，未知或错域
   root fatal。
6. EBR reclaim 穷尽 switch record domain：

   - `private_partition!=UINT32_MAX` → owner-private resolver；
   - `kHwccIndex/kHwccMetadata` → dynamic-HWCC resolver；
   - `kSharedPayloadSwcc` → shared-payload resolver；
   - 其它 domain或 owner/partition不一致 → hard-fail。

   不能让 corrupt record落入 generic default，也不能为便于回收读取另一 owner的
   private allocator control。
7. 保留 `RegionAllocator::FromOffset` 给 allocator自身 freelist/bump内部使用；删除
   最终生产 row/Clock/EBR data path 对全池 `hwcc().FromOffset/swcc().FromOffset`
   的直接调用。不得增加 allocation map、object header、generation或远端检查 RPC。
8. 本阶段先把 `MPSCRingBuffer::entries_buffer` 机械改为 HWCC allocator-relative
   `RegionOffset`。构造时用已经返回的本地 allocation pointer完成初始化并编码
   offset；enqueue/dequeue每次通过 `ResolveTransport(offset,
   entry_struct_size*entry_num)`取得瞬时 base，检查乘法/加法 overflow。attach还要
   检查各 ring entries range非空、不重叠且均在 transport bound内。不得保留
   `offset_ptr` fallback、缓存解析 VA或改 ring算法；G阶段只恢复原
   receive/reservation控制流，不再次改变该布局。

#### 必要验证

- 与 C1--C3 共用阶段 C 唯一 gate；
- 在现有 allocator/BTree/EBR focused target中合并一个短 resolver case：

  - 每类合法 range的首个对齐对象和末个可容纳对象可解析；
  - whole-pool SCC offset与 HWCC/SWCC region-relative offset各按唯一基址解析，
    把一种 offset误交另一 resolver必须拒绝；
  - 同一个全池内但属于其他 owner-private/dynamic-HWCC、其他 domain或跨 end 的
    offset被拒绝；shared-payload whole-pool offset 可由非owner VM 解析，但必须只
    命中一个 immutable shared-payload descriptor；
  - 1 partition/4 VM 时 VM1不能解析 VM0 owner-private offset，但可按共享协议解析
    VM0 dynamic-HWCC/shared-payload offset；
  - EBR合法三类对象仍由原 allocator回收一次。
  - transport/EBR root和 ring entries offset只能由各自 static typed accessor解析，
    同为 HWCC但错 static domain时拒绝；

- C阶段用 `ContainsRange` 验证合法/非法边界，不新增 death-test；阶段 L 的一个
  corrupt locator case直接调用 resolver验证最终 hard-fail分类；
- `rg` 审查全池 `FromOffset` 的剩余调用，每一处必须是 allocator内部或明确静态
  domain root。
- `rg`确认正式 BTree/CXLTable/MPSC persistent类型不再含
  `boost::offset_ptr/AtomicOffsetPtr`；未链接 legacy参考文件不清理。

#### 完成条件

每个生产 RegionOffset 在解引用前都由其唯一 arena/domain binding验证；全池内错
owner-private/dynamic-HWCC owner、错域或跨界 offset不能被解释为另一种对象；原
whole-pool SCC payload offset只可命中一个 shared-payload arena，且未新增 lookup
state。

### C5. facade 预先 `PollTransport()` 使每个公共操作通常进入 EBR 两次

#### 当前证据

- `KVStore::Put/Get/Delete/Scan/CompareExchange/Increment` 在调用 engine前都先
  执行一次 `impl_->engine->PollTransport()`；
- `KVEngine::PollTransport()` 自己构造 `EbrOperationScope`，随后真正的
  `KVEngine::{Put,...}` 又各构造一次 scope；
- 两个 scope并不嵌套，因此一次普通公共操作会先推进一次 epoch，再以第二次
  enter访问 tree/row；这偏离原 Executor“一个前台操作 enter一次”的语义；
- `AwaitResponse()` 内的 `PollTransport()` 当前处于 engine操作外层 scope内，
  TLS depth正确抑制重复 enter；facade Busy重试两次完整 attempt之间的 poll则是
  合法 idle service。

#### 修改方案

1. 删除六个 facade API开头无条件的 pre-poll；参数非法时直接在 facade返回，不
   为无效请求制造 transport/EBR操作。
2. 在六个 engine API各自现有 `EbrOperationScope` 建立后、第一次 tree/shared-row
   访问前调用一次现有 `PollTransport()`。该调用因 TLS depth嵌套而不再 enter，
   同时保留原 Executor每轮先处理自己 inbox的服务顺序。
3. `AwaitResponse()` 内 poll保持嵌套并沿用同一 epoch；不得拆出第二个 operation
   scope。一次 engine attempt返回后 TLS depth必须回到0。
4. facade对 Busy的持续重试仍在两次完整 attempt之间调用公开
   `PollTransport()`；此时没有外层操作，它作为 idle/已完成 worker服务 peer而
   enter一次，严格符合 §3.16。
5. 不新增 EBR active/leave状态、scope栈、epoch缓存或另一条 poll入口；只移动现有
   调用点。demuxer不访问树，仍不占 EBR slot。

#### 必要验证

- 与 C1--C4 共用阶段 C唯一 gate；
- C gate在现有`cxl_ebr_test`只检查同一meta的嵌套scope不重复enter，并用静态
  调用图确认六个engine API均在首次tree/shared-row访问前建立scope；
- 无RPC公共操作一次enter、RPC等待期间nested poll不增加enter、Busy attempt之间
  idle poll各一次、invalid facade参数零次enter及显式
  `KVStore::PollTransport()`一次enter，统一并入H本来就要运行的
  `kv_engine_test --focused-stage=H`一个直接序列；不保留C selector、不额外运行
  engine；
- 不新增线程矩阵、不运行4VM。

#### 完成条件

每个有效公共 KV attempt在访问树前恰好进入原 EBR一次；等待沿用该 epoch，只有
attempt之间的 idle service独立进入。

### C6. `BindWorker`重置 TLS EBR回收进度，但同一 worker id允许线程交接

#### 当前证据

- `CXL_EBR::thread_init_ebr_meta()` 每次调用都把 TLS
  `EBRMetaLocal::last_freed_epoch`重置为0。
- `KVEngine::ReleaseWorker()`清空`worker_owners_[worker_id]`，随后另一线程可合法
  `BindWorker`同一 id；现有测试也多次采用这种交接。
- owner retire queue已按 worker/epoch持久在 owner-private SWCC，global/local epoch
  继续推进。若旧线程已回收到较后的 epoch，新线程从0开始会重复读取旧进度；当
  `epoch_to_reclaim>1`时还会直接违反 master的
  `epoch_to_reclaim==last_freed_epoch+1`断言。
- `KVEngine::Open()`又在构造`KVEngine/worker_runtime_`之前调用
  `ebr->thread_init_ebr_meta(node_id,0)`，随后只用
  `ScopedKvWorkerTidBinding`把startup root/sentinel工作接到worker0的TID。按本项
  机械迁移storage后若保留这个顺序，startup线程仍会先创建一份legacy匿名TLS
  EBR状态，而不是使用最终worker0 slot。

这不是 EBR算法本身的问题，而是把原“一个 Executor线程终身拥有一个
EBRMetaLocal”接到可解绑 API时丢失了 worker生命周期。

#### 修改方案

1. 保留原 `EBRMetaLocal`类型和全部字段/统计/推进逻辑，只把其 storage从函数内匿名
   TLS对象机械放进 H3最终的既有`KVEngine::WorkerRuntime`；它仍是进程DRAM、
   每 foreground worker恰好一份，不进入HWCC/SWCC。
2. engine创建 worker slots时按`(node_id,worker_id)`一次初始化各
   `EBRMetaLocal`，包括`last_freed_epoch=0`；`BindWorker`只让 CXL_EBR现有TLS保存
   指向该 slot meta的non-owning指针，`ReleaseWorker`只清除此TLS指针。再次绑定同一
   id不得重置任何回收进度或统计。
   `Open()`中必须删除engine构造前的
   `thread_init_ebr_meta(node_id,0)`；先创建engine及全部slot，再逐slot完成上述唯一
   初始化。现有startup用的`ScopedKvWorkerTidBinding`机械扩为同时临时绑定同一个
   worker0 slot中的TID与`EBRMetaLocal`，root/sentinel初始化退出该scope时两种TLS
   引用一起清除。不得让startup调用公开`BindWorker()`而触发foreground绑核，也
   不得为bootstrap保留专用slot、匿名TLS meta或第二份last-freed进度。
3. legacy Executor仍使用原无外部storage的 thread-local默认入口；只把
   `get_local_ebr_meta()`主体参数化为“已绑定KV slot，否则原legacy TLS”，两者进入
   完全相同的`enter/add/print`函数体。不得复制 EBR算法、增加 map、active/leave
   状态或持久 last-freed字段。
4. `BindWorker`仍拒绝两个线程同时占同一 id；thread handoff只发生在前一线程
   `ReleaseWorker`之后。engine析构前所有已绑定TLS必须已释放；现有
   `worker_owners_`继续只管理并发所有权，不成为 EBR进度副本。

#### 必要验证

- 与 C1--C5共用阶段 C唯一 EBR gate，不新增 target或阶段轮次；
- 在现有 `cxl_ebr_test`中让两个线程依次绑定同一个外部 worker0 meta：第一线程
  推进并回收至少到`epoch_to_reclaim=2`后解绑，第二线程再绑定并 enter；确认
  `last_freed_epoch`单调、旧queue不重复处理且原连续epoch断言成立；
- 同时保持两线程并发绑定同一 id立即拒绝，legacy默认TLS最小 case不变；
- H3本来就要运行的`kv_engine_test --focused-stage=H`用同一个直接序列覆盖
  Release→另一线程Bind及C5公共operation enter位置；C gate只检查外部meta交接和
  静态调用图，不提前运行engine数据面；
- 同一H直接序列静态/运行时确认`Open()`返回后startup TLS已清空，首次
  `BindWorker(0)`看到的正是初始化期间使用过的worker0 meta而非一份重置副本；
- 只做一次确定性交接，不建立多worker/多epoch压力矩阵、不运行4VM。

#### 完成条件

原每-worker EBR local进度跨线程交接保持唯一且单调；TLS只是瞬时绑定，不再把
Release/Bind误解释为一个全新的Executor worker。

## 7. 阶段 D：SCC、metadata 和 helper 发布序列

### D1. helper 中仍存在 KV-only 协议体，尚未全部收敛到 legacy/KV 共用 primitive

#### 当前证据

`TwoPLPashaHelper.h` 前半部加入大量 KV-specific 方法，后半部仍保留 legacy 主体。
其中部分 resolver 模板已经实现共用，这是正确方向；但以下类别仍需要逐一证明是
薄 adapter，而不是第二套协议：

- shared pin/unpin、valid 发布；
- owner/remote insert placeholder；
- move-in/move-out；
- Delete adjacency；
- migrated row lookup、锁和 release；
- Clock access。

当前 `KVPartition` 还组合这些方法自行完成状态转换，说明“同一函数/模板/类实现”的
目标尚未完全达到。

当前helper为这些平行主体又定义了
`KvSharedResult/KvDeleteSharedResult/KvMoveOutSharedResult/`
`KvMoveInPublishResult/KvRemoteDeletePrepareResult`五个KV-only枚举。它们分别把
原helper的锁结果、bool callback、`migration_result`或删除前检查再次包装；其中
`kRemoveFailed/kInvalid`还是应当hard-fail的不变量。若只切换函数却保留这些枚举，
施工者仍可继续维护第二套控制流。

#### 修改方案

1. 按下表收敛生产调用；表中“共同主体”必须是 legacy 与 KV 实际调用的同一个
   函数模板，不得只保留相同命名或复制函数体：

   | 行为 | 唯一共同主体 | 必须删除/折叠的 KV-only 主体 |
   |---|---|---|
   | owner read/write lock 与 value copy | `take_read_lock_and_read`、`take_write_lock_and_read`、原 release | 外层 lmeta/smeta 分支和二次 locator 判断 |
   | remote migrated lookup/ref | `get_migrated_row`、`release_migrated_row` | `kv_pin_shared_ref`、`kv_unpin_shared_ref` 的独立协议体 |
   | remote read/write lock | `remote_take_*`、原 remote release/abort | KV 自行组合 latch/ref/SCC 的分支 |
   | insert adjacency | `insert_and_update_next_key_info` | placeholder 的独立 next-row 锁循环 |
   | valid 发布 | `modify_tuple_valid_bit`、`remote_modify_tuple_valid_bit` | `kv_owner_publish_insert`、`kv_remote_publish_insert` 的独立状态机 |
   | phantom/B+Tree move-in/out | `move_from_btree_to_shared_region`、`move_from_btree_to_partition`；legacy通用dispatch wrapper与KV固定phantom路径都进入这两个主体 | `move_private_row_to_shared`、`finish/rollback_move_out_shared` 及KV callback内的平行控制流 |
   | Delete | `delete_and_update_next_key_info` | offset 版完整 Delete/adjacency 状态体 |
   | Scan adjacency | master Scan callback 的原 predicate/顺序 | `scan_row_adjacency_ok` 的偏离分支 |

   若 current helper 已经只是上述共同主体的 resolver overload，则保留 overload 并在
   allowlist 逐行证明；否则切换调用后删除它。master 的
   `move_from_partition_to_shared_region`/`move_from_shared_region_to_partition`
   只保留为 legacy 选择 HashMap/B+Tree 的原 dispatch wrapper；KV正式
   `enable_phantom_detection=true` 路径不得通过它保留运行时 HashMap 分叉，而应直接
   调用上表两个 B+Tree共同主体的 RegionOffset overload。
2. row pointer/RegionOffset 不兼容时，只增加 resolver 或 row-view 参数；legacy 和
   KV wrapper 必须进入同一锁/prepare/finish/ref/valid 主体。
   B+Tree move-in只允许在原函数末尾再加两个有legacy默认值的薄参数：
   `allow_invalid=true`和可空的栈上`RowOutcome *`。legacy调用点不改语义；普通KV
   migration传`allow_invalid=false`，remote-insert对本次刚创建的placeholder传
   `true`。原callback取得current lmeta锁后若看到invalid且不允许，只设置
   `RowOutcome::Busy`并按原位置解锁，不分配/发布任何对象；不得在锁外预判后继续
   move-in。原`search_and_update_next_key_info`返回false时，legacy仍保留原
   DCHECK，KV则只在输出参数中返回Busy，表示首次owner search后与Delete竞争而
   丢失；不能让初始化为`FAIL_OOM`的原`migration_result`把它伪装成OOM。
   首次owner `table.search`已经稳定返回空的请求在进入helper前直接Missing；不做
   第二次existence lookup。只有callback真实进入分配分支后失败才保留
   `migration_result::FAIL_OOM`。这些只是原回调的结果适配，不增加重试、锁或
   平行move-in主体。
3. 保留 KV 所需的定长 value bytes、固定 table id 和单操作结果适配；不要构造
   dummy Transaction。
4. `tid/valid/payload` 继续在 shared SWCC，并严格走原
   `prepare_read/finish_write`；不要因错误分类或延迟问题再次拆分 SCC。
5. 对不变量失败使用明确 hard-fail，不让 bool `false` 同时表示 Busy、Missing 和
   Corruption。
   切回上述原主体后同时删除
   `KvSharedResult/KvDeleteSharedResult/KvMoveOutSharedResult/`
   `KvMoveInPublishResult/KvRemoteDeletePrepareResult`；普通锁/查找结果只使用L1
   唯一`RowOutcome`，线上migration/insert只使用L1两个wire枚举，owner内部move-in
   只使用原`migration_result`，原bool callback的false若不属于进入callback前已
   分类的Busy/Missing则直接fatal。不得把这五个名字改名后保留同型中间层。
6. 收敛 helper时不得改变已经由 §3.0 固定的物理布局；保留并继续编译期断言：

   ```text
   offsetof(TwoPLPashaMetadataShared, ref_cnt) == 8
   sizeof(TwoPLPashaMetadataShared) == 16
   offsetof(TwoPLPashaSharedDataSCC, data) == 34
   sizeof(TwoPLPashaSharedDataSCC) == 40
   second_chance_bit_index == 37
   ```

   `atomic_word`的其余 bit、`ref_cnt`宽度以及原 SCC header/cacheline span不变；
   `legacy_ref_cnt_padding`和24B `legacy_policy_padding`只能由
   `finish_write/flush`按原字节范围覆盖，生产代码零次读取/写入其状态。Clock只在
   smeta latch内使用bit37，不新增 policy blob/ref对象或尾部状态。

#### 必要验证

- D1--D3 完成后，两个现有 SCC target 在同一次命令中运行；
- `scc_protocol_test`、`kv_shared_protocol_test`；
- 双 host valid/read/write/invalidate、pin/ref、move-in/out focused 路径；
- 同一move-in case覆盖首次search Missing、search后callback因Delete竞态返回Busy、
  普通migration拒绝invalid，以及remote-insert允许其唯一invalid placeholder；
  断言前三者不分配/发布且真实allocation fault才返回`FAIL_OOM`；
- raw access 计数确认一次 primitive 只 prepare/finish 一次；
- layout case同时断言上述五个常量、padding零生产访问和完整 SCC allocation
  prepare/finish字节数；并入现有两个 target，不新增 layout target；
- 静态调用图确认 legacy/KV 进入同一主体。
- `rg`确认上述五个KV-only结果类型为零，L1三种允许枚举与原
  `migration_result`之外没有第四种通用/中间row结果。

#### 完成条件

同一行状态转换只存在一个 helper 主体；KV-only 代码只剩参数和结果适配。

### D2. master/current move-out 的 `DCHECK` 使用赋值，Debug 会篡改 valid

#### 当前证据

`TwoPLPashaHelper::move_from_btree_to_partition()` 在取得 current lmeta 后写成：

```cpp
DCHECK(cur_lmeta->is_valid = true);
```

该行在当前工作树和 `master` 都存在。Debug 构建会无条件把 invalid row 改回 valid；
Release 构建则不执行该断言。它是明确的原始单行 bug，不属于需要保留的算法行为，并会
破坏本文要求的 Debug/GDB 证据可信度。

#### 修改方案

1. 严格只把赋值运算符改为比较：

   ```cpp
   DCHECK(cur_lmeta->is_valid == true);
   ```

   保持宏、所在行、lmeta lock和前后控制流不变。
2. 不把 `DCHECK` 升级为 `CHECK`，不增加 Release分支，也不把 invalid改成
   Busy/NotFound；其它正式 corruption hard-fail只能由 L1 已列出的真实协议检测点
   负责，不能借这一处原 bug扩大。
3. 不顺手修 `master` 中未链接 Sundial 等其它赋值式 DCHECK；本项只修改正式
   TwoPLPasha/PolicyClock 可达路径。
4. 将这一行作为 §3.0 “master 原 bug 的最小修正”单独列入 allowlist，不借此改变
   move-out callback 的邻接、锁序、SCC 或返回语义。

#### 必要验证

- 与 D1、D3 共用同一次阶段 D gate；
- valid=true 的正常 move-out 不改变 valid 后再进入原 SCC copy；
- 一个 Debug death/assert case把 callback current row设为 invalid，确认 DCHECK
  失败且该字段未被写回 true；不建立 death-test矩阵；
- `rg` 确认正式 TwoPLPasha 路径不存在 `CHECK/DCHECK(... = true)`。

#### 完成条件

Debug 断言只观察不修改 row；Release生成代码与 master除去该 Debug表达式副作用后
完全相同。

### D3. KV复制并扩大了 `TwoPLPashaMetadataLocal`，没有复用原 lmeta主体

#### 当前证据

master的 `TwoPLPashaMetadataLocal` 已包含 owner行所需的
latch/tid/valid/migrated/dirty-cache及两个指针。当前另在
`kv_types_layout.h` 定义 `alignas(64) PrivateMetadataLocal`，复制构造、lock/unlock
和全部状态，只把两个指针改名为 offset；它还增加命名 `reserved`并把自然布局
扩到64B。

RegionOffset替换确实必要，但复制整个 lmeta并改变大小不是必要条件。这会增加每行
owner-private SWCC占用、让 D1/O4需要为两种类型维护分支，也使“同一个 helper
主体”难以静态证明。

#### 修改方案

1. 把原 lmeta机械抽成一个 storage-parameterized结构主体，字段、构造函数、
   `pthread_spinlock_t`、lock/unlock和顺序逐行来自 master：

   ```text
   TwoPLPashaMetadataLocalStorage<MigratedRowRef, SccDataRef>
   TwoPLPashaMetadataLocal =
     Storage<char *, TwoPLPashaSharedDataSCC *>          // legacy alias
   TwoPLPashaMetadataLocalOffset =
     Storage<RegionOffset, RegionOffset>                 // KV alias
   ```

   默认/raw alias保持所有 legacy声明和调用语义；KV alias只改变两个持久引用的
   表示，resolver在使用点把它们解析为瞬时 VA。
2. 删除独立 `PrivateMetadataLocal` struct、`alignas(64)`和命名 `reserved`。不得
   为对齐再加 padding state；allocator本身仍按64B返回起始地址，但对象
   `sizeof/alignof`严格沿用原自然布局。
3. 在 x86正式构建上用 static_assert固定两个 alias的
   `offsetof(latch/tid/is_valid/is_migrated/is_data_modified_since_moved_out/
   migrated_row/scc_data)`逐项相等，`sizeof`也相等且等于未经修改的 master布局。
   若平台指针与RegionOffset宽度不同则在配置/编译期明确拒绝，不引入变长布局。
4. D1原 helper只对 LocalMeta/ref resolver参数化；legacy和KV alias进入同一
   lock/条件/字段访问函数体。E1 `RegionOffsetRowStorage`分配并构造 offset alias，
   Delete经同一 EBR退休其真实 `sizeof`。
5. O4的 access hook只按 alias选择“legacy DRAM no-op / KV owner-private SWCC
   record”，不再把一个复制类特判为平行协议。本项不改变字段语义、锁范围、
   memory order或 dirty-cache规则。

#### 必要验证

- 与 D1、D2共用阶段 D唯一 gate，不新增 target；
- 编译期布局断言加一个直接构造 case：legacy pointer与KV offset alias初值、锁、
  valid/migrated/dirty状态一致；
- 同一个 helper read/write最小 case分别实例化两个 alias，确认走同一模板主体，
  KV resolver只解析两个offset；
- 静态确认生产中不存在 `struct PrivateMetadataLocal`、命名reserved/padding状态或
  第二份 lock/unlock实现；不增加容量/压力测试。

#### 完成条件

owner lmeta只有原 storage主体；KV只以RegionOffset替代两个指针，不扩大对象、不复制
锁和状态逻辑。

## 8. 阶段 E：原 table wrapper 和 adjacency

### E1. 当前 `TableBTreeOLC` 是复制出的 KV class，不是 master 同一类的机械 offset 适配

#### 当前证据

`kv/engine/kv_migration.{h,cpp}` 新定义了完整 `TableBTreeOLC`，自行实现
search/scan/insert/remove/update/move_all 等方法；master 的原类在 `core/Table.h`。
这不满足 §3.0 “复用必须落实到同一函数/模板/类实现”。

已观察到的差异包括：

- current `move_all_into_cxl` 与 master 的
  `scanForUpdateNoContention` 路径不完全相同；
- 部分失败返回和 master 的 CHECK/回调合同不同；
- sentinel、ValueStruct offset 和 adjacent callback 由复制类自行维护。

#### 修改方案

1. 以 `core/Table.h::TableBTreeOLC` 为唯一方法主体。
2. 给该原 class template只在现有参数末尾追加两个有默认值的编译期参数：

   ```text
   BTreeTemplate = btreeolc::BPlusTree
   RowStoragePolicy =
     RawPointerRowStorage<ValueType, MetaInitFunc>
   ```

   所有 legacy实例不改调用点；`BTreeTemplate`只用于令同一原 table主体在 KV
   owner-private SWCC上实例化 C阶段已经机械 offset化的
   `btreeolc_cxl::BPlusTree`，不得定义第三种 tree或运行时分支。
   `BTreeOLCValue::row` 的类型固定为 `RowStoragePolicy::StoredRow`。
   policy允许的接口封闭为：

   ```text
   StoredRow Null()
   bool IsNull(StoredRow)
   MetaDataType *Meta(StoredRow)
   void *Data(StoredRow)
   StoredRow AllocateAndConstruct(const void *value, bool is_placeholder)
   void Update(StoredRow, const void *value)
   void Serialize(Encoder &, const void *value)
   void Deserialize(StoredRow, StringPiece)
   size_t ValueSize()/FieldSize()
   void Retire(StoredRow)
   void DestroyUnpublished(StoredRow)
   ```

   `RawPointerRowStorage`内部保留原
   `ValueStruct{atomic meta; ValueType data}`及原 `new`/直接字段访问，默认 policy
   的 `DestroyUnpublished/Retire`不得顺手改变 legacy释放时机。KV 的
   `RegionOffsetRowStorage`只持进程内 non-owning allocator/EBR/value-size binding；
   它按 H阶段最终形态分别分配 lmeta和
   `PrivateValueStruct{meta offset + fixed_value_size bytes}`，返回 owner-private
   `RegionOffset`，每次只解析瞬时 meta/data view。这样不以4KiB最大数组代替运行时
   定长 value，也不为不同 value size实例化多套 table。
   原三个insert入口仍在调用B+Tree前分配row；每个入口的B+Tree返回false时必须在
   原返回点调用`RowStoragePolicy::DestroyUnpublished`。legacy默认实现保持no-op以
   不改变master，KV实现立即释放尚未进入tree的ValueStruct/lmeta（它从未发布，
   不进EBR）。成功insert后不得再由table隐式释放。永久Delete和 I2 中“已成功进入
   tree后再摘除”的对象只由原 adjacent callback在摘除成功后调用`Retire`进入
   EBR；table generic remove不得另做第二次retire。
3. private/shared B+Tree 与 root 的区域差异继续由阶段 C 已固定的 tree/root
   binding提供。原 table保存选中的现有 `BTree`对象和一个 policy；除增加一个把
   binding/persisted-root转发给现有 CXL BTree构造器的模板构造重载外，不增加
   factory/class。每个原方法只把 `row->meta/data`、`new`、value copy和
   serialize位置机械换成上述 policy调用；条件、callback、扫描入口、锁范围和
   返回值保持 master。
4. `KvPartitionTable` 只能继承或持有
   `TableBTreeOLC<..., RegionOffsetRowStorage>` 的一个 non-owning 实例，提供固定
   table id 0、fixed key/value 和 partition/owner constructor binding；不得重写
   任何 ITable operation。
5. 切换后删除 `kv_migration.{h,cpp}` 中 current-only `TableBTreeOLC` 的所有
   search/scan/insert/remove/update/move_all 方法体，不保留运行时选择。
6. 保持 master 的 `scanForUpdate`/`scanForUpdateNoContention` 选择、callback 参数、
   `tid_check` 可替代约束和失败语义。
7. 只保留 `partition优化方案.md` §2第15条已确认的原 table 单行 bug fix：
   `remove_and_process_adjacent_tuples` 已计算 `cur_data` 后，callback 的 current-row
   实参从错误的 `cur_value` 改为 `cur_data`。不改变 prev/current/next 取得顺序、
   leaf latch范围或 callback签名，并把该行单独写入 allowlist。
8. shared table同样不得留下未验证 `offset_ptr`旁路。只给原
   `core/CXLTable.h::CXLTableBTreeOLC` 追加一个有默认值的末尾
   `SharedRowReferencePolicy=LegacyOffsetPtrRowReference`：

   - legacy默认的 `StoredRow=offset_ptr<void>`和 `.get()`行为不变；
   - KV实例的 `StoredRow=RegionOffset`，policy只持 C4 的 expected-owner
     non-owning resolver binding；
   - 原 shared table的 search/scan/insert/remove/adjacent方法只在原
     `row.get()/row=pointer`位置调用 `Resolve/Encode`，其它控制流和 leaf内
     `atomic<bool> is_valid`保持 master；
   - `is_valid`继续是原 shared-index leaf状态且位于 HWCC；copy/assignment及
     search/insert/remove中的每次真实 load/store/RMW前直接使用现有
     `HwccAtomicLoad/Store/Rmw`，不通过可变 TLS猜域、不移动原访问或增加第二个
     validity位；
   - 删除 `KVPartition::LookupSharedOffset` 等绕过原 table或在返回 VA后才验证的
     helper，不新增第二 shared-table wrapper。

#### 必要验证

- E1、E2全部完成后只运行一次阶段 E最小 table/row-allocation/adjacency gate；
- table search/insert/remove/update/move_all；
- 默认 raw policy与 KV offset policy命中同一 table方法；offset meta/data分离及
  root reattach；
- shared CXL table的 legacy offset_ptr/KV RegionOffset实例也命中同一方法，KV
  错 owner/domain row offset在 policy解析前 hard-fail；
- adjacent callback 参数和值地址，明确断言 current-row 实参是该 row 的真实
  data/lmeta view，而不是 leaf 内 `BTreeOLCValue` wrapper 地址；
- 一个重复insert让BTree返回false，KV policy对未发布ValueStruct/lmeta各直接释放
  一次且不入EBR；一个成功insert→Delete只在callback后各retire一次；
- legacy/KV 调用图命中同一类方法。
- persistent sentinel的实际 startup/锁/Scan断言延后到 H阶段最终
  ValueStruct/lmeta接线后执行；E不在过渡 row上提前创建第二种 sentinel。

#### 完成条件

生产中不再存在第二个完整 TableBTreeOLC 实现；KV 只是原类的 offset storage
实例/薄 adapter。

### E2. owner row第二段分配失败会泄漏已构造的 lmeta

#### 当前证据

`KVPartition::AllocateValue()`当前顺序是：

```text
AllocateMetadata()并placement-new lmeta
→ AllocateOwnerPrivate(ValueStruct+fixed_value_size)
→ 构造ValueStruct并写meta offset/value
```

第二次 allocation抛`std::bad_alloc`时，第一块owner-private lmeta已经计入allocator
used并带已初始化spinlock，但没有进入tree、没有返回给调用者，也没有任何清理路径。
在有限owner-private SWCC中，create/OOM重试可稳定消耗该arena。

#### 修改方案

1. 不保留`KVPartition::AllocateValue/AllocateMetadata`两段协议；由 E1唯一
   `RegionOffsetRowStorage::AllocateAndConstruct`按原“lmeta与ValueStruct分开”布局
   执行。
2. 固定顺序为：

   ```text
   allocate + construct TwoPLPashaMetadataLocalOffset
   → try allocate ValueStruct+fixed_value_size
       bad_alloc：立即用同一partition/owner allocator直接free尚未发布的lmeta，
                  原样rethrow bad_alloc
   → placement构造ValueStruct
   → 写meta RegionOffset和完整定长value
   → 返回StoredRow
   ```

   ValueStruct allocation成功后的初始化均为不抛异常的固定字段写；若后续新增了可抛
   步骤必须先停止并修订本文，不能另加通用allocation transaction。
3. lmeta/ValueStruct在返回前都不进入tree或EBR，失败直接free；成功返回后的
   duplicate/tree失败统一由 E1 `DestroyUnpublished`各直接free一次。不要新增
   scope-guard class、rollback list、tombstone或reserved row。
4. `std::bad_alloc`继续由 I/L规定的最外层primitive boundary映射NoMemory；本项不
   改结果协议或触发Clock/migration。

#### 必要验证

- 与 E1共用阶段 E唯一 gate；
- 在现有allocator fault hook上只令第二次owner-private allocation失败，断言lmeta
  used恢复到调用前、tree无key、EBR无record；
- 随后取消fault并插入同一key成功，证明未留下隐藏占用/placeholder；不做多尺寸或
  重复OOM矩阵。

#### 完成条件

owner row任一分配失败都不泄漏先前的未发布对象；成功路径仍保持原lmeta与
ValueStruct分离布局。

## 9. 阶段 F：PolicyClock

### F1. Clock tracker 算法仍回调到 `KVPartition` 重新实现

#### 当前证据

`PolicyClock.cpp` 的 `OffsetClockTracker` 表面保留原接口，但
`lock/track/untrack/move_forward_and_get_cursor/reset_cursor` 全部调用
`KVPartition::ClockTracker*`。链表搜索、head/tail/cursor、offset 解析和一致性检查
主体位于 `kv_partition.cpp`。

此外，head/tail/cursor 和 node 虽已在 owner-private SWCC，
`kv/engine/kv_partition.h::clock_lock_` 却仍是每个 wrapper 构造时在进程 DRAM
初始化、析构时销毁的 `pthread_spinlock_t`。master 的 lock 是 tracker control 的
成员；当前放置既拆散了一个控制对象，也使重建 wrapper 时获得另一把锁。

这与 §3.7 要求相反：offset adapter 应保留原 `PolicyClock::ClockTracker` 算法所有权，
KVPartition 只提供存储/解析，不应成为第二个 Clock 实现。

#### 修改方案

1. 使用阶段 C 已固定在 `kv_types_layout.h` 的
   `OwnerPrivateClockTrackerControl`，它只含：

   ```text
   pthread_spinlock_t lock
   RegionOffset head
   RegionOffset tail
   RegionOffset cursor
   ```

   每个真实 partition 的 `OwnerPrivateArenaHeader` 已内嵌恰好一个该 control；
   head/tail/cursor 不再作为散落字段保留。本阶段不得再次修改 layout/version。
2. 沿用阶段 C 的唯一初始化边界：owner reset时对 lock 恰好一次
   `pthread_spin_init(..., PTHREAD_PROCESS_PRIVATE)`；非 owning wrapper attach不重复
   init，wrapper析构不 destroy。不能复用 allocator lock，也不能在进程 DRAM保留
   镜像锁。
3. 把 master `ClockTracker::track/untrack/move_forward_and_get_cursor/reset_cursor`
   的条件、指针推进和 CHECK 顺序逐行保留在 `PolicyClock` 的同一 tracker 主体；
   唯一机械差异是：

   - node/head/tail/cursor pointer → owner-private `RegionOffset`；
   - `new/delete` → owner-private allocate/free；
   - table/key/row 持久指针 → 已规定的 fixed key 与 row/smeta offset resolver；
   - control/node 的真实 read/write/atomic → 对应 Private `mem_access` wrapper。

4. partition adapter 只返回该 control、allocate/free node 和 resolve offset；
   删除 `clock_lock_`、`clock_lock_inited_` 及全部 `KVPartition::ClockTracker*` 算法。
5. `PolicyClock::move_row_in`继续保持master的tracker lock覆盖整个callback与track，
   但有限owner-private SWCC中的node分配固定机械前移到调用原
   `move_from_partition_to_shared_region`之前：

   ```text
   tracker lock
   → allocate一个尚未发布的ClockTrackerNode
      失败：不调用move-in callback，解锁并返回FAIL_OOM
   → 调原move-in callback
      SUCCESS：用已返回的smeta/row填充该node并按原track
      FAIL_ALREADY_IN_CXL或FAIL_OOM：直接free尚未发布node，不track
   → tracker unlock
   ```

   node字段初始化、link和track均为不抛的固定写；`SUCCESS`之后到track之间不得再有
   allocation。这样只改变可失败资源的准备时点，不改变成功时的callback→track、
   tracker锁范围、victim或second-chance算法，也不需要撤销已经发布的shared row。
   不增加reserve pool、备用node、allocation transaction或rollback状态机。
6. `move_row_out` 成功和 migrated Delete 的 `need_move_out=true` 路径都必须在原
   tracker lock 内按 key 线性找到 node、执行原 untrack，并在 untrack 后释放恰好
   一个 owner-private node；`need_move_out=false` 不 untrack/free。不得保留
   master 的错误 `CHECK(need_move_out == false)`。
7. 不增加 key→node map、reverse link、count、第二水位或进程本地 tracker 控制。
   保持 master tracker lock 覆盖 move-in callback、victim callback 和 Delete
   callback 的范围。

#### 必要验证

- F1--F3 全部修改完成后合并到一次阶段 F Clock gate；本问题不单独运行 target；
- track/untrack/head/tail/cursor/reset；
- wrapper 销毁并 reattach 后仍解析同一 control/offset，且没有重复 init/destroy；
- 每次 move-in 分配一个 node，move-out/delete 分别释放恰好一个；
- 同一fault hook只令move-in前的node allocation失败一次，断言callback零次、行及
  policy counter不变；callback返回非SUCCESS时该尚未发布node直接释放且不入EBR，
  不为node/smeta/payload建立组合OOM矩阵；
- 两个同 VM worker 的确定性交错证明共享同一 tracker lock；该地址落在所属
  partition 的 owner-private SWCC，且不与 allocator lock 重叠；
- lock/head/tail/cursor/node 的访问域计数均为 Private，不出现 HWCC/SCC；
- 调用图确认 KVPartition 不再拥有 Clock 链表算法。

#### 完成条件

Clock candidate/victim/tracker 控制流只有 PolicyClock 一份，KVPartition 仅提供
owner-private storage；tracker control、lock 和 nodes 全部位于同一 owner-private
arena。

### F2. Clock budget 仍由物理 dynamic HWCC 使用量镜像驱动

#### 当前证据

- `KvMigrationRuntime::SyncHwCcUsage()` 把
  `partition.hwcc_used_bytes()` 写入进程本地 `CXLMemory::TOTAL_HW_CC_USAGE`。
- `PolicyClock::move_row_out()` 用该值与 `hw_cc_budget` 比较。
- 该值来自物理 dynamic HWCC allocator 会计，而 §3.7 要求的是按原增减时机维护的
  per-owner policy counter。

这样把“物理容量/OOM 报告”和“Clock 策略预算”合并为一个动态镜像，违背两者分开
报告和独立语义的要求。

#### 修改方案

1. 使用 C1 `OwnerAllocatorControl` 中唯一的
   `TOTAL_HW_CC_USAGE` 原子作为该进程 owner的 policy counter；不再保留
   `CXLMemory::size_total_hw_cc_usage` 这个进程 DRAM镜像。
2. 直接复用现有 `CXLMemory::bind_dual_region_allocator(regions,owner)`：
   `get_stats(TOTAL_HW_CC_USAGE)`、原 allocation/free wrapper对 TOTAL的增减通过
   `DualRegionAllocator`访问 bound owner control，并在真实 load/RMW前记录
   `PrivateAtomicLoad/Rmw`。未绑定、错 owner或减计数下溢调用 C4/L1唯一
   `ProtocolFatal`；不提供 DRAM fallback。
3. 增减类别和字节数严格保留 master wrapper时机：INDEX按其 allocation bytes，
   METADATA在正式 Clock模式按完整16B smeta，SCC开启时DATA不进入 HWCC policy
   counter；move-out/Delete仍在 master对应 free wrapper时减。不得改用 allocator
   size class、physical used或tracker count推算。
4. 删除 `SyncHwCcUsage`、`set_total_hw_cc_usage` 及 handler/MoveOut 对物理 used
   的镜像。其它 legacy-only分类统计若仍为未链接参考代码所需可留进程 DRAM，但
   不能参与 production Clock、Memory或成为第二个 TOTAL权威。
5. 物理 allocator used/peak继续只用于 OOM和显式 Memory/report；现有
   `owner_migration_dynamic_budget_bytes`继续报告 policy limit。为避免增加统计
   字段，policy current usage只由 Clock gate通过现有 `get_stats(TOTAL...)`读取，
   不加入 `MemoryStats`、不读取其他 owner control。
6. 不修改物理 HWCC 总容量；如 focused 测试证明过小 fixture无法容纳必要结构，
   可增大测试 HWCC，而不是添加复杂驱逐兜底。

#### 必要验证

- 与 F1、F3 共用同一次 Clock gate；
- move-in/out/delete 前后分别断言 policy counter 和物理 counter；
- 修改一个 counter 不应改变另一个；
- Clock gate 只读 policy counter；
- allocation/free各只在 master原 wrapper时机触发一次 policy RMW，错 owner和
  underflow复用 L阶段同一个 fatal case，不另建矩阵；
- 小容量只验证必要阈值，不做 allocator 耗尽矩阵。

#### 完成条件

Clock policy budget 与 physical capacity/usage 明确分离，报告同时给出两者。

### F3. 多条非原始路径额外设置 second chance

#### 当前证据

`KVPartition::NoteSharedAccess()` 在 owner migrated Put/Get/CAS/Increment 和 remote
Scan 等路径调用 `migration_manager->access_row()`。master 的优化命中路径由
`get_migrated_row(ref=true)` 设置 second chance；migration-response continuation
使用 `ref=false`，不应给 fresh row 额外 second chance。

额外调用会使行比原 Clock 多存活一轮，影响迁移行为和性能公平性。

#### 修改方案

1. 删除 `KVPartition::NoteSharedAccess` 和所有外层补调用。
2. 只保留原 `get_migrated_row(..., ref=true)` 中的 `access_row()`。
3. owner local/migrated 操作按 master 路径处理，不因 facade 类型额外加机会。
4. migration response 和 range move-in continuation 不设置 second chance。

#### 必要验证

- 与 F1、F2 共用同一次 Clock gate，不为每种访问运行独立进程；
- fresh move-in bit=0；
- 原 shared-index hit 恰好置 1；
- owner local 操作、owner Scan、response continuation 不额外置位；
- victim 第一次清 1、下一次才 move-out。

#### 完成条件

second chance 状态严格是
`0 → 原 access_row 置1 → victim 清0 → 下次 victim move-out`。

## 10. 阶段 G：dispatcher、Message 和 wire

### G1. 新增 `CxlIncomingDispatcher`，而不是复用原 `IncomingDispatcher` 的同一 CXL 主体

#### 当前证据

`core/CxlIncomingDispatcher.h` 是 current-only 类，仅 KVEngine 使用。它复制
`BufferedReader → drain → callback` 骨架，原 `core/Dispatcher.h::IncomingDispatcher`
仍独立存在。

“直接 include 原 Dispatcher 会带入 cxlalloc/Transaction 依赖”只能证明需要拆依赖，
不能自动证明复制第二个 class 符合复用要求。

#### 修改方案

1. 删除 `class CxlIncomingDispatcher`。从
   `core/Dispatcher.h::IncomingDispatcher` 的现有 CXL 分支机械提取唯一的
   dependency-light 函数模板
   `RunCxlIncomingLoop(MPSCRingBuffer &, coord_id, worker_count, stop, enqueue)` 到
   `core/CxlIncomingDispatcher.h`；该文件不再定义 class 或持久状态。
2. `RunCxlIncomingLoop` 的固定主体是：

   ```text
   构造一个 BufferedReader(ring)
   while (!stop.acquire):
     message = reader.next_message()  // 仅此调用的malformed exception补node/ring上下文后ProtocolFatal
     message == null → std::this_thread::yield(); continue
     依赖 BufferedReader 完成 framing 校验；校验 dest_node_id==coord_id
     worker_id >= worker_count → malformed fatal
     enqueue(worker_id, message.release())  // 原 queue 满时 yield/backpressure
   ```

   一次循环只取一个原 `Message`，不保留 current-only `Drain(max_messages=64)` 批
   callback。`unique_ptr` 只在成功交给目标队列时 release；worker pop 后立即恢复
   `unique_ptr<Message>`。`next_message()`的deadbeef/length/truncation异常只在这
   个CXL receive边界捕获并委托C4唯一`ProtocolFatal`，补充coord/ring上下文；不
   捕获后继续、不丢帧、不转Busy。enqueue/handler中的异常由各自L1边界处理，不能
   用一个宽泛demuxer catch吞掉。
3. 原 `IncomingDispatcher::start()` 的 `use_cxl_transport` 分支调用该函数；
   socket branch 和 internal coordinator transfer 保持 master。KV demuxer也直接调用
   同一函数，只传 `group_id=0/io_thread_num=1` 已唯一决定的 worker SPSC enqueue；
   两者不得再各自写 framing/worker-id/backpressure 循环。
4. KV 只提供固定 ring、coord id、foreground worker count 和 worker SPSC binding。
   不新增 dispatcher object、batch state、pending map、stealing 或第二次 worker-id
   路由。
5. 不恢复 socket grouping、Transaction Executor、OutgoingDispatcher 或第二 service
   thread。
6. 保持 Message ownership、worker-id routing、原 queue backpressure 和 framing
   fatal。
7. 保留 `partition优化方案.md` §2第15条对 `BufferedReader` 的唯一必要原 bug fix：
   CXL constructor 允许
   `socket=null`，因此 `next_message/fetch_message` 的 DCHECK 都固定为
   `use_cxl_transport ? cxl_ringbuffer != nullptr : socket != nullptr`。不得借此
   修改 reader buffer、recv循环、Message所有权或 socket branch。
8. 同一G阶段把`MPSCRingBuffer`的reservation/count/head/tail/ready、partial-dequeue
   拒绝、send/recv循环和memory order逐行恢复到master；只保留C4的
   `RegionOffset`/transport resolver、真实`mem_access`和对损坏metadata/超长frame
   的最小fatal诊断。当前`DelayActiveScopeNow`调用在G暂时只视为待O删除的计费点，
   不允许它伴随新的CAS、publication状态、恢复扫描或ready顺序变化。G gate先证明
   无延迟逻辑时的原队列功能；O1/O6随后只移除提前结算、补几何访问记录，不得首次
   改变ring状态机。
9. 原`Message`/`MessagePiece`解析器和legacy多piece dispatch能力保持原主体，但KV
   formal receive在调用原handler前验证该Message恰好包含一个完整piece，且
   `Message`总长度恰好等于prefix加该piece长度；零piece、第二piece或尾随字节都以
   source/dest/worker上下文`ProtocolFatal`。发送端仍由现有per-destination
   outbound Message在每次单操作flush后clear，不能因原容器支持append就批包。

#### 必要验证

- G1--G2 完成后合并到一次阶段 G transport/message gate；
- worker id 投递；
- Message ownership 恰好释放一次；
- SPSC 满时按原 backpressure；
- MPSC full/empty、一个完整frame enqueue→dequeue及slot复用按master状态/memory
  order完成一次；不做长压力；
- malformed/oversized frame fatal；
- 零piece、两个合法piece拼成一个Message及尾随字节各并入同一个malformed
  table case，均fatal；正常单piece只dispatch一次；
- socket 与 CXL 两种 constructor 各走一次最短 reader case，确认 DCHECK 检查对应
  transport对象；
- 静态确认生产不再链接第二个 dispatcher class。

#### 完成条件

legacy/KV 共享同一个 CXL receive 主体，KV 只保留依赖剥离所需 binding。

### G2. `TwoPLPashaMessagePrimitive` 目前主要由 KV 使用，legacy factory/handler 未共用

#### 当前证据

current-only `TwoPLPashaMessagePrimitive` 定义 request/response append/decode，但
搜索调用者主要在 `KVEngine`；legacy factory/handler 仍保留自己的编码/解码主体。
这仍是两套看似相同的 wire 实现。

`ServeTransportRequest()` 还直接选择 owner migration/insert/delete/scan/Clock 动作，
而现有 allowlist 却声称 handler 只做 framing。

此外，`KVEngine`又定义 `RpcKind` 和通用 `RpcResult`。后者把 migration与remote
insert的不同结果合同压成一套，并通过 `EncodeResult(StatusCode)`转换；这既偏离
方案规定的两个小枚举，也为 L1 的 default-Busy误分类提供了入口。

#### 修改方案

1. 删除 `TwoPLPashaMessagePrimitive` class，不新建替代 class/namespace协议层。
   只在原 `TwoPLPashaMessageFactory` 中给对应的四类 request factory增加同名
   raw-field overload（显式 transaction sequence/key_offset/定长 bytes）；原
   Transaction版本只取出原字段后委托该 overload，KV也直接调用它。每类
   request的 header、字段顺序和 size表达式因此只有这一份。
2. 只在原 `TwoPLPashaMessageHandler` 中给同四类 decode/row action/response
   append增加 transaction-free overload；原 handler完成 decode后调用它并更新
   Transaction set，KV handler调用同一 overload并更新一个 OperationContext。
   row action只能是 master handler原有的
   `move_row_in/remote insert/delete/range move-in/OnDemand Clock`顺序，不把控制流
   留在 `KVEngine::ServeTransportRequest`。
3. 删除 `RpcKind`和通用 `RpcResult/EncodeResult/ResultStatus`。KV发起端直接以
   `TwoPLPashaMessage` 的四个原 request type选择 factory/期望 response；线上结果
   严格使用 D/L固定的 migration枚举、remote-insert枚举、Delete空ack和Scan bool，
   两个小枚举不得合并或经过通用 `Status`转换。
   现有每 worker单一 `OperationContext`只保留
   `expected_response_type/expected_source_owner/partition_id/done/result`：
   `Forward`从既有 route写入预期 owner，response decode前同时核对 Message
   source、piece type/table/partition和适用的key_offset。错误 source直接委托
   C4/L1 fatal。原 request中的 transaction id仍由现有单调 sequence填充以保持
   framing，但 response原本不回显它；不得为此增加token字段、pending map或
   correlation表。
4. 保留原 `MessagePiece` header 和固定 table id 0；不建立通用 Status 序列化。
5. 只保留方案允许的最小结果字段：

   - migration 小枚举；
   - remote insert 小枚举；
   - remote delete 空 ack；
   - scan 原 `bool success + key_offset`。

6. 同时逐行落实 `partition优化方案.md` §2第15条已确认、且只允许按原字段修正的
   三个 Message bug：

   - `remote_insert_response_handler` 的类型断言只从
     `REMOTE_DELETE_REQUEST` 改为 `REMOTE_INSERT_RESPONSE`；
   - scan migration request 的 exact length 固定为
     `header + 2*key_size + sizeof(limit) + sizeof(transaction_id) +
     sizeof(key_offset)`；
   - owner 完成原 range move-in 循环后、编码 response 前只补
     `success=true`。

   不借这些修正改变字段顺序、response 形态或 handler 控制流。
7. `ServeTransportRequest` 只做 type/table/partition/owner 校验、调用原 Handler
   overload、
   flush→OnDemand Clock→send；不自行实现 row lock/SCC/adjacency。
8. 保留当前已正确实现、且 §3.14 明确要求的 startup尺寸门禁：用固定 key/value
   和允许的四类 request/response逐个按原 header/payload公式计算
   `maximum_message_bytes`，要求每个 ring
   `entry_data_size >= maximum_message_bytes`且
   `BufferedReader::BUFFER_SIZE >= maximum_message_bytes`。计算必须调用共用 framing
   的 size helper或同一常量表达式，不能维护第二张手写消息表；恰好相等允许，
   少1字节在启动时配置 fatal，不到 enqueue/dequeue后再处理，也不增加分片。

#### 必要验证

- 与 G1 共用同一次 gate；正常 framing 和最小 malformed case 在同一进程覆盖；
- legacy factory 与 KV primitive 的 byte-for-byte framing；
- 每种允许消息的 exact length、table 0、partition、key_offset；
- 当前 pending response来自预期owner时完成一次；错误 source即使 type/partition
  相同也fatal；
- scan request 逐字段 decode 完后剩余字节为 0，response 的 success 在完成 move-in
  后为 true；remote insert response type DCHECK 命中正确枚举；
- unknown enum/type 和 malformed length fatal；
- 最大 frame恰好容纳可启动、ring或 reader少1字节拒绝；并入同一个 framing case；
- response flush→Clock→send 顺序；
- `rg`确认没有 `TwoPLPashaMessagePrimitive/RpcKind/RpcResult/EncodeResult`，四类
  request的 encode/decode/row action各只有原 Factory/Handler中的一个主体；
- 不构造大规模网络故障矩阵。

#### 完成条件

wire/framing/row handler 只有一个原式主体，KV 只替换 Transaction set completion。

## 11. 阶段 H：点操作、锁和 shared miss

### H1. stable shared miss 为分类又查一次 shared B+Tree

#### 当前证据

`KVPartition::TryPinShared()`：

1. 调 `get_migrated_row()` 做一次 shared tree lookup；
2. 返回 null 后又调用 `LookupSharedOffset()` 做第二次 lookup；
3. 用第二次结果区分 Missing/Retry。

这违反 `model_cxl_search_overhead=false` 和 §3.3 的单 lookup
Done/Missing/Busy 要求，也可能在两次查找之间观察到不同状态。

#### 修改方案

1. 扩展原 `get_migrated_row` primitive 的结果为 Done/Missing/Busy。
2. 在同一次 tree lookup/latch/smeta 检查中完成分类。
3. 成功命中仍按原 ref/Clock 语义；stable miss 不再二次探测。
4. 不加 routing cache、negative cache 或第二索引。

#### 必要验证

- 纳入阶段 H gate，与 H2 的点操作路径合并运行一次；
- 计数断言 hit、stable miss、contention 各只查一次 shared tree；
- hit 仅原路径调用一次 `access_row`；
- Delete/move-out 竞争不能被误判 stable miss。

#### 完成条件

每次 CXL-first 点探测只进行一次 shared index lookup。

### H2. `KVPartition` 仍拥有大量 point/migration 状态机，而非薄 row/table adapter

#### 当前证据

`KVPartition` 仍直接实现或组合：

- Put/Get/CAS/Increment private/shared；
- owner insert placeholder；
- move-in/move-out；
- Delete；
- Clock tracker；
- local/shared Scan；
- allocator/EBR retirement 顺序。

即使其中调用了 helper，整体锁取得、locator recheck、状态分支和失败清理仍由
current-only 大函数控制。这与 §3.3、§3.5、§3.6、§3.9 和 §3.12 的“最多保留薄
adapter”不符。

#### 修改方案

1. 按阶段 H→K 逐条把生产调用切到原 helper/table/policy primitive。
2. `KVPartition` 最终只保留：

   - partition/owner binding；
   - RegionOffset allocate/free/resolve；
   - fixed key/value row view；
   - 原 callback 所需的最小 adapter。

3. 每切换一条路径并完成 focused 测试后，删除对应 current-only 状态体。
4. 不一次性重写 KVPartition；每个补丁只处理一个原函数对应关系。
5. 本阶段接入 E1 的最终 `RegionOffsetRowStorage`与原
   `TwoPLPashaMetadataLocal`后，才由每个 owner按 C1启动顺序通过原 table insert
   创建唯一 persistent sentinel。同步完成 B1延后的空 partition、最后真实 row、
   next-key lock、物理会计和 Scan过滤断言；不得在 E之前的过渡 row上保留另一枚
   sentinel，也不得把 sentinel创建放到 VM0替其他 owner执行。

#### 必要验证

- 每条切换路径先用 Debug assert/最小直接 case 确认；H1--H3 全部切换完成后只运行
  一次阶段 H gate，不为每个 helper 重跑整个 `kv_engine_test`；
- `rg`/调用图确认旧主体已无生产调用；
- 同一 H gate覆盖每个实际 partition恰好一枚最终原式 sentinel，逻辑结果排除而
  owner-private物理 used包含它；不另跑 B/E target；
- 阶段 N 再统一删除死声明，不在本阶段做无关清理。

#### 完成条件

KVPartition 不再拥有独立行协议，只保存 offset/固定 KV/table binding。

### H3. facade 与 engine 为同一个 foreground worker 维护两套 `RuntimeStats`

#### 当前证据

- `KVStore` 自己持有 `vector<WorkerRuntime>`，并用
  `TlsRuntimeOwner/TlsRuntimeStats` 记录 logical/commit/private 等 facade counter；
- `KVEngine` 又持有另一份 `vector<WorkerRuntime>`，记录 shared/migration/network/
  Scan counter 和同 worker 的 `max_tid`；
- `KVStore::Runtime()` 先聚合 facade slots，再手工挑选 engine slots 的部分字段
  拼接，字段新增时很容易漏加或重复；
- `KVEngine::Scan()`又用`ScanTlsDiag/ScanTlsFlushGuard`临时累计两个Scan
  counter，返回时才加到engine worker slot。调用线程已经由唯一worker id绑定，
  这份TLS计数副本没有并发或原实现需求，增加了第三个短期统计位置和异常退出时的
  清理状态；
- 同一个操作因此写两个 cacheline slot，不符合 §3.0
  “每 foreground worker 一组 cacheline 隔离 RuntimeStats”的放置和热路径要求。

#### 修改方案

1. `KVEngine::WorkerRuntime` 是唯一 worker runtime slot，继续
   `alignas(64)`，只含完整 `RuntimeStats`、该 worker 的 `max_tid`，以及 C6 已
   机械迁入的原 `CXL_EBR::EBRMetaLocal`。三者都属于同一个 foreground worker 的
   进程 DRAM生命周期；不得再为 EBR local另建 vector/TLS owning对象或统计副本。
2. `WorkerMailbox` 继续拥有原式 SPSC inbox/outbound、一个 `OperationContext` 和
   sequence；不为合并统计搬动消息所有权，也不把 mailbox 放进共享内存。
3. `KVStore` 删除自己的 `WorkerRuntime` vector、`TlsRuntimeOwner` 和
   `TlsRuntimeStats`。`BindWorker/ReleaseWorker` 只委托 engine 建立/清除现有唯一
   TLS worker id；同时按 C6把 CXL_EBR 的TLS non-owning指针绑定/清除到该 slot
   中已初始化的 `EBRMetaLocal`，不得在这里重新初始化它。
4. engine 增加一个仅供 facade 使用的进程内引用 accessor：
   `CurrentWorkerRuntime()`；它先执行现有 `RequireBoundWorker()`，再返回当前唯一
   slot。`KVStore::ThreadRuntime()` 只调用该 accessor，不保存第二个 TLS 指针。
5. 把现有纯加法 `AddRuntimeStats` 从 `kv_store.cpp` 移到
   `kv_engine.cpp` anonymous namespace，字段列表不变；
   `KVEngine::EngineRuntime()` 对每个 quiesced worker slot调用它聚合**全部字段**，
   随后只把 demuxer 独占的
   `network_rx_bytes` 加入一次；`KVStore::Runtime()` 直接返回该快照，不再手工列出
   shared 字段。
6. 删除`ScanTlsDiag/ScanTlsFlushGuard`；`KVEngine::Scan()`开头取得一次上述唯一
   `RuntimeStats&`，在现有probe/RPC成功发起位置直接递增
   `scan_partition_probes/scan_migrate_rpcs`。不增加scope guard、atomic或另一个
   Scan统计对象，计数点和含义不变。
7. 不把 counter 改成 atomic，不增加全局统计锁，不改变任何 counter 的定义或
   增减时机；本项只消除重复存储和不完整聚合。

#### 必要验证

- 与 H1、H2 共用一次阶段 H gate；
- 单 worker 执行一个 private 操作和一个 shared/RPC 操作，断言 logical、commit、
  private/shared、network 计数均来自同一 slot且只增一次；
- 两个绑定 worker 各执行一个短操作，quiesce 后聚合等于两 slot 逐字段之和；
- 未 Bind 的 facade 操作仍立即报绑定错误，Release 后不能继续写旧 slot；
- 静态确认生产中只剩一个 `vector<WorkerRuntime>`、一个 worker-id TLS，且
  `EBRMetaLocal`只有该 slot storage加 legacy默认TLS两种合同规定的storage；
  KV Bind/Release不重置它；同时不存在`ScanTlsDiag/ScanTlsFlushGuard`。

#### 完成条件

每个 foreground worker 只有一个 cacheline-isolated runtime权威槽；
facade、engine、TID、EBR local 与 mailbox 都由同一个 worker id 绑定，聚合不再
手工挑字段。

## 12. 阶段 I：remote PUT、CAS 和 Increment

### I1. owner CAS/Increment 在 helper 返回后重新读 locator，存在同型迁移竞态

#### 当前证据

`KVPartition::CompareExchangePrivate()` 和 `IncrementPrivate()` 都：

1. 调一个只返回 `write_locked` 和 `migrated` 的 helper；
2. helper 返回后再依据普通 `metadata->is_migrated` 或旧 `migrated` 快照决定 private
   分支；
3. migrated 分支重新锁 lmeta、检查 locator，再单独取得 shared write lock。

可能交错：

- helper 观察 migrated，随后 move-out，裸读变成 private，但本操作没有 private
  write lock；
- helper 取得 private write lock，随后 move-in 转移状态，裸读变成 migrated，
  本操作再取 shared lock并可能泄漏/重复持锁。

Put/Delete 已使用更完整的 owner write-lock continuation；CAS 和 Increment 不应各自
维护另一套 locator recheck。

#### 修改方案

1. 复用原完整 `take_write_lock_and_read(LocalMetadata, ResolveShared, ...)` 主体，
   在 lmeta→smeta 顺序内确定并取得唯一权威 write lock；返回固定 continuation：
   `PrivateWriteLock{tid}`、`SharedWriteLock{smeta,tid}`、`Missing` 或 `Busy`。
2. CAS 和 Increment 都只依据该 continuation 操作和释放，不再读取
   `metadata->is_migrated`、不重新解析 locator、不再调用第二次
   `remote_take_write_lock_and_read`。
3. CAS CompareFailed、Increment decode/overflow 失败也按同一个 continuation
   释放；shared continuation 的 lmeta 生命周期由原 helper 明确持有到 release，
   不由调用者猜测。
4. helper 保持 master 的 lmeta→smeta 锁序和 SCC prepare/read；KV 只适配
   RegionOffset resolver 与固定 value bytes。
5. 不增加 CAS/Increment 专用锁、generation、migration pin 或循环内局部重试。

#### 必要验证

- 阶段 I 只运行这个确定性交错，不附带其他 point/Scan workload；
- 在 helper 返回/迁移边界加 Debug-only barrier；
- 同一个短 case覆盖 CAS、Increment 分别与 move-in、move-out 的四个 barrier
  交错；每个交错只执行一次确定性调度，不做概率长压；
- CAS CompareFailed 和 Increment overflow各带一个 release 断言，不再另启进程；
- 断言每次只取得/释放一个权威 write lock，无双锁、泄漏和错误写 private；
- 若 stall，用 GDB 记录 lmeta/smeta/Clock 锁，而不是猜测修复。

#### 完成条件

CAS、Increment 与 Put 使用同一个原 owner write-lock+locator primitive。

### I2. remote insert 失败回滚会把 unpublished placeholder 暂时发布为 valid

#### 当前证据

`KVPartition::InsertRemotePlaceholder()` 的
`rollback_unpublished_placeholder` 当前执行：

```text
lock lmeta
metadata->is_valid = true
unlock lmeta
DeletePrivate(key)
```

普通 owner foreground 可以在 `valid=true` 与 Delete 之间查到该行并返回 placeholder
value；“尚未给 requester 回 ack”不能阻止 owner 本地观察。这既违反 never-published
placeholder 合同，也借普通 Delete/Clock 路径掩盖了一次 insert rollback。

#### 修改方案

1. owner table insert首先穷尽返回：成功创建本操作唯一 invalid placeholder、
   `AlreadyExists`或`Busy`；后两者在未创建任何行时直接上浮，不进入rollback。
   成功创建后，F1先在同一原tracker锁内准备一枚尚未发布的Clock node，再调用
   原`move_row_in(inc_ref=true)`；node分配失败时完全不调用row move-in，直接执行
   下述placeholder rollback。move-in唯一正常结果必须是原
   `star::migration_result::SUCCESS`并持有一个requester ref，随后编码
   `Inserted` ack。若返回`FAIL_OOM`，
   必须由共同 move-in primitive先在原失败出口撤销并直接释放本次尚未发布的
   payload/smeta，F1同时直接释放尚未track的Clock node，恢复lmeta为invalid
   local且无新cached offset，并证明
   locator/shared index/Clock/ref均不存在，才执行下述rollback；不得把这份清理
   留给KVPartition另写状态机。对这份刚创建且唯一的invalid placeholder，若
   `move_row_in(inc_ref=true)` 返回 `FAIL_ALREADY_IN_CXL` 且带回 requester pin，
   按 master 忽略 already-shared 结果并编码 `Inserted`（并发
   `DATA_MIGRATION_REQUEST`/Scan range move-in 可先迁入同一 invalid 行）；仅当
   already-shared 却无 pin、或留下无法解释的部分发布状态时才是协议不变量破坏并
   调用 C4/L1 fatal，绝不能尝试回滚已可见对象。
2. rollback 直接调用原 table
   `remove_and_process_adjacent_tuples(key, callback)` 摘除这一个 invalid private
   row，不调用 `DeletePrivate`、PolicyClock 或 migration manager。
3. callback 在原 adjacent leaf 锁范围内：

   - 取得 current lmeta，hard-check `is_valid==false`、`is_migrated==false` 且 key/row
     offset 与本次 placeholder 一致；
   - 调原 `set_adjacent_migrated_rows(prev,next,resolver)` 恢复 insert 前被清除的
     两侧 adjacency；
   - 不把 current valid 置 1，不创建 shared row/ref/Clock node。

4. tree 摘除成功后，把 placeholder `ValueStruct/lmeta` 作为
   owner-private 对象进入现有 EBR retire queue；不得直接 free，因为并发 owner
   lookup 可能已经取得 transient row pointer。grace period 后由原 allocator回收。
5. table remove 失败、placeholder 已 valid/migrated、offset 不匹配或 adjacency
   恢复失败都是不变量破坏并 hard-fail，不能返回 Busy。OOM 本身通过 L1 的显式
   `NoMemory` 结果传递，不用异常文本分类。

#### 必要验证

- 与 I1 合并在同一次阶段 I gate；
- Debug-only barrier 固定停在 placeholder 插入后、rollback 摘除前；并发 owner Get
  只能得到 Busy/Missing，绝不能返回 placeholder value；
- rollback 后 private/shared lookup 均 Missing，prev/next adjacency 恰好恢复，
  Clock tracker/ref/policy counter均无新增，本次move-in分配过的payload/smeta/node
  各释放一次且没有残留cached offset；
- 记录该 row 只 retire 一次，并在一个 EBR grace period 后可回收；
- I阶段静态确认`FAIL_ALREADY_IN_CXL`带 pin 时走 Inserted、无 pin 时 fatal且无
  rollback分支；L阶段可用一个Debug fault覆盖无 pin 非法结果，不另加 I测试轮次；
- 不增加 allocator OOM 矩阵或长时间并发压测。

#### 完成条件

remote insert 的失败路径从未把 placeholder 发布为用户可见，且不借普通 Delete
执行 insert rollback。

## 13. 阶段 J：Delete

### J1. Delete callback 已退休 smeta 后，PolicyClock 又访问并清 write lock

#### 当前证据

- `KVPartition` Delete callback 摘除索引并把 smeta/payload 加入 EBR retire queue。
- `PolicyClock::delete_specific_row_and_move_out()` 返回 callback 后又根据
  `migration_policy_meta`：

  ```text
  smeta->lock()
  smeta->clear_write_locked()
  smeta->unlock()
  ```

- 注释以“EBR 暂时保留对象”为理由允许事后访问。

但 §3.6 明确规定：successful callback 消费原 write lock/ref；owner 清理并退休后，
requester/PolicyClock 不得再访问旧 row。EBR 防止立即复用不等于允许协议在 retirement
之后继续操作对象。

#### 修改方案

1. Delete callback 在摘除/retire 前完成最后一次 valid/write/ref/adjacency 状态转换。
2. callback 成功返回后，PolicyClock 只做 tracker untrack/free 和返回 ack，不再解锁
   smeta。
3. local Delete、remote Delete 分别保留方案规定的唯一 invalid 发布。
4. 失败/Busy 路径由仍存活的 row 按原 helper 释放；成功路径零次事后访问。

#### 必要验证

- J1、J2完成后只运行一个 Delete 生命周期 gate；
- local migrated Delete、remote Delete；
- 记录旧 smeta offset 和 post-callback lock/unlock/ref 调用计数，必须为 0；
- 删除后重新 Put；
- Delete 与 Get/Put/move-out/Scan 的小型并发 focused；
- 不做长时间 allocator reuse 压测。

#### 完成条件

Delete callback 返回成功即代表旧 row 生命周期结束；之后没有任何 smeta/payload
访问。

### J2. Delete 在可能失败前已清邻接，Busy/RemoveFailed 只恢复 current valid

#### 当前证据

当前 `DeletePrivateForMigrationManager()` 的 offset callback顺序是：

```text
clear_adjacent_migrated_rows(prev,next)
→ current valid=false
→ delete_migrated_shared_row(...)
→ 若 Busy/RemoveFailed：只把 current valid=true后返回false
```

因此 shared删除失败会留下 prev/next adjacency已被清除、current却仍有效的半完成
状态。随后 facade把 Busy持续重试也不能自动恢复这些 bit。

master没有这套 rollback：调用方已持有 current write lock，PolicyClock又持有对应
tracker lock；进入 `remove_and_process_adjacent_tuples` 后，callback按原顺序完成
邻接、invalid、shared index移除和retire，内部 remove失败仅由 DCHECK揭示原不变量。
RegionOffset适配不能把它改成一个可部分失败的状态机。

#### 修改方案

1. local/remote Delete先按 §3.6和 D1共同 primitive取得唯一 current write
   lock/ref；无法取得、row Missing或move状态尚不能稳定时，在调用 table
   `remove_and_process_adjacent_tuples` **之前**返回 Busy/Missing并按原 release
   释放，任何 adjacency/valid/index均不修改。
2. 一旦在 PolicyClock tracker lock内进入原 adjacent callback，就直接复用 D1的
   `delete_and_update_next_key_info`唯一主体，严格保持 master顺序：

   ```text
   更新并释放 prev/next adjacency
   → current local/shared invalid与locator转换
   → shared index remove
   → 记录need_untrack/retire对象
   → callback成功
   ```

   callback内部没有正常 Busy/RemoveFailed返回。shared locator、writer/ref状态或
   tree remove与预锁前提不一致时调用 C4/L1 `ProtocolFatal`，不得恢复一半字段后
   重试。
3. 删除 current-only `DeleteFailure`、`failure_detail`和
   `delete_migrated_shared_row`结果回滚分支；若该 helper还有其它调用，收敛为原
   callback内的无失败 primitive，否则删除，不保留平行 Delete主体。
4. callback成功后才按原对象域分别入同一 owner EBR queue：
   `ValueStruct`、lmeta、存在的HWCC smeta、Clock node，以及只在永久Delete释放的
   cached shared-SWCC SCC allocation；每条 retire record传实际
   `record_partition`。普通move-out仍不释放 cached SCC allocation。
5. 本项不增加 adjacency snapshot、undo log、generation或重试循环；I2的
   unpublished-insert rollback是进入前状态不同的独立原 table remove callback，
   不能复用为 Delete rollback。

#### 必要验证

- 与 J1共用阶段 J唯一 gate；
- Debug barrier制造“尚未进入 adjacent callback”的writer/Clock竞争，断言Busy后
  current valid及prev/next adjacency逐位不变，竞争释放后facade可完成；
- local migrated与remote Delete各确认进入callback后只返回成功、四类对象按实际
  存在性各retire一次，cached payload只在Delete释放；
- 一个callback内部强制 shared-remove不一致的Debug fault直接fatal，且不把它扩成
  rollback故障矩阵；
- 函数级 diff确认邻接/invalid/index顺序与master相同，生产无
  `DeleteFailure/failure_detail`或半回滚分支。

#### 完成条件

正常竞争只在Delete修改任何状态前返回Busy；进入原callback后原子地完成整个
leaf-adjacent Delete，内部不变量失败fatal，不存在半恢复状态。

## 14. 阶段 K：Scan

### K1. remote Scan adjacency 添加了偏离 master 的 lower-bound/range-boundary 规则

#### 当前证据

`ScanSharedPartition()` 必须把 lower-bound 左边界适配限定为唯一一处，且仅挂在
**range move-in 之后的同参重跑**上：

- 首次 CXL probe：严格 master `key == min / size == limit / otherwise`（不启用
  lower-bound 豁免）。否则会在 `[min, island)` 仍为 private-only 时接受远处已迁入
  island，`move_in(min, limit)` 只填 min 起前 `limit+1` 私有键，永远修不到 island
  尾缘 `prev=1 next=0` Busy 活锁（Debug 4VM GDB 已证）。
- 同参重跑：`result_count==0 && key >= min` 的第一个 lower-bound row只要求
  `next_real`；
- `key > max` 的 finite range boundary 不能当成 limit boundary；
- sentinel/terminal不能通过 current-only 条件放宽 next adjacency。

内部最大键 sentinel 是唯一物理右边界：迁入 shared 时以既有 `next_real` 位表示
终端边界已闭合，remote callback 仍走上面的原三分支，不在 terminal 处添加豁免。

master 的原判断顺序是：

```text
key == min       → 只要求 next_real
result size==limit → 只要求 prev_real
其他             → 同时要求 prev_real 和 next_real
```

master 的 exact-min 分支在 start 不存在时本应进入“其他”，但 master scan migration
只从 min 向前迁入、不会迁入 predecessor。冷 shared tree 重跑时首个 lower-bound row
因此永久 `prev_real=false`，形成 Busy move-in 活锁；该反例已记录在
`验证证据.md`。唯一设计合同据此允许把本 fragment 的首个 lower-bound row视作左边界，
但不得扩大为 resolved-min、分页或任意 partial range 接受；finite max shortcut 仍可能
在并非 limit+1 时错误放宽 next bit。

另：master already-migrated 分支假定 current adjacency 位已正确、只懒更新邻居。
insert `clear_adjacent` 可在后继仍已迁入时清掉当前行 `next_real`；KV
`MoveInForMigrationManager` 的 already-migrated 路径必须按观测到的 private 邻居
迁移状态重建 **当前行** prev/next bit（与 fresh move-in 同一观测），再调用原
`set_adjacent_migrated_rows`。不得新增平行邻接状态机。

#### 修改方案

1. 直接复用 master remote scan callback/公共 primitive 的原三分支和顺序；KV
   engine 首次 probe 关闭 lower-bound 豁免，仅在规定的 move-in 后同参重跑上，
   当调用方 result vector为空且`key >= min`时启用该豁免。
2. adapter 只提供 FixedKey compare、RegionOffset row view、§3.2 sentinel以及上述
   无状态首行判定开关；sentinel move-in 仅将其原 `next_real` 初始化为终端边界完成，
   不改变 remote callback predicate。
3. start 不存在继续依赖原 B+Tree lower-bound traversal；不得解析或伪造
   resolved-min，首行判定也不得延续到第二行。
4. 只有原 predicate 判定不完整时，发送现有 range move-in；owner 不返回 value。
5. 不增加 resolved-min、Ready/Exhausted、分页、partial CXL+owner 混源或新的
   range 状态。
6. 共用 primitive按 K3直接读取调用方已有 result vector的 `size()/back()`；legacy
   handler因而自然保留已累积 transaction scan results，不传镜像 count/key。
   terminal-lock开关只服务 master原 no-phantom local分支。KV每次单 partition
   固定传空 held-row vector并启用原 phantom terminal lock；跨 partition只由
   facade传剩余 limit和下一个 range lower bound，不能把 driver参数发展成 KV
   pagination/continuation状态。
7. already-migrated 路径：在持有 lmeta/smeta latch 下按 `prev_migrated` /
   `next_migrated||terminal_sentinel` 写回当前行 adjacency bit，然后
   `set_adjacent_migrated_rows`；legacy helper already-migrated 主体保持 master
   邻居-only 形状。

#### 必要验证

- K1--K3 完成后合并运行一次阶段 K Scan gate；
- start 精确存在；
- start 不存在且第一 shared row 的 prev/next 分别缺失；
- 首次 probe 对 gap-before-island 必须 `migration_required`（不启用 K1）；
- move-in 后同参重跑启用 K1 并成功读出 min 起连续前缀；
- limit boundary；
- `key > max` 且未达到 limit；
- 空 partition、最后真实 row、sentinel；
- range move-in 后以同一 min/max/limit 重跑并成功；
- 静态断言 legacy 的三种原调用形态不变，KV 只使用上述固定参数形态；
- 本阶段不启动 4VM；小规模 E 统一延后到阶段 M。

#### 完成条件

除（1）仅挂在 move-in 后重跑上的 first lower-bound left-boundary 豁免，以及
（2）already-migrated 按邻居观测重建当前行 adjacency bit 这两项由
forward-only move-in / clear_adjacent 必需的薄适配外，remote Scan adjacency 的
所有布尔输入和分支与 master 一致，只加必要 sentinel storage adapter。

### K2. owner local Scan 对 migrated row 重复执行 SCC read

#### 当前证据

local Scan 的 `take_read_lock_and_read()` 已：

- 在 migrated 分支完成 SCC `prepare_read`；
- 必要时刷新 owner mirror；
- 把完整 value 复制到 `row.private_value`；
- 持有 shared reader 直到 fragment 释放。

结果组装时当前代码又对 `row.shared_data->data` 调一次
`scc_manager->do_read()`，造成重复 payload read/计费，并偏离 §3.9
“local 从原 ValueStruct data copy；remote 才直接 copy shared SCC payload”。

#### 修改方案

1. local Scan 无论 private/migrated，都使用 helper 已复制出的
   `row.private_value`。
2. 删除结果循环中的第二次 `SharedPayloadRead/do_read`。
3. 保留 fragment 级行锁/right-boundary lock 和统一 release，不缩短原锁范围。

#### 必要验证

- 与 K1、K3 共用同一次 Scan gate；
- owner private row 和 owner migrated row 返回值一致；
- SCC/raw access 计数证明 migrated local row 每次只由原 helper read 一次；
- remote Scan 仍直接在其 reader/ref 下 copy shared payload；
- 使用现有最小 partition/scan case，不单独再跑一遍完整 target。

#### 完成条件

local Scan 不再重复 SCC 读取，且结果/锁生命周期与原 callback 一致。

### K3. 独立 `TwoPLPashaScan.h` 把原 callback 扩成了新的 Scan 状态框架

#### 当前证据

当前新增的 `protocol/TwoPLPasha/TwoPLPashaScan.h` 定义了
`TwoPLPashaScanAcquire`、`TwoPLPashaScanFragment<Row>`以及 local、remote、
move-in 三个模板主体；fragment自己拥有 rows/next-row和
success/migration/busy flags。legacy Executor、MessageHandler和 KV再把原有状态
搬入/搬出该对象。

施工合同要求一个由 legacy/KV共用的“原 scan callback primitive”，但不要求新增
一套 Scan结果对象或独立框架。当前做法增加状态与可选参数面，也与本文 §3.4
“I--K只允许短 continuation/枚举”的最小新增预算冲突。

#### 修改方案

1. 删除独立 `TwoPLPashaScan.h`、`TwoPLPashaScanFragment` 和
   `TwoPLPashaScanAcquire`；不得仅换文件名保留同型状态容器。
2. 只在现有 `TwoPLPashaHelper` 内机械抽取两个无状态 template driver：

   ```text
   scan_local_fragment  = master phantom local callback主体
   scan_remote_fragment = master remote callback主体
   ```

   driver直接接收调用方已有 `rows`、`next_row`、`scan_success`、
   `migration_required`及 lock/acquire/release/row-key薄 callback；acquire沿用
   master的 `bool lock_success`，不为单一 Busy再造枚举。它们不分配/缓存第二份
   rows，不保存跨调用状态，不拥有 facade跨 partition编排。
3. legacy有 phantom local、remote和 no-phantom local三种原调用形态。driver参数
   只保留这三者实际需要的 terminal-lock差异；duplicate suppression和
   `scan_results.size()/back()`直接读取调用方原 vector，不再传
   `initial_result_count/initial_previous_key`镜像。Executor各分支的条件、结果
   vector、next-row lock bit和commit/abort release继续保持 master原位置。
4. KV每次 partition创建一个空的短生命周期 held-row vector，调用同一 driver；
   K1的 adjacency callback与K2的 value-copy callback是仅有 KV adapter。跨
   partition只在 `KVEngine::Scan`顺序拼接，driver不知道 partition范围。
5. range move-in不进入上述 driver。把 master
   `data_migration_request_for_scan_handler` 的 `ITable::scan → move_row_in`
   循环保留为现有 `TwoPLPashaMessageHandler::move_in_scan_range`唯一主体，
   legacy handler和 transaction-free handler直接调用它；不得在第三个 helper中
   再复制。
6. 逐行以 `git show master:protocol/TwoPLPasha/TwoPLPashaExecutor.h` 和
   `.../TwoPLPashaMessage.h`核对两个 driver及 move-in主体；除 callback参数化、
   RegionOffset row adapter和 D/L已编号修复外，分支/锁/release差异为0。

#### 必要验证

- 与 K1、K2共用阶段 K唯一 Scan gate，不新增 target或轮次；
- 原三种 legacy调用形态各保留一个已有 compile/最小行为断言，KV复用同一函数
  来源；不建立参数组合矩阵；
- `rg`确认生产中不存在 `TwoPLPashaScan.h/TwoPLPashaScanFragment/
  TwoPLPashaScanAcquire`，KV没有复制 scan traversal/adjacency状态机；
- 函数级 diff确认 move-in循环只存在于原 MessageHandler主体。

#### 完成条件

Scan只新增把 master callback参数化所必需的两个无状态 driver；legacy与KV直接共用，
没有独立结果框架、第三份move-in循环或跨partition状态。

## 15. 阶段 L：唯一 Busy retry 和错误分类

### L1. Corruption/协议不变量会被映射成 Busy 或 InvalidArgument

#### 当前证据

- `KVEngine::EncodeResult(StatusCode)` 对未列出的状态默认返回
  `RpcResult::kBusy`，包括 `kCorruption`。
- owner request handler 捕获 runtime error 后可能产生 `kCorruption`，随后编码成
  Busy；facade 现在持续重试，因此真实损坏会变成无限循环。
- owner CAS 用异常文本是否包含 `"busy"` 来分类；其他 runtime error 被映射成
  `InvalidArgument`。
- `kv_remote_publish_insert()` 对 `ref_cnt==0` 和“valid 已经为 true”统一返回 false，
  上层统一当 Busy；前者是明确不变量破坏，不能重试隐藏。
- `TwoPLPashaMetadataShared::decrement_ref_cnt()` 本身无下溢检查；多个原
  remote release/abort调用点只先做 `DCHECK(ref>0)`，Release构建可回绕。
- `increment_ref_cnt()` 本身也不防 `uint8_t`回绕；正常调用者虽检查饱和并返回
  Busy/NoMemory，primitive缺少最后的不变量保护。
- `RegionAllocator::AccountFree()`、`FreeOwnerPrivate()`、
  `TakeRetired()`及 CXLMemory policy/category counter的减法要么先 `fetch_sub`
  再 throw，要么完全不检查；异常可在 engine边界被转成 Busy/Corruption
  `Status`，没有统一进程级 hard-fail。

这违反“竞争返回 Busy；非法 offset、会计 underflow、malformed/corrupt 和协议不变量
hard-fail”的边界。持续 Busy retry 只有在 Busy 分类准确时才安全。

#### 修改方案

1. 正常控制流只保留协议确实需要的最窄枚举，不使用 typed exception：

   ```text
   enum class RowOutcome : uint8_t
     { Done=0, Missing=1, Busy=2 }
   enum class RemoteInsertOutcome : uint8_t
     { Inserted=0, AlreadyExists=1, Busy=2, NoMemory=3 }
   enum class MigrationResponseOutcome : uint8_t
     { Migrated=0, Missing=1, Busy=2, NoMemory=3 }
   ```

   `RowOutcome`是 D1 的唯一普通行结果；不要给无分配的 read/write/delete再加入
   `NoMemory`。`MigrationResponseOutcome`和`RemoteInsertOutcome`分别逐值对应
   §3.3、§3.5已经固定的两个四态线上小枚举；不得增删、合并或重排值。
   owner move-in内部继续直接使用 master 既有的
   `star::migration_result {SUCCESS, FAIL_ALREADY_IN_CXL, FAIL_OOM}`，不再为它
   包一层新枚举。普通migration handler先用原`table.search`一次：稳定空直接
   `RowOutcome::Missing`；命中后调用D1同一B+Tree helper，invalid placeholder或
   search后与Delete竞争导致原adjacent callback未命中时由其栈上输出
   `RowOutcome::Busy`，且不分配/发布。不得把原result初值
   `FAIL_OOM`用于这两种正常结果，也不得再查一次owner/shared index。普通
   migration request中原 `SUCCESS`和
   `FAIL_ALREADY_IN_CXL`都编码为线上`Migrated`，真实分配失败
   `FAIL_OOM`才编码`NoMemory`。I2刚创建的唯一invalid placeholder则必须直接
   断言原结果为`SUCCESS`；此处`FAIL_ALREADY_IN_CXL`是协议不变量破坏，不得借
   普通migration映射掩盖。不要再定义通用 `RpcResult`、`PrimitiveOutcome`或
   三者之间的中间转换枚举。调用者必须穷尽 switch。
   `ClientInvalid` 只由 `KVStore` facade 在进入 EBR/tree/transport 前返回，不进入
   helper 或 wire。
   `Corruption/Invariant` 不属于任何枚举，检测点直接调用统一
   `ProtocolFatal(context, reason)`。C4 已建立该唯一函数；现有
   `TransportFatal` 只改成补充 Message frame上下文后委托它，不能保留第二套
   formatter/abort入口。
   requester端到 facade结果的映射也固定，不得自行解释：

   ```text
   RowOutcome::Done/Missing/Busy
     → Ok/NotFound/Busy
   RemoteInsertOutcome::Inserted
     → 继续原 requester valid发布
   RemoteInsertOutcome::AlreadyExists 或 Busy
     → Busy，由 facade从 CXL-first 开始重做完整 Put/CAS/Increment
   RemoteInsertOutcome::NoMemory
     → OutOfMemory
   MigrationResponseOutcome::Migrated
     → requester继续原 shared访问
   MigrationResponseOutcome::Missing/Busy/NoMemory
     → NotFound/Busy/OutOfMemory
   ```

   `AlreadyExists`不能直接冒充 Put成功：并发创建者可能尚未完成valid发布；对
   empty-expected CAS也必须重做后观察已存在值并得到正确 CompareFailed。新建
   smeta的requester ref不可能饱和；已有shared row的 `ref_cnt==UINT8_MAX` 只映射
   `Busy`，等待现有ref释放，不能伪装成 allocator `NoMemory`。
2. 按 G2 删除通用`EncodeResult`。原 Factory/Handler 的相应 overload直接接收
   本操作的线上枚举；encoder用无`default`的穷尽switch写显式`uint8_t`值，
   decoder先验证原始值只在0--3，再用无`default`的穷尽switch恢复相应枚举。
   普通migration handler只在这一处把原
   `SUCCESS/FAIL_ALREADY_IN_CXL/FAIL_OOM`映射为
   `Migrated/Migrated/NoMemory`；Missing来自helper前的首次search，Busy来自D1
   原callback内的invalid/竞态结果。
   未知原始值直接`TransportFatal`，不能default Busy，也不得恢复通用结果
   encoder/decoder。
3. 删除通过异常字符串包含 `"busy"` 的分类。
4. `std::bad_alloc` 只在最外层 primitive boundary 转成 `NoMemory`；正常
   Busy/Missing/AlreadyExists 不 throw。其它 `std::exception` 到达
   `ServeTransportRequest` 说明内部错误，记录上下文后 fatal，不转
   InvalidArgument/Busy。
5. ref==0、underflow、未知 enum、错 owner/table/partition 带
   node/worker/partition/key/offset 诊断 hard-fail。所有 RegionOffset只调用 C4
   的有界 resolver；包括“仍在全池内但属于另一 owner/domain”的 offset，resolver
   失败后直接 `ProtocolFatal`，不得先解引用或转成枚举。
   其中会计只作以下最小集中修正：

   - 在 `TwoPLPashaMetadataShared::decrement_ref_cnt()` 内、调用者已持有 smeta
     latch时检查 `ref_cnt>0`，否则 fatal，再按原宽度减1；所有 release/abort删除
     外层重复 `DCHECK/get_ref_cnt`，避免为同一减法多一次 HWCC访问；
   - `increment_ref_cnt()` 在原正常饱和检查之后仍以 `ref_cnt<UINT8_MAX`作为
     invariant fallback；已有shared row的正常 saturation仍由调用者按上文唯一
     返回Busy，不改成NoMemory或fatal；I2新建placeholder不可能饱和，若该路径观察
     到饱和则是协议不变量并fatal；
   - allocator/EBR/CXL policy counter保持原 RMW和锁顺序，但任何
     `before<delta`直接调用同一 `ProtocolFatal`，绝不 throw或继续；不增加
     shadow counter、CAS状态机或通用 checked-arithmetic class。
6. wire 保持 G2 的最小形式：migration 与 remote insert 只编码各自小枚举，
   remote Delete 成功路径仍为空 ack，Scan 仍是原
   `bool success + key_offset`；不得为统一错误模型新增通用 Status payload。
7. 保留 facade 对真实 Busy 的持续重试；不恢复固定次数、timeout 或 sleep。

#### 必要验证

- 阶段 L 只运行一个包含三种结果分类的短 case；
- 一个真实 lock contention 返回 Busy 并最终可继续；
- client key/value 长度错误返回 InvalidArgument；
- 注入一个全池外 locator、一个全池内错 domain locator、一个非法 ref/enum 时
  Debug fatal，而不是 Busy/NotFound；合并在同一短 death/assert case，不扩展矩阵；
- 同一 case令 I2 的新 remote-insert placeholder在move-in时非法返回
  `FAIL_ALREADY_IN_CXL`，确认fatal而非发布或rollback；
- 同一普通结果case检查 remote insert的
  `Inserted/AlreadyExists/Busy/NoMemory`及 migration的
  `Migrated/Missing/Busy/NoMemory`各只命中上述唯一映射，并直接断言普通
  migration中原`SUCCESS`与`FAIL_ALREADY_IN_CXL`都只编码为`Migrated`；复用
  同一table-driven小断言，不建立并发/容量组合矩阵；
- 同一 fatal case再直接覆盖 ref=0 decrement和一个 allocator/EBR accounting
  underflow；正常 ref饱和仍走原可重试结果，不新增组合矩阵；
- 只做少量现有 death/assert 测试，不构造工业级故障矩阵。

#### 完成条件

只有理论上可因继续竞争而成功的状态进入无限 Busy retry；损坏永远不会被重试掩盖。

## 16. 阶段 M：一次合并功能 gate 和一次小型 4VM

阶段 M 不修改生产代码，只验证 A--L 的合并结果。严格分成两个串行步骤：

1. 先确认没有 QEMU/E2E/runners，再在 Debug 下把本轮实际受影响的 focused target
   合并为一条串行 CTest 命令，每个 target 只执行一次。集合固定为：

   ```text
   unit_tests
   kv_startup_test
   region_allocator_test
   cxl_ebr_test
   kv_layout_test
   btree_binding_test
   scc_protocol_test
   kv_shared_protocol_test
   kv_partition_test
   kv_engine_test
   ```

   唯一命令为：

   ```bash
   ctest --test-dir build-debug -j1 --output-on-failure \
     -R '^(unit_tests|kv_startup_test|region_allocator_test|cxl_ebr_test|\
kv_layout_test|btree_binding_test|scc_protocol_test|kv_shared_protocol_test|\
kv_partition_test|kv_engine_test)$'
   ```

   不得从集合删除 target，也不得加入 E2E、脚本测试、latency target 或其它 CTest。
   该集合就是 §4 所说的“全部 focused 功能测试”，不是全部 CTest。所有 file-backed
   fixture 遵守 §3.2 的 32MiB+32MiB 下限。

2. focused gate 全部通过后关闭所有测试进程，只启动一套 4VM/QEMU。在同一次 VM
   session 中按顺序执行：

   - 最小 direct Get/Put/Delete/Scan smoke，唯一复用现有
     `scripts/e2e_trace/run_guest_ycsb_workflows.sh`与`e2e_trace_runner`，不新增
     direct binary、guest脚本或runner模式。为它在本轮证据目录创建独立、此前
     不存在的`m-direct-traces/{load,workloada}`与`m-direct-logs`：

     - 一次性Python binary writer从根正式config解析三个已经规范化的32B split，
       固定四个key为`00`重复32次及三个split原字节；先断言四个key均恰好32B、
       严格递增、分别按同一`upper_bound`规则路由到partition 0--3，且均非
       all-`ff`内部哨兵；
     - `load/worker0..15.txt`中只让全局worker `4*p`对partition `p`的key写一条
       `PUT 32 32 <key>`，其余12个文件只写固定comment。因此四个owner各创建一行，
       load有效操作总数精确为4；
     - `workloada/worker0..15.txt`中只让全局worker `4*i`操作partition
       `p=(i+3)%4`的key，固定顺序为
       `GET→SCAN(limit=1)→DELETE→PUT→GET`，其余12个文件只写固定comment。四个
       requester都不是该key owner、四个key互不相同，run有效操作总数精确为20；
       写完后按`tools/e2e_trace_format.h::ParseLine`的固定格式机械核对每条key长度
       和操作/LEN，不另写语义parser；
     - 该唯一workflow命令显式使用最终Debug
       `e2e_trace_runner/cxl_pool_initer`、根正式32/32 config、4VM、每VM 4
       foreground，并在同一个命令前缀中设置
       `TIGONKV_E2E_TEST_VALUE_HEX=$(printf '5a%.0s' {1..32})`、
       `TIGONKV_E2E_REQUIRE_GET_FOUND=1`、
       `TIGONKV_E2E_SCAN_EXPECT_NONEMPTY=1`；不得export到父shell。四个load和
       20个run操作、四VM pass marker、每VM topology及非零总Scan行数全部精确
       核对。日志只写`m-direct-logs`，不能与后面的YCSB-A/E混用；
   - 用现有 trace 准备入口生成并回放固定 `10,000` 条 load +
     `10,000` 条 operation 的 no-latency YCSB-A；
   - reset backing 后回放同一规模、同一 load 数据集和 split 的
     no-latency YCSB-E。

   三个子项均使用 4VM×4 foreground+1 demuxer、32B KV 和正式四个 range
   partition；A/E 使用同一次准备产物而不是各生成一份 load。该 `10k/10k` 只用于
   smoke，不进入P阶段证据或正式比较；不运行 B/C/D，不启用延迟，不使用标准数据量，
   不重启第二套 VM。若它不足以复现本阶段已经观察到的失败，只允许把这同一份
   A/E 数据量一次提高到 `100,000/100,000` 后定位，不增加第三档数据量或压力矩阵。
   准备命令固定显式设置
   `TIGONKV_YCSB_WORKLOADS="A E"`、`YCSB_RECORD_COUNT=10000`、
   `YCSB_OPERATION_COUNT=10000`、`YCSB_WORKERS=4`和`TIGONKV_VM_COUNT=4`；不能因
   B6把通用默认改为A--E而额外生成B/C/D。与Debug wrapper相同，只让split工具
   修改一份本轮临时config副本，并显式令
   `TIGONKV_YCSB_PARTITION_SPLITS=build-debug/ycsb_partition_splits`；实际回放仍
   使用未改写的根正式config/split；
   临时副本在回放前删除，阶段M前后根config SHA必须相同。direct smoke完成后先
   fresh pool再开始A；A完成后再fresh pool开始E，但三者共用同一套已验证的四个
   QEMU，不重启或并行创建第二套。A/E只调用一次现有workflow，workload参数固定
   为`"a e"`、log root固定为独立且此前不存在的`m-ycsb-logs`；命令显式设置
   `build-debug/e2e_trace_runner`、`build-debug/cxl_pool_initer`、根正式config、
   `TIGONKV_YCSB_THREADS_PER_VM=4`和
   `TIGONKV_E2E_SCAN_EXPECT_NONEMPTY=1`，并用`env -u`清除
   `TIGONKV_E2E_TEST_VALUE_HEX`、`TIGONKV_E2E_REQUIRE_GET_FOUND`及
   `TIGONKV_E2E_SCAN_MAX_KEY`。不得让默认
   `build-relwithdebinfo`路径把这轮Debug smoke静默换成Rel binary，也不调用正式
   summarizer。
   direct与A/E两个workflow命令都在同一`env`前缀中清除
   `TIGONKV_VM_SSH_BASE_PORT`、`TIGONKV_VM_SSH_KEY`、
   `TIGONKV_VM_REMOTE_ROOT`、`TIGONKV_VM_REMOTE_CONFIG`、
   `TIGONKV_VM_REMOTE_RUNNER`、`TIGONKV_SHARED_MEMORY_PATH`、
   `TIGONKV_SHARED_SIZE_MB`和`TIGONKV_SHARED_NUMA_NODE`；显式设置
   `TIGONKV_VM_COUNT=4`、`TIGONKV_YCSB_THREADS_PER_VM=4`、
   `TIGONKV_E2E_TIMEOUT_SEC=7200`和`TIGONKV_E2E_STALL_SEC=90`。direct还清除
   `TIGONKV_E2E_SCAN_MAX_KEY`后再设置上述三个专用oracle。由正式config唯一决定
   SSH port、backing、capacity和NUMA，不能继承旧VM/旧backing覆盖，也不新增环境
   清洗脚本。

失败处理：

- 立即停止后续子项，保存最小结构化诊断；
- 清场后用 file-backed 或更小 4VM trace 定位；
- 修复只重跑受影响阶段 gate，再重新执行阶段 M 一次；
- 阶段 M 通过后，除非生产数据路径再次变化，不再重复它。

完成条件：一次合并 focused gate 和一次干净的小型 4VM session 全部通过，且全过程
没有 unit/E2E 并行、旧进程、旧 VM 或旧 backing 干扰。

## 17. 阶段 N：删除 current-only 双路径

### N1. 在前述切换完成后仍需统一清理第二主体和虚假文档断言

#### 当前证据

前述阶段完成后，至少复查并删除：

- current-only 完整 `TableBTreeOLC` class；
- current-only `CxlIncomingDispatcher` class；
- 只由 KV 使用的重复 Message encode/decode/handler 主体；
- `KVPartition::ClockTracker*` 算法；
- `KVPartition::NoteSharedAccess`；
- `KvMigrationRuntime::SyncHwCcUsage`；
- `TryPinShared` 的第二次 lookup；
- 已被D1/L1唯一结果和原callback替代的`SharedAccessState`、
  `ClockTrackerNodeView`、`OwnerNextRowLock`；
- C3 已替代的 `PersistPrivateRootIfChanged` 和 private-root mirror/counter；
- 已由共用 helper 替换的 Put/Get/CAS/Increment/migration/Delete/Scan 状态体；
- 仅为这些路径存在的声明、include、统计和测试 helper；
- `MemoryStats::retired_pending_bytes/reclaimed_total_bytes` 及 DumpStats/test 文本：
  两者从未从 EBR/allocator 读取，当前永远输出默认 0，不是可信统计；
- `Config::strict_swcc_access`（全仓只有声明）和派生后再无读取者的
  `Config::hwcc_atomic_ns`；实际三个 atomic latency字段继续保留；
- 只被 `kv_layout_test` 调用、生产从不使用且内部绕过 `mem_access` 的
  `SharedLayoutHeader::IsCompatible` 测试 helper；attach只保留 C1唯一校验路径；
- 无任何调用者的 `tests/fixtures/ycsb_experiment_config.jsonc`：其
  16 partition/1000B value 口径也不是正式 4VM 配置。
- `tigonkv_run_ycsb_experiment.sh`的`--skip-standalone-load`、
  `skip_load`变量和`[[ "$skip_load" == true ]] || true`：脚本从未执行独立于每个
  workload的standalone load，该选项无论取值都不改变控制流，属于会误导正式命令
  的死脚手架。

#### 修改方案

1. 每个对象必须在替代路径 focused 测试通过后才删除。
2. 不删除 master 保留的 legacy transaction/benchmark/reference 源码。
3. 不顺手清理无关 TODO、风格、旧协议或未链接类。
4. production adapter 的 virtual 方法不能用空函数、无条件 false 或
   `logic_error("unreachable")` 代替实现。
5. 删除两个永远为 0 的 Memory 字段、DumpStats 行和只检查字符串存在的测试；
   不为保住输出新增 EBR 热路径 counter。`unclassified_shared_bytes=0` 作为三类
   allocator 使用量精确分解后的显式不变量继续保留。
6. 删除两个死 Config成员和仅测试 helper及其断言；不把它们替换成新模式或新
   compatibility API。
7. 删除无人引用的旧 YCSB fixture；正式实验只认根目录
   `experiment_config.jsonc`，小型 multivm 功能脚本只认 B3 修正后的专用 fixture。
8. 删除无作用的`--skip-standalone-load`及其变量/no-op，不以另一种skip选项替代；
   每个A--E workflow仍严格执行自己的`fresh pool→load→run`。

#### 必要验证

- 阶段 N 只做静态清理 gate和受影响 target 的一次增量重建；

```bash
rg -n 'CxlIncomingDispatcher|NoteSharedAccess|SyncHwCcUsage|\
ClockTracker(Lock|Unlock|Track|Untrack|MoveForward|ResetCursor)|\
PersistPrivateRootIfChanged|private_root_publishes' \
  common core protocol kv
rg -n 'ScanCertificate|mutation_generation|lookupAdjacent|LockNeighborhood|\
BreakAdjacency|RefreshAdjacency|promote_updated_row|ClockEvictUntilUnderBudget' \
  common core protocol kv
rg -n 'retired_pending_bytes|reclaimed_total_bytes|ycsb_experiment_config' \
  --glob '!下一步修改.md' .
rg -n 'strict_swcc_access|hwcc_atomic_ns|IsCompatible' \
  --glob '!下一步修改.md' common core protocol kv tests
rg -n 'pointer_to_pool_offset|pool_offset_to_pointer|FromPoolOffset' \
  --glob '!下一步修改.md' common core protocol kv
rg -n 'KvSharedResult|KvDeleteSharedResult|KvMoveOutSharedResult|\
KvMoveInPublishResult|KvRemoteDeletePrepareResult|RpcKind|RpcResult' \
  --glob '!下一步修改.md' common core protocol kv
rg -n 'SharedAccessState|ClockTrackerNodeView|OwnerNextRowLock|\
ScanTlsDiag|ScanTlsFlushGuard' \
  --glob '!下一步修改.md' common core protocol kv
rg -n 'skip-standalone-load|skip_load' tigonkv_run_ycsb_experiment.sh \
  tests scripts --glob '!下一步修改.md'
```

- 检查正式 target 的链接/调用图；
- Debug 和 RelWithDebInfo 只重新构建受影响 target；
- 对删去的shell死选项只执行`bash -n tigonkv_run_ycsb_experiment.sh`并静态核对
  `--help`期望；不生成trace、不启动VM；
- 若只删除已证明无调用的死声明，不重跑已通过的 H--L 功能 gate；
- 不在本阶段重复 4VM。

#### 完成条件

每类生产协议只有一个主体，剩余 current-only 代码都是不可避免的 offset/fixed-KV/
transaction-free 薄适配。

## 18. 阶段 O：最终延迟审计

### 阶段 O 的充分审计口径

阶段 O 不能被理解为“修完 O1--O6、跑过一次 `latency_modes_test` 就自然完成”。
O1--O6 是当前已知缺口；进入 O 时还必须对 **N 阶段清理后的最终 production 调用图**
做一次从入口到结算点的充分软件延迟插入审计。测试只验证审计结论，不能代替逐路径
源码核对。唯一审计产物是同步更新后的 `延迟插入审计报告.md`，不得另建第二份长期
报告或新增运行时审计状态。

1. 最终报告必须逐项覆盖下列生产路径，不能只抽查发生过 bug 的函数：

   ```text
   startup / reset / attach / layout、root和ready发布
   private B+Tree lookup/insert/remove/split/merge/adjacent callback
   shared B+Tree lookup/insert/remove/split/merge/adjacent callback
   owner-private ValueStruct/lmeta/payload
   HWCC smeta/ref/reader/writer/adjacency/Clock bit
   shared-SWCC SCC tid/valid/padding/payload及prepare/finish/flush
   Get/Put/Delete/CAS/Increment/Scan的local、migrated和remote分支
   move-in / move-out / permanent Delete / PolicyClock victim循环
   static、dynamic-HWCC、owner-private及shared-payload allocator
   EBR enter/leave/epoch/retire/reclaim
   request/response ring、demuxer、worker SPSC handoff和Await continuation
   Memory/RuntimeStats/最终report读取
   ```

2. 对每一条实际访问，在报告表中固定记录以下八列；任一列无法填写都表示审计未
   完成：

   ```text
   最终文件/函数及调用阶段
   对象与实际物理域（DRAM / private SWCC / shared SWCC / HWCC）
   read/write/atomic/copy/flush类型
   唯一mem_access wrapper及精确字节或cacheline范围
   当时持有的leaf/lmeta/smeta/row/ref/Clock/EBR/ring状态
   SCC或普通publication/flush先后
   所属非嵌套scope
   EndScopeAndDelay安全点
   ```

   同一访问只能记录一次；上层 broad wrapper与下层真实访问重复时删除上层补记，
   漏记则在原访问点补最窄 wrapper。不能用估算计数、对象总大小或“同类函数相同”
   替代实际字段/范围核对。
3. 审计必须同时做三类反向检查：

   - 从最终共享/owner-private layout中的每个生产字段反查全部读写调用者，确认
     没有裸访问、错域 wrapper或非owner读取；
   - 从全部 `mem_access` 调用点正查真实对象域，确认没有重复计费、把DRAM记为CXL、
     把shared SCC记成HWCC或把dynamic-HWCC block记成private SWCC；
   - 从全部 `BeginScope/LatencyScope/EndScopeAndDelay` 调用点检查作用域和退出边，
     确认无嵌套scope、无遗留active scope，且结算时不持leaf/lmeta/smeta、逻辑
     reader/writer/ref、Clock/EBR锁或ring reservation。

4. disabled fast path必须单独由源码和最小 instrumentation 断言证明：热路径只经过
   一个可预测门禁，不读取TSC、不创建/查找TLS map、不维护cache/filter、不更新
   stats，也不改变原函数返回值、锁序、memory order或flush。enabled路径必须证明
   TSC校准失败hard-fail、没有 `sleep_for`/sleep fallback，raw访问数不因cache命中
   减少，且最终 delayed ns与所选 `cache_model=none`合同一致。
5. 审计过程中若发现 O1--O6 之外的新漏记、重复记账或不安全结算点，必须先把它以
   最小问题项补入本节，直接在原访问点修复；若根因属于 A--N 的功能/锁/发布路径，
   返回对应阶段修复并只重跑受影响 gate，然后从最终调用图重新完成本节审计。
   不得用延迟 wrapper掩盖功能 bug，也不得为审计新增镜像counter、scope stack、
   pointer-domain推断、后台线程或第二套访问框架。
6. “充分”不等于扩大测试矩阵：动态验证仍只使用 O1--O6 合并后的唯一
   `latency_modes_test` gate和 P 阶段既定的一次 RelWithDebInfo focused A/E。
   完备性主要由上述双向源码审计表、静态调用图、访问域/锁/flush/结算证明建立，
   不增加重复4VM轮次、随机长压或每条路径一个独立测试。
7. 只有 `延迟插入审计报告.md` 已按最终源码逐项更新、表中没有“待确认/沿用旧结论”
   且 O gate通过，才能进入阶段 P。后续任何生产内存访问、锁、SCC、migration、
   transport或allocator路径变化都会使受影响行失效；必须先重审并更新报告，不能
   继续引用旧摘要宣布完成。

### O1. MPSC 在持有 reservation/slot 状态时执行延迟 busy-spin

#### 当前证据

`common/MPSCRingBuffer.h` 在以下位置直接调用 `DelayActiveScopeNow()`：

- producer 已增加 count/tail、尚未 release-store ready；
- consumer 等待 ready 的每次 poll；
- consumer 尚未发布 slot free/head/count 时。

同一对象中 `entry_struct_size`、`entry_data_size`、`entry_num` 和
`entries_buffer` 都持久化在 HWCC；当前 enqueue/dequeue/getter 直接读取前三项，
并通过 `boost::offset_ptr` 直接解析最后一项。除 C4 已要求把
`entries_buffer` 改成 HWCC allocator-relative `RegionOffset` 外，这些静态几何
字段仍缺少 transport/HWCC `mem_access` 记录。它们不是进程本地配置副本，不能因
初始化后不变而按 DRAM 裸读。

这会在 ring reservation 或 slot 生命周期内 busy-spin，并改变原 MPSC
backpressure/阻塞特征。`partition优化方案.md` §3.11 明确规定：

- request 阶段完成 enqueue 后结束 scope；
- receive scope 是 dequeue→worker SPSC enqueue；
- 结算时不持 ring reservation/queue 状态；
- 允许 publication 先于最终软件延迟结算。

当前 `延迟插入审计报告.md` 反而写成“必须在 transport ready/slot release 前结算”，
与唯一施工合同冲突。

#### 修改方案

1. 逐行确认G1已经恢复MPSC reservation/head/tail/ready算法；若仍有条件或memory
   order差异，先回到G1修正并重跑G gate，O本身不首次修改队列状态机。
2. ring 内只记录真实 `mem_access`，不在 reservation/ready polling 内结算 TSC
   busy-spin。
3. 构造并发布 ring 时以 `TransportWrite` 记录完整静态几何范围；每次
   getter/enqueue/dequeue 在首次使用前以 `TransportRead` 记录实际读取的
   `entry_struct_size`、`entry_data_size`、`entry_num` 和 C4 的
   `entries_buffer` offset，再通过 C4 resolver 取得瞬时 VA。只允许把这些值复制
   到当前调用的栈局部变量并在本次调用内复用，不新增持久/进程级 ring handle、
   geometry cache 或第二份状态。head/tail/count/ready 继续使用现有 HWCC atomic
   wrapper，entry metadata/data 继续使用现有 Transport wrapper。
4. send/receive wrapper 在 enqueue/dequeue 完整结束、无 reservation 后结束对应
   scope。
5. demuxer 在 dequeue→worker SPSC enqueue 后结算。
6. 不改 ready memory order，不加 sleep/yield fallback，不新增 publication 状态。
7. 同步修正 `延迟插入审计报告.md`，以 §3.11 的软件 scope 口径为准。

#### 必要验证

- 阶段 O 只运行一次 latency gate；
- `latency_modes_test`：disabled fast gate、TSC gate、settlement；
- 小 ring send/receive 断言结算发生在 enqueue/dequeue 返回之后；
- 同一个小 ring case 断言 geometry/offset 计入 HWCC transport 域且没有 SWCC
  误记；不另建测试目标；
- enabled 小延迟下检查没有在 leaf/smeta/Clock/EBR/ring reservation 内 busy-spin；
- 本阶段不运行 latency-enabled A/E；该项只在阶段 P 的最终验证中执行一次。

#### 完成条件

MPSC 算法保持G阶段已验证的master主体；O只令延迟在无reservation的scope边界结算，
文档与代码一致。

### O2. startup/Memory 仍有 HWCC layout/counter 裸读，绕过 `mem_access`

#### 当前证据

- `KVEngine::Open()` 计算 static HWCC 时直接
  `layout.domains[domain].used_bytes.load()`；
- `KVEngine::Memory()` 汇总 static HWCC、HWCC allocator metadata 和 SWCC allocator
  metadata 时也直接 load 同一 HWCC layout 中的原子；
- `DualRegionAllocator::Attach()` acquire观察 magic后，直接读取整组 immutable
  layout/header字段做 compatibility校验和错误诊断，没有对应 `HwccRead`；
- owner初始化/ready、Allocate/Retire/Memory等运行时路径反复直接读取
  `layout.vm_count/partition_count/owner_dynamic_arenas`。这些值已有经过验证的
  process-local `config_`和 C4 bounds副本，继续每次裸读 HWCC既漏计又没有必要；
- allocator其它 HWCC atomic control访问已有 wrapper，因此这些
  reporting/startup裸读不是允许的本地 DRAM快照。
- placement-new `CXL_EBR` 时写入 HWCC-resident
  `coordinator_num/thread_num/global_epoch/local_epoch[]`，当前构造路径没有对这段
  初始化写做 `HwccWrite`；root publication不能替代对象内容的真实访问记录。

这违反“所有 HWCC 访问经现有 `mem_access` 记录”的合同，也会使 latency/access-domain
审计少计明确的 cacheline。

#### 修改方案

1. C1/C4 attach在 acquire magic后，对即将校验和复制的 immutable
   `DualRegionPersistentHeader`字段按实际连续范围调用一次或最少不重叠的
   `HwccRead`；随即把 vm/partition数量、owner-control几何和 dynamic bounds复制
   到现有 process-local handle。错误诊断只使用这份已记录的局部副本，不二次裸读。
2. startup之后的循环/owner检查使用现有 `config_`和 C4 local bounds；只有
   `magic/state/ready bitmap/root/domain counter`等真实共享动态字段继续读取 HWCC，
   且每次真实 atomic/read前紧邻正确 wrapper。不得为此新增另一份 layout cache
   class或可变镜像。
3. 在 `DualRegionAllocator` 只增加一个
   `ReadStaticDomainUsedBytes(AllocationDomain domain) const`：

   ```text
   校验 domain 是 kHwccLayout/kHwccAllocatorMetadata/kTransport/kHwccEbr/
                    kSwccAllocatorMetadata 之一
   counter = &layout.domains[domain].used_bytes
   mem_access::HwccAtomicLoad(counter)
   return counter->load(memory_order_relaxed)
   ```

   非法/动态 domain hard-fail；不返回 counter 指针，不增加缓存或镜像。
4. `KVEngine::Open()` 的 static capacity clamp 和 VM0 的 `Memory()` static snapshot
   只调用该 accessor，删除所有直接 `layout.domains[].used_bytes.load()`。
5. dynamic HWCC/shared payload/owner-private 统计继续调用 C1 已固定的 owner-local
   accessor；不得为统一接口让 VM0 读取其他 owner-private counter。
6. 该 reporting accessor只记录访问，不自行开始/结束 foreground latency scope，
   不 busy-spin，也不改变 Clock budget/物理容量公式。
7. VM0构造 global EBR时，在写完对象且发布 EBR root前以一个覆盖实际对象的
   `HwccWrite`（或最少不重叠连续字段范围）记录初始化；之后
   `thread_init_ebr_meta`和 epoch热路径继续使用已有最窄 HWCC read/atomic wrapper。
   不给 EBR增加 init state、cache或额外 flush协议。

#### 必要验证

- 与 O1 合并在同一次 `latency_modes_test` gate；
- 一个 Memory snapshot 断言每个读取的 static counter各记录一次 HWCC atomic load，
  disabled/enabled 下数值相同；
- attach case断言 compatibility读取被记录为 HWCC且之后 owner-local循环不因
  vm/partition常量重复增加 HWCC raw reads；
- 同一 startup case确认 EBR对象初始化计入 HWCC且未误记 SWCC；
- `rg` 确认 `KVEngine::Open/Memory`无直接 `layout.domains.*load`，allocator
  runtime没有对 immutable `layout.vm_count/partition_count/descriptor`的裸读；
- 不单独重跑 allocator、engine 或 4VM。

#### 完成条件

startup/report 的 immutable layout和 static HWCC counter访问全部可由
`mem_access`审计，runtime使用已验证 local binding，且不改变任何 capacity、policy
budget或聚合语义。

### O3. Clock tracker 与 owner-private EBR retire queue 仍有未记录的 SWCC 访问

#### 当前证据

`partition优化方案.md` §3.11 明确要求 owner-private Clock control/node 和 retire
queue/record 计入 private SWCC。当前仍有以下裸访问：

- `DualRegionAllocator::Retire()` 裸读 `arena->owner_shard`，裸写 record 字段，裸
  load/CAS `retire_heads`，裸 fetch_add `retire_counts`；
- `RetireCount()` 裸 load 每个 `retire_counts`；
- `TakeRetired()` 裸 exchange head、裸读 `record->next`/整条 record、裸 fetch_sub
  count；
- 上述三个函数还把 master 的每 VM/worker/epoch 一条 retire list扩成
  per-partition heads/counts，并在每次 epoch检查和回收时遍历该 owner的全部
  partition；这不是 RegionOffset迁移所必需的算法差异，也会增加热路径和状态量；
- 当前 `KVPartition::ClockTracker*` 对 head/tail/cursor/node 的一部分分支先读后只
  记录 write，Debug consistency traversal 也裸读；F1 切回原 PolicyClock 后如果只
  搬控制流而不逐访问迁移 wrapper，仍会保留漏计；
- `pthread_spin_lock/unlock` 对 F1 新 owner-private tracker lock 的访问也必须有
  明确 Private atomic 记录，不能因 libc 内部实现不可见而漏掉。

这些字段不是进程 DRAM；“只有 owner VM 访问”只决定使用本地 CPU 原子/锁，不免除
SWCC access-domain 记录。

#### 修改方案

1. 不新增新的 access API、计数状态或 latency scope；只使用现有
   `PrivateRead/PrivateWrite/PrivateAtomicLoad/Store/Rmw`，并把 wrapper 紧邻放在
   每个最终真实访问之前。
2. F1 的最终 `OwnerPrivateClockTrackerControl` 和 node 固定按下表记录：

   | 实际访问 | 唯一 wrapper |
   |---|---|
   | `pthread_spin_lock(&lock)` | 调用前一次 `PrivateAtomicRmw(&lock)` |
   | `pthread_spin_unlock(&lock)` | 调用前一次 `PrivateAtomicStore(&lock)` |
   | head/tail/cursor 或 node link/key/offset 普通读 | 覆盖实际字段范围的 `PrivateRead` |
   | 上述普通写 | 覆盖实际字段范围的 `PrivateWrite` |

   原子 wrapper只做延迟会计，不替代 pthread lock，不把 lock 改成 HWCC 或 SCC。
3. 先按 C1 删除 per-partition retire heads/counts；`Retire`、`RetireCount`、
   `TakeRetired`只访问 bound `OwnerAllocatorControl` 中与原算法一一对应的
   `[worker][epoch]` head/count，绝不扫描 partition。record仍在调用方指定且属于
   本 owner的 `record_partition` arena分配/回收；链中每条 record携带该
   `record_partition`，不增加 owner级record allocator。
4. EBR publish 顺序及 wrapper 固定为：

   ```text
   PrivateRead(owner_control.owner_shard) → 校验 bound owner
   PrivateRead(queue arena.owner_shard) → 校验 record_partition 属于 owner
   allocate record
   PrivateWrite(record, sizeof(record))
     → 初始化 object/bytes/domain/private_partition/record_partition
   PrivateAtomicLoad(head) → load relaxed
   每次循环：
     PrivateWrite(record.next) → record.next=observed
     PrivateAtomicRmw(head) → compare_exchange_weak release/relaxed
   PrivateAtomicRmw(count) → fetch_add relaxed
   ```

   不能在 head release publish 后再补写 record。
5. `RetireCount` 对 owner control唯一 count在 acquire load前记录一次
   `PrivateAtomicLoad`。`TakeRetired` 固定为：

   ```text
   PrivateAtomicRmw(head) → exchange(null, acq_rel)
   每个 offset：
     以 C4 owner-private resolver校验 record及record_partition后
       PrivateRead(record, sizeof(record))
     copy record/读取 next
     调现有 FreeOwnerPrivate(record_partition)
   removed!=0：
     PrivateAtomicRmw(count) → fetch_sub relaxed
     underflow hard-fail
   ```

6. 同一真实访问只能由最窄 wrapper记录一次；删除 F1 重构后遗留的 broad pre-record
   或重复 wrapper。Debug consistency check 同样记录真实 owner-private read，但
   不进入 Release 正式开销。
7. 除 C1 已规定的 per-partition→per-owner 机械归位外，本项不改变 memory order、
   retire publish/reclaim顺序或 tracker lock范围。
8. 所有 wrapper 只累计；`DelayActiveScopeNow/EndScopeAndDelay` 仍只在 O1 的外层
   安全点执行，绝不放进 tracker lock、retire CAS 循环或 reclaim 遍历。

#### 必要验证

- 与 O1、O2 共用唯一一次 `latency_modes_test` gate；
- 在该 target 的一个 64MiB file-backed 单 owner fixture 中：

  - 用 F1 的最终真实 tracker primitive 做一次 track→cursor→untrack；
  - 用真实 allocator 做一次跨两个 partition的
    Retire→一次 owner RetireCount→一次 TakeRetired，确认两条 record由各自
    record_partition arena回收而 head/count只有一组；
  - enabled 下断言只增加预期的 SWCC read/write/atomic 类别，HWCC 类别不因这些
    owner-private 控制访问增加；
  - disabled 下结果和结构状态相同，instrumentation counter不更新。

- 复用同一个 fixture，不另跑 `kv_partition_test/cxl_ebr_test`；阶段 F/C 已分别
  覆盖其功能正确性，本阶段只验证最终访问域；
- 静态审计最终 `PolicyClock`、`Retire/RetireCount/TakeRetired`，确认上述字段无裸
  load/store/RMW。

#### 完成条件

最终 Clock tracker 和 owner EBR 的每个持久访问都有且只有一个正确的 private-SWCC
记录；无 lock/EBR critical section 内延迟结算，算法和 memory order 不变。

### O4. owner-private lmeta 的 latch/TID 访问漏计，现有 broad wrapper 又与 helper 脱节

#### 当前证据

- `PrivateMetadataLocal::lock/unlock` 直接调用 `pthread_spin_lock/unlock`，没有记录
  owner-private SWCC atomic；
- `RecordPrivateMetadataRead/Write` 从
  `offsetof(PrivateMetadataLocal, is_valid)` 开始，明确漏掉更前面的 `tid`；
- 这些 broad wrapper 位于 `KVPartition` 外层，而真正读写
  `tid/is_valid/is_migrated/locator` 的主体在共用 `TwoPLPashaHelper` 内；部分 early
  return/refresh 分支依赖调用者猜测 `local_refreshed` 后再补 record；
- D1/H2 切回共用 helper 后若继续保留外层补记，会同时产生漏分支、重复记录和
  helper 与 adapter耦合。

lmeta 位于 owner-private SWCC；它不是进程 DRAM。latch 使用同 VM CPU coherence
是正确的，但 latch/TID/state cacheline 仍必须按 private SWCC 计费。

#### 修改方案

1. 保留 D3与 legacy共用的 `TwoPLPashaMetadataLocalOffset`自然字段布局、
   `pthread_spinlock_t` 和 master 锁范围，不把 lmeta 搬到 HWCC/SCC，不新增
   shadow state或恢复64B复制类。
2. 在共用 helper旁定义唯一的零状态 access adapter：

   ```text
   LocalMetadataAccess<master TwoPLPashaMetadataLocal>:
     Read/Write/Lock/Unlock = no-op

   LocalMetadataAccess<TwoPLPashaMetadataLocalOffset>:
     Lock   = PrivateAtomicRmw(&latch)
     Unlock = PrivateAtomicStore(&latch)
     Read   = PrivateRead(&tid, sizeof(lmeta)-offsetof(tid))
     Write  = PrivateWrite(&tid, sizeof(lmeta)-offsetof(tid))
   ```

   它只记录访问，不执行锁、不保存 dirty bit、不改变 metadata 字段或 helper结果。
   两个 alias都定义在原 protocol metadata旁，不需要 protocol头反向include KV
   类型；access adapter只按同一storage specialization选择no-op或记录，不能复制
   一份 KV helper。
3. 每个最终共用 local-metadata critical section 固定顺序：

   ```text
   Access::Lock(lmeta)
   lmeta.lock()
   Access::Read(lmeta)             // 第一次读取任何 tid/state 前
   原 master 条件与字段访问
   若该分支实际修改任一 tid/state/locator：
       Access::Write(lmeta)        // 最后一次字段写之后、unlock 前
   Access::Unlock(lmeta)
   lmeta.unlock()
   ```

   每条 early return/exception release都遵守同一顺序。一次 critical section最多
   一个 state Read和一个必要的 state Write；latch record各一次。
4. `PrivateValueStruct::meta` 继续在实际 acquire-load/release-store 前分别使用现有
   `PrivateAtomicLoad/Store`；value payload继续用最窄 `PrivateRead/Write`，不并入
   lmeta state range。
5. 删除 `KVPartition` 中外层
   `RecordPrivateMetadataRead/Write`、`kPrivateMetadataStateOffset/Bytes` 以及根据
   `local_refreshed` 猜测补记的代码。`local_refreshed` 如不再有协议用途则删除；
   若 helper结果仍需它决定 copy/continuation，只保留结果语义，不能再用于补会计。
6. legacy master metadata 的 adapter严格 no-op，不能给进程 DRAM插入 SWCC 延迟；
   本项不改变 lmeta→smeta 锁序、SCC prepare/finish或 Busy 分类。
7. 只累计访问；任何 delay settlement仍在完整 helper/row lock释放后的 O1 外层安全
   点。

#### 必要验证

- 与 O1--O3 共用唯一一次 `latency_modes_test` gate，并复用 O3 的同一个 64MiB
  fixture；
- 在一个短序列中覆盖：

  - 直接调用一次 private read helper成功：latch lock/unlock、state read及
    read-count写各一次；
  - writer占用时直接调用一次 read helper并得到 Busy：latch/state read各一次，
    无 facade无限重试、无伪造 payload write；
  - 直接调用一次 private write helper成功：state read/write各一次；
  - 直接调用一次 migrated dirty owner-read helper：lmeta state和private mirror
    写、shared SCC read各落入正确 pool。

- disabled 下返回值/最终 lmeta相同且 instrumentation counter不变；
- 静态确认最终 helper所有 lmeta lock critical section都有上述 hook，生产中不再
  存在 `RecordPrivateMetadataRead/Write` 外层补记；
- 不重跑 H 阶段功能 target；本项只检查最终访问域，功能语义已由 D/H gate覆盖。

#### 完成条件

owner-private lmeta 的 latch、TID、valid/migrated/locator和payload访问全部由真实
helper临界区直接、无重复地记录；legacy DRAM路径不增加 SWCC会计。

### O5. `RegionAllocator` 用一个域标记同时记录 control 与 free block，动态 HWCC 必然错域

#### 当前证据

`RegionAllocator` 当前只有 `metadata_is_hwcc_`，并由同一组
`RecordMetadata*`/`RecordAtomic*` 同时记录：

- `RegionAllocatorHeader`、shard lock/bump/freelist head和传入的
  `DomainCounter`；
- 每个 allocation前置 `RegionFreeBlock` 的 `next/size_class/owner_shard`。

static HWCC allocator的两类对象都在 HWCC，global SWCC allocator的两类对象都在
SWCC；但 dynamic HWCC allocator故意把 external header/counter放在 owner-private
SWCC，把 free block和实际 allocation放在该 owner的 dynamic HWCC arena。当前为它
传 `metadata_is_hwcc=false`，因此会把 HWCC block header错误记录成 private SWCC；
若改成 true又会把真正的 owner-private control误记为 HWCC。这不是延迟参数问题，
而是现有 handle把两个物理域错误合并。

#### 修改方案

1. 不新增 allocator、wrapper class或可变判定。只把现有 process-local
   `RegionAllocator` binding中的单一 bool机械拆成两个构造时固定的域：

   ```text
   control_domain = RegionAllocatorHeader/shard/DomainCounter 所在域
   block_domain   = RegionFreeBlock及该 allocator payload 所在域
   ```

   两者使用现有 `PoolKind`或两个不可变 bool均可，但只能保存在 non-owning
   process handle，不能持久化进共享布局，也不能在热路径根据 pointer猜域。
2. 四种现有实例的绑定唯一固定：

   | 实例 | control domain | block/payload domain |
   |---|---|---|
   | VM0 startup static HWCC | HWCC | HWCC |
   | VM0 init-only global SWCC | private SWCC | private SWCC |
   | per-owner dynamic HWCC | private SWCC | HWCC |
   | per-owner shared payload SWCC | private SWCC | shared SWCC |

   C1完成后 global SWCC实例只在 VM0初始化栈内存在；attach没有该实例。
3. 现有 control helper只用于 header/shard/counter；增加的 block helper只在
   `RegionFreeBlock`真实 load/store/RMW及 allocator对新 block的初始化点调用。
   `Allocate/Free`的用户对象 payload不因 allocator本身自动记一遍；B+Tree/smeta/
   SCC调用者仍在自己的真实访问点记录，避免重复会计。
4. shared-payload block header物理在 shared SWCC，但只在该 allocation尚未发布或
   EBR后由 owner allocator访问；仍按 shared SWCC普通 read/write/atomic记录，不给
   它加 SCC或跨 VM锁。dynamic-HWCC block header按 HWCC记录，但 allocator锁和
   freelist head仍是 private SWCC。
5. `FlushAllocatedRanges/FlushOwnedRange`若在最终调用图仍使用，必须分别按 control
   与 block range的真实域记录/flush；若 C1 后无生产调用，N1直接删除，不为保留
   死接口增加双域分支。
6. 不改变 size class、bump/freelist、锁、memory order、allocation bytes或会计
   公式；L1的 underflow修复仍在原 RMW位置。本项仅修访问域选择。

#### 必要验证

- 与 O1--O4及O6共用阶段 O 唯一 `latency_modes_test` gate和同一个64MiB fixture；
- 各做一次 dynamic-HWCC 与 shared-payload allocate→free：

  - 两者的 owner control lock/bump/counter只增加 private SWCC记录；
  - dynamic-HWCC block header只增加 HWCC记录；
  - shared-payload block header只增加 SWCC记录；
  - disabled 下 allocation结果、会计和freelist状态相同且无 instrumentation计数；

- 静态确认不存在单一 `metadata_is_hwcc_`同时服务 control与block的调用，也没有
  pointer-domain推断、第二 allocator class或重复 payload记录；
- 不重跑 allocator功能 target或4VM；其算法/功能已经由阶段 C gate覆盖。

#### 完成条件

每个 allocator实例的 control和block按实际物理域唯一记录；dynamic HWCC不再被
误记为全 SWCC或全 HWCC，allocator算法与布局不变。

### O6. helper仍在逻辑row lock/ref释放前提前结算，simulator又保留嵌套scope兜底

#### 当前证据

- current `TwoPLPashaHelper::remote_read_lock_release()` 和
  `remote_write_lock_abort()` 都先调用 `DelayActiveScopeNow()`，之后才重新取得
  smeta latch并减少reader/write lock与requester ref；即使此刻没有持有smeta
  latch，逻辑row lock/ref仍在占用，busy-spin会延长其他worker的真实竞争窗口。
- `MPSCRingBuffer`的三个同名调用和 current `CxlIncomingDispatcher::Drain` 的
  per-message callback也使用“scope不结束但中途付费”来维持可见性先后；§3.11已
  明确允许ring publication先于最终软件延迟，不需要这套current-only机制。
- `LatencySimulator::ThreadState`仍有`scope_depth`；`BeginScope()`遇到已有active
  scope时静默递增，`EndScopeAndDelay()`再逐层递减。这会掩盖错误嵌套，等价于合同
  明确禁止的简化scope stack。
- 阶段C5/G1完成后，正常调用图已经可以严格分段：facade foreground →
  request enqueue后结束 → 无scope Await → peer request独立scope → continuation
  新scope；保留提前结算API和嵌套兜底没有剩余必要性。

#### 修改方案

1. 删除 `LatencySimulator::DelayActiveScopeNow()`、
   `mem_access::DelayActiveScopeNow()`及全部调用；不保留改名alias、只供测试入口或
   publication-before-delay模式。
2. 两个原 helper release/abort恢复 master的锁与状态顺序，只移除提前结算：

   ```text
   smeta latch
   → 校验并释放reader/write lock及可选ref
   → smeta unlock
   → 最终外层foreground/request scope结束时统一结算
   ```

   不移动reader/write/ref位、不增加guard，也不在helper内部调用
   `EndScopeAndDelay()`。
3. O1恢复的ring enqueue/dequeue只累计访问，完整返回后由调用阶段结束scope。
   G1唯一 `RunCxlIncomingLoop` 的每次循环迭代内部使用一个现有
   `LatencyScope(kForeground)`，范围只覆盖一次
   `reader.next_message()`以及成功时的一次worker SPSC handoff；析构发生在下一次
   reader尝试或yield之前。不得恢复`Drain(64)`、settle callback或batch scope。
4. 发送请求/response继续使用调用者已有的foreground/request scope：
   `SendTransportMessage`完成ring enqueue后，`Forward`结束请求scope再进入
   Await；peer handler在response enqueue完成并返回后结束独立scope；合法response
   decode后才开始continuation。`OperationContext`不保存scope状态。
5. 从`ThreadState`删除`scope_depth`。启用且该`ScopeKind`应计费时，
   `BeginScope()`要求当前`active==nullptr`，违反即使用现有fatal/CHECK直接揭示
   编程不变量；`EndScopeAndDelay()`只结算并清除一个active scope。disabled或该
   kind未启用的路径仍立即返回，不创建TLS entry、不读TSC。
6. 保留现有 `HasActiveScopeForCurrentThread`、
   `EndActiveScopeAndDelay`和`BeginForegroundScope`作为`Forward/PollTransport`
   的三个无状态阶段操作；它们不得保存/恢复外层scope。若 O阶段静态调用图表明
   其中某个已无调用者，N1规则在本阶段直接删除该死入口，不换成RAII栈。
7. 同步更新`延迟插入审计报告.md`：可见发布不要求提前busy-spin，唯一约束是所有
   真实访问已累计且最终结算点不持leaf、row/smeta、Clock、EBR或ring状态。

#### 必要验证

- 与 O1--O5共用阶段 O唯一`latency_modes_test`，不新增target或测试轮次；
- 删除现有两个直接调用`DelayActiveScopeNow()`的测试片段；保留并收紧已有
  request结束→无active scope→continuation新scope case，确认两阶段各结算一次；
- 同一 O1小ring case确认每次send/receive只在完整publication/handoff之后结算；
  O4既有read/write Busy case确认helper返回前已释放逻辑row lock/ref，期间没有
  TSC spin；
- 静态`rg`确认生产和测试均无`DelayActiveScopeNow|scope_depth|Drain(64)`，且
  `RunCxlIncomingLoop`每次迭代只有一个非嵌套scope；
- disabled fast gate不变，不运行额外4VM或latency workload。

#### 完成条件

软件延迟只在非嵌套阶段scope的安全末端统一结算；helper、ring和demuxer均不再用
提前busy-spin延长真实锁/ref/reservation生命周期。

## 19. 阶段 P：文档和最终验证

### P1. 多份根目录文档把目标或过期结构写成当前事实

#### 当前证据

至少存在以下不准确内容：

- `master差分allowlist.md` 声称 heavy KV 状态机和第二协议已不存在；
- `当前对比口径.md` 声称 physical/policy counter 已分开、生产协议不存在第二主体；
- `缓存一致性设计.md` 仍列出 checkpoint metadata，虽当前合同明确没有 checkpoint；
- `allocator审计.md` 声称有 remote free、每线程 TLS cache/batch refill 等当前代码
  不存在或不应保留的能力；
- `内存布局.md` 仍使用容易误导的 `PrivateRow` 名称；
- `延迟插入审计报告.md` 的 publication-before-delay 规则与 §3.11 相反；
- `验证证据.md` 只证明 Debug focused build/test 和一次很小的 no-latency 4VM E，
  不能支撑 A--E/latency/final completion。

#### 修改方案

1. 只有代码和 §5 最终验证完成后，才统一更新根目录 Markdown。
2. `当前对比口径.md` 只写最终实际调用图，不提前写目标。
3. `master差分allowlist.md` 只写已逐函数证明的差分。
4. 删除 checkpoint、remote free、TLS allocator cache、旧 PrivateRow、错误 latency
   publication 口径等失实描述。
5. `YCSB指南.md`、`当前对比口径.md`和最终实验报告在存在P2“正式公平比较前”
   字段表所定义的合格cxlkv证据时，固定把A--D的同trace比较与Workload E分开：
   E明确披露“TigonKV为range
   partition顺序原生Scan，cxlkv为单全局树Scan，非同结构工作量”，只分别报告
   结果，不能冒充同工作量吞吐；不得通过重写Tigon Scan消除该差异。没有合格
   cxlkv证据时三份文档只报告本仓A--E并明确“不作跨仓结论”，不能拿旧结果补表。
6. `下一步修改.md` 在全部任务完成后转为完成记录或清空，不得继续成为第二合同。

#### 必要验证

```bash
find . -maxdepth 1 -name '*.md' -print
rg -n 'checkpoint|16 partition|hash partition|k路|PrivateRow|remote free|\
TLS cache|promote|current-only|已完成|最终通过' -- *.md
git diff --check
```

- 检查所有相对链接和命令/target/script 名；
- 静态确认三份正式口径文档均有上述Workload E结构差异说明；有合格cxlkv证据时
  A--D汇总表/结论没有纳入E，没有时则不存在跨仓汇总表；不为这一报告检查增加
  YCSB轮次；
- 纯文档准确性修订不重跑长测；
- 若文档核对暴露代码/config 错误，修复后只重跑受影响测试及 §5 要求的最终 YCSB。

#### 完成条件

根目录全部 Markdown 与最终源码、layout version、正式配置、测试证据和公平口径一致。

### P2. 当前验证覆盖远未达到最终验收

#### 当前证据

现有 `验证证据.md` 仅记录：

- Debug `tigonkv`/`kv_partition_test` 构建；
- 一次 `kv_partition_test`；
- 一次 RelWithDebInfo、no-latency、4VM、load 100k、Workload-E run 500 的短回放。

尚未证明：

- Debug 全部 CTest；
- RelWithDebInfo 核心测试；
- load + A/B/C/D/E trace 显式生成、digest、采样 split 和 full-load 分布；
- throwaway pool 上 owner/non-owner 代表 key Get；
- Debug 4VM load/A/E；
- fresh pool 标准 A/B/C/D/E；
- latency-enabled focused A/E；
- 最终源码/二进制/config/trace 身份和干净测试编排。
- 当前证据也没有绑定一份使用同一B6 manifest、32B KV和相同解析后物理/延迟字段
  的cxlkv结果；相邻仓当前
  `src/tree/test/e2e_trace/delta_policy_config_release.jsonc`虽为32B KV，但其
  `latency_inject.enabled=true`、`cache_model=per_thread_lru`和
  `cache_hits_enabled=true`，不能与本仓正式no-latency的
  `false/none/false`仅凭“数值延迟为0”冒充相同配置。

#### 修改方案

1. 只接受绑定最终生产源码 digest、构建类型、配置 digest、trace digest、VM/backing
   身份和时间窗口的新证据。
2. 为每个 §5 条目记录命令、退出状态、四 VM marker、操作数和产物目录。
   所有guest命令都不得继承调用shell的oracle残留：
   `TIGONKV_E2E_TEST_VALUE_HEX`、`TIGONKV_E2E_REQUIRE_GET_FOUND`和
   `TIGONKV_E2E_SCAN_MAX_KEY`在非throwaway命令中固定通过`env -u`移除，
   `TIGONKV_E2E_SCAN_EXPECT_NONEMPTY`则由每条命令显式设0或1。throwaway的
   value/Get-found只作为该次workflow的命令前缀，禁止`export`；命令退出后再次
   确认三项oracle变量未进入父shell。不得依赖“新终端通常为空”的假设，也不为此
   增加持久run state。
   同理，每条真实guest workflow/CTest命令都先用`env -u`清除以下会绕过本轮解析
   config、把SSH/sync/reset指向旧VM或另一backing的兼容覆盖：

   ```text
   TIGONKV_VM_SSH_BASE_PORT
   TIGONKV_VM_SSH_KEY
   TIGONKV_VM_REMOTE_ROOT
   TIGONKV_VM_REMOTE_CONFIG
   TIGONKV_VM_REMOTE_RUNNER
   TIGONKV_SHARED_MEMORY_PATH
   TIGONKV_SHARED_SIZE_MB
   TIGONKV_SHARED_NUMA_NODE
   ```

   remote root/key因而固定使用当前正式入口的既有
   `/root/tigon2`与`/root/.ssh/id_rsa`，port/backing/size/NUMA只来自本轮config；
   binary/config的Debug/Rel选择仍由各命令下方明确的runner/pool/config变量完成。
   所有YCSB workflow还显式设置`TIGONKV_E2E_STALL_SEC=90`，不得继承调用shell的
   调试阈值。不要为这组命令前缀增加新的env清洗脚本或持久profile。
3. 历史短 E 继续作为历史调试证据，但不升级为最终验收。
4. 只在 A--O 全部完成后执行一次完整最终验证，避免施工期间反复长测。
5. P开始时重新记录`git rev-parse master`和按本文开头唯一命令计算的最终
   production digest，并保存两条production diff与untracked逐文件diff；A2的
   compiler/version、真实compile/link命令，A3的host/guest binary SHA-256，
   B6的canonical manifest SHA-256，以及最终config SHA-256都写入同一证据目录。
   目录名或旧日志不能替代内容身份。trace生成前还必须执行既有
   `scripts/tigonkv_ycsb_cpp_pin.sh`，记录
   `git submodule status -- thirdparty_libs/YCSB-cpp`、
   `git -C thirdparty_libs/YCSB-cpp status --short`、生成器脚本和实际
   `thirdparty_libs/YCSB-cpp/build/ycsb`的SHA-256。若该binary尚不存在，只允许由
   现有pinned generator首次调用时构建一次并记录其build log，不另建第二个YCSB
   build目录或重复构建。
   当前子模块内部可能已有未跟踪`build/`，顶层因而显示
   `? thirdparty_libs/YCSB-cpp`；它必须按P开始时baseline原样披露，不能冒充gitlink
   源码漂移，也不得为获得“干净status”删除用户预存artifact。除此之外若子模块
   tracked文件或HEAD变化则立即失败。
6. Debug“全部CTest”按§21隔离拆成两个串行集合，但覆盖集合不得改变：

   - 在确认无test/runner/QEMU后，先对最终源码只做一次固定Debug
     configure与clean-first build：

     ```bash
     cmake -S /root/code/tigon2 -B /root/code/tigon2/build-debug \
       -DCMAKE_BUILD_TYPE=Debug
     cmake --build /root/code/tigon2/build-debug --clean-first -j2 --verbose
     ```

     保存完整build log并确认真实compile/link命令无LTO；这是P阶段唯一完整Debug
     重建，不在两个CTest集合之间再次build；
   - 再用`ctest --test-dir build-debug -N`保存完整注册test名称；
   - 确认无QEMU/runner后，以一次`ctest -E`运行除
     `e2e_08_test|e2e_09_test|e2e_ycsb_test`外的全部测试；
   - 确认非guest进程全部退出后，清理旧VM/backing，只启动一套新4VM，以一次
     `ctest -R`串行运行上述三个真实guest测试；完成后停止guest runner和VM；
   - 两次实际执行的test名称并集必须与`-N`完整清单完全相等、交集为空。

   `e2e_ycsb_entrypoints_test`只是help/静态入口，留在非guest集合。不得以
   `RUN_SERIAL/RESOURCE_LOCK`替代“单测时没有QEMU”的进程隔离，也不得因拆分调度
   漏测或重复某个CTest。该拆分只是实现“Debug全部CTest”的隔离方式，不修改
   CMake注册、不增加setup/cleanup test或第三轮CTest。
   guest集合开始前，要求调用者已设置指向本轮新建、此前不存在的绝对目录
   `TIGONKV_FINAL_EVIDENCE_DIR`；固定创建其中的`debug-ycsb-traces`和
   `debug-guest-logs`。前者必须为空，使现有`e2e_ycsb_test.sh`自行生成本轮
   100k/100k功能trace，不能因默认`results/e2e_ycsb_traces`恰有16个旧worker文件
   就复用；后者绑定三个guest test的本轮日志。无需给wrapper增加manifest、删除
   用户旧trace或另跑准备测试。命令固定为：

   ```bash
   ctest --test-dir build-debug -N
   ctest --test-dir build-debug -j1 --output-on-failure \
     -E '^(e2e_08_test|e2e_09_test|e2e_ycsb_test)$'
   test -n "${TIGONKV_FINAL_EVIDENCE_DIR:-}" &&
     test ! -e "$TIGONKV_FINAL_EVIDENCE_DIR/debug-ycsb-traces" &&
     test ! -e "$TIGONKV_FINAL_EVIDENCE_DIR/debug-guest-logs" &&
     mkdir -p "$TIGONKV_FINAL_EVIDENCE_DIR/debug-ycsb-traces" \
              "$TIGONKV_FINAL_EVIDENCE_DIR/debug-guest-logs"
   env -u TIGONKV_E2E_TEST_VALUE_HEX \
       -u TIGONKV_E2E_REQUIRE_GET_FOUND \
       -u TIGONKV_E2E_SCAN_MAX_KEY \
       -u TIGONKV_VM_SSH_BASE_PORT \
       -u TIGONKV_VM_SSH_KEY \
       -u TIGONKV_VM_REMOTE_ROOT \
       -u TIGONKV_VM_REMOTE_CONFIG \
       -u TIGONKV_VM_REMOTE_RUNNER \
       -u TIGONKV_SHARED_MEMORY_PATH \
       -u TIGONKV_SHARED_SIZE_MB \
       -u TIGONKV_SHARED_NUMA_NODE \
       -u TIGONKV_E2E_TRACE_RUNNER \
       -u TIGONKV_POOL_INITER \
       -u TIGONKV_E2E_BINARY_DIR \
       -u TIGONKV_E2E_EXPERIMENT_CONFIG_JSONC \
       -u TIGONKV_EXPERIMENT_CONFIG_JSONC \
       -u TIGONKV_E2E09_MIXED_ONLY \
       -u TIGONKV_E2E09_INCLUDE_MIXED \
     TIGONKV_E2E_SCAN_EXPECT_NONEMPTY=1 \
     TIGONKV_E2E_THREADS=4 \
     TIGONKV_YCSB_THREADS_PER_VM=4 \
     TIGONKV_E2E_TIMEOUT_SEC=7200 \
     TIGONKV_E2E_STALL_SEC=90 \
     TIGONKV_E2E08_TOTAL_KEYS=100000 \
     TIGONKV_E2E09_TOTAL_KEYS=100000 \
     TIGONKV_E2E10_RECORD_COUNT=100000 \
     TIGONKV_E2E10_OPERATION_COUNT=100000 \
     TIGONKV_E2E_YCSB_TRACES="$TIGONKV_FINAL_EVIDENCE_DIR/debug-ycsb-traces" \
     TIGONKV_E2E_CTEST_LOG_ROOT="$TIGONKV_FINAL_EVIDENCE_DIR/debug-guest-logs" \
     TIGONKV_E2E_YCSB_CONFIG_JSONC="/root/code/tigon2/experiment_config.jsonc" \
     CXLKV_YCSB_BUILD_JOBS=2 \
     ctest --test-dir build-debug -j1 --output-on-failure \
     -R '^(e2e_08_test|e2e_09_test|e2e_ycsb_test)$'
   ```

   第一条只枚举不执行；两个`ctest`实际执行集合的test name从CTest
   verbose/log解析后做并集/
   交集核对，不以手写预计数量代替。Debug YCSB显式使用进入P前已经固化在根正式
   config中的三个split；第3项重新采样后必须证明generated config中的三个split
   与它逐字节相等，否则本轮Debug证据失效，先修正根config再从第1项重新开始，
   不能在P中途替换config后继续。三个guest test在共同
   `debug-guest-logs`中的实际子目录分别是
   `round1/e2e_08`、`round1/e2e_09`和
   `round01/round1-workloada-*`，开始前断言这些路径均不存在，结束后按各自固定
   路径取证；不得让后续单独Debug-E或Rel日志复用/覆盖此目录。
7. 正式公平比较前，从本仓和`../cxlkv`本轮实际使用的最终解析配置各输出同一张
   固定字段表，而不是人工抄注释。表中逐项必须相等：

   ```text
   shared total bytes
   HWCC offset/bytes
   SWCC offset/bytes
   VM count、每VM foreground数
   fixed key bytes、fixed value bytes
   VM CPU/内存 NUMA node
   shared backing NUMA node
   latency enabled、cache model、cache line、cache filter参数
   SWCC read/write/flush ns
   HWCC read/write/atomic-load/atomic-store/atomic-rmw ns
   ```

   任一字段不等时在启动QEMU前拒绝本轮比较；不得靠报告注明差异后继续。两仓各自
   的migration/merge policy budget不要求相等于物理HWCC，也不互相伪装成physical
   capacity，分别单列。两仓算法固有的service/background线程可以不同，但必须按
   实际CPU角色全部披露；不得为追求线程数相等而关闭TigonKV demuxer或CXLKV后台
   机制。该核对只解析现有最终配置并写证据，不修改`../cxlkv`、不新增运行轮次。
   配置表之外再固定核对双方run metadata均为load 1M、run logical 1M、A--E，
   且引用同一个B6 manifest SHA；这些不是config字段，不混入layout digest。
   本合同的§5只验收TigonKV，不授权修改或额外运行`../cxlkv`。因此cxlkv一侧只能
   读取一份**已经独立完成**、同时记录实际runtime config SHA、B6 manifest SHA、
   binary SHA和A--E marker的证据目录，不能读取相邻仓当前默认文件并假定它就是
   当时运行配置。若没有这份证据，或上表/run metadata任一项不等，本轮仍按后文
   完成TigonKV自身§5，但最终报告固定写“无可比cxlkv证据，本轮不作跨仓吞吐结论”，
   不生成A--D胜负/比例/headline，也不为填空启动cxlkv、复制改写其配置或增加运行
   轮次。以后若用户另行授权跨仓实验，必须先在cxlkv自己的工作流中生成满足上表
   的实际配置和证据，再复用本轮manifest；不能回填或重解释本轮Tigon日志。

#### 必要验证

严格执行 `partition优化方案.md` §5：

1. Debug 全部 CTest按上述两个互斥集合各运行一次；三个guest marker必须显示
   `binary_dir=build-debug`，其同步后二进制SHA-256与host Debug target相同；
2. 在无QEMU/runner状态下重新配置/构建RelWithDebInfo并运行 §5 指定核心测试；
   保存一条`tigonkv` production object编译命令和一条`e2e_trace_runner`最终链接
   命令，确认GNU均含`-flto`或Clang均含`-flto=full`，且有
   `-O3 -g3 -march=native`；Debug命令无LTO。记录双方compiler/version，不另跑
   构建轮次。固定构建命令为：

   ```bash
   cmake -S /root/code/tigon2 -B /root/code/tigon2/build-relwithdebinfo \
     -DCMAKE_BUILD_TYPE=RelWithDebInfo
   cmake --build /root/code/tigon2/build-relwithdebinfo \
     --clean-first -j2 --verbose
   ```

   `--clean-first`只用于保证同一次最终build log含真实最终源码的compile/link命令，
   不另删build目录、不在正式trace前再次build；随后运行的核心测试命令严格使用
   `partition优化方案.md` §5第2项给定正则，不增删target；
3. 用根目录唯一正式入口的**准备阶段**生成一次正式trace。先要求
   `"$TIGONKV_FINAL_EVIDENCE_DIR/formal-ycsb"`此前不存在，再固定执行：

   ```bash
   CXLKV_YCSB_BUILD_JOBS=2 \
     /root/code/tigon2/tigonkv_run_ycsb_experiment.sh \
     --out-dir "$TIGONKV_FINAL_EVIDENCE_DIR/formal-ycsb" \
     --base-config /root/code/tigon2/experiment_config.jsonc \
     --rounds 1 --record-count 1000000 --operation-count 1000000 \
     --threads-per-node 4 --workloads a,b,c,d,e \
     --shared-size-mb 32768 --sample-stride 64 --no-latency \
     --skip-build --prepare-only
   ```

   `--prepare-only`不可省略：本项只生成/采样，不在throwaway和Debug检查前提前运行
   正式A--E。不得使用入口默认的`100000/100000`，也不得在同一目录再次生成trace。
   stride=64若触发既有full-load 1.25 hard-fail则本轮停止并保留诊断；不能现场尝试
   多个stride、改热度或hash fallback，只有先修订本文明确一个新固定stride后才能
   从新的formal目录重来。按B6生成一次恰好
   96行的canonical
   `trace_sha256.txt`并在host校验；记录manifest自身SHA-256、
   `load_key_digest`、sample stride、三个split、full-load partition/owner计数，
   且trace/split无内部哨兵；四个partition/owner计数都必须非0并满足
   `max_partition_keys/min_partition_keys <= 1.25`。split工具从同一次完整load
   核对为每partition输出按FixedKey顺序遇到的第一枚真实key作为唯一代表key，证据
   重新确认它确实存在于manifest绑定的load文件；不得人工另选或使用run trace。
   generated config的三个canonical 32B split必须与进入P前根
   `experiment_config.jsonc`中已经固化、且被第1项Debug guest使用的split逐字节
   相等；只比较解析后的32B，不用JSON格式/注释文本。任何一个不同都停止本轮，
   将新split按阶段B合同固化到根config并从P第1项重跑，不能让Debug和Rel使用两套
   路由。
   同时按B6记录六个phase的`replayed_trace_operations`，不得把A展开后的trace行数
   写成logical YCSB request数；
4. throwaway检查不增加probe API/二进制，唯一按以下现有runner输入完成：

   - 新建证据目录下临时trace root，`load/worker0..15.txt`逐文件复制第3项
     manifest已校验的canonical load；不得重新生成key或修改这些文件；
   - 固定
     `known_value_hex=$(printf '5a%.0s' {1..32})`并令
     `TIGONKV_E2E_TEST_VALUE_HEX="$known_value_hex"`运行这次load，
     让现有`FixedTraceValue`把所有load value写成独立已知的32B oracle；
   - 临时`workloada/worker0..15.txt`中只让全局worker`4*i`写两条GET：
     partition `i`的代表key（owner请求）和partition`(i+3)%4`的代表key
     （该key owner为前一VM，故是non-owner请求）；其余12个文件只有comment，
     因而四VM×4 foreground仍全部在线服务，但实际GET严格为4×2；
     这16个文件只用一次性的Python binary write生成：对每个
     `representative_key_hex`执行`bytes.fromhex()`并先断言恰好32B，真实操作行
     固定为`b"GET 32 0 " + key_bytes + b"\n"`，comment行固定为
     `b"# no probe assigned\n"`。不得经shell变量/`echo`重编码尾随空格，不新增
     trace helper或probe脚本；生成后直接断言16个文件、8个非comment行和每条
     解析前字节长度均正确；
   - 用同一现有guest workflow顺序执行`load→run`，同时设置
     `TIGONKV_E2E_REQUIRE_GET_FOUND=1`和同一value hex；runner现有GET检查必须逐字节
     等于32个`0x5a`，并由B6修正后的workflow把两个变量显式传入guest；host环境中
     仅设置但远端命令未出现这两个键不算执行。四个owner与四个non-owner请求都
     成功后删除throwaway pool及
     临时trace copy，保留八条GET清单、canonical load来源SHA和四VM marker。
     workflow的log root固定为此前不存在的
     `$TIGONKV_FINAL_EVIDENCE_DIR/throwaway-get-logs`，runner/pool-initer分别显式
     指向`build-relwithdebinfo/e2e_trace_runner`和
     `build-relwithdebinfo/cxl_pool_initer`，并显式设置
     `TIGONKV_YCSB_THREADS_PER_VM=4`及
     `TIGONKV_EXPERIMENT_CONFIG_JSONC=$TIGONKV_FINAL_EVIDENCE_DIR/formal-ycsb/configs/experiment_config_ycsb_4vm.jsonc`；
     不得依赖调用shell残留环境或把日志写入
     `formal-ycsb/round_logs`。
     三个oracle变量和`TIGONKV_E2E_SCAN_EXPECT_NONEMPTY=0`必须与runner/pool路径
     一起放在该次`env`命令中；同一命令还按本节第2项清除八个拓扑覆盖并固定
     `TIGONKV_E2E_STALL_SEC=90`，不得预先export；退出后父shell仍无这些键。

   该workflow使用第2项最终RelWithDebInfo runner；不用owner第一次GET的返回值充当
   non-owner oracle，不扩大成key矩阵，也不把这次固定value load计入正式性能。
   完成后停止这套runner/QEMU/ivshmem并确认退出；下一项Debug必须从清场后新建一套
   VM开始，不能只reset pool后把Rel与Debug证据混在同一guest进程；
5. Debug 4VM×4短回放不重复第1项已经完成的A：

   - 第1项`e2e_ycsb_test`在fresh
     `debug-ycsb-traces`中生成并以Debug binary完成的`100,000`条load→
     `100,000`条A operation，直接作为本条A证据；必须有四VM pass marker、根正式
     config SHA及A3的Debug binary SHA，不能再跑第二次A；
   - 用同一已pin的`generate_cxlkv_trace.sh`只向该目录补生成
     `workloade/worker0..15.txt`：固定`--phase run --run-name workloade
     --nodes 4 --threads-per-node 4 --record-count 100000
     --operation-count 100000 --request-distribution zipfian --force`及仓内
     `workloade`文件；不重新生成/覆盖load或A；
   - 清场后启动一套新Debug 4VM，只调用现有guest workflow对workload E执行一次
     fresh pool→同一100k load→100k E，设置nonempty检查，随后停止该组。两种
     workload都使用根config中正式固化的三个split；不截取或改写第3项1M
     canonical trace，也不生成Debug manifest。stall必须GDB，不增加B/C/D、第二次
     A或重复轮次。该调用的log root固定为此前不存在的
     `$TIGONKV_FINAL_EVIDENCE_DIR/debug-e-logs`，且必须显式设置：

     ```bash
     env -u TIGONKV_E2E_TEST_VALUE_HEX \
         -u TIGONKV_E2E_REQUIRE_GET_FOUND \
         -u TIGONKV_E2E_SCAN_MAX_KEY \
         -u TIGONKV_VM_SSH_BASE_PORT \
         -u TIGONKV_VM_SSH_KEY \
         -u TIGONKV_VM_REMOTE_ROOT \
         -u TIGONKV_VM_REMOTE_CONFIG \
         -u TIGONKV_VM_REMOTE_RUNNER \
         -u TIGONKV_SHARED_MEMORY_PATH \
         -u TIGONKV_SHARED_SIZE_MB \
         -u TIGONKV_SHARED_NUMA_NODE \
       TIGONKV_E2E_TRACE_RUNNER=/root/code/tigon2/build-debug/e2e_trace_runner \
       TIGONKV_POOL_INITER=/root/code/tigon2/build-debug/cxl_pool_initer \
       TIGONKV_VM_COUNT=4 \
       TIGONKV_YCSB_THREADS_PER_VM=4 \
       TIGONKV_E2E_TIMEOUT_SEC=7200 \
       TIGONKV_E2E_STALL_SEC=90 \
       TIGONKV_E2E_SCAN_EXPECT_NONEMPTY=1 \
       TIGONKV_EXPERIMENT_CONFIG_JSONC=/root/code/tigon2/experiment_config.jsonc \
       /root/code/tigon2/scripts/e2e_trace/run_guest_ycsb_workflows.sh \
         "$TIGONKV_FINAL_EVIDENCE_DIR/debug-ycsb-traces" \
         "$TIGONKV_FINAL_EVIDENCE_DIR/debug-e-logs" 1 e
     ```

     不设置这两个Debug binary路径时下层默认会取RelWithDebInfo，不能只凭QEMU组或
     目录名称宣称Debug；本项不调用summarizer。
6. RelWithDebInfo下的“一轮标准no-latency 4VM E2E”唯一指
   `e2e_08_test`和`e2e_09_test`各一次、串行执行；不把紧接着的完整YCSB-A再通过
   `e2e_ycsb_test`重复一遍。开始前确认第5项Debug组已经全部退出，再清理backing并
   启动唯一一套新RelWithDebInfo 4VM。随后使用第3项同一份1M canonical trace，按
   `fresh pool → load → workload`分别运行 A/B/C/D/E；每组load恰好
   `1,000,000` logical records、每组run由YCSB生成器请求恰好`1,000,000`
   logical operations；各 VM都有pass marker，四VM runner `ops`之和必须等于该
   phase实际`replayed_trace_operations`。A因UPDATE→GET+PUT允许且预期大于1M，
   但报告必须按B6分开标注，不能因此把本轮称为超过1M logical YCSB。E启用
   nonempty检查。每个fresh workflow
   开始前记录backing
   path/inode/size、同一本轮四个QEMU pid/cmdline和guest boot id，确认没有旧VM/
   旧runner。标准08/09和五个YCSB workflow只使用预先清场后启动的这一套新4VM；
   “fresh pool”只调用现有pool reset，不为每个workload重启QEMU。每次重置前后
   复核四个pid/boot id未变且仍只有这一套，不能并行启动第二套；
   08/09日志固定写入此前不存在的
   `$TIGONKV_FINAL_EVIDENCE_DIR/rel-e2e-logs`，执行命令固定为：

   ```bash
   env -u TIGONKV_E2E_TEST_VALUE_HEX \
       -u TIGONKV_E2E_REQUIRE_GET_FOUND \
       -u TIGONKV_E2E_SCAN_MAX_KEY \
       -u TIGONKV_E2E_SCAN_EXPECT_NONEMPTY \
       -u TIGONKV_VM_SSH_BASE_PORT \
       -u TIGONKV_VM_SSH_KEY \
       -u TIGONKV_VM_REMOTE_ROOT \
       -u TIGONKV_VM_REMOTE_CONFIG \
       -u TIGONKV_VM_REMOTE_RUNNER \
       -u TIGONKV_SHARED_MEMORY_PATH \
       -u TIGONKV_SHARED_SIZE_MB \
       -u TIGONKV_SHARED_NUMA_NODE \
       -u TIGONKV_E2E_TRACE_RUNNER \
       -u TIGONKV_POOL_INITER \
       -u TIGONKV_E2E_BINARY_DIR \
       -u TIGONKV_E2E_EXPERIMENT_CONFIG_JSONC \
       -u TIGONKV_EXPERIMENT_CONFIG_JSONC \
       -u TIGONKV_E2E09_MIXED_ONLY \
       -u TIGONKV_E2E09_INCLUDE_MIXED \
     TIGONKV_E2E08_TOTAL_KEYS=100000 \
     TIGONKV_E2E09_TOTAL_KEYS=100000 \
     TIGONKV_E2E_THREADS=4 \
     TIGONKV_E2E_TIMEOUT_SEC=7200 \
     TIGONKV_E2E_CTEST_LOG_ROOT="$TIGONKV_FINAL_EVIDENCE_DIR/rel-e2e-logs" \
     ctest --test-dir /root/code/tigon2/build-relwithdebinfo \
       -j1 --output-on-failure -R '^(e2e_08_test|e2e_09_test)$'
   ```

   两个CTest的实际子目录是`round1/e2e_08`和`round1/e2e_09`；不得复用Debug
   日志目录，也不得运行`e2e_ycsb_test`。
   五组完成后按B6从同一manifest经stdin验证4VM×5 workload×2 phase实际remote
   worker0--3文件，不能拿host全局worker路径直接校验guest。第3项已经
   `--prepare-only`，本项**不得**以同一out-dir重跑根入口的
   `--skip-trace-gen`：当前入口会先从base重新写generated config，从而抹掉采样
   split。只调用根入口本来就调用的现有执行主体：

   ```bash
   env -u TIGONKV_E2E_TEST_VALUE_HEX \
       -u TIGONKV_E2E_REQUIRE_GET_FOUND \
       -u TIGONKV_E2E_SCAN_MAX_KEY \
       -u TIGONKV_VM_SSH_BASE_PORT \
       -u TIGONKV_VM_SSH_KEY \
       -u TIGONKV_VM_REMOTE_ROOT \
       -u TIGONKV_VM_REMOTE_CONFIG \
       -u TIGONKV_VM_REMOTE_RUNNER \
       -u TIGONKV_SHARED_MEMORY_PATH \
       -u TIGONKV_SHARED_SIZE_MB \
       -u TIGONKV_SHARED_NUMA_NODE \
     TIGONKV_VM_COUNT=4 \
     TIGONKV_YCSB_THREADS_PER_VM=4 \
     TIGONKV_E2E_TIMEOUT_SEC=7200 \
     TIGONKV_E2E_STALL_SEC=90 \
     TIGONKV_E2E_SCAN_EXPECT_NONEMPTY=1 \
     TIGONKV_E2E_TRACE_RUNNER=/root/code/tigon2/build-relwithdebinfo/e2e_trace_runner \
     TIGONKV_POOL_INITER=/root/code/tigon2/build-relwithdebinfo/cxl_pool_initer \
     TIGONKV_EXPERIMENT_CONFIG_JSONC="$TIGONKV_FINAL_EVIDENCE_DIR/formal-ycsb/configs/experiment_config_ycsb_4vm.jsonc" \
     /root/code/tigon2/scripts/e2e_trace/run_guest_ycsb_workflows.sh \
       "$TIGONKV_FINAL_EVIDENCE_DIR/formal-ycsb/traces" \
       "$TIGONKV_FINAL_EVIDENCE_DIR/formal-ycsb/round_logs" \
       1 "a b c d e"
   python3 /root/code/tigon2/scripts/summarize_ycsb_experiment.py \
     --log-root "$TIGONKV_FINAL_EVIDENCE_DIR/formal-ycsb/round_logs" \
     --out-dir "$TIGONKV_FINAL_EVIDENCE_DIR/formal-ycsb" \
     --run-meta "$TIGONKV_FINAL_EVIDENCE_DIR/formal-ycsb/run_meta.json"
   ```

   runner/pool-initer使用命令中显式给出的最终RelWithDebInfo路径；A3 marker和
   第8项SHA核对证明身份。这里不新增`--resume`、prepared状态或第二个正式脚本；
   根入口仍是唯一生成/config/metadata入口，执行时直接复用其原有下层函数。
7. RelWithDebInfo latency-enabled只运行一组focused A/E，不再生成trace或增加数据
   档位：

   - 复用第5项已生成的`100,000/100,000` Debug功能trace（它只是输入文件，runner
     改用最终RelWithDebInfo binary），顺序仍为fresh pool→load→A、fresh
     pool→load→E各一次；不运行B/C/D，不把本项作为吞吐结论；
   - 从第6项最终no-latency generated config复制一个
     `latency-focused-config.json`，只把
     `../cxlkv/src/tree/test/e2e_11/delta_policy_config_release.jsonc`当前解析后的
     `latency_inject`完整字段块机械复制为一个内存中的canonical临时对象，然后只
     按`partition优化方案.md` §3.11把该对象的
     `cache_model`固定改为`"none"`、`cache_hits_enabled`固定改为`false`，再写入
     `tigon_kv.latency_inject`；当前cxlkv profile虽为`per_thread_lru/true`，本项
     不是cache消融或性能比较，不能偏离合同顺带测试filter。除这两个明确规范化
     字段外，其余enabled/foreground/merge/stats、cache几何及所有ns字段必须与
     来源逐字段相等；同时确认Tigon的`verbose=false`、`extra_check=false`。不得
     人工挑几个ns字段、使用另一个profile或修改
     partition/HWCC/SWCC/worker/Clock配置；
   - 写入前后用canonical JSON副本删除唯一latency block后逐字节比较，必须完全
     相等；再保存来源原始block与规范化block的canonical JSON，断言二者差异键
     精确且仅为上述两个cache字段，其余字段逐值相等，并按第7项固定字段表确认
     实际focused配置为`cache_model=none/cache_hits_enabled=false`。记录来源文件
     SHA-256和新config SHA-256；不得把规范化后的Tigon block误报为与来源原文
     “逐字段相同”；
   - 复用第6项同一套Rel 4VM，仅在fresh pool前同步该config，不另启一套QEMU。
     四VM A/E都必须pass；每个VM日志的`swcc_raw/hwcc_raw`及
     `swcc_delayed_ns/hwcc_delayed_ns`均大于0，启动未出现TSC calibration fatal，
     source静态扫描仍无latency simulator `sleep_for` fallback。该检查与O阶段
     已完成的分类断言合起来满足`partition优化方案.md` §5第7项，不新增计时轮次或
     性能阈值。保存marker/SHA后
     停止runner、四QEMU和ivshmem，确认无残留再进入文档终检。调用的log root固定
     为此前不存在的
     `$TIGONKV_FINAL_EVIDENCE_DIR/latency-focused-logs`，命令固定为：

     ```bash
     env -u TIGONKV_E2E_TEST_VALUE_HEX \
         -u TIGONKV_E2E_REQUIRE_GET_FOUND \
         -u TIGONKV_E2E_SCAN_MAX_KEY \
         -u TIGONKV_VM_SSH_BASE_PORT \
         -u TIGONKV_VM_SSH_KEY \
         -u TIGONKV_VM_REMOTE_ROOT \
         -u TIGONKV_VM_REMOTE_CONFIG \
         -u TIGONKV_VM_REMOTE_RUNNER \
         -u TIGONKV_SHARED_MEMORY_PATH \
         -u TIGONKV_SHARED_SIZE_MB \
         -u TIGONKV_SHARED_NUMA_NODE \
       TIGONKV_E2E_TRACE_RUNNER=/root/code/tigon2/build-relwithdebinfo/e2e_trace_runner \
       TIGONKV_POOL_INITER=/root/code/tigon2/build-relwithdebinfo/cxl_pool_initer \
       TIGONKV_VM_COUNT=4 \
       TIGONKV_YCSB_THREADS_PER_VM=4 \
       TIGONKV_E2E_TIMEOUT_SEC=7200 \
       TIGONKV_E2E_STALL_SEC=90 \
       TIGONKV_E2E_SCAN_EXPECT_NONEMPTY=1 \
       TIGONKV_EXPERIMENT_CONFIG_JSONC="$TIGONKV_FINAL_EVIDENCE_DIR/latency-focused-config.json" \
       /root/code/tigon2/scripts/e2e_trace/run_guest_ycsb_workflows.sh \
         "$TIGONKV_FINAL_EVIDENCE_DIR/debug-ycsb-traces" \
         "$TIGONKV_FINAL_EVIDENCE_DIR/latency-focused-logs" 1 "a e"
     ```

     不调用summarizer，不覆盖`formal-ycsb/round_logs`，A/E两组日志只作分类和
     正确性证据。
8. 对正式RelWithDebInfo runner与四guest实际文件、最终config分别做SHA-256，
   与证据开头的host binary/config一致；记录
   `foreground=4 + demuxer=1`及双方全部service/background CPU、HWCC/SWCC
   physical offset/capacity和各自policy budget，不能只写总内存；保存上述两仓
   固定字段表及逐项相等检查结果。TigonKV线程披露固定分成：

   ```text
   foreground KV workers: 4，独立绑核
   inbound demuxer: 1，独立绑核
   runner main/orchestration thread: 1，使用进程allowed mask，低频等待/heartbeat，
                                     不计入kv_threads=5或吞吐分子
   ```

   第三项虽然不是KV service thread且没有专用CPU，也必须在报告中出现，不能因现有
   marker只打印`foreground/demuxer`而隐去；host watcher/SSH/QEMU/ivshmem及
   `../cxlkv`对应的runner/service/background角色另列“实验编排/虚拟化线程”，不与
   KV foreground混算。无需为披露增加新线程、绑核或运行轮次；
9. 更新文档，再执行 `git diff --check`、`git diff --cached --check`和
   `git status --short`；对状态中仍未跟踪、且确属本任务待提交的每个文本文件再
   执行`git diff --no-index --check /dev/null <path>`，其中“存在新增diff”的返回1
   正常，但任何实际whitespace诊断均失败。这样不能因文件尚未`git add`而绕过
   `diff --check`；YCSB `build/`等已披露二进制artifact不作为文本diff读取。
   同时再次执行
   `git submodule status -- thirdparty_libs/YCSB-cpp`及
   `git -C thirdparty_libs/YCSB-cpp status --short`。顶层submodule的`?`只有在
   内部差异精确等于第5项已记录的`build/` artifact baseline/本轮唯一生成产物时
   才是预期内容；任何tracked源码或gitlink变化都不是“构建噪声”。
10. 确认工作树只有预期内容后提交并推送最终文档/验收状态；不新建分支、不提 PR。

不得缩减 §5 的最终条目，也不得在每个阶段重复整套最终验证。

#### 完成条件

§5 的每个条目都有绑定最终源码/config/trace 的 fresh 证据，且没有旧 VM、旧 backing
或并行测试干扰。

## 20. 后续每个 bug 的强制记录格式

每个实际修复必须在施工记录中填写：

```text
问题编号：
复现与 Debug/GDB/assert 证据：
master 对应文件、函数及原控制流：
直接根因：
最小修改范围：
明确不修改的相邻行为：
直接复用的 master 函数：
删除的 current-only 主体：
新增 production 符号及不可复用 master 的原因：
production 新增/删除行数：
focused 测试：
所属阶段 gate（若与同阶段其他问题合并，记录同一证据目录）：
测试进程/VM/backing 隔离证明：
结果：
```

若无法给出复现/不变量证据、master 对应控制流或最小修改边界，不得用大范围重写
代替分析；“新增 production 符号”未逐个解释或存在方案预算外符号时不得进入测试。

## 21. 测试隔离和快速调试规则

### 21.1 每次测试前

1. 检查并停止遗留进程：

   ```bash
   pgrep -af '[u]nit_tests|[k]v_.*_test|[r]egion_allocator_test|\
[c]xl_ebr_test|[b]tree_binding_test|[s]cc_protocol_test|\
[l]atency_modes_test|[c]test|[e]2e_trace_runner|[e]2e_.*test|\
[e]2e_08|[e]2e_09|[c]xl_pool_initer|[r]un_guest_ycsb_workflows|\
[r]un_guest_e2e_workflows|[t]igonkv_run_ycsb|[i]vshmem-server|[q]emu-system'
   ```

   方括号写法用于避免 `pgrep`/当前 shell因命令行中包含搜索字符串而误报自身。
   test/runner/CTest/cxl_pool_initer命中一律先查明并清理。unit/focused前或创建fresh
   VM组前，QEMU/ivshmem命中同样必须清理；已经进入本轮经preflight确认的唯一4VM
   session后，四个预期QEMU和该组唯一ivshmem service是合法基础设施，阶段间只复查
   pid/cmdline/backing身份及“没有旧runner”，不得把本轮VM误杀后又并行启动一组。
2. 单元/focused 测试时不得同时存在 QEMU/guest E2E/trace runner。
3. 真实 4VM 时先运行授权的 kill/cleanup 入口，确认旧 QEMU、旧 runner、
   `/mnt/xz_shared_mem` 挂载和旧 backing 已清理。
4. 只启动一套新 VM；用 check 脚本确认四台 guest、SSH、ivshmem、NUMA、共同
   backing 和目标二进制身份。

### 21.2 调试顺序

1. 先用小数据量稳定复现，方便多轮 Debug；
2. 小数据不足以稳定复现时再增大；
3. 并发 stall/deadlock 用 Debug + GDB 获取所有 worker、demuxer、Clock、lmeta、
   smeta、ring head/tail/ready；
4. verbose 输出只保留结构化关键字段；长日志先用 `rg`、计数、最后状态和断言筛选，
   不直接让模型通读冗长输出；
5. 修复后先运行最小触发项；只在相邻协议被同一修改直接触及时检查它，随后把本
   阶段 gate 完整运行一次，不重跑无关阶段。

### 21.3 禁止事项

- unit/CTest 与 E2E 并行；
- 新旧 VM 或多个 backing runner 同时存在；
- 用降低 worker 数、sleep、timeout、固定 retry 次数掩盖竞争；
- 为小 HWCC fixture 编写复杂驱逐/回收兜底；
- 在一个 bug 中同时改消息骨架、并发模型、allocator、恢复机制和相邻模块；
- 以一次短 Workload-E 通过宣布全部方案完成。

## 22. 总完成判定

只有同时满足以下条件，本文任务才完成：

1. A--O 按固定顺序全部修复并完成各阶段唯一 focused gate，没有按问题重复长测；
2. B+Tree、TwoPLPasha helper/SCC、PolicyClock、Message/dispatcher、EBR 的生产
   主体均直接复用 master，只保留必要 offset/fixed-KV/transaction-free adapter；
3. §3.4 预算外或无法解释的 production新增符号为 0；每个 current-only算法主体
   均已删除或恢复到 master，不以新增并行实现换取通过；
4. startup 不观察半发布 layout，配置接受的组合都能真正启动；private/shared
   B+Tree 分别直接绑定 owner-private/HWCC atomic root slot，不存在进程内 root
   mirror；
5. 所有生产 RegionOffset 都先按唯一 owner/arena/domain binding验证，错域 offset
   hard-fail；
6. 所有有效功能 fixture 至少为 32MiB HWCC+32MiB SWCC，正式物理 HWCC 仍为
   1024MiB；
7. Clock tracker control/lock/node 全在 owner-private SWCC，policy/physical
   capacity 分离，second chance 与 master 一致；
8. 每 foreground worker 只有一个 RuntimeStats 权威槽；
9. Delete 无 post-retire 访问，CAS/Increment 无 locator 重定位竞态，remote insert
   rollback 从不发布 invalid placeholder；
10. Scan adjacency、锁、SCC copy 和 range move-in 与原实现及方案一致；
11. 只有真实竞争返回 Busy，损坏 hard-fail；
12. 已按阶段 O 的八列审计表充分覆盖最终 startup、B+Tree、row/smeta/SCC、全部
    KV操作、migration/Clock、allocator/EBR、transport和report调用图；每个共享/
    owner-private字段都已反查调用者，每个`mem_access`和scope结算点都已正查实际
    域与锁/pin，`延迟插入审计报告.md`与最终源码一致且无待确认项。scope严格
    非嵌套，MPSC/逻辑row lock/其它锁内没有错误busy-spin，disabled只有最小门禁，
    startup/report HWCC counter及owner-private Clock/EBR/lmeta访问都经唯一正确
    `mem_access`；
13. §5 最终验证全部 fresh、隔离、绑定最终源码；
14. 根目录全部 Markdown 与最终事实一致；
15. 每个问题按边界提交/推送，最终验收提交只在 §5 与文档终检完成后产生。

在这些条件满足前，不得把 `partition优化方案.md` 的目标误报为已实现。
