# 外部参考与搬运清单

## 延迟模拟器 TSC 忙等

- 规范参考：CXLKV 的通用访问/延迟工具和四份硬件模拟迁移指南；实现参考为
  cxlkv `984ad91a614ae65b57d0fe53ccc174bb6e962bcd`，但当前源码已成为本仓独立实现。
- 落点：`kv/engine/latency_inject.cpp` 与 `kv/engine/latency_inject.h`。
- 改写：实现四个可独立启停的模块；固定延迟不含 cache-hit/miss 模型，HWCC 和
  atomic 统计来自执行型 wrapper，远程模块使用本仓 stable pool identity、全局
  sequence/event log 和 LRU/LFU replay。只使用本仓 include、构建目标和测试，
  不引入 sibling 工程的构建或运行时依赖。

## VM 编排脚本

- 来源：参考 `../cxlkv` 的 VM 初始化职责与 QEMU/ivshmem 参数合同；基准为
  cxlkv `984ad91a614ae65b57d0fe53ccc174bb6e962bcd`。
- 落点：`tigonkv_make_vm_img.sh`、`tigonkv_init_vms.sh`、
  `tigonkv_kill_vms.sh`、`tigonkv_check_vms.sh` 与 `scripts/tigonkv_vm_common.sh`。
- 改写：使用本仓库的 mkosi、配置和 pid 文件；默认状态变更受
  `--allow-state-change` 闸门保护，未引入对兄弟工程脚本、镜像或运行时文件的依赖。

## YCSB 编排与汇总

- 来源：参考 `../cxlkv/scripts/run_ycsb_trace_experiment.sh` 的参数与产物合同；
  基准为 cxlkv `984ad91a614ae65b57d0fe53ccc174bb6e962bcd`。
- 落点：`tigonkv_run_ycsb_experiment.sh`、`scripts/summarize_ycsb_experiment.py`
  和 `YCSB指南.md`。
- 改写：trace 仅经本仓 `thirdparty_libs/YCSB-cpp` 生成；VM 回放仅调用本仓
  `scripts/e2e_trace/run_guest_ycsb_workflows.sh`。Workload E 走原生 range-partition
  Scan，已纳入正式 A--E 验收；它不与 cxlkv 的单全局树 Scan 冒充同一工作量。
