# TigonKV 本地实现边界

本仓库的固定延迟工具、VM 脚本和测试均由 Tigon2 独立维护。可以借鉴通用访问域和
TSC busy-wait 的设计原则，但最终源码只能 include/link/import 本仓库文件；运行时
只能读取本仓库的配置、镜像、binary、trace 和 backing。

## 当前落点

- 延迟实现：`kv/engine/latency_inject.{h,cpp}`、`kv/engine/mem_access.h`。
- VM 编排：`tigonkv_init_vms.sh`、`tigonkv_kill_vms.sh`、`tigonkv_check_vms.sh`、
  `scripts/tigonkv_vm_common.sh`。
- 配置与 fixture：`experiment_config.jsonc`、`tests/fixtures/*.jsonc`。
- 定向验证：`tests/latency_modes_test.cpp`、`tests/hardware_sim_disabled_benchmark.cpp`
  和 `tests/*` 的 layout/SCC/engine 测试。

实现只保留 fixed-latency line coverage、独立 thread-local pending scope 和校准 TSC
结算；不保留访问计数、原子计数、远程事件日志、replay、隐藏 reserve 或额外设备。
SCC、OLC、PolicyClock、EBR 和生产 runtime/memory accounting 属于业务路径，不能因
清理模拟模块而删除。
