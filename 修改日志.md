# TigonKV 当前修改状态

本文件只保留当前基线与最终验收状态；施工细节、原始对照和逐函数约束以
[partition优化方案.md](partition优化方案.md) 及
[master差分allowlist.md](master差分allowlist.md) 为准。

## 当前基线

- 单表、定长 32B key/value KV API；`MessagePiece.table_id` 固定为 0。
- 四个连续 range partition，4VM × 4 foreground；每 VM 另有 1 个 inbound demuxer。
- owner-private row/tree/Clock/EBR retire queue 位于 owner-private SWCC；shared
  tree/smeta/root/epoch/transport 位于 HWCC；migrated payload 只经 WriteThrough SCC
  位于 shared SWCC。
- Scan 从 `PartitionForKey(start)` 顺序跨范围：本地 owner locator，远端 CXL-first，
  adjacency 不完整时 key-only range move-in 后重探针；无混源、证书、全局 mutex 或
  k 路归并。
- `Busy` 只在 `KVStore` 的完整逻辑操作边界协作重试，直到竞争解除；真实 allocator
  失败继续报告 `OutOfMemory`，损坏的 transport/offset/会计不变量 hard-fail。

## 当前验证状态（2026-07-29）

- 本工作树只执行了必要构建、轻量 Debug `kv_partition_test` 和一个隔离 4VM
  Workload-E 回放；没有把历史 CTest、1M A--E 或 latency 结果宣称为当前源码结果。
- 回放使用 32GiB backing（HWCC 1024MiB、SWCC 31744MiB）、4 foreground + 1 demuxer、
  固定 32B key/value、100k load 和 500-op E run；load/run 四台 guest 均通过。
  完整身份、digest、Scan migration 计数和环境清场记录见
  [验证证据.md](验证证据.md)。
- latency enabled 的当前工作树验证尚未执行；它不是本轮功能正确性的前置条件，也不能
  用历史产物替代。

此状态不等同于性能结论。跨仓报告仍必须同时披露相同物理容量、NUMA、延迟参数、
trace、计时窗口、foreground=4 + demuxer=1，以及 TigonKV 原生范围 Scan 与 cxlkv
单全局树的结构差异。

## 2026-07-31：YCSB-E Scan 大数据量 stall 修复（原 bug修复.md 已并入本文与验证证据.md）

### 修复内容（提交 988d2cc）

- **根因**：`common/btree_olc_cxl/BTreeOLC_CXL.h` 的 `BTreeLeaf::split` 漏更新旧右
  兄弟的 `pre_` 回链（master 同缺陷）。split 后叶链单向损坏，叶首键在
  `_lookupForNextKeyUpdate` 回调读到错误前驱，`prev_key_real_bit` 被写错（前驱
  已迁移却仍为 0）；YCSB-E probe 对这些行永久失败，
  `Forward→move_in→re-probe fail→Busy→retry` 风暴独占 per-partition 单飞旗标与
  Clock，4VM 全 `ops=0`。
- **补丁**：`BTreeLeaf::split` 补 `newLeaf->next_->pre_ = newLeaf`（镜像
  erase-merge 回链 + 同域 `RecordTreeDataWrite`），共 9 行；不碰 Clock/SCC/消息
  骨架/Scan 协议，不新增状态机。
- **legacy 树取舍（维持 KV-only）**：`common/btree_olc/BTreeOLC.h` 保持 master
  原样（同款漏回链属有意不修）：生产路径只用 `btreeolc_cxl`；legacy 树仅
  source-only 参考、不编译进 `tigonkv`；仓库合同要求该文件保持 master
  （`下一步修改.md` §J1、`partition优化方案.md`、`master差分allowlist.md`）。
- **验证**：Rel 20k/25k/50k×2/100k/1M（正式入口）E 与 Debug 100k
  （`STALL_SEC=90`）全部 PASS；unit/kv_layout/btree_binding/kv_partition/
  kv_engine 测试 PASS。详细证据与复现命令见
  [验证证据.md](验证证据.md)「YCSB-E Scan 大数据量 stall 根因与修复证据
  （2026-07-31）」。

## 当前验证状态（2026-07-31 最终验收，HEAD=988d2cc）

- 正式 1M A/B/C/D/E（每 workload fresh pool）全部 PASS：A run 34.4s、B 27.9s、
  C 28.0s、D 33.6s、E 507.2s（scan_ops=950,195），均 replayed 1,000,000 ops。
- Debug 全量 CTest 19/19 PASS（含 guest e2e_08/09/ycsb）。
- latency-enabled 4VM focused A/E PASS（cxlkv profile 派生、cache_model=none；
  `TIGONKV_LATENCY_STATS` 确认注入生效）。
- throwaway pool owner/non-owner 8×GET PASS（value=32×0x5a、RequireGetFound）。
- 全部证据目录：`/mnt/xz_vm_storage/tigon2-final-acceptance-20260731T134818Z/`；
  详见 [验证证据.md](验证证据.md)「最终提交 988d2cc 全面验收证据」。
- 已知缺口收口状态见 [最终版校验.md](最终版校验.md)（§8 全部标记完成）。
