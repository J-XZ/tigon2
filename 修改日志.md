# TigonKV 当前修改状态

本文件只保留当前基线与最终验收状态；施工细节、原始对照和逐函数约束以
[partition优化方案.md](partition优化方案.md) 及
[master差分allowlist.md](master差分allowlist.md) 为准。

## 当前基线

- 单表、定长 32B key/value KV API；`MessagePiece.table_id` 固定为 0。
- 四个连续 range partition，4VM × 4 foreground；每 VM 另有 1 个 inbound demuxer。
- owner-private row/tree/Clock/EBR retire queue 位于 owner-private SWCC；shared
  tree/smeta/root/epoch/transport 位于 HWCC；migrated payload 只经 WriteThrough SCC
  位于 shared SWCC。
- Scan 从 `PartitionForKey(start)` 顺序跨范围：本地 owner locator，远端 CXL-first，
  adjacency 不完整时 key-only range move-in 后重探针；无混源、证书、全局 mutex 或
  k 路归并。
- `Busy` 只在 `KVStore` 的完整逻辑操作边界协作重试，直到竞争解除；真实 allocator
  失败继续报告 `OutOfMemory`，损坏的 transport/offset/会计不变量 hard-fail。

## 当前验证状态（2026-07-29）

- 本工作树只执行了必要构建、轻量 Debug `kv_partition_test` 和一个隔离 4VM
  Workload-E 回放；没有把历史 CTest、1M A--E 或 latency 结果宣称为当前源码结果。
- 回放使用 32GiB backing（HWCC 1024MiB、SWCC 31744MiB）、4 foreground + 1 demuxer、
  固定 32B key/value、100k load 和 500-op E run；load/run 四台 guest 均通过。
  完整身份、digest、Scan migration 计数和环境清场记录见
  [验证证据.md](验证证据.md)。
- latency enabled 的当前工作树验证尚未执行；它不是本轮功能正确性的前置条件，也不能
  用历史产物替代。

此状态不等同于性能结论。跨仓报告仍必须同时披露相同物理容量、NUMA、延迟参数、
trace、计时窗口、foreground=4 + demuxer=1，以及 TigonKV 原生范围 Scan 与 cxlkv
单全局树的结构差异。
