# TigonKV 当前修改状态

> 本文件只保留对下一步施工仍有用的状态，不再保存逐提交历史。
> 下一步工作的唯一实施清单是
> [partition优化方案.md](partition优化方案.md)；原始 Tigon 代码基准为当前
> `master` 分支头部。

## 当前基线

- 当前工作分支：`my-work`。
- 本次裁剪时 HEAD：`3d1a8e09be38`。
- 项目目标：去掉原始 Tigon 的昂贵事务层和实际不用的多表开销，把原本位于
  计算节点本地 DRAM 的私有结构迁到 SWCC 的 owner-private 区域，同时保持
  HWCC/SWCC 使用纪律，并通过同一套字符串 KV 接口与 `../cxlkv` 公平比较。
- 设计取舍优先级：复用原始 Tigon 路径；只在原代码无法复用时补最少代码；
  不额外优化原始实现已有的低性能因素，也不以降低并发度换取通过测试。

## 已具备的基础

- 已有单表 KV facade、四 VM 运行框架、MPSC transport、请求 demuxer、
  owner-private SWCC arena、共享 HWCC 索引/元数据和共享 SWCC/SCC payload。
- transport 损坏采用进程级 hard-fail；前台统计已与多 worker 并发相容。
- 软件延迟模拟已有统一配置、启用门禁、TSC busy-spin 和 disabled 快速门。
- 已有范围路由框架，但当前边界生成、核心操作、迁移与 Scan 仍需按实施方案
  进一步恢复原始 Tigon 语义，因此当前实现不能作为最终性能口径。

## 尚未完成

以下内容以 `partition优化方案.md` 的详细说明为准：

1. 用显式、连续、均衡的范围边界替换当前不正确的默认边界和哈希遗留语义。
2. 修复共享元数据并发协议，改为固定长度 value，并删除多余的长度状态。
3. 恢复原始 `TwoPLPashaExecutor`/`ITable`/B+Tree adapter、锁和 TID 规则。
4. 让 Put、CAS、Increment、Delete、Clock eviction/migration 和 Scan 尽量直接
   复用原始 Tigon 路径，不再维护平行实现。
5. 只在外部 facade 保留必要的 Busy 重试，内部错误和损坏不得软吞。
6. 功能稳定后重新审计软件延迟；最后删除被替代的代码、调试脚手架和过期文档。

## 施工顺序

1. 完成范围配置和布局校验。
2. 完成共享元数据、固定 value 和原始 adapter/锁语义。
3. 依次恢复 Put/Delete、Clock/migration、Scan 和 facade 重试。
4. 先在关闭软件延迟时完成针对性测试和四 VM 功能测试。
5. 功能无残留 bug 后，按
   [延迟插入审计报告.md](延迟插入审计报告.md) 重新审计并测试延迟注入。
6. 清理死代码和文档，再执行最终验证。

## 当前验证口径

- 现有结果只证明当前提交曾通过部分 Debug、单元和 local-fork 测试，不构成
  完成上述重构后的验收结果。
- 范围边界和核心路径尚未收敛前，不发布正式性能结论。
- 每次修复先重复触发问题的最小测试；功能稳定后再扩大到四 VM 和 YCSB。
- 若功能修复改变了内存访问路径，必须在该功能通过测试后重新检查相应延迟埋点。

## 维护规则

后续只更新“当前基线、未完成事项和验证状态”。不要再追加逐日、逐提交或已被
替换方案的历史记录；需要追溯时直接使用 Git 历史。
