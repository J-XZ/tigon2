# Tigon2 实施日志

开始时间：2026-07-17 UTC

## 基线审计

* 工作目录：`/root/code/tigon2`。
* Tigon2 初始 SHA：`ccd567a50116b7bada06df71a3bf0a07c424572e`。
* cxlkv `my-work` SHA：`93aa007a60ce072993498d443e0e822599b0adfb`。
* YCSB-cpp SHA：`746415127173e7711f134944dbcd92b8216c47e7`。
* 初始状态：仅用户提供的 `PLAN.md` 未跟踪；没有覆盖已有源码修改。
* 初始 VM/QEMU/Tigon/CXLKV 进程：0；`/dev/ivpci0` 不存在；
  `/mnt/xz_shared_mem/ivshmem_shared_mem` 不存在。
* `/mnt/xz_vm_storage` 和 `/mnt/xz_shared_mem` 目录存在；未创建或删除 backing。
* NUMA：2 node；node 0 CPU 0--31、node 1 CPU 32--63；距离 32；SMT active=0。
  当前没有运行进程可用于页位置采样。宿主机状态未修改。

## 架构决策

* KV 对外只暴露统一 namespace；table/partition/owner 仅在内部路由。
* stable FNV-1a hash 计算 partition，owner 为 `partition % vm_count`。
* 固定布局 mmap slot 使用 offset 可重建引用；新 key 的非 owner 请求逻辑转发到 owner
  创建 private row，remote access 在共享锁内 promotion 为 `SHARED_ACTIVE`。
* shared lookup 使用 hash index，SCAN 使用持久化 sorted slot-id index；两者都属于
  逻辑 HWCC layout，不在本地 DRAM 保存按 KV 数量增长的 shadow index。
* shared payload 使用 Tigon SCC WriteThrough 语义：完整写入、write-back、释放
  shared lock；本实现的文件-backed test path 用 `msync` 表示实际 write-through。
* 首次 shared publication 的线性化点是 slot state 从 PRIVATE 写为 SHARED_ACTIVE，
  并且在这之前初始化整个 value、key 和 metadata。
* `RETIRING` 的实验约束由 tombstone + 双 index 摘除 + 全局锁 grace fast path +
  reclaim counter 实现；默认不缓存无 shared index 的 payload。
* `NonCoherentSwccTestBackend` 显式区分每 host cache、visible backing 和 publication，
  fault injection 拒绝 dirty payload 的过早发布。
* 延迟模拟按显式 PoolKind/AccessKind 计数，支持 fixed-hit-rate/per-thread-LRU 模型，
  并在锁释放后才 drain 软件延迟。

## allocator 决策

现有闭源静态库 SHA256：
`1048cfb372ddc5feea484a881cf4e8dfa153a5938f6ec83ec88343f0a5b39636`。
由于本机没有 `/dev/ivpci0`、backing 或运行中的 QEMU VM topology，原 cxlalloc 的真实 ivshmem multi-VM
capability 不能宣称通过。已实现并运行普通 mmap 文件的 capability matrix：offset
稳定、attach/restart、跨实例共享、process-shared lock、free/reuse、OOM、容量/域
统计、checkpoint 和重复 migration。最终选择 `allocator_mode=global`：一个共享
映射加显式逻辑 domain，`physical_region_split=0`，HWCC 逻辑预算固定不超过 1 GiB，
`unclassified_shared_bytes=0`。真实设备能力若失败，下一步应切换 dual-region，不能
silent fallback。

## 修改模块

* `kv/kv_store.*`：统一 PUT/GET/DELETE/SCAN/CAS/INCR、mmap layout、持久化
  open-addressed shared index、owner/private/shared 状态、checkpoint、统计；index
  publication 在 value 初始化并 write-back 后才发生；index/layout 本身计入逻辑
  HWCC 使用和 allocator overhead；backing 初始化/attach 使用文件锁，checkpoint
  在共享协议锁内写回，失败或禁用 clean checkpoint 时保持 dirty marker；完整解析
  experiment_config 的 NUMA/VM/网络/同步/allocator 策略字段，并纳入 attach fingerprint。
* `kv/memory_domains.h`：AccessClass、PoolDomain、PersistentPtr。
* `kv/latency_simulator.*`：HWCC/SWCC raw access、flush、cache hit/miss 和 delayed_ns；
  fixed-hit-rate 与容量受限 per-thread LRU；非硬件一致性/发布故障注入后端。
* `CMakeLists.txt`：C++17、Debug/RelWithDebInfo、CTest、KV/e2e/allocator target；
  保留原 bench targets；独立 `latency_modes_test` 覆盖 disabled/none/fixed-hit/LRU。
* `experiment_config.jsonc`、`内存布局.md`、`缓存一致性设计.md`、`allocator审计.md`：
  配置、布局、对象分类和能力证据。
* `tools/numa_placement_probe`、`tools/cxl_pool_initer`：只读 `move_pages` 页采样和显式
  backing 初始化，不移动页或改变 NUMA policy；指定 expected node 时无 resident 页、查询
  失败或 misplaced 页返回 hard-fail，而不是报告空样本通过。
* `tools/e2e_trace_runner.cpp`、`scripts/e2e_trace/*`：严格解析 cxlkv 的无空格 key
  格式、reset/attach contract、SCAN 校验、ready/done barrier、YCSB A-E 调用入口；
  concurrent workflow 自动使用 `/mnt/xz_vm_storage` 下的 fresh barrier；失败时输出
  trace/phase/line/op/key/partition/owner 和当前机器可读统计快照。
* `tests/e2e_08.cpp`、`tests/e2e_09.cpp`：100000 key 的本地多实例
  remote-promotion、shared update、完整 value 校验和 delete/reclaim；CTest 的
  `e2e_trace` 现在是真实 fixture replay，不再是占位 alias。
* `tests/tigonkv_tests.cpp`：增加真正跨进程的 remote promotion/shared update、dirty
  close 拒绝 attach，以及 child runtime flush 统计回传检查；覆盖 root experiment_config
  的公平性字段解析；shared row 读引用计数和 retiring guard 由完整 suite 覆盖。
* `scripts/vm/*`、`tests/e2e/*`：只复用现有 VM，缺少拓扑时 hard fail；不创建网络、不重启 VM。
  同步不再删除远端文件；停止进程要求显式 PID 文件和 executable path；SSH runner
  保存每节点日志并检查 pass marker。
* `THIRD_PARTY_NOTICES.md`、`搬运清单.md`：来源和独立性记录。

## 构建和测试

已执行：

* `cmake -S . -B build -DCMAKE_BUILD_TYPE=Debug`。
* `cmake --build build -j2 --target unit_tests allocator_tests e2e_trace_runner`。
* `./build/unit_tests`：通过，覆盖 config、private KV、promotion、restart、delete/reinsert、
  CAS/INCR、strict owner guard、延迟和 NonCoherent backend；含 fork multi-process、
  thread concurrency 和 slot reuse。
* `cmake --build build -j2`：通过；原 `bench_tpcc`/`bench_ycsb`、KV、probe、pool
  initializer 和所有测试 target 均可编译。
* `ctest --test-dir build --output-on-failure`：Debug 15/15 通过。
* `ctest --test-dir build --repeat until-fail:10 --output-on-failure`：通过；本地套件
  重复 10 轮无失败；最新修改版本运行完成且未见失败输出。
* `cmake --build build-rel -j2` 与 `ctest --test-dir build-rel --output-on-failure`：
  RelWithDebInfo 15/15 通过。
* `ctest --test-dir build-rel --repeat until-fail:10 --output-on-failure`：最新源码的
  RelWithDebInfo 全量 CTest 15 项重复 10 轮完成，未出现失败输出，总耗时 669.16 秒。
* 添加独立 `latency_modes_test` 后，最新 Debug/RelWithDebInfo 全量 CTest 均 16/16
  通过，总耗时分别为 103.82 秒和 103.36 秒。
* trace smoke：`cxl_pool_initer /tmp/tigonkv-trace-backing 16` 加 `e2e_trace_runner`
  回放 5 条记录并输出 pass marker、memory/runtime stats；临时 backing 不属于正式
  `/mnt/xz_shared_mem` 路径。
* `scripts/vm/check_environment.sh`：按预期 hard fail，因为正式 backing 和
  `/dev/ivpci0` 缺失；没有启动 VM 或修改宿主机。

* `e2e_08` 100000 个 8B key/value 通过，最近一次输出
  `E2E_08_PHASE_TIME_US=357998`、`E2E_08_OP_P50_US=0`、`E2E_08_OP_P99_US=133`、
  `E2E_08_STRESS_TIME_US=11502583`；结束时 `active_shared_rows=0`、payload 使用量为
  0、`logical_hwcc_used_bytes=6148284`、`reclaimed_total_bytes=438400000`、
  `rss_kb=2539920`，private flush=0、shared runtime flush=84805。
* `e2e_09` 100000 个 32B key、1000B value 的 generation 0/1 更新和完整 value
  校验通过，最近一次 `E2E_09_PHASE_TIME_US=11392674`；结束时
  `active_shared_rows=0`、shared payload 为 0、HWCC used=6148284、reclaimed bytes=
  438400000、RSS=2533128，private flush=0、shared runtime flush=100000。
* YCSB smoke 使用同一 YCSB-cpp SHA、10000 records/operations、2 worker traces，
  load + A/B/C/D 共 16 个 trace process 全部输出 pass；这是本地顺序 smoke，不是
  正式多 VM 轮次。
* YCSB 默认规模 smoke 已执行：100000 records/operations、2 worker traces，A-D 共
  16 个 process、E 共 4 个 process，均 pass；worker 按 node0/node1 交替分配，E 最后
  一次 node1 统计为 `logical_hwcc_used_bytes=14807624`、private payload=896241、
  shared payload=68777771、`unclassified_shared_bytes=0`、active rows=103675。
  另以 `TIGONKV_E2E_CONCURRENT=1` 完成 E 10k 并发 worker smoke。它们仍是单机共享
  backing，不替代真实 VM/网络并发轮次。
* 最新 workflow smoke：A、100 records/operations、2 workers，load/run 两阶段并发
  barrier 均通过；shared promotion 非零，`private_swcc_flushes=0`。
* 最新 latency/NUMA smoke：容量为 1/2 line 的 LRU eviction、KV latency enabled/disabled
  统计均通过；未 prefault backing 的 NUMA performance probe 返回 rc=3，functional probe
  只读报告 resident_pages=0。

真实多 VM、e2e_08/e2e_09 多轮、YCSB 多轮和正式 backing 页 placement：本机没有现成
VM/backing/device，且没有运行中的 QEMU VM topology；PLAN 禁止未经允许启动/重启 VM，因此没有伪造通过；安全 runner
会在状态不满足时退出。只读 probe 对临时未 prefault backing 报告 resident_pages=0，
没有把该结果当成正式 NUMA placement 证据。

YCSB-E：SCAN 已实现 sorted-index binary search、排序/limit 校验，并完成 100k 本地
load+run 与 10k 并发 worker smoke；仍未完成正式多 VM 多轮。

## 最终统计接口

`KVStore::DumpStats()` 输出 `TIGONKV_MEMORY_STATS`、`TIGONKV_RUNTIME_STATS`，包括
logical HWCC/SWCC usage、owner-private/shared payload、allocator overhead、unclassified
bytes、active rows、promotion、private/shared flush、checkpoint flush、reclaim、RSS 和
latency/cache hit-miss。checkpoint flush 在运行时窗口之外单独计数；普通 private
operation 不计 SCC flush。

## 已知限制

1. 真实 cxlalloc ivshmem backend 尚未在缺失设备的环境中验证；当前 global wrapper 使用
   mmap backing，不能替代真实多 VM 页一致性测试。
2. 当前公共测试覆盖的是可重复本地/多实例模型；SSH runner 和 VM helper 不会在无拓扑
   时自动创建或重启资源。
3. crash recovery 不在承诺范围内；active/dirty 状态不得被当作 clean restart。
4. 当前环境无运行中的 QEMU VM topology、`/dev/ivpci0` 和正式 backing，因此多 VM SSH、真实页 NUMA
   placement、完整规模多轮 E2E/YCSB 尚未执行；安全脚本只做状态检查并 hard fail。

本地 commit 列表：`96a9aff`（row ref/retiring guard、failure diagnostics）、
`1dfd548`（公平性 fingerprint mismatch hard-fail）、
`e205410`（完整 experiment_config 解析与 fingerprint）、
`ec3e671`（LRU/latency runtime、NUMA probe hard-fail、验证日志）、
`3b2b1b7`（checkpoint/并发初始化、跨进程验证、YCSB barrier）、
`f05e497`（indexed layout、预算/回收、SCC/latency、SCAN/YCSB、
  E2E 和 NUMA probe 收尾）、`dfda5fb`（实现）、`f9c0b09`（验证日志）、`331c8a8`（提交记录）、
`8a78533`（owner 字段修正）、`0312c94`（indexed layout、真实 e2e_08/e2e_09、trace
runner/YCSB 修复、统计和最终 CTest wiring）、`f68b874`（最终验证日志）。当前工作树
的 `git diff --check` 无输出。未 push 任何远程。
