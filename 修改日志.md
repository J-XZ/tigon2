# TigonKV 改造修改日志

## M5 回归：SCAN transport 回压

- 日期：2026-07-24
- 改动：双进程 `kv_engine_test` 增加 1,024 个 node 0 owner 条目的跨 owner
  SCAN 验证；测试量超过 1MB transport 配置下每入环 256 条消息的容量，以覆盖
  流式回传时的 enqueue backpressure 和请求方轮询排空。
- 验证：`cmake --build build-relwithdebinfo --target kv_engine_test -j4`；
  `timeout 30s build-relwithdebinfo/kv_engine_test` 通过。

## M6 检查点：门面作用域与实际访问统计

- 日期：2026-07-24
- 改动：新增 `kv/engine/mem_access.h`，将 private-row 和 MPSC transport 访问
  接入进程级 `LatencySimulator`；门面每个前台操作建立/结束安全作用域，关闭时
  经 relaxed gate 立即返回。`DumpStats` 的 latency 段现在输出真实快照而非常量。
- 验证：`cmake --build build-relwithdebinfo --target unit_tests latency_modes_test
  kv_partition_test kv_engine_test -j4`；相关 CTest 4/4 通过。`unit_tests` 在
  启用注入后断言 `swcc_reads`/`swcc_writes` 非零。
- 未完成项：B+树、shared payload 的细粒度 wrapper 与上游完整 simulator API
  仍待接线；本检查点不宣告 M6 完成。

## M6 检查点：SCC、transport 与 checkpoint wrapper

- 日期：2026-07-24
- 改动：MPSC ring 的真实入队/出队 memcpy、`SCCManager` 的 clflush/clwb 以及
  allocator checkpoint flush 均改经 `mem_access` 记录。引擎层原有的重复
  transport 记账已删除。
- 验证：`cmake --build build-relwithdebinfo --target unit_tests kv_engine_test
  scc_protocol_test latency_modes_test -j4`；相关 CTest 4/4 通过。门面测试断言
  启用注入后 `swcc_reads`、`swcc_writes`、`swcc_flushes` 均非零。

## M6 检查点：按分配域的 B+ 树访问记账

- 日期：2026-07-24
- 改动：`BTreeOLC_CXL` 的 lookup/insert/remove/scan 公共路径按实例的 concrete
  allocation binding 记录至少一个树页访问；owner-private 绑定归 SWCC，shared
  index 绑定归 HWCC，保持无虚调用的热路径选择。
- 验证：`cmake --build build-relwithdebinfo --target btree_binding_test
  kv_partition_test unit_tests -j4`；对应 CTest 3/3 通过。

## M6 检查点：shared payload 访问记账

- 日期：2026-07-24
- 改动：迁移行的 GET/PUT/CAS/INCR、move-in/out 与 SCAN 在调用 SCC payload
  read/write 前统一记录 SWCC 访问；私有与 shared payload 统计保持不同 wrapper。
- 验证：`cmake --build build-relwithdebinfo --target kv_partition_test
  kv_engine_test unit_tests -j4`；对应 CTest 3/3 通过。

## 回归修复：trace fixture 的单进程路由与布局容量

- 日期：2026-07-24
- 改动：`e2e_trace_test` 的单进程 fixture 改为单 VM，避免将 trace key 转发给
  未启动的远端 owner；HWCC/SWCC 划分调整为 4MB/12MB，并使小 fixture transport
  环为 1MB 以满足物理容量约束。YCSB fixture 同步采用 1MB transport 环。
- 验证：`timeout 30s ctest --test-dir build-relwithdebinfo -R
  '^e2e_trace_test$' --output-on-failure` 通过（1/1）。此前完整构建发现的
  配置错误与等待问题均已复现并消除。

## M8 检查点：独立 YCSB 编排与汇总

- 日期：2026-07-24
- 改动：新增 `tigonkv_run_ycsb_experiment.sh`、独立 Python 汇总器和
  `YCSB指南.md`。编排器生成本仓 YCSB 4-VM 配置、run metadata、load/run trace，
  并在真实拓扑已存在时复用本仓 guest workflow；未完成 Scan 验收的 workload E
  硬失败并标为 `ycsb_e=unsupported`。
- 验证：`bash -n tigonkv_run_ycsb_experiment.sh`、`python3 -m py_compile
  scripts/summarize_ycsb_experiment.py`；`ctest --test-dir build-relwithdebinfo
  -R '^(vm_scripts_test|ycsb_scripts_test)$' --output-on-failure` 通过（2/2）。
  `--prepare-only --skip-trace-gen` 已生成配置、metadata 与空日志汇总；未执行
  trace 构建、VM 创建或 guest 回放。

## M8 检查点：独立 VM 脚本与无副作用预检

- 日期：2026-07-24
- 改动：新增项目根 `tigonkv_make_vm_img.sh`、`tigonkv_init_vms.sh`、
  `tigonkv_kill_vms.sh` 与 `tigonkv_check_vms.sh`。脚本独立解析本仓 JSONC
  配置、校验 NUMA/CPU/共享池契约，精确按 pid 文件处理 QEMU；初始化的实际
  backing/VM 状态变更必须显式传 `--allow-state-change`，`--dry-run` 仅打印
  QEMU/ivshmem 参数。
- 验证：对全部新 shell 文件执行 `bash -n`；`tigonkv_init_vms.sh --dry-run`
  成功输出 4 个 QEMU 命令；新增 CTest `vm_scripts_test`，通过（1/1）。本机未
  运行真实 VM 创建、终止或镜像构建；`shellcheck` 在当前环境中不可用。

## M6 检查点：TSC 安全点延迟补齐

- 日期：2026-07-24
- 改动：`LatencySimulator::DrainPending()` 改用一次性校准的 TSC 忙等，在 x86
  上以 `_mm_pause` 自旋至累计目标；无有效 TSC 校准时仍使用可移植休眠兜底。
  延迟仍只在操作安全点补齐，不在协议临界区内等待。
- 验证：`cmake --build build-relwithdebinfo --target latency_modes_test -j4`；
  `ctest --test-dir build-relwithdebinfo -R '^latency_modes_test$' --output-on-failure`
  通过（1/1）。

## M5 检查点：完整统计键导出

- 日期：2026-07-24
- 改动：`KVStore::DumpStats()` 现在完整输出 `MemoryStats` 的 allocator/回收
  字段，以及 `RuntimeStats` 的 private/shared、checkpoint、retry 和 network
  字段，保持既有分段格式。门面单测断言关键新键存在。
- 验证：`cmake --build build-relwithdebinfo --target unit_tests -j4`；
  `ctest --test-dir build-relwithdebinfo -R '^unit_tests$' --output-on-failure`
  通过（1/1）。

## M5 检查点：跨 owner SCAN 转发

- 日期：2026-07-24
- 改动：在持久 MPSC 协议中增加 `kScanRequest`、`kScanItem` 与 `kScanDone`。
  发起节点本地扫描其分片，并按 owner 向远端请求有序条目；每个远端 owner 完成
  本地分片合并后流式回传，发起端再作全局字典序合并并应用 `limit`。保留 1,048,576
  的安全上限和迁移重试错误传播。
- 验证：`cmake --build build-relwithdebinfo --target kv_engine_test -j4`；随后
  `timeout 30s build-relwithdebinfo/kv_engine_test` 通过。测试通过 fork 建立两个
  VM，覆盖节点 1 请求节点 0 的 SCAN 条目与本地条目的全局聚合。

## M0 基线

- 日期：2026-07-24
- 工作分支：`tigonkv-plan`
- 本仓工作起点：`a8fc218ec712f3cbba55093b9a510158da994e29`（计划内历史起点 `8ab9294bbd29c1cc5e668a78779e524a8f805961`）
- 原始 Tigon 引擎基准：`ccd567a50116b7bada06df71a3bf0a07c424572e`
- cxlkv 对照：`my-work` @ `984ad91a614ae65b57d0fe53ccc174bb6e962bcd`（计划钉死 SHA：`e282a65a7e4f76ac1f9f772f99301d96f7fca5de`）
- YCSB-cpp gitlink：`746415127173e7711f134944dbcd92b8216c47e7`
- 基线偏离：当前 cxlkv 比原计划 SHA 新增 CloudLab R650 配置、根实验配置和
  VM 清理脚本修正；已在 `PLAN.md` 更新对照 SHA，后续以本仓独立实现对齐其合同。
- 构建工具链：系统 GCC 11.4 不接受 Clang 专用 `-flto=full`；CMake 对 GNU 使用
  语义等价的 `-flto`，其他编译器仍使用 `-flto=full`。

## M0 完成记录

- 日期：2026-07-24
- 检查点：`ae7697e711af0b27e500229251318fa47fe7992a`（M0 基线、构建合同和
  cxlkv 对照 SHA 更新）。
- 主机审计：未发现 QEMU/ivshmem VM 进程或现有 ivshmem 共享文件；NUMA node 0
  与 node 1 均可用，距离矩阵为本地 10、远端 20。
- 验证：`cmake -S . -B build-relwithdebinfo -DCMAKE_BUILD_TYPE=RelWithDebInfo`
  与 `cmake --build build-relwithdebinfo -j2` 成功；
  `ctest --test-dir build-relwithdebinfo --output-on-failure` 为 16/16 通过。
- 偏离决策：旧 benchmark 仍在本次 M0 构建中编译；后续按 PLAN.md 范围从默认
  构建入口移除不再维护的旧 target，不删除原始源码。

## M1 检查点：共享布局 ABI

- 日期：2026-07-24
- 检查点：`f592fc31ab71f5c73b34cae6380f431a897c4d8a`。
- 新增 `kv/engine/kv_types_layout.h`：固定 key 字节比较、区域内 offset、
  SharedLayoutHeader、根表、附着校验和按域的 used/peak 计数。所有持久引用
  约定为 offset，避免不同进程 mmap 基址不一致。
- 验证：使用 RelWithDebInfo 等价编译选项执行
  `c++ -std=c++17 -O3 -g3 -march=native -flto -I. -x c++ -fsyntax-only`，通过。

## M1 检查点：持久区域分配基础

- 日期：2026-07-24
- 检查点：`2fd6c88` 新增 `region_allocator` 的持久头、attach 校验、64B 对齐
  分配、offset 转换与按域记账；`2934057` 新增 attach/offset/记账的 CTest。
- 验证：`cmake --build build-relwithdebinfo --target region_allocator_test -j2` 与
  `ctest --test-dir build-relwithdebinfo -R '^region_allocator_test$' --output-on-failure`
  通过（1/1）。
- 尚未宣告 M1 完成：free/reuse、remote-free、静态 shard、backing-file 映射和
  `CXLMemory` 就地路由尚未接通，不能代替 §6.1-1 完整能力矩阵。

## M1 检查点：持久分片与回收

- 日期：2026-07-24
- 检查点：`d21e1c9c6f76bcceba66b36c07a6628b55ff3717`（持久分片 allocator）。
- 改动：`RegionAllocator` 升级为区域内固定 shard 目录、1.25× size class、
  本地 free-list 复用和跨 VM remote-free 栈；所有链路用区域 offset，远端发布
  采用 cacheline writeback + fence。新增同 mmap 文件的独立映射/进程 attach、
  remote-free 收割、并发、OOM 与按域统计测试。
- 验证：`cmake --build build-relwithdebinfo -j2 --target
  allocator_capability_test allocator_multi_process_test allocator_multi_vm_test
  allocator_restart_attach_test allocator_global_pool_test allocator_concurrency_test
  allocator_reclaim_test allocator_accounting_test allocator_domain_budget_test
  allocator_local_dram_test`；随后
  `ctest --test-dir build-relwithdebinfo -R '^(region_allocator_test|allocator_)'
  --output-on-failure`，11/11 通过。
- 仍未完成：双物理区域的配置/映射、owner-private arena 与 `CXLMemory`/EBR
  就地路由尚未接通，因此本检查点不是 M1 退出记录。

## M1 检查点：双区域物理边界

- 日期：2026-07-24
- 检查点：`bd06e1267340bc597b38e5414c157bee7b25fe0e`。
- 改动：新增 `DualRegionAllocator`。一个映射池按配置的 HWCC/SWCC offset 和
  size 划分为两个不重叠的持久 allocator；HWCC index/metadata/EBR/layout/
  transport 域固定路由至 HWCC，owner-private 和 shared-payload 域固定路由至
  SWCC，跨区域 free hard fail。attach 校验 layout hash、区间、VM/partition
  数和 fixed key/value 规格。
- 验证：RelWithDebInfo 下 allocator 相关 CTest 11/11 通过（同上命令）；新增
  dual-region 地址范围、独立 attach、按域 used 计数和跨区拒绝的覆盖。
- 仍未完成：从 `Config` 创建/映射该布局、owner-private arena 细分以及
  `CXLMemory`/EBR 的就地路由。

## M1 检查点：配置合同收敛

- 日期：2026-07-24
- 检查点：`76f6c2333afd24fd391472c6da7d3df82cbe1ba7`。
- 改动：`Config`/JSONC 改用 `hw_cc_budget_mb`、
  `owner_private_swcc_fraction`、`migration_policy=Clock`、
  `when_to_move_out=OnDemand`、`scc_mechanism=WriteThrough` 与
  `transport_ring_total_mb`；删除旧 `hwcc_budget_mb`、`hwcc_reserved_mb`、
  `shared_payload_swcc_fraction` 的解析入口。延迟注入字段改为与计划一致的
  per-line/atomic/cache schema；根配置和测试 fixtures 同步。
- 验证：RelWithDebInfo 下 `unit_tests`、`e2e_tests`、`e2e_trace_test` 3/3 通过；
  parser 回归测试覆盖旧键 hard fail。
- 仍未完成：NUMA 整数数组解析、完整字段必填检查与新 dual-region engine 的
  映射接线。

## M1 检查点：默认构建移除旧 allocator 链接

- 日期：2026-07-24
- 检查点：`66ca0363320ca03dc34f2e49dfc87a90a21b54f7`。
- 改动：从默认 CMake 图移除旧 transaction benchmark 与 `misc_cpp`，从而不再
  链接 `dependencies/cxlalloc/libcxlalloc_static.a`；原始源码保留作后续就地迁移
  参考，符合 PLAN.md 的“原始文件只改不删”规则。
- 验证：RelWithDebInfo 全量构建完成；`unit_tests`、10 个 allocator tests、
  `e2e_tests` 均通过。全量 CTest 在现有 `e2e_08` 卡于不可中断 I/O（D 状态），
  已按精确 PID 终止本轮测试父进程；该 e2e 未计为通过，待 M7/M8 harness 重做后
  重新验收。

## M1 检查点：backing-file 映射与安全块头

- 日期：2026-07-24
- 检查点：`bce9adbe3b085bac7a07d96e5d8bc1d456a2ebf8`。
- 改动：新增 `DualRegionMappedPool`，以 `flock` 串行化初始化，支持 reset 创建与
  不同虚拟基址上的无写入 attach；同时将 free-list 块头置于用户 payload 之前，
  避免 allocator 元数据被写入的 value 覆盖。新增 fork 后重映射同一文件、offset
  可达性和跨区域分配的回归。
- 验证：RelWithDebInfo allocator 相关 CTest 11/11 通过。
- 仍未完成：将该 mapped pool 接入 KV 引擎创建路径与原始 `CXLMemory`/EBR。

## M2 检查点：KV 持久布局

- 日期：2026-07-24
- 检查点：`0b8bc392b776b7d5245438f6cfba72f625bd4fbc`。
- 改动：扩充 `kv_types_layout.h`，加入 owner-private `PrivateRow` 布局和每
  partition 的私有/共享 B+树根、arena offset、migration sequence 目录项。
  所有持久引用继续使用 region offset，不新增 raw pointer。
- 验证：RelWithDebInfo `kv_layout_test` 与 `region_allocator_test` 2/2 通过。
- 仍未完成：将 BTreeOLC_CXL 节点分配绑定到私有/共享分配域，并完成树根 attach。

## M2 检查点：B+树创建域绑定

- 日期：2026-07-24
- 检查点：`0f48983f38952920e1e29395439dd8f36655e61d`。
- 改动：`BPlusTree` 新增构造期 `TreeNodeAllocation` binding；root、leaf split、
  inner split 与新 root 直接调用绑定的 `DualRegionAllocator`，不再走默认
  `INDEX_ALLOCATION`。FixedKey 改为平凡定长 32B 字节布局，以便复用原树的
  trivial-key split/merge 路径。`CXL_EBR` 的 retire record 增加 owner shard，
  最终 free 改为直接调双区域 allocator。
- 验证：RelWithDebInfo `btree_binding_test` 通过：私有与共享树各插入、split、
  lookup 400 个 FixedKey，且分别计入 SWCC/HWCC 域；树/allocator 相关测试
  尚待下一步汇总重跑。
- 仍未完成：BTree remove/merge 的 EBR retire 接线、树根持久 attach 与 KV
  partition 组装，故本检查点不代表 M2 退出。

## M2 检查点：B+树回收与跨映射恢复

- 日期：2026-07-24
- 检查点：`7c24c6ac9d89619cc82dd6ca8ee57bc68ac338f2`。
- 改动：节点页不再保存进程内 allocator/EBR 指针；split、merge 和回收由树实例
  显式传入运行时 binding，因此持久节点仅保留可重映射的 offset 链接。新增根指针
  导出及 attach 构造路径，root 由调用方保存为相应 region offset；所有 leaf/inner
  merge 与删根 retire 均使用绑定的 EBR 和 owner shard。
- 验证：RelWithDebInfo `btree_binding_test` 通过：私有和共享树各插入 400、删除
  200、查验剩余键；随后 fork 子进程以同一 backing file 的新映射 attach 私有树根，
  查找 200 个保留键均成功。
- 仍未完成：用 `PartitionDirectoryEntry` 实际创建每 partition 的两棵树和私有 arena，
  并由新的 KV 引擎统一管理其 root/attach 生命周期。

## M2 检查点：partition 树目录组装

- 日期：2026-07-24
- 检查点：`9bbfca233f4fda7bd8d7cc807dd1d31351cc65df`。
- 改动：新增 `kv_partition`，为一个 partition 直接持有私有 SWCC 树和共享
  HWCC 树，并将两者根写入 `PartitionDirectoryEntry` 的区域 offset。私有行按
  `PrivateRow` 格式从 owner-private SWCC 域分配；attach 从目录重建两棵树句柄。
- 验证：RelWithDebInfo 下 `kv_layout_test`、`btree_binding_test`、
  `kv_partition_test`、`region_allocator_test` 4/4 通过。partition 测试覆盖
  private put/update/get/delete 与 fork 后不同映射地址 attach。
- 仍未完成：SWCC 内 owner-private arena 的静态分段及其专属 allocator、共享行
  协议和新 `KVEngine` 对所有 partition 的生命周期管理。

## M2 检查点：owner-private SWCC 静态 arena

- 日期：2026-07-24
- 检查点：`51d976d788a1f2c59fd32fdeb192ab6951cc2a66`。
- 改动：SWCC 在初始化时按 `owner_private_swcc_fraction` 保留固定前缀并按
  partition 等分；通用 SWCC allocator 显式跳过该区，避免 shared payload 与
  私有对象交叠。每个 arena 持久化 owner、边界和 bump 状态，目录写入 arena
  offset；私有树 binding 与 PrivateRow 均改为该 arena 的直接分配入口，非 owner
  分配 hard fail。
- 验证：RelWithDebInfo `kv_layout_test`、`btree_binding_test`、
  `kv_partition_test`、`region_allocator_test` 4/4 通过；测试断言目录 arena
  offset、私有树根地址范围和 owner 违规拒绝。
- 仍未完成：owner-private arena 的 EBR 安全回收 freelist（M4）、共享行协议与
  引擎组装；因此 M2 尚未退出。

## M2 检查点：CXLMemory 双区域直接路由

- 日期：2026-07-24
- 检查点：`790be08ab65fb5473ce3f3c6675604cfff1f9a24`。
- 改动：`CXLMemory` 不再将旧 cxlalloc 调用一律抛错；显式绑定当前
  `DualRegionAllocator` 后，INDEX/METADATA/TRANSPORT/MISC 直接路由 HWCC，
  DATA 路由 shared SWCC，且原根表接口存取 `SharedLayoutHeader::roots` 的 HWCC
  区域 offset。free wrapper 仍仅做旧类别记账，安全释放继续由 EBR 执行。
- 验证：RelWithDebInfo 相关 4 项 CTest 通过；`kv_partition_test` 额外验证
  wrapper 的 HWCC/SWCC 地址落位与 root 发布/恢复。`btree_binding_test` 新增
  FixedKey scan（删除后闭区间）覆盖。
- 仍未完成：新 `KVEngine` 的创建路径绑定该 allocator；CXLMemory 的旧事务调用者
  尚未迁入 KV 引擎，不能作为 M1/M2 完成声明。

## M3 检查点：SCC 整池 offset 基址

- 日期：2026-07-24
- 检查点：`cbc2acda8579819c33863155eb9df408f0ffb8cd`。
- 改动：`DualRegionAllocator` 提供受范围校验的整池 offset 编解码；
  `TwoPLPashaMetadataShared` 不再使用 cxlalloc 的堆偏移函数，改为把 SCC payload
  作为“pool base + 37 位 offset”编码，超出 37 位立即断言。全仓生产代码中已无
  `cxlalloc_pointer_to_offset` / `cxlalloc_offset_to_pointer` 调用。
- 验证：RelWithDebInfo 构建 `tigonkv` 和 `kv_partition_test` 成功；测试验证
  SWCC payload offset 往返及 < 2^37 限制。
- 仍未完成：KV 专用 shared-row 读写锁/SCC 路径、NonCoherent 后端和迁移协议。

## M3 检查点：NonCoherent SCC 后端

- 日期：2026-07-24
- 检查点：`6b53f675e7e4ca71f8e8207dd349c79d0b54289a`。
- 改动：新增仅测试目标链接的 `tests/scc_test_backend.*`，为每个模拟 host
  维护独立 cache/dirty copy，只有 WriteBack 更新 visible copy、Invalidate 才
  刷新远端 cache；Publish 在仍有 dirty 字节时拒绝。
- 验证：RelWithDebInfo `scc_protocol_test` 通过 14 条正反断言，覆盖缺
  write-back/缺 invalidate 读旧值、错误提前发布被拒绝、第二 host 写回后的
  可见性，以及重复 invalidate 的记账。
- 仍未完成：将该语义接到 KV shared-row 的实际 read/write/move-in/out 调用链，
  并完成 §4.3 的真实 WriteThrough 协议测试。

## M3 检查点：KV shared-row SCC 调用序

- 日期：2026-07-24
- 检查点：`bb76e3e0f2d1f25fdc33f919d15a529c96c025a5`。
- 改动：在原 `TwoPLPashaHelper` 添加无事务 KV shared read/write 路径，继续
  使用 `TwoPLPashaMetadataShared` 的 latch、reader_count、write bit 及 SCC
  元数据。读侧 reader pin 覆盖 `prepare_read` 到 `do_read`；写侧等待零 reader，
  `do_write → valid → finish_write` 后才清 write bit。生产库现在链接
  `SCCManager.cpp` 的全局 manager 定义。
- 验证：RelWithDebInfo `kv_shared_protocol_test` 通过，使用记录型 SCC manager
  验证跨 host 写/读数据、prepare/read/write/finish 次序计数及操作结束后的
  reader/write lock 清零。
- 仍未完成：将该路径接入 `KVPartition` 的 shared 树和 `is_migrated` 分支；
  move-in/out 仍未完成。

## M3 检查点：owner migrated 分支

- 日期：2026-07-24
- 检查点：`ef251a90a9f48d394b69ae33a525dbd993305fb0`。
- 改动：`KVPartition::GetPrivate` 现在持 PrivateRow latch 检查
  `is_migrated`：未迁移仅复制私有 value；已迁移仅经 shared B+树查 smeta 并走
  `kv_shared_read`，不再把 PrivateRow 的旧 value 作为权威。新增 promotion
  路径按 payload/smeta 分配、SCC metadata 初始化、shared tree 可达、设置
  `migrated_smeta_off/is_migrated`、`finish_write` 的顺序发布。
- 验证：RelWithDebInfo 4 项相关 CTest 全通过；partition 测试含 owner put →
  promotion → owner read，且 fork 后 attach 仍经 shared 行读回正确值。
- 仍未完成：move-out 三条件 quiescence、shared delete 和 EBR retire/回收，进入 M4。

## M4 检查点：owner move-out 与 EBR retire

- 日期：2026-07-24
- 检查点：`177885d6bffe157de6c3ee5bd922b88a970d13db`。
- 改动：新增 `KVPartition::MoveOutPrivate`。它持 PrivateRow latch 后，从 shared
  树确认 smeta，持 shared latch 检查 `payload.ref_cnt==0 && reader_count==0 &&
  !write_locked` 三条件；满足时设写锁、SCC 读回私有行、清迁移壳、摘 shared 树，
  最后把 smeta 与 shared payload 按 owner shard 放进 `CXL_EBR` retire 队列。
- 验证：RelWithDebInfo 4 项相关 CTest 通过；partition 测试覆盖 put → move-in →
  owner shared read → move-out → 私有读，并断言 migration 序列号。
- 仍未完成：等待/重试 quiescence 的并发场景、强制 epoch drain 和 Clock 预算驱动。

## M4 检查点：quiescent EBR drain

- 日期：2026-07-24
- 检查点：`a33f247f56df9568a3ec91c373e8b16741d36814`。
- 改动：`CXL_EBR::drain_quiescent` 在调用者已满足 quiescence 时排空本线程所有
  retire epoch bucket，按原 category、owner shard 调双区域 allocator free；它是
  checkpoint/move-out 的显式回收动作，未将 msync 伪作 SCC 或 epoch。
- 验证：RelWithDebInfo `kv_partition_test` 在 move-out 后 drain，断言 HWCC
  metadata 与 shared SWCC payload 的 domain used 计数下降；相关测试继续通过。
- 仍未完成：多 worker 等待/重试与 Clock 预算触发的批量 move-out。

## M4 检查点：owner-private EBR 可复用回收

- 日期：2026-07-24
- 检查点：`3f02946ccc8eed411e00af8c3d11c17f48b6f7e2`。
- 改动：owner-private arena 现在以 cacheline 块头与持久 free-list 分配，free 后可
  同地址重用。`CXL_EBR::retired_object` 扩展 private partition；B+树私有节点
  retire 将 partition 传入 EBR，epoch/drain 时调用 `FreeOwnerPrivate`，不再把
  私有节点错误送至通用 SWCC allocator。
- 验证：RelWithDebInfo 相关 4 项 CTest 通过；partition 测试断言 owner-private
  alloc/free 后同地址复用，并保留 move-out/EBR drain 计数回落覆盖。
- 仍未完成：并发 quiescence retry、Clock 预算与完整引擎组装。

## M5 检查点：owner-partition KVEngine 装配骨架

- 日期：2026-07-24
- 检查点：`a40e9010d0623d636d738f2e575f44e759552ccd`。
- 改动：新增 `kv/engine/kv_engine.*`，以映射的双区域池、EBR 与
  WriteThrough SCC 组装 owner 所属 partition；路由严格为
  `partition = FNV1a(key) % partition_count`、`owner = partition % vm_count`，
  非 owner 先明确返回待 M5 transport 接管的 `kOwnerViolation`。同步修正
  persistent owner-private arena 的 owner 映射；EBR 的有界 worker 元数据扩至
  64，避免原 benchmark 的 5-worker 上限否决合法 KV 实验配置。
- 验证：RelWithDebInfo 重新生成并构建 `kv_engine_test`/`kv_partition_test`；
  `ctest --test-dir build-relwithdebinfo -R '^(kv_engine_test|kv_partition_test)$'
  --output-on-failure` 为 2/2 通过。新测试覆盖单 owner PUT/GET/DELETE、clean
  attach 后读取持久值，以及双 VM 路由与 remote 拒绝。
- 仍未完成：messages/ring 转发、所有 owner 的初始化协调、SCAN/checkpoint/
  stats 门面与 Clock 预算驱动；本检查点不宣称 M5 已完成。

## M4 修正：EBR worker identity

- 日期：2026-07-24
- 检查点：`2f6ed504c29f8ad2ec4e4afac6d078cbfa65e426`。
- 改动：`CXL_EBR::thread_init_ebr_meta` 改为使用调用者给定的 worker id，并对
  coordinator/thread 范围断言；删除会跨独立引擎实例递增、最终越界的进程全局
  thread-id 计数器。
- 验证：RelWithDebInfo `kv_engine_test`、`kv_partition_test` 2/2 通过。

## M4 检查点：迁移行的单权威更新与物理删除

- 日期：2026-07-24
- 检查点：`5050968573a39a0d4413381072524f09b8600ad9`。
- 改动：迁移后的 owner PUT 现在持 PrivateRow locator latch 后只经
  `kv_shared_write` 更新 SCC payload，私有壳不再保存/暴露新值；owner GET 将
  locator latch 覆盖 shared 查找与读取，避免 move-out 在查找和读之间回收。
  DELETE 在已迁移状态下同时摘 shared/私有树、使 payload 无效，并将 smeta、
  payload 与 owner-private 行全部送入 EBR；未迁移删除也改为 EBR 回收私有行。
- 验证：RelWithDebInfo `ctest --test-dir build-relwithdebinfo -R
  '^kv_partition_test$' --output-on-failure` 通过。测试新增 migrated PUT 后
  owner read 的 shared 权威断言、migrated DELETE 后 miss/EBR drain/重新 PUT。
- 仍未完成：Clock victim tracker、预算阈值与 quiescence 重试，M4 尚未完成。

## M4 检查点：owner-local Clock tracker

- 日期：2026-07-24
- 检查点：`878a2615d54d01f27d77d5e3126926e034dceeea`。
- 改动：每个 `KVPartition` 维护仅 owner 可见的有界 DRAM Clock key tracker；
  move-in/删除/move-out 同步增删，attach 从 persistent shared B+树扫描重建。
  使用原 SCC metadata word 的 bit 37 作为 second-chance 位，shared GET/PUT 与
  move-in 置位；Clock 首轮清位、下一轮调用既有三条件 quiescence move-out。
  分配器同时公开 shared payload 的真实可分配容量，供 90% 高水位预算策略接线。
- 验证：RelWithDebInfo `kv_partition_test` 通过；新增测试覆盖 Clock 首轮跳过
  热 key、第二轮选择并 move-out、owner 读取回私有值及 payload 用量/容量边界。
- 仍未完成：在 KVEngine 的 move-in/转发路径按 HWCC 与 90% payload 高水位
  自动触发 Clock（该路径随 M5 messages 一并接线），以及失败 victim 的 16 次
  有界重试。

## M5 检查点：persistent ring KV forwarding

- 日期：2026-07-24
- 检查点：`14e221bdd2e5d64c209dbecfc4766c9b815a7126`。
- 改动：新增固定大小、无虚拟地址的 `kv_messages.h` 编码，复用原
  `MPSCRingBuffer`：reset 节点在 HWCC 分配并通过 transport root 发布每 host
  一个 2048B 入环；attach 节点按 root 恢复。KVEngine 在同步等待转发响应时也
  轮询本机环并服务请求，因此无需专职服务核。PUT/GET/DELETE 非 owner 均可
  发往权威 owner；remote GET 命中会触发 move-in，并接线 HWCC/90% payload
  高水位下至多 16 次 Clock victim 尝试。配置新增 node id 与 transport-HWCC
  容量边界校验。
- 验证：RelWithDebInfo `ctest --test-dir build-relwithdebinfo -R
  '^(kv_engine_test|kv_partition_test)$' --output-on-failure` 为 2/2 通过。
  engine 测试以 fork 的两个独立映射进程覆盖 node1→node0 的 PUT/GET/DELETE
  请求、owner 轮询服务和响应回传。
- 仍未完成：non-owner shared-tree hit 的本地 SCC 快路径、CAS/INCR/SCAN 请求、
  KVStore 薄门面、checkpoint 与完整统计。

## M5 修正：转发响应有界等待

- 日期：2026-07-24
- 检查点：`24be6ab`。
- 改动：转发请求使用 `sync_timeout_sec` 限制等待 owner 响应的时长；超时返回
  明确错误，避免 owner 未启动或失联时前台 worker 无限自旋。
- 验证：RelWithDebInfo `kv_engine_test` 通过。

## M5 检查点：分区内迁移一致性扫描

- 日期：2026-07-24
- 改动：`KVPartition::ScanOwned` 同时采样私有树与 shared B+ 树，并以 private
  locator 的迁移状态为准合并两侧权威值；扫描前后若 migration-in/out 序号变化则
  有界重试，避免迁移切换期间混入旧私有副本或重复项。结果按 key 排序并在分区层
  应用 limit。
- 验证：RelWithDebInfo `kv_partition_test` 通过；新增断言覆盖已 move-out 行、
  删除行、替换私有行的有序合并以及 limit 截断。
- 仍未完成：引擎跨 partition/跨 owner 的 SCAN 编排与 transport 请求、CAS/INCR
  和 KVStore 薄门面。

## M5 检查点：owner 单行 CAS/INCR

- 日期：2026-07-24
- 改动：owner 分区新增以 PrivateRow latch 覆盖完整 RMW 的 CAS/INCR；行已迁移时
  仍只读取并更新 shared SCC payload，不会回退读取 locator 中的旧值。INCR 严格
  校验完整 `int64` 文本及溢出。
- 验证：RelWithDebInfo `kv_partition_test`、`kv_engine_test` 2/2 通过；覆盖私有
  与 migrated CAS、失败 CAS、INCR 及引擎返回的 `kCompareFailed`。
- 仍未完成：CAS/INCR 的跨 owner 消息分段编码和转发；本检查点不将 remote 路径
  误报为已支持。

## M5 检查点：单 VM 全 partition SCAN 归并

- 日期：2026-07-24
- 改动：KVEngine 现在调用各 owner-local partition 的迁移安全扫描，并在引擎层
  做有序去重、全局 limit 截断；同时落实 1,048,576 条安全上限。跨 owner SCAN
  尚未有完整流式响应编码，因此明确返回错误而非静默漏掉远端 partition。
- 验证：RelWithDebInfo `kv_engine_test` 通过，覆盖跨 partition 合并后的有序
  `alpha/counter` 结果。

## M5 检查点：跨 owner INCR 转发

- 日期：2026-07-24
- 改动：定长 transport 新增 `kIncrement`。请求携带十进制 `int64` delta，owner
  在其 PrivateRow latch/SCC 原子 RMW 路径执行后回传十进制结果；响应端严格校验
  格式，拒绝损坏帧。该编码只传递一个小整数，不改变 2048B ring entry 合同。
- 验证：RelWithDebInfo `kv_engine_test` 通过；fork 的 node1→node0 场景覆盖远端
  创建计数器、INCR(+2) 与结果 3。
- 仍未完成：CAS 的双最大值分段请求、跨 owner 流式 SCAN，以及薄门面替换。

## M5 检查点：跨 owner CAS 分段转发

- 日期：2026-07-24
- 改动：CAS 采用 `kCasPrepare`（expected）与 `kCasCommit`（desired）两个固定帧；
  owner 在 process-local pending 表按 request id/source/key 关联，在 commit 时才进入
  分区的原子 CAS。由此同时支持两个最大 1000B 值，而不破坏 2048B ring contract。
- 验证：RelWithDebInfo `kv_engine_test` 通过；fork 场景覆盖 node1→node0 成功 CAS
  与 `kCompareFailed`，随后仍可继续远端 INCR。
- 仍未完成：跨 owner 流式 SCAN、薄门面、checkpoint 与统计。

## M5 检查点：新引擎内存统计来源

- 日期：2026-07-24
- 改动：KVEngine 直接从双区域 persistent domain counter 汇总 HWCC、owner-private
  SWCC、shared-payload SWCC 用量，并由 owner-local Clock tracker 汇总 active shared
  rows；不再依赖旧 slot/header 的伪统计模型。
- 验证：RelWithDebInfo `kv_engine_test`、`kv_partition_test` 2/2 通过。
- 仍未完成：将该统计接到 KVStore/DumpStats，checkpoint 的 EBR drain 与按已用范围
  cacheline writeback，以及旧门面删除。

## M5 检查点：allocator 范围 checkpoint

- 日期：2026-07-24
- 改动：新增按 RegionAllocator shard bump 高水位和 owner-private arena bump 范围的
  cacheline flush/fence；KVEngine checkpoint 先有界轮询本机 transport、drain EBR，
  再执行这些范围 writeback 并更新 persistent clean epoch/state。未调用 `msync` 或
  `fdatasync`，也未将 checkpoint 叙述为 SCC。
- 验证：RelWithDebInfo `kv_engine_test` 通过，新增 checkpoint 后 clean attach 读取
  持久值覆盖。
- 仍未完成：KVStore 薄门面调用此路径、跨 owner SCAN 及日志/统计协议接线。

## M5 检查点：KVStore 薄门面切换

- 日期：2026-07-24
- 改动：`KVStore` 运行时路径现在仅委托 `KVEngine`，CHECKPOINT 调用新的 allocator
  range writeback；CMake 将 glog 作为 tigonkv 的传递依赖，修复门面真正引用引擎后
  的链接缺口。原 slot/global-lock/msync 源与其测试暂以编译期排除保留，供 M10
  物理删除审计，绝不再进入运行路径。
- 验证：RelWithDebInfo `unit_tests` 通过；新门面测试覆盖 PUT/GET/SCAN/CAS/INCR
  not-found/Checkpoint/clean attach/Delete 与物理 region split 统计。
- 仍未完成：清除临时 retired 源块、跨 owner SCAN、完整统计字段和新多 VM 测试。

## M5 修正：CAS/INCR 新键 upsert

- 日期：2026-07-24
- 改动：owner CAS 在 key 缺失且 expected 为空时创建 desired；owner INCR 在缺失时以
  delta 作为初值创建。两者仍由 owner 私有树路径执行，远端转发自然复用该语义。
- 验证：RelWithDebInfo `unit_tests`、`kv_partition_test`、`kv_engine_test` 3/3
  通过；覆盖 private 新 CAS、新 INCR 与 facade 新计数器。

## M5 修正：门面操作间隙轮询 transport

- 日期：2026-07-24
- 改动：KVStore 的每个前台操作在委托 KVEngine 前轮询本 VM 入环，落实无专职服务
  核的 worker-poll 模型；未新增公开 API。
- 验证：RelWithDebInfo `unit_tests` 通过。

## M5 修正：非整除 partition 路由

- 日期：2026-07-24
- 改动：移除 `partition_count % vm_count == 0` 的非合同配置限制；唯一 owner 仍严格
  由 `partition % vm_count` 决定，因此尾部不均匀 partition 同样具备合法 owner。
- 验证：RelWithDebInfo `unit_tests` 通过，新增 7 partition/2 VM 配置验证。

## M5 检查点：真实 transport 字节统计

- 日期：2026-07-24
- 改动：`KVEngine` 在固定帧写入对端 ring 与从本机 ring 成功解码时，分别以原子
  计数累计实际 TX/RX 字节；`KVStore::Runtime()` 与 `DumpStats()` 现从存活引擎汇总
  该统计，不再恒为零。
- 验证：RelWithDebInfo `unit_tests`、`kv_engine_test` 2/2 通过；跨进程路由测试断言
  两端通信后引擎收发字节均大于零。

## M5 修正：跨 owner SCAN 并发请求

- 日期：2026-07-24
- 改动：SCAN 先为全部远端 owner 注册并发请求并统一发出，再由本地 ring 轮询收集
  所有响应，避免一个 owner 的完整扫描阻塞其余 owner 的请求启动；TX 字节仅在
  消息成功入 ring 后计入。
- 验证：RelWithDebInfo `unit_tests`、`kv_engine_test` 2/2 通过。
- 仍未完成：协议级有界流式 cursor/k-way heap 归并；当前每个远端响应仍暂存其
  `ScanItem` 向量，不能满足最终 SCAN 内存上界合同。

## M5 修正：non-owner shared 快路径

- 日期：2026-07-24
- 改动：所有 VM 现在恢复每个 partition 的持久树句柄，但 owner 私有路径仍由
  `OwnedPartition` 严格判定。非 owner 对已迁移行会在映射的私有行锁保护下确认
  shared-tree/smeta，再通过 SCC 执行 GET/PUT/CAS/INCR；共享 miss、并发 move-out
  或新键仍转发 owner。创建根时以稳定 partition owner 绑定 owner-private arena。
- 验证：RelWithDebInfo `kv_partition_test`、`kv_engine_test`、`unit_tests` 3/3
  通过；跨进程测试断言 remote GET promotion 后两次 CAS 不新增 TX 字节。

## M5 修正：shared 句柄不扩大 owner SCAN 范围

- 日期：2026-07-24
- 改动：所有节点恢复所有 shared-tree 句柄后，`ScanOwnedPartitions` 显式只遍历
  `partition % vm_count == node_id` 的 owner 分区，防止分布式 SCAN 从每个节点
  重复返回同一 partition。
- 验证：RelWithDebInfo `kv_engine_test` 通过。

## M7 检查点：e2e_08 本机双进程 owner 服务

- 日期：2026-07-24
- 改动：旧 e2e_08 的同进程双 `KVStore` 模型替换为 node0 请求方加 fork 出的
  node1 `KVEngine` worker-poll 服务进程；控制 pipe 只用于测试结束，不参与数据面。
  共享行统计改由持久 shared tree 观察，避免非 owner 进程的本地 Clock 缓存过期。
- 验证：RelWithDebInfo `e2e_08` 100k（填充、跨 owner 随机读、SCAN、删除）通过；
  `ctest -R '^e2e_08_test$'` 1/1 通过。
- 仍未完成：RuntimeStats 的迁移/SCC 分项仍需从新引擎实际路径汇总；e2e_09 及多 VM
  rounds 尚未验证。

## M7 检查点：e2e_09 本机双进程大 value 路径

- 日期：2026-07-24
- 改动：e2e_09 同样替换为 node0 请求方与 fork node1 worker-poll owner 服务；保留
  100k、固定 1000-byte value 的 fill/update/read/delete 合同，并在删除后对 node0
  执行 checkpoint。
- 验证：RelWithDebInfo `e2e_09` 100k 完整通过；`ctest -R '^e2e_09_test$'` 1/1
  通过。
- 仍未完成：remote owner 的 EBR retire 必须经所有 owner drain 后才回收，当前 node0
  单边 checkpoint 不足以使全局 shared-payload used 回零；该全节点同步/回收验证留作
  §6.3.2-D 的未完成项。

## M7 修正：全 owner EBR drain

- 日期：2026-07-24
- 改动：e2e_09 测试控制面增加 checkpoint/drain 命令和确认；删除后 node0 与 node1
  均执行 checkpoint，才验证全局 shared-payload used 回零。控制 pipe 不进入数据面。
- 验证：RelWithDebInfo `e2e_09` 100k 输出 `shared_payload_swcc_used_bytes=0`；
  `ctest -R '^e2e_09_test$'` 1/1 通过。

## M7 修正：e2e_08 全 owner EBR drain

- 日期：2026-07-24
- 改动：将同一 all-owner checkpoint/drain 确认协议接入 e2e_08，并在删除后断言
  `active_shared_rows=0` 和 `shared_payload_swcc_used_bytes=0`。
- 验证：RelWithDebInfo `e2e_08` 100k 输出两项均为零；
  `ctest -R '^e2e_08_test$'` 1/1 通过。

## M5 检查点：新引擎 RuntimeStats 汇总

- 日期：2026-07-24
- 改动：KVEngine 以原子计数汇总实际 shared GET/PUT、SCC writeback、成功 move-in/out
  与 transport 字节；KVStore Runtime/DumpStats 将其与门面操作统计合并，不再把新路径
  的 shared/迁移字段固定输出为零。
- 验证：RelWithDebInfo `unit_tests`、`kv_engine_test` 2/2 通过；跨进程 promotion
  断言 `migration_in>0` 且 SCC writeback 计数不小于 promotion 数。

## M7 检查点：trace runner 双节点并发 fixture

- 日期：2026-07-24
- 改动：新增 2 VM、双向跨 owner PUT/GET 的 trace fixture 与 CTest 脚本；两个
  `e2e_trace_runner` 以 barrier 同步启动，验证每个 worker 的计时行和实际 TX 统计。
- 验证：RelWithDebInfo `ctest -R '^e2e_trace_multivm_test$'` 1/1 通过。

## M8 检查点：e2e_ycsb 唯一入口

- 日期：2026-07-24
- 改动：新增根目录 `prepare_e2e_ycsb_traces.sh` 与 `run_e2e_ycsb_rounds.sh`，分别
  封装本树 trace 生成与 replay，固定 §6.3.1 load+A、100k/100k、4 worker/VM 和
  默认 10 轮合同；rounds 脚本输出每轮日志后调用本树汇总器。
- 验证：RelWithDebInfo 重新配置后
  `ctest -R '^e2e_ycsb_entrypoints_test$'` 1/1 通过。
- 限制：实际 trace 生成需要本树 YCSB-cpp submodule；真实多 VM replay 仍须遵守
  VM 授权要求，未在本轮执行。

## 综合回归：RelWithDebInfo CTest

- 日期：2026-07-24
- 验证：`ctest --test-dir build-relwithdebinfo --output-on-failure` 27/27 通过，覆盖
  分配器、B+树、SCC、引擎、100k e2e_08/e2e_09、单/双节点 trace 与脚本入口。
- 边界：这是本机回归，不替代 §6.3.1 的真实多 VM 连续 10 轮、§6.3.2 补充集成或
  §7.4 最终清扫。

## M10 检查点：删除 retired slot/msync 源

- 日期：2026-07-24
- 改动：物理删除 `kv_store.cpp` 中被编译期排除的 slot/global-lock/msync 旧引擎，
  以及 `tests/tigonkv_tests.cpp` 中对应的 retired 测试；当前 KVStore 仅保留
  KVEngine 门面运行路径和 active 门面回归。
- 验证：RelWithDebInfo `unit_tests`、`e2e_tests`、`e2e_08_test`、`e2e_09_test`
  4/4 通过。
- 仍未完成：继续清除仅服务旧引擎的辅助结构，并完成 §7.4 全树死代码审计。

## M10 检查点：收敛 KVStore 门面

- 日期：2026-07-24
- 检查点：`e960373`（`M10 remove retired slot implementation`）。
- 改动：删除 `kv/kv_store.cpp` 中仅被 retired slot/global-lock/msync 引擎使用的
  333 行布局、哈希、同步、锁、统计及 `KVStore::Impl` 状态；保留 JSONC 解析与
  配置边界校验。`KVStore::Impl` 现在仅持有活跃 `KVEngine`，消除了两套存储状态
  同时存在的误导性脚手架。
- 验证：RelWithDebInfo 下先执行
  `cmake --build build-relwithdebinfo --target unit_tests kv_engine_test -j4` 和
  `ctest --test-dir build-relwithdebinfo -R '^(unit_tests|kv_engine_test)$' --output-on-failure`
  （2/2 通过），随后执行
  `ctest --test-dir build-relwithdebinfo --output-on-failure`（27/27 通过）。
- 未完成项：§6.0 所要求的真实多 VM `e2e_08`/`e2e_09`/`e2e_ycsb` 各 10 轮、
  §6.3.2 补充集成、VM 实机验证和 §7.4 的完整基线差异清单仍未执行；本记录不宣告
  改造完成。

## M10 检查点：架构文档补齐

- 日期：2026-07-24
- 检查点：`9db312e`（`docs: describe TigonKV memory and coherence design`）。
- 改动：新建 `内存布局.md` 与 `缓存一致性设计.md`，分别对拍 dual-region/
  RegionOffset/attach 以及 shared payload 的 WriteThrough、NonCoherent 测试边界；
  新建 `THIRD_PARTY_NOTICES.md`，更新 `allocator审计.md` 为当前实现状态，并把
  README 的默认验证命令改为 RelWithDebInfo。
- 验证：`git diff --check` 通过；文档描述的实现和测试路径已由本次 M10 前的
  RelWithDebInfo 全量 CTest 27/27 回归覆盖。真实 VM、YCSB 与多轮 e2e 门槛仍未
  执行，文档中均明确标注为未完成。

## M10 检查点：移除 retired memory-domain 接口

- 日期：2026-07-24
- 检查点：`e228dd1`（`M10 remove retired memory domain interfaces`）。
- 改动：删除无消费者的改造期 `kv/memory_domains.h`，将活跃的延迟注入实现从旧的
  `kv/latency_simulator.*` 迁入 `kv/engine/latency_inject.*`，并同步 CMake、门面、
  引擎 wrapper 和测试引用；实现语义及现有测试 API 保持不变。
- 验证：RelWithDebInfo 下重新配置并构建 `unit_tests`、`latency_modes_test`、
  `kv_engine_test`，对应 CTest 3/3 通过；随后 `ctest --test-dir
  build-relwithdebinfo --output-on-failure` 为 27/27 通过。

## M10 检查点：删除 trace alias 脚手架

- 日期：2026-07-24
- 检查点：`59d6f6a`（`M10 remove unused trace runner alias`）。
- 改动：审计确认 `e2e_trace_runner_alias.cpp` 无 CMake、脚本或测试消费者后删除；
  活跃入口保持为 `e2e_trace` 和 `e2e_trace_runner`。
- 验证：RelWithDebInfo 全量重新配置、构建与
  `ctest --test-dir build-relwithdebinfo --output-on-failure` 均通过（27/27）。
