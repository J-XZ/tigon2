# TigonKV 当前修改摘要

本文只保留当前迁移的短摘要；详细施工过程和旧实验日志由 Git 历史保存。

## fixed-latency-only 收敛

- 将 `latency_inject` 收敛为单一 `fixed_latency` 配置和执行型 HWCC/SWCC wrapper。
- 删除访问统计、原子计数、远程 cache 模型、共享事件日志、replay、隐藏 HWCC reserve
  和第二 ivshmem；恢复业务 allocator 使用完整配置容量。
- 保留 B+Tree/OLC、SCC WriteThrough、owner-private、PolicyClock、EBR、transport、
  production Put pacing/backoff 和业务 runtime/memory stats。
- 前台与 CXL receive/RPC background scope 分离；延迟只在安全出口用校准 TSC 结算。
- 旧配置字段 hard-fail；布局版本更新为 28，旧 backing 不作为验证依据。

## 当前验证

- Debug、RelWithDebInfo 和 compile-off 构建均完成；非 VM CTest、fixed-latency 定向
  测试和 disabled benchmark 结果见 [验证证据.md](验证证据.md)。
- VM 验证只记录由本仓库镜像、配置、二进制和新 backing 创建的 4VM；无延迟 trace 与
  非零 canary 分开记录。
- 当前比较边界见 [当前对比口径.md](当前对比口径.md)，访问域和安全点见
  [延迟插入审计报告.md](延迟插入审计报告.md)。
